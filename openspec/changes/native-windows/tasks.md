## 1. Adopt the verified native boundary

- [ ] 1.1 Adopt the independently verified native-platform APIs in product dependency/task wiring; verify no reverse dependency, no consumer unsafe-code exception and no duplicate platform implementation.
- [ ] 1.2 Map consumer process intent to typed native spawn descriptions and lifetime policy; verify actual product command/probe ownership and cleanup using the foundation's primitives.
- [ ] 1.3 Adapt consumer lifetime channels to the verified private asynchronous IPC surface; verify real supervisor cancellation, expected-peer identity and complete product runtime shutdown.
- [ ] 1.4 Replace consumer-specific filesystem mechanics with checked platform handles/publication; verify real memory and delivery identities, private inherited ACLs and domain receipt reconciliation under native failures.

## 2. Embedded engine and memory lifecycle

- [ ] 2.1 Add the independent `kuru-archive` strict ZIP codec and extend the embedded-runtime target manifest/preparation with verified full Windows Dolt and LICENSES; verify target selection, the actual upstream physical inventory, duplicate/malformed/oversized input rejection and no-network Cargo builds.
- [ ] 2.2 Adapt memory supervision through the shared asynchronous platform channel while retaining one SQL supervisor; verify bounded unconfigured startup, creator-only EOF, graceful headless shutdown, Job escalation and lease retention through actual Dolt quiescence, plus separately verified enclosing-Job crash recovery.
- [ ] 2.3 Port memory cache, locking, SQLite backup and activation to held native identities and durable transitions; use the explicit external Windows lifecycle root and full-identity key while preserving Unix locks; verify lease retention across directory moves, lossless committed-WAL import with normal SHM coordination, no substitution, failure preservation and staged recovery.
- [ ] 2.4 Run real native memory/runtime scenarios for persistence, isolated dreams, expected-base conflict, accepted-operation reconciliation and compensating undo; verify later chats/preferences and stable identifiers survive the existing cancellation/error cases.

## 3. Native CLI, tools, connectors and terminal

- [ ] 3.1 Implement native user-directory defaults, explicit override precedence and drive/UNC path classification; verify CLI behavior with HOME absent and Unicode/spaced fixture paths.
- [ ] 3.2 Adapt asynchronous connector/auth command resolution and shell execution to explicit native argv/shim semantics and owned process trees; preserve configured cmd /c npx, deliberate shell source and usable PATH entries; verify real compiled fixtures, stock PowerShell output/exit status, bounded device authentication, cancellation and no unintended shell interpretation.
- [ ] 3.3 Preserve file-tool confinement under Windows reparse, hardlink, ADS, device-name and trailing-alias semantics; verify allowed CRUD and rejected escapes leave external fixture data unchanged.
- [ ] 3.4 Add actual native ConPTY/input fixtures for chat, selectors, navigation, resize, focus and cancellation; verify persistent choices, completed-frame animation quiescence and console restoration on normal/error exits while retaining Unix terminal tests.

## 4. Native delivery and updates

- [ ] 4.1 Centralize the target catalog and add the exact three-member Windows ZIP plus checksums; verify packager/updater inventory agreement, adversarial ZIP rejection and unchanged existing tar.gz contracts.
- [ ] 4.2 Add package-owned stock PowerShell bootstrap and thin entrypoint with options/env precedence, frozen latest version, bounded I/O and private staging; verify real installation, cancellation and exact old-byte preservation using packaged fixtures without Bash/compiler/Dolt.
- [ ] 4.3 Implement safe running-image publication with retained old identity, durable recovery phases and trusted cleanup ownership; verify actual loaded-executable success, failure/interrupt recovery and candidate non-execution at each boundary on Windows.
- [ ] 4.4 Route native source installation through prepared app mise tasks and apply static CRT only to explicit shipping MSVC target builds; verify embedded offline demo/reopen and inspect actual Kuru/Dolt PE imports for OS-only DLL dependencies without changing host proc-macro compilation.
- [ ] 4.5 Verify the actual native mise GitHub backend against controlled release metadata and genuine packaged candidate bytes using supported URL replacements and isolated configuration; assert native selection, version/digest, persistent offline demo, corruption rejection, SHA256SUMS discovery and API download fallback without live fallback or user installation changes.

## 5. Required CI, release and documentation

- [ ] 5.1 Add package-owned native Windows format/lint/test/coverage tasks and required windows-2025/aggregate CI wiring; verify actual platform test counts and coverage, failure on missing required prerequisites and preservation of the workspace threshold and Unix gates.
- [ ] 5.2 Add the fifth release build from the prepared version SHA with packaged offline-runtime smoke; verify five-target planning/recovery fixtures, immutable asset checks and the unchanged strategy-only release graph with Pages after publication.
- [ ] 5.3 Update README, guides and AGENTS for native directories, commands, embedded dependencies, Windows update behavior and package ownership; verify every documented command against the implemented interface and keep mise/PowerShell instructions before source.
- [ ] 5.4 Complete cross-package native acceptance on the actual candidate commit and packaged Windows artifact; record observed CI, ConPTY, database, bootstrap, running-image update and native mise fixture results, distinguish simulated metadata, and retain the separately pending post-publication mise procedure in release documentation without treating unrun or skipped checks as passes.
