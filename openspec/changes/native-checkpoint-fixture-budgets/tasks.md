## 1. Native shell fixtures

- [x] 1.1 Align wrapper deadlines and stock Windows compatibility setup with the real shell operation while preserving hostile/secret exclusions.
- [x] 1.2 Embed controlled CLI fixture values in authorized PowerShell source instead of relying on excluded custom environment variables.
- [x] 1.3 Give runtime replay the ordinary shell budget and retain the failed receipt in parse diagnostics.
- [x] 1.4 Compare the ToolHost Windows shell projection with exact controlled lexical values and report labeled failed checks in its bounded receipt.
- [x] 1.5 Record fixed private stage labels around the isolated ToolHost shell operations and include at most 4 KiB of those labels when the real shell call fails.

## 2. Refresh and TUI fixtures

- [x] 2.1 Give real refused-connect refresh gates enough test-only time to be observed and verify awaited owner cleanup on failure.
- [x] 2.2 Verify picker persistence through memory-backed behavior rather than the deliberately storage-free config snapshot command.
- [x] 2.3 Reuse the checked movable approval-record boundary for trust corruption fixture mutations while retaining the pinned store anchor.

## 3. Verification

- [x] 3.1 Run granular formatting, checks and strict Cospec validation, recording native Windows execution as deferred to hosted CI.

## 4. Unix terminal ownership

- [x] 4.1 Launch Unix terminal children with their requested PTY as the controlling terminal and preserve input, resize, restoration and bounded cleanup behavior.
- [x] 4.2 Add a regression with conflicting outer and inner terminal dimensions, plus bounded raw-output diagnostics for frame timeouts.
- [x] 4.3 Run the focused Unix terminal regressions and affected static checks, then record exact evidence.
