## 1. Configuration, phases and exact sizing

- [x] 1.1 Add bounded default-compatible compaction threshold and reserve configuration plus published schema/docs fields, and verify native parser/schema parity covers defaults, limits and reserve-greater-than-window refusal.
- [x] 1.2 Add typed Compact actor/usage phases and stable invocation identity inputs without changing existing phase ordinals, and verify durable accounting/export fixtures distinguish compaction from speaking, consultation and dream work.
- [x] 1.3 Expose pure connector estimates for the full pre-omission ordinary request candidate and exact compaction request/largest whole oldest source prefix, and verify mapped/fallback sizing, structural allowance, prior-summary inclusion, one-row overflow, threshold visibility before P7 omission and no provider dispatch.

## 2. Checked storage projections

- [x] 2.1 Consume P29's bounded cursor-selected summary window through local/remote live/candidate views, and verify exact-session/source rolling reads and policy-authorized broader reads return current records only with view/revision and 1,024-row/32-MiB bounds.
- [x] 2.2 Build the runtime source proof from the P29 cursor, cursor-selected prior summary and next session-filtered sequenced page, and verify actor/session/source/view/revision/range mismatches refuse before provider dispatch.
- [x] 2.3 Publish the strict operation-attributed summary, cursor and any producer-private Compact reasoning through P29's one conditional checkpoint and managed receipt reconciliation, and verify exact success, invalid binding refusal before send, relevant-source/cursor stale no-effect, accepted lost reply and candidate isolation with real-Dolt fixtures.
- [x] 2.4 Verify captured Dolt revision ancestry and exact historical/current consumed rows plus selected prior cursor/summary inside the checkpoint transaction; allow unrelated peer commits, reject changed content with the same sequence/count, and preserve receipted sidecar/usage behavior with real-Dolt interleavings.

## 3. Runtime compaction orchestration

- [x] 3.1 Implement one no-tools compaction invocation from fixed instruction, prior rolling summary and largest fitting source prefix, and verify settled text is bounded, empty/refusal/tool outputs do not publish, and prior summary remains usable after failure or cancellation.
- [x] 3.2 Add automatic per-actor threshold/hard-overflow compaction before ordinary dispatch with one-attempt and no-recursion guards, then rebuild once; verify below/exact threshold, rescued fit and mandatory-overflow refusal with deterministic providers.
- [x] 3.3 Route successful, failed, cancelled and recovered compaction through ordinary usage observation and settlement, and verify frozen price, partial usage, exact retry and one invocation/no duplicate checkpoint.
- [x] 3.4 Revalidate actor, session, policy, namespace and pinned view before dispatch/publication and drain owned cancellation, and verify authority change, inactive actor and post-cancel continuation leave no cursor effect.

## 4. Context and policy integration

- [x] 4.1 Assemble actor and relationship requests from the latest admitted rolling summary plus current-session raw rows after its cursor, and verify simultaneous-session/identity sentinels, null legacy rows, superseded summaries and P08 reasoning records do not leak.
- [x] 4.2 Admit cross-session cursor-selected summaries only through the effective mode visibility and memory policies, and verify allowed summaries appear while denied summaries and raw histories remain absent.
- [x] 4.3 Preserve candidate summaries through exact promotion/conflict/abandon and retain the turn's coherent context snapshot, and verify sibling live writes cause explicit conflict without lost conversations or candidate leakage.
- [x] 4.4 Revalidate actor/session source and current prior summary before dispatch without rejecting unrelated peer HEAD advances; verify concurrent peer requests retain their distinct context, complete and report all settled usage.

## 5. Manual command and user disclosure

- [x] 5.1 Register `/compact [ID]` once in the shared invocation-local registry and implement named or stable-ordered all-active dispatch, and verify help/completion/parse/handler parity, invalid target refusal and no provider prompt containing the slash command.
- [x] 5.2 Publish bounded settled/no-op/refusal notices with exact identity/range/summary coordinates and explicit retained-original language, and verify cancellation stops later identities and resume/status/cost remain truthful.
- [x] 5.3 Update configuration, usage, memory and public command/tool documentation with threshold, reserve, cost, policy, candidate and no-deletion boundaries, and verify owning docs format/build/content/link checks.

## 6. Integrated acceptance and closeout

- [x] 6.1 Run the real-Dolt isolation, stale/lost-reply, candidate and cancellation fixtures plus deterministic provider threshold/accounting groups, and record exact observed outcomes in `verification.md` without upgrading unrun hosted evidence.
- [x] 6.2 Run the registered command runtime tests and a real deterministic PTY through manual/automatic compaction notices and cancellation, and record effective model selection if any paid live request is separately authorized.
- [ ] 6.3 Run core/connectors/memory/runtime/TUI all-target lint/typecheck, strict Cospec validation, docs checks and combined coverage; archive only after every critical ledger row is observed or explicitly deferred with an owner and reason.
