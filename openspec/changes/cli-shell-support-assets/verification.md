## 1. Current CLI generates pure shell and man output [critical]

- [x] 1.1 @integration (agent) invoke a real built `kuru` for Bash, Zsh, Fish, PowerShell and man output from an empty foreign project with isolated data/config homes -> each output is bounded and reflects actual top-level and nested Clap commands/options; no project, trust, memory, provider, tool or network effect occurs.
- [~] 1.2 @integration (agent) run the app-owned generator from each selected native release binary and package only its checked five-file output while retaining that binary's before/after hash -> defer: the host binary's five files were generated twice with unchanged source hash and byte-exact sidecar roundtrip, but the five selected release binaries exist only in `release.yml`'s native build/package matrix after merge. Delivery owns checking all five target jobs' generated bytes, sidecar roundtrips and before/after binary hashes in that exact Release run; PR-native builds do not execute this release packaging task.

## 2. Release envelopes preserve strict executable compatibility [critical]

- [~] 2.1 @integration (agent) package each target binary's five generated files into its paired Unix tar.gz or Windows ZIP envelope and validate through the owning archive readers -> defer: local codec tests passed for all five target descriptors and the host-generated envelope round-tripped byte-exactly, but pairing with each actual target binary and its Release checksum is performed only by the five `release.yml` build/package jobs. Delivery owns recording those five envelopes and checksums from the exact Release candidate.
- [ ] 2.2 @integration (agent) inject missing, extra, duplicate, linked, traversing, oversized, truncated and changed-digest support members plus a marked core with a missing sidecar -> candidate assembly and installer refuse before executable mutation, while the existing core archive still rejects unexpected core members.
- [x] 2.3 @integration (agent) parse checksum-verified bounded core README content -> exactly one supported marker requires the selected target's paired sidecar; malformed, duplicate or unknown marker refuses; the new packager and release verifier refuse an accidentally unmarked new core, while verified historical unmarked cores remain executable-only.
- [ ] 2.4 @e2e (agent) run the retained v0.4.2 upgrade fixture and applicable v0.4.1 compatibility fixture against a newly laid out release candidate -> published older clients accept the unchanged three-member executable archive and do not falsely claim shell assets.

## 3. Native installation and update publish owned support safely [critical]

- [x] 3.1 @e2e (agent) use the current Unix bootstrap and native updater with a local exact release candidate -> checked versioned support is present under the explicit install directory before binary success, shell profiles stay untouched, and installed/update cold offline memory reopens.
- [ ] 3.2 @e2e (agent) use stock Windows PowerShell bootstrap and the native updater with the staged exact ZIP candidate -> checked support is installed in the selected root, no other root/profile is changed, and installed offline Kuru still reopens its bundled memory.
- [ ] 3.3 @integration (agent) inject support-stage failure, binary publication failure, stable Unix man-file publication failure and interrupted retry -> support failure leaves old binary untouched; an uncertain binary outcome retains its existing receipt; post-binary man failure reports a partial installation with explicit repair; only exactly owned unreferenced support may be cleaned or later revalidated.

## 4. Published inventory and user activation remain truthful [critical]

- [~] 4.1 @integration (agent) assemble the full release candidate and run staged Windows verification -> defer: local release-workflow and candidate-assembly fixtures passed their exact inventory/refusal checks, but the five target-paired archives, candidate artifact and staged Windows verifier exist only in the single `release.yml` run after merge. Delivery owns recording `assemble-candidate` and `verify-staged-windows` outcomes for that exact version commit before release promotion.
- [~] 4.2 @e2e (agent) run the published-download verifier against the exact immutable release after publication -> defer: there is no P24-containing immutable public release before merge and Release promotion. Delivery owns the actual published-download verifier in that Release run and must record all five sidecar digests and inventories, or a separate post-publication failure, without treating a staged candidate as published proof.
- [ ] 4.3 @integration (agent) use the fixed installed executable's pure Bash/Zsh/Fish/PowerShell activation commands in stock shells where supported and view ordinary `man kuru` through the selected install root's stable Unix MANPATH -> after a settled update and shell reload the output matches the installed executable and verified versioned sidecar bytes; no profile path pins an old version and no installer edits a profile. An unresolved replacement is reported rather than counted as complete activation.
- [x] 4.4 @integration (agent) upgrade through the retained old executable-only installer, then run the new binary's pure generation commands into an explicit user-selected directory -> missing support can be repaired without a provider request, live download or private-state creation. The old installer fixture is not evidence that an old `kuru update` binary ran.
- [~] 4.5 @e2e (agent) run an actual checksum-verified v0.4.x executable's old `kuru update` against the new immutable P24-containing release in an isolated installation, then use the installed new binary's pure generation commands for explicit missing-support repair -> defer: that immutable release does not exist before merge. Delivery owns the post-publication check and must record its exact old executable, release digest, selected install root, generation bytes and absence of private/provider effects; the deterministic retained-installer fixture does not satisfy it.

## 5. Repository gates and documentation

- [x] 5.1 @integration (agent) run owning format/lint/typecheck, docs build and link checks, shell/bootstrap checks, strict Cospec and normal combined coverage -> all pass with the existing 90% workspace line threshold.
- [x] 5.2 @integration (agent) inspect README/install/update/shell docs against actual direct-install paths and legacy behavior -> docs separate shipping from activation and do not claim the unimplemented Homebrew tap.

## Observed local evidence — 2026-09-25

The real `kuru` integration fixture `shell_support_generation_bypasses_invalid_workspace_authority_without_state` passed 1/1 with all five bounded outputs, an invalid explicit project config, isolated data/config homes, and no created private state. Its generated manual includes the nested `memory export` command. A package-owned host build then generated the exact five files twice from one retained binary (SHA-256 `473cb5f970ff70d1eedc26c5f559f0f2ccfd645a9e181e2d68ee9438a26620a0`); the two output trees were byte-equal, the binary digest stayed unchanged, and the roff-escaped nested export description and `--format` option agreed with `kuru memory export --help`. The host tar.gz packaging task round-tripped each generated file byte-for-byte and wrote a matching sidecar checksum. This is local host evidence, not five-target release-binary proof for row 1.2.

The package-owned delivery `test:install` task passed archive 17/17 and Unix bootstrap 20/20 with `RUST_TEST_THREADS=2`; it includes marked-core sidecar refusal, exact installed support/man files, historical unmarked core acceptance, and one byte-identical retained v0.4.1/v0.4.2 Unix bootstrap script run against a newly marked core. A stable-man obstacle after changed executable publication produced the explicit partial-install error; removing it and rerunning repaired the man file. Delivery's release-workflow integration target passed 20/20 in the pinned Cocogitto mise environment; delivery CLI passed 8/8, shell-support units 3/3, published-Windows units 6/6, and the foreign-authority CLI filter passed 1/1. These local checks do not establish native Windows PowerShell execution, native installed/offline memory, five-target release generation, or post-publication downloads.

Delivery and TUI all-target typecheck and Clippy passed on the integrated source. `cargo fmt --all -- --check`, `git diff --check`, and `mise run docs:check` passed, including the docs build and local links/anchors. The current Cospec validation passed with 0 errors and 0 warnings; apply returned exit 0/clear. Normal combined coverage, hosted native jobs, exact published release verification and archive remain pending.

The first permitted local combined-coverage attempt reached the full behavioral suite but failed three stale fixture inputs: the macOS private instrumented executable exceeded the unchanged 128 MiB package cap, a synthetic updater core had only one member, and a marked Windows package fixture omitted its paired support checksum. These were fixture failures, so this attempt is not a coverage pass. After correcting them, exact focused tests passed 1/1 each for the Unix updater checksum/no-mutation path and Windows marked package/support install. The instrumented embedded-runtime filter passed 1/1 with a 129,760,392-byte private stripped copy below the unchanged 134,217,728-byte cap, a nonempty LLVM profile from that copy, exact installed five-file support and stable man bytes, and real offline direct install plus self-update conversations. The TUI and delivery owning all-target Clippy checks and Rust format/diff checks passed after these fixture edits. A complete corrected combined-coverage result remains pending.

The corrected package-owned `mise run coverage` then completed locally with exit 0 and 75,133/80,053 covered lines (93.85%) against the unchanged 90% gate; its report is `target/coverage.lcov`. The application unit suite passed 105/105, memory passed 247/247, runtime passed 188/188, and the three formerly failing integration cases each passed within this complete instrumented run. This local result predates the source-only rebase onto main `b0d624cb`, whose only resolved source overlap is the published-Windows verifier combining PR86 cleanup with the P24 support checks. Hosted native behavior, five-target release generation, exact published release verification and archive remain pending.

The public `2445ec69` normal pre-push hooks passed on the integrated source with 75,169/80,132 instrumented lines (93.81%) and 188/188 runtime library tests. This is local hook evidence, not a hosted native result. Row 2.3 is complete from `archive::tests::shell_support_marker_is_strict_without_rejecting_historical_readmes`, the marked-core pairing/refusal fixtures, and the release candidate tests that reject an unmarked new core. Row 3.1 is complete on the Unix host from the normal bootstrap 20/20 and the instrumented embedded-runtime cold direct install/self-update fixture, which compares installed support and stable man bytes while exercising real offline memory after both operations. Native Windows installation, five-target generation and post-publication verification remain open.

PR88's first exact-head native Windows installation/offline job in run `36122189012` failed in `packaged_install_and_update_preserve_complete_offline_memory` with a bare `The system cannot find the path specified. (os error 3)` after packaging input; this is not a Windows installation pass. Source comparison found that the fixture unconditionally reads the Unix-only stable `share/man/man1/kuru.1`, while stock PowerShell publishes the exact five files only under the versioned support path. A narrow, reviewed fixture correction keeps both direct-install and self-update five-file versioned-byte comparisons on every platform and restricts only stable-man byte checks to Unix. The error lacks a failing path, so that source finding is a likely cause, not yet a proven hosted diagnosis; corrected Windows native execution remains required.

The same first PR88 run also failed Windows delivery/archive `omitted_version_freezes_simulated_release_roots_before_fetching_real_native_zip`: its synthetic frozen root contained the marked core and combined checksum manifest but omitted the required paired support ZIP. The corrected fixture places the same checked sidecar in that selected immutable version root while retaining the negative latest-root/version-freeze assertion. At that checkpoint, the retained byte-identical v0.4.1/v0.4.2 PowerShell reader fixture and installed shell-activation/old-installer repair checks had no execution result; the later host proof below does not establish their Windows behavior.

The corrected host package-owned `//apps/kuru-tui:test:embedded-runtime` task passed 3/3 on macOS after the new activation and repair checks. Its selected ordinary debug executable was 142,605,048 bytes; an independently verified private copy stripped to 127,793,752 bytes under the unchanged 134,217,728-byte archive cap, and the 57,569,366-byte package completed real offline direct install and self-update conversations. After update, stock Bash and Zsh loaded completion output from that fixed installed executable, and `man -w kuru` resolved the selected install root's stable page. The retained v0.4.1/v0.4.2 Unix installer then installed the new core without support; explicit generation from that installed binary produced all five exact support bytes in a user-selected directory despite invalid project configuration, without creating private data or configuration. The original documented Bash `source <(...)` command failed in the stock shell even when reading a valid generated file, while synchronous `eval "$(...)"` registered completions; the matching documentation and fixture commands were corrected. This is host Unix evidence for row 4.3 and local proof of row 4.4, not stock Windows activation or the deferred old executable updater proof. TUI and delivery all-target Clippy, Rust format/diff checks and `mise run docs:check` passed after these changes. The first docs-check attempt stopped in pinned tool setup because the sandbox denied Git config writes; the permitted rerun passed.
