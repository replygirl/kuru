## 1. CLI-owned generation

- [x] 1.1 Add pure `kuru completions` and `kuru man` output from `Cli::command()` with exact pinned generator dependencies and verify output from an empty foreign project creates no Kuru state or authority request.
- [ ] 1.2 Add the app-owned mise generation task consuming an already-built selected binary, and verify bounded deterministic five-file output plus nested-command help agreement.

## 2. Exact release content

- [ ] 2.1 Package the five read-only outputs as bounded tar.gz and ZIP support envelopes through delivery's existing codecs; add one strictly parsed shell-support-format marker to the checksum-covered core README, and verify exact names, modes, sizes, hashes, marker forms and rejection of unsafe members.
- [ ] 2.2 Update release candidate assembly, `SHA256SUMS`, staged Windows and published-download inventories for five target-paired envelopes, and verify each sidecar's raw file hashes against its own native executable while retaining strict three-member core archives.

## 3. Native installation and compatibility

- [ ] 3.1 Extend the Unix bootstrap, stock PowerShell bootstrap and Rust updater to verify and stage the matching support envelope before existing executable replacement; after confirmed Unix executable publication, publish a checked stable man file. Verify exact versioned files and Unix man inside only the selected install root with no profile mutation.
- [ ] 3.2 Preserve legacy executable-only versions and upgrade readers, and verify actual v0.4.2 plus retained v0.4.1 fixture upgrade paths accept the unchanged core archive and can repair missing support through the new pure generation commands.
- [ ] 3.3 Exercise support-stage, executable partial-publication, failure and retry outcomes with exact owned-root assertions; verify truthful diagnostics, retained previous or unresolved executable receipt, and no claim of cross-file atomicity.

## 4. Integrated acceptance and documentation

- [x] 4.1 Document direct-install versioned files and fixed installed-executable Bash/Zsh/Fish/PowerShell/man activation without claiming Homebrew tap delivery, and verify docs build, links and examples.
- [ ] 4.2 Run and record the verification ledger's actual native generation/install/update/offline and release-inventory cases, affected formatting/lint/typecheck/bootstrap checks, strict Cospec and normal 90% combined coverage before archive.
