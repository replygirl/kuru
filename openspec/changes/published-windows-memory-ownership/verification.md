## 1. Published Windows command ownership [critical]

- [ ] 1.1 @regression (agent) run the native Windows delivery command around an actual IndependentService child -> the command returns after its root exits while the service remains owned independently; the unchanged source previously failed Job quiescence on v0.8.0 Release attempt 2, job 107954080562, despite a completed first Kuru response.
- [ ] 1.2 @integration (agent) run the installed Kuru through native mise in a fresh isolated project and retire its service through `memory purge --yes` -> cold conversation and durable reopen succeed, the exact project maintenance path retires the owner, and only the disposable fixture data is removed.
- [ ] 1.3 @integration (agent) inject a failed verifier command and failed cleanup in the isolated fixture -> both causes are reported and the uncertain root is retained; successful cleanup after a primary failure preserves that first failure.

## 2. Public release and repository gates [critical]

- [ ] 2.1 @integration (agent) run owning delivery typecheck/lint, release-workflow contract checks, Cospec strict/apply, formatting and normal hooks -> all pass without a new memory dependency or workflow/asset change.
- [ ] 2.2 @e2e (agent) verify a later immutable public Windows release through the corrected same-SHA download task -> checksum, native mise install, cold conversation, durable reopen, bundled Dolt bytes and cleanup receipt all pass. The v0.8.0 assets and failed original Release run remain unchanged.

Observed baseline, 2026-09-25: v0.8.0 Release run `36092220979` selected signed commit `66c0be8fa837160b11c68b50bb5489761e771969`; the independently checked public Windows ZIP matched the staged candidate that passed native mise acceptance. Post-public attempt 1 job `107951310322` timed out waiting for memory readiness, with the startup cause unproved. Same-run attempt 2 job `107954080562` produced `Memory: ready` and a completed cold response, then the default command Job retained the independent service and failed its bounded process-tree quiescence check. Both jobs failed; neither is success evidence. Published tag/assets remain immutable.

Current evidence: `MiseInstall::command` uses the existing Windows fixture breakaway-permitting Job, and cleanup invokes the installed CLI against only the verifier-created project/data root after all receipt assertions; a failed cleanup retains that root. Independent review of those boundaries, the native lock-regression source, and the corrected project-only purge assertion was clear. Strict Cospec validation passed with zero errors/warnings and apply exit 0/clear. Owning delivery all-target typecheck and Clippy, the focused all-host cleanup/error-pairing test (1/1), two exact release-workflow/launcher contract tests (1/1 each), Rust formatting and diff-check passed on macOS. An initial host typecheck exposed a Windows-only method call on the Unix command alias; the call is now gated to Windows, and the rerun passed. A separate broad delivery test attempt in the restricted sandbox could not bind its local HTTP fixtures and is excluded from behavior evidence; the two exact contract tests passed afterward. The Windows regression, installed-purge fixture, normal hooks, hosted CI and later-release end-to-end verification remain unrun and open above.
