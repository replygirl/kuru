## 1. Resolve prerequisites and the official payload contract

- [ ] 1.1 Complete and archive native-windows, retain the resolved asset/interface/budget decisions and reread prerequisite context; verify strict validation and an actual clear apply before changing product code.
- [ ] 1.2 Refactor delivery preparation into one byte-only ArchivePin/CacheOptions engine with explicit Dolt/Codex adapters and backward-compatible default Dolt CLI behavior; verify existing import/download/cancellation/locking regressions plus cross-kind manifest rejection and isolated environment/CLI precedence without compiling either consumer.
- [ ] 1.3 Add the exact connector-owned manifest, pinned notice files and local TARGET-aware build verifier; wire its native mise preparation to the helper CLI/hash-addressed file contract and verify real offline prepared builds, foreign-target preparation with a host helper, explicit host override, notice mismatch rejection and Cargo rejection of missing, corrupt and wrong-target inputs.

## 2. Bundle and resolve the default client

- [ ] 2.1 Implement bounded typed extraction, private versioned cache publication and native lifetime ownership for embedded client/notices; verify malicious archive, cancellation, concurrent extraction and actual member digest cases.
- [ ] 2.2 Route provider initialization and every TUI authentication command through the same bundled-default resolver with explicit external override; verify actual native initialize/model-list/login-help, preserved configuration precedence and consistent override failures.
- [ ] 2.3 Preserve client-owned authentication, restricted inference and dynamic model/effort transport; verify unknown capabilities and structured tool proposals, and record user-authorized live login/chat separately from isolated fixtures.

## 3. Deliver and verify all supported platforms

- [ ] 3.1 Apply Codex 128/320 MiB and outer delivery 256 MiB ceilings consistently while preserving Dolt 64/128 MiB limits; record actual release and packaged-test executable plus compressed/expanded archive sizes on all five targets, and verify over-limit updates preserve the installed executable.
- [ ] 3.2 Integrate preparation into native source/direct/mise install and self-update paths; verify release-built install/update and independent cold Dolt/client caches on all five native targets without smaller substitute package fixtures.
- [ ] 3.3 Verify upgrades from the published v0.1.0 size-limited Unix client through the existing bootstrap or mise path; preserve the old executable on rejected in-app updates and conversation data across reinstallation, and document the version boundary without a bridge release.
- [ ] 3.4 Update owning install/auth/development/security guides and native CI acceptance; verify working command syntax, no separate default-client prerequisite and unchanged release/recovery/final Pages topology.
- [ ] 3.5 Run the granular mise/hk checks and required native CI, keeping static categories on Ubuntu and behavior on supported platforms; record actual results and coverage, strictly validate and archive the completed change before its final branch commit, and verify no pending required acceptance row or unsupported native/live-auth claim remains.
