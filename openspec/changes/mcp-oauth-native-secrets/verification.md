## 1. Discovery, issuer and registration binding [critical]

- [x] 1.1 @integration (agent) drive loopback protected-resource and authorization servers through challenge, path/root metadata fallbacks, OAuth and OIDC discovery, configured registration, advertised CIMD and advertised compatibility DCR with `application_type=native` -> each supported path selects the exact issuer/client/resource, uses the required priority and bounded request sequence, and invalid/mismatched/unadvertised paths send no authorization or registration request
- [x] 1.2 @regression (agent) serve hostile metadata with wrong issuer/resource, non-HTTPS external endpoints, redirect escape, oversized bodies, multiple authorization servers and unsupported PKCE -> the selected alias fails closed with bounded diagnostics and no credential, browser, route or other-alias effect

Observed on macOS: `mcp_oauth::tests` passed 11/11 in 10.02s with native loopback permission. The module covers exact discovery fallback order, no forwarded secrets, URL/redirect/body bounds, issuer/resource mismatches, PKCE and capability gating.

Dependency evidence for task 1.2: the workspace pins `keyring =4.2.0` with default features disabled; `kuru-platform` selects only its `v1` API plus the native Windows, Apple or Linux Secret Service backend for each target. The fake HTTPS `rcgen =0.14.10` and `tokio-rustls =0.26.5` dependencies are TUI dev-only. `Cargo.lock` records exact registry checksums; the cached published crate manifests identify MIT OR Apache-2.0 licensing and repository provenance for the added crates/backends. Owning package typecheck/lint use `--locked`; the existing CI RustSec and bundle/license-input checks are separate. There is no dedicated Rust dependency-license task, so none is claimed here. Final native target jobs remain delivery gates.

## 2. Browser, headless and device authorization [critical]

- [ ] 2.1 @e2e (agent) run CLI login against a fake authorization server and the real owned loopback callback across the RFC 9207 advertised/present issuer matrix -> exact redirect/state/PKCE S256/resource/issuer/code exchange succeeds once, wrong, required-missing or repeated callbacks fail before redemption, and cancellation/expiry releases the listener without publishing a credential
- [x] 2.2 @integration (agent) exercise advertised RFC 8628 success, pending, slow-down, denial, expiry and cancellation -> verification details are bounded, polling honors server pacing, only success publishes, and no request occurs when the endpoint/grant was not advertised
- [ ] 2.3 @manual (agent) inspect browser and no-browser output in a practical local and remote/headless terminal -> the desktop handoff does not own browser lifetime, and printed guidance accurately states same-host/forwarded-loopback reachability and the advertised device alternative

Observed on macOS: the connector device matrix passed all pending/slow-down/success/denial/expiry/cancellation/absent-capability branches within the 10/10 module result. The real-CLI `--no-browser` PTY fixture passed 1/1 in 1.61s through the owned local callback. A subsequent native 1/1 run sent the advertised state, issuer and code through a distinct local TCP forwarding listener while retaining the callback Host, path and query; the fake token endpoint received the original redirect URI and code with the matching S256 PKCE verifier, exactly once, and CLI completed sign-in. This is a same-machine functional forwarding simulation, not a remote deployment or completed desktop-browser handoff. Those remaining observations keep row 2.3 open.

## 3. Native credential storage and lifecycle [critical]

- [ ] 3.1 @runtime (agent) expected-absence-create, race a second stale create, reopen, generation-replace and generation-delete an isolated synthetic Kuru MCP record through the real macOS, Windows and Linux native-store implementations on supported native jobs -> only the exact service/account binding is visible, restart retains it, stale create/replace/delete lose, and cleanup leaves no test record
- [ ] 3.2 @integration (agent) run OAuth with the native facility missing, locked and denied -> login/refresh/status report distinct actionable unavailable states, no plaintext/session fallback appears, and a configured static-header alias continues to work
- [ ] 3.3 @regression (agent) place similarly named synthetic records for another application beside the Kuru fixture -> Kuru does not enumerate, read, migrate, report or delete them

Observed on macOS: `native_store_reopens_replaces_deletes_and_isolates_adjacent_records` passed 1/1 in 0.50s. The synthetic `ItemStore` Denied/Unavailable get/create/replace/delete matrix passed 1/1: each refusal returned its fixed actionable diagnostic without exposing the synthetic secret or changing any stored item, and the prior readable state remained after clearing the fault. This is a credential-vault fault proof, not an observed locked OS keychain or an external CLI/status/static-header result. Linux Secret Service and Windows Credential Manager native execution remain required hosted evidence.

The Linux-only `mcp_cli_missing_secret_service_refuses_without_fallback_or_static_alias_loss` fixture is authored but unrun on this macOS host. It requires CI's owned real Secret Service: seed a synthetic Kuru credential, give only selected CLI children a missing D-Bus socket and private runtime directory, require native-store-unavailable status and refusal before another device/token request, prove a static-header MCP tool still lists with its exact header, then restore the original bus and delete the unchanged native record. Its owned children have bounded output, kill and reap. A passing Linux job is required before counting this as observed OS evidence; synthetic Denied remains separate from a genuinely locked native collection.

## 4. Refresh, scope step-up and cancellation settlement [critical]

- [x] 4.1 @integration (agent) store a logical credential spanning multiple native items; assert the exact 67,899-byte envelope, 27-chunk maximum, 124-byte two-descriptor manifest and 160-byte complete native item; interrupt before and after prepared, committed-new cleanup, stable-new and committed-deletion publication; then expire a token, return a rotating refresh token, cancel after the accepted response and race a second process/login/logout -> recovery exposes either the complete old or complete new digest-bound generation, retains enough descriptor state to remove every retired secret without enumeration, authoritative deletion cannot be resurrected, one refresh request settles the exact native generation, stale writers lose, and cancellation returns only after safe publication/reconciliation
- [x] 4.2 @integration (agent) vary initial challenge scopes against protected-resource metadata and return invalid-token plus insufficient-scope challenges before an MCP body is accepted -> Kuru performs at most one eligible refresh or explicit step-up, uses the exact allowed initial authority or prior-grant/challenge union, refuses scopes outside the configured ceiling, and recreates only the selected alias session
- [x] 4.3 @integration (agent) lose the response to a rotating refresh and separately lose/abort a bearer tool-call response after dispatch -> neither request is blindly replayed; refresh requires stored-generation recovery or relogin and the tool call retains the connector's ambiguous-effect result

Observed on macOS: `mcp_credentials::tests` passed 4/4 in 0.09s, including every publication phase, exact envelope/manifest bounds and prepared-collision rollback. `browser_login_settles_native_publication_then_refreshes_and_logs_out` passed 1/1 in 0.50s and proves accepted callback settlement, native rotation, scope step-up, serialized logout and absence of every synthetic code/token from the shared logout DTO. `rotating_refresh_lost_response_is_returned_without_replay` and `failed_call_retains_unconfirmed_http_close_without_replay` each passed 1/1: the fixtures observed exactly one refresh or tool request and returned the ambiguous outcome without replay.

## 5. Alias, resource, header and catalog isolation [critical]

- [x] 5.1 @integration (agent) configure two OAuth resources on one issuer with non-authorization resource headers plus one static-authorization alias and capture every request -> each bearer/resource/client/scope binding reaches only its exact alias, non-authorization headers appear only on the selected protected resource, no static header enters OAuth authority requests, and OAuth headers never enter the static alias
- [x] 5.2 @integration (agent) change endpoint, issuer, client identity, reviewed authority, scopes and local credential generation across cached catalog records -> mismatched OAuth metadata is ineligible, logout does not reveal stale authorized tools, and only current live discovery creates a route
- [ ] 5.3 @e2e (agent) inspect mixed disabled/live/stale/degraded inventory through `kuru tools` and `/tools`, and compare selected OAuth authorization through `kuru mcp status` and `/mcp status` -> each paired surface gives the same bounded projection, disabled aliases perform no discovery/store access, and no state grants permission or dispatch

Row 5.1 native evidence: `shared_issuer_keeps_two_oauth_resources_and_one_static_alias_separate` passed 1/1 in 0.52s. Two resource GET probes returned their own 401 challenges without bearer credentials; authorized MCP POSTs carried only their own bearer and resource header; authority requests carried neither resource nor static headers; and the static alias retained only its configured authorization.

Observed on macOS: `mixed_alias_failures_preserve_live_and_stale_catalogs_independently` proves live, stale, degraded and disabled aliases coexist without route/effect leakage. `oauth_status_tracks_mixed_alias_state_without_projecting_secrets` covers authorized, disabled, refresh-required and login-required states; serialized status and `/tools` catalog projections omit recognizable client, access and refresh secrets. Its former project-root rename was removed because it did not simulate a missing or denied native secret facility; row 3.2 remains open. Two native row 5.2 filters each passed 1/1: the cache identity matrix rejects changed endpoint, client, scopes, reviewed authority and credential generation, while the live fake OAuth fixture rejects a changed issuer, retires logout routes before later dispatch, preserves zero additional tool requests after caller-cancelled logout, and requires new live discovery after reauthentication. The canonical login/revoke/status UX is projected by the dedicated `mcp status` command, while `tools` projects inventory availability; it does not require inventing a catalog authorization field or an extra native secret read. The real-PTY device login/status/logout parity fixture passed 1/1 in 6.98s, but the complete mixed inventory and unavailable-store matrix remains open in 5.3.

## 6. Login, status and logout command parity [critical]

- [ ] 6.1 @e2e (agent) exercise `kuru mcp login|status|logout` and `/mcp ...` through fake servers and a real PTY synchronized to completed frames -> help/completion/argument parsing and ordered final output agree, commands make no provider request, and the user can continue the same TUI session
- [ ] 6.2 @regression (agent) request unknown, disabled, STDIO and unapproved automatically configured aliases -> every surface refuses before discovery, credential access or process startup and leaves the prior credential/catalog unchanged

Observed on macOS: the typed command parser and CLI authority projection each passed 1/1; `oauth_commands_refuse_unknown_disabled_and_stdio_aliases_before_activation` passed 1/1 in 0.38s with zero HTTP requests and zero STDIO conversations. The real-CLI `mcp_cli_refuses_unselected_aliases_without_starting_stdio_or_memory` fixture failed before the selected-only status correction because valid selected OAuth status started an unrelated synthetic STDIO alias, then passed 1/1 after the correction; it covers selected status plus missing, disabled and STDIO login/status/logout refusals, exact refusal diagnostics, no unrelated process marker and no memory creation. It also passed with an unapproved automatically configured OAuth alias: CLI status/login/logout each refused at workspace trust preflight before any unrelated STDIO start or memory creation. The real-PTY `real_pty_mcp_refusals_keep_composer_and_other_aliases_idle` fixture passed 1/1 in 4.88s: nine `/mcp` status/login/logout refusals kept the same composer usable with zero unrelated STDIO, HTTP MCP or provider requests; an unapproved automatic claim was separately cancelled at trust preflight before alternate-screen entry, and neither path printed the synthetic client secret. The matrix does not yet compare a seeded prior credential/catalog before and after every refusal, so row 6.2 stays open. The real-CLI `mcp_cli_device_login_status_logout_uses_synthetic_verified_https` fixture passed 1/1 natively in 3.49s with generated local CA, advertised device flow, authorized status, remote revocation and local logout; a same-CA wrong-host request failed TLS before a device request. The real-PTY `real_pty_mcp_device_login_status_logout_matches_cli` fixture passed 1/1 in 6.98s using that verified HTTPS fixture: one TUI session completed device login, showed authorized `/mcp status`, logged out, returned to login-required, and kept the composer usable while matching the dedicated CLI typed-status projection. The real-CLI local and forwarded `--no-browser` callback fixtures completed sign-in with one exact token exchange. These prove fake device and local headless CLI/TUI paths, not a remote deployment or the complete browser/argument matrix in row 6.1. The completed-frame PTY status/quit fixture passed 1/1 in 2.91s and proves the raw terminal stream omits its configured recognizable client secret.

## 7. Local deletion and advertised remote revocation [critical]

- [x] 7.1 @integration (agent) advertise a valid RFC 7009 endpoint and accept access/refresh revocation -> logout serializes with refresh/login, sends only the bound credential to the exact endpoint, reports remote success and removes the local record
- [x] 7.2 @integration (agent) omit the endpoint, refuse it, lose its response and return a hostile body -> each outcome is reported distinctly and safely, local deletion still completes, and Kuru never claims remote revocation without evidence
- [x] 7.3 @regression (agent) cancel logout before and after remote dispatch while another process refreshes -> the operation retains one coherent local generation outcome and never restores deleted credentials or repeats an uncertain revocation

Observed on macOS: advertised empty-body revocation reports `revoked` and leaves no local record in the integrated browser test. The pure revocation classification fixture passed 1/1 for absent, lost and hostile outcomes. `logout_settles_local_deletion_across_cancellation_and_generation_races` passed 1/1 in 1.86s: cancellation before dispatch allowed a lease-owning refresh to settle, then logout revoked/deleted that exact new generation; cancellation after observed dispatch still completed exact deletion; stale publication lost and each admitted logout made one remote attempt. The same fixture then exercised no advertised endpoint, HTTP refusal, response loss and an oversized hostile response; every case generation-deleted locally, and only the proved success reported `revoked`.

## 8. Configuration, authority, bounds and secret exclusion [critical]

- [x] 8.1 @integration (agent) validate accepted and hostile OAuth TOML against native parsing and `configuration.v1.schema.json` -> supported configured/CIMD/DCR fields, restrictive scope defaults and non-authorization headers match, while literal secrets, STDIO OAuth, static `Authorization`/`Proxy-Authorization` conflicts, unknown fields, incompatible choices and bound violations fail before activation
- [ ] 8.2 @integration (agent) resolve automatic project OAuth configuration before and after exact-root trust -> no secret environment/native-store/discovery access occurs before approval, changed final-leaf authority invalidates it, and command-specific inspection activates only applicable claims
- [x] 8.3 @regression (agent) inject recognizable synthetic secrets into every token/client/error field and inspect stdout, stderr, `/tools`, `/mcp status`, config, cache and debug/error projections -> no secret, callback code, raw authorization body or credential-derived identifier is emitted or persisted outside the native record

Observed on macOS: native and JSON-schema parity each passed 1/1, including explicit non-HTTPS resource refusal, literal-secret rejection and static authorization conflicts. Protocol response/debug tests redact recognizable token, client-secret, device-code and hostile error fields. `static_header_environment_reference_applies_to_the_whole_http_session` proves resolved environment secret values never enter the persisted catalog, whose OAuth cache identity contains only binding digests and opaque generation. The mixed status fixture proves serialized `/tools` and `/mcp status` projections omit injected client/access/refresh secrets; the integrated browser fixture proves the shared logout DTO omits callback code and both token generations. The completed-frame PTY test scans its complete raw stdout/stderr stream and omits its configured recognizable client secret. `automatic_oauth_claim_requires_exact_root_trust_before_selected_status` passed natively 1/1: preapproval CLI status/logout/device login made zero fake HTTPS requests, approved selected status made a positive network request, and another root or changed final-leaf authority refused without a further request. This does not directly count OS keychain reads, so row 8.2 remains open pending the native-store boundary proof. No live or paid provider was contacted.

## 9. Documentation and protocol provenance

- [x] 9.1 @integration (agent) build and check curated docs and configuration examples -> published guidance covers MCP 2026-07-28 authorization discovery/resource/PKCE/issuer/scope rules applied to Kuru's existing negotiated transport, optional advertised device/revocation behavior, native-store setup, callback reachability and static/OpenAI separation without claiming full 2026 wire support or exposing repository evidence or secrets
- [x] 9.2 @manual (agent) compare the implementation constants and fixtures with the pinned official MCP 2026-07-28 authorization page plus RFC 7009, RFC 8252, RFC 8628, RFC 8707, RFC 8414, RFC 9207 and RFC 9728 -> every mandatory or optional behavior is attributed accurately, DCR is identified as compatibility fallback, no optional extension is presented as core MCP, and no whole-protocol 2026 conformance is claimed

Observed on macOS: `mise run //apps/kuru-docs:check` passed formatting, lint, build, content and local-link checks. An agent compared the official MCP 2026-07-28 authorization page and all eight named RFCs with `mcp_oauth.rs`, `mcp.rs`, the CLI browser/device paths, the proposal, design and published protocol guidance. The comparison found HTTP authorization discovery, PKCE, resource and issuer rules attributed to MCP; device authorization and revocation treated as optional advertised OAuth capabilities; DCR described as deprecated compatibility fallback after configured registration/CIMD; and no full 2026 stateless-wire claim. This is source/protocol review, not practical browser or remote callback evidence; row 2.3 remains open.

## 10. Owning checks and supported hosts [critical]

- [x] 10.1 @integration (agent) run granular core/platform/connectors/TUI format, lint, typecheck and deterministic OAuth/command suites through package-owned mise tasks -> all affected crates and feature combinations pass without contacting a paid/live provider
- [ ] 10.2 @runtime (agent) run native macOS, Linux and Windows credential/browser/CLI fixtures plus combined workspace coverage -> supported hosts pass real native behavior, subprocess/listener cleanup is bounded, and workspace line coverage remains at least 90%
- [ ] 10.3 @integration (agent) run strict Cospec validation, apply gate, docs checks and commit hooks -> artifacts remain coherent, dependencies are archived, generated schema is current and no required gate is skipped

Observed locally: affected core/platform/connectors/runtime/TUI all-target/all-feature Clippy passed with `-D warnings`; `cargo fmt --all -- --check` and `git diff --check` passed. Deterministic focused results are recorded above and used no live or paid provider. Linux/Windows native behavior, combined coverage, hooks and archive remain Delivery gates.
