## 1. Observe actual post-termination lock release

- [x] 1.1 Localize the native failure before changing the assertion. CI 34617836357, Windows job 103324074576, passed every captured-output, EOF, quiescence-error and successful-cleanup assertion before `held.try_lock()` returned contention. The original plain shell case passed at log line 605. Native log: /tmp/kuru-shell-plain-ci-job-103324074576.log; independent diagnosis: /tmp/kuru-windows-command-capture-lock.md. No actual unlock delay was measured.
- [x] 1.2 In packages/kuru-delivery/tests/windows_command_capture.rs, wait on the same retained file for at most five seconds, retry only WouldBlock and preserve command evidence on failure. The real two-handle control requires a timeout while held and successful acquisition after explicit unlock; all original owned-tree and captured-output assertions remain. Independent review found no blockers and checked the pinned Rust/Windows lock APIs: /tmp/kuru-command-lock-review.md. Native execution remains pending in 1.3.
- [ ] 1.3 Run normal granular hooks and native CI, record the actual control and cleanup results, then strictly validate and archive the completed test correction before the final PR commit. Keep skipped shipping/coverage steps from the failed run distinct from later passing evidence.
