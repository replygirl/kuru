## 1. Pin coverage [critical]

- [x] 1.1 @unit (agent) run the pin test -> every nested per-variant sample set covers exactly serde's variant list and the fixture matches at 1.7. `protocol_surface_matches_the_pinned_protocol_version` passed with per-variant samples for content_block (6), session_turn_checkpoint (4), public_transcript_position (2), usage_proof (4, from the real ledger proofs), price_basis (2), cache_write_terms (2) and export_phase (6, all populated, through `ExportCursor`). `price_at_invocation` is populated. No `null` remains in the fixture
- [x] 1.2 @regression (agent) bless with the recorded version equal, and with the current version lower than recorded -> both refuse; bless with a strictly greater minor writes. End-to-end: at a temporary 1.6 the test failed with "a protocol version never decreases" and bless refused. A temporary field rename at 1.7 failed with bump instructions and bless refused, leaving the fixture checksum unchanged both times. With the rename at 1.8, the test failed with "regenerate", bless wrote `protocol 1.8` and `view_operation.get {key_name:string}`, and the next run passed. All edits were reverted and the checksum verified. The unit test `regeneration_requires_an_absent_fixture_or_a_strictly_greater_version` covers absent, greater minor, greater major, equal, lower minor, lower major and a missing version line
- [x] 1.3 @regression (agent) regenerate the 1.7 fixture through the tooling -> the fixture was introduced by this same unmerged branch, so it was removed with `git rm` and recorded by `KURU_BLESS_PROTOCOL_PIN=1` (absent-fixture path), not edited by hand. Its diff against the previous commit adds 29 lines (three enum lists and 26 nested samples) and changes one line (`ledger_operation.admit` now shows the populated price schedule in place of `null`). No existing request or enum line changed, so no wire change is recorded

## 2. Equivalence

- [x] 2.1 @equivalence (agent) run the complete `kuru-memory` package test task -> exit 0. Library: 273 passed (267 pre-existing plus 6 contract/pin tests). Integration targets: 6, 5, 12 and 1 passed. The classification table is unchanged and passes
- [x] 2.2 @integration (agent) package clippy `-D warnings`, formatting and strict cospec validation -> `//packages/kuru-memory:lint` exit 0, `format:code` exit 0, `docs:check` exit 0 and `cospec validate --strict` passed
- [~] 2.3 @e2e (human) native Windows and Linux CI -> defer: test and classification code without platform branches; branch CI provides native evidence
