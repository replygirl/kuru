## 1. Immediate and fair scheduling [critical]

- [x] 1.1 @regression (agent) Inspect the pre-fix source's completion-drain-then-blocking-poll order, hold the current scheduler waiting before its animation deadline with all input sources pending, then deliver a typed completion -> native macOS named runtime fixture exited 0 in `/private/tmp/kuru-async-completion-waiting.log`. Root reviewed two pending polls of the actual scheduler before the completion send, followed by that same future waking, applying the exact command through `apply_completion` and leaving the animation frame unchanged. Baseline commit `4e552b8` drains `rx.try_recv` before synchronous `event::poll`, establishing the former arrival-during-poll delay by source inspection; no measured execution of deleted code is claimed.
- [x] 1.2 @unit (agent) Keep terminal and activity sources continuously ready while completion and then the animation deadline are ready -> native macOS `mise run //apps/kuru-tui:test -- ui::tests::` exited 0, 16/16 passed in `/private/tmp/kuru-async-ui-unit-final.log`. Root reviewed the production `next_wake` regression: continuously ready terminal input, queued completion/activity and an elapsed persistent deadline are serviced in one four-source rotation; a ready sibling progresses through the production cooperative yield. Native Windows remains separately required by 4.3.
- [x] 1.3 @unit (agent) Queue more activity than the completion-drain cap while another sender continues producing -> final native macOS runtime filter passed 5/5 in `/private/tmp/kuru-async-runtime-final.log`. Root reviewed 300 queued events plus 128 events from a separate sender thread: the production completion handler consumes 256 receives, leaves 172 queued, applies the exact command, refreshes real Harness fields and settles. The bounded view tail remains intact; new sends do not extend the captured receive budget.

## 2. Completion, cancellation and failure order [critical]

- [x] 2.1 @regression (agent) Inject same-generation success, command feedback and dispatch failure plus an obsolete completion around cancellation -> native macOS `mise run //apps/kuru-tui:test -- ui::runtime_tests::` exited 0, 5/5 passed in `/private/tmp/kuru-async-runtime-final.log`. Root reviewed the production `apply_completion` fixture: current turn text/token metadata, command notice and dispatch failure settle through one handler; deliberately stale runtime fields are refreshed from the real Harness for each current outcome. An obsolete generation preserves transcript, status, busy/operation state, runtime fields, completion lock and the live job handle. The actual cancellation/next-turn PTY and native Windows are separately tracked in 2.2 and 4.3.
- [x] 2.2 @regression (agent) Cancel a controlled provider request, preserve a prepared draft, then submit and complete a second turn in the real PTY -> native macOS named PTY fixture exited 0 in `/private/tmp/kuru-async-native-cancel.log`. Root reviewed the real controlled HTTP request, preserved draft, next-turn response and exact 32/20 token metadata retained across resize, absent late response, one persisted completed turn and restored terminal. Production cancellation awaits abort before generation fencing; stopped history retains the next draft and excludes late output. Native Windows remains separately required by 4.3.
- [x] 2.3 @integration (agent) Close the activity sender while terminal input and completion remain usable -> native macOS UI filter passed 16/16 in `/private/tmp/kuru-async-ui-unit-final.log`. Root reviewed actual broadcast closure through the production scheduler, monotone source disabling, then usable terminal resize and typed completion with a future animation deadline. The separate real-Harness completion and native PTY cases establish completion application/rendering; this is composed scheduler/handler/native evidence, not a claim that the live Harness dropped its own sender during that PTY run.

## 3. Terminal failure and compatibility [critical]

- [x] 3.1 @regression (agent) Drive terminal-stream EOF, terminal read error and draw/backend failure while a controlled TUI job is active -> historical native macOS execution passed in `/private/tmp/kuru-async-real-loop.log`, but exact-head PR 13 Linux run 34739401407 job 103676414200 failed the read-error case with `started=1, dropped=0`. Source inspection showed that the UI awaited only its outer dispatch while `Actor::Drop` aborted the nested provider-owning task without awaiting it; the controlled provider also published its started notification before installing its drop guard. After correcting both ownership levels, the exact real-Harness fixture exited 0 locally on macOS, 1/1 passed in 2.80 seconds, `/private/tmp/kuru-async-nested-cleanup-regression.log`; all EOF/read/draw cases preserve their original contextual error and observe every announced provider future dropped before return. Exact-head PR 13 run 34741242743 job 103681192150 then passed the corrected native Linux coverage and behavior step, including this regression, source install and packaged offline runtime.
- [x] 3.2 @e2e (agent) Exercise the applicable real PTY/ConPTY terminal error path and inspect the native terminal after process exit -> native macOS `real_pty_error_unwind_restores_terminal_and_reports_the_original_error` exited 0 in `/private/tmp/kuru-async-native-error-unwind.log`. Root reviewed completed-frame synchronization, one real EventStream consuming the parent's key, reader drop, a controlled input failure, explicit successful TerminalSession restoration, process exit and native termios/mode escape assertions. Combined with 3.1's real-loop failure propagation and owned-job cleanup, this establishes Unix restoration; it is not an OS-origin read-error injection. Actual Windows console modes/ConPTY remain required by 4.3.
- [x] 3.3 @equivalence (agent) Run the complete existing view, scene, runtime-adapter, slash-command, visual, PTY, preference, trust and persistence behavioral targets once under coordinated workspace coverage -> `mise run coverage` in `/private/tmp/kuru-phase0-checkpoint` exited 0 (session 83966), 19,989/20,960 lines = 95.37%, `/private/tmp/kuru-phase0-checkpoint-coverage.log`. Existing TUI library (34 tests), terminal-driver and other presentation/integration targets passed, including the original saved-selection/database-write-failure PTY case. The deliberately ignored terminal fixture entry is driven by its parent native regression. This tree uses the established memory backend and excludes the separate ordered-migration implementation.
- [x] 3.4 @regression (agent) Prime one real Unix EventStream, stop its PTY child, make SIGWINCH and bracketed-paste input ready, then resume it without sending another event -> observed 2026-09-13 on macOS: the parent boundedly observed the owned child's actual `SIGSTOP` state before queuing either event; the default Mio baseline and corrected level-polled source each reported both exact events in 32/32 real PTY attempts; the synchronized corrected probe again passed 32/32 and the corrected exact application smoke passed. This does not reproduce the Linux failure on macOS. The Linux edge-loss cause remains an inference from Crossterm's pinned source and the failed Linux smoke until 3.5 passes natively.
- [x] 3.5 @regression (agent) Run the co-ready EventStream probe and the exact navigation smoke on native Linux with the level-polled source -> before correction, PR 13 run 34737517678 job 103671394472 timed out after resize with an empty composer instead of `pasted text`. Exact-head PR 13 run 34741242743 job 103681192150 passed its native Linux coverage and behavior step with both `real_event_stream_preserves_co_ready_resize_and_paste` and `real_pty_accepts_chat_navigation_commands_and_restores_terminal`; the former observes the requested resize and exact bracketed paste without later input, and the latter reaches restored process exit.

## 4. Dependency and repository gates

- [x] 4.1 @integration (agent) Inspect the resolved locked graph and compile all TUI targets -> root's locked offline `cargo tree -p kuru -e features -i crossterm` exited 0 and attributes event-stream to the TUI. Reviewed async-specific Cargo.lock changes contain only crossterm-to-futures-core and kuru-to-futures/async-trait edges, without version changes; the sibling provider-retry change separately adds the already-locked direct `httpdate` edge. Final TUI all-target typecheck exited 0 in `/private/tmp/kuru-async-typecheck-final.log`; the interactive path constructs one EventStream and contains no synchronous poll/read.
- [x] 4.2 @integration (agent) Run focused TUI format, lint, typecheck and scheduler/native terminal targets through owning mise tasks, then the single coordinated workspace coverage task -> Previously recorded focused TUI checks remain applicable to byte-identical source; checkpoint root format and docs checks exited 0. `mise run coverage` in `/private/tmp/kuru-phase0-checkpoint` exited 0 (session 83966), 19,989/20,960 lines = 95.37%, `/private/tmp/kuru-phase0-checkpoint-coverage.log` with the production scheduler included. After selecting `use-dev-tty`, the locked offline feature tree showed the app-owned edge and only the existing locked `filedescriptor` package was added to Crossterm's lock entry; focused TUI format, all-target typecheck and lint passed. This is local macOS/static evidence; final exact-head coordinated coverage and native Linux/Windows remain required.
- [ ] 4.3 @integration (human) Run the applicable terminal scheduling, cancellation, EOF/error and restoration targets in the supported native Windows job before merge -> actual EventStream and ConPTY behavior passes; local Unix execution or Windows cross-compilation is not accepted as runtime evidence.
