## 1. Checked move outcome

- [x] 1.1 In `packages/kuru-memory/src/files.rs`, add a private typed proof for an initially uncertain move whose held source identity and name remain present while the destination is absent; preserve the original `PublicationError` and OS code, reclassify it as rejected, and keep existing `move_directory` callers and moved-success reconciliation behavior unchanged. The frozen `files.rs` is `98d1f3d2e170d2d07e73cdb2e22aa48b4dacb67098bbd0b0dd73b70b5d153872`; independent review confirmed the typed proof and unchanged erased caller contract.
- [x] 1.2 Add focused filesystem regressions for moved success, proven no-move, occupied or rebound destination, changed or unobservable source, observation failure, and non-access-denied errors; verify only the exact proven no-move result exposes the private retry marker and every original typed error remains available. The host provision native-test selection passed 7/7 and the exact untyped uncertain no-move error test passed 1/1; Windows-only unsafe-state execution remains task 3.3.

## 2. Windows runtime activation recovery

- [x] 2.1 In `packages/kuru-memory/src/provision.rs`, retry only Windows bundled-runtime activation marked proven no-move with raw OS error 5, at 20-millisecond spacing for at most two seconds, using one retained source `Directory`, private stage and cache lock in the same async future; use async waits, preserve explicit stage-before-lock release, and leave all other activation and non-Windows behavior unchanged. The frozen `provision.rs` is `5c87c5cc158b68b0d8c5685ca952987521ff829eb6b9982a1b8c825fa8475304`; independent review confirmed the narrow predicate, async timing, ownership and release ordering.
- [x] 2.2 Replace the existing manual held-descendant recovery with an actual native regression that fails before the fix, releases the real blocker only after observing the first denied no-move attempt, and passes through production activation after the fix; add persistent-blocker, preflight-permission-denied, unsafe-state and original-error controls that prove bounded failure, stage and lock retention, and no ineligible retry. The frozen native tests are `00a1fd636e1b7dce9e8e23222b770afad0a8fabb9585a547bcb41d4309b584d3`; the release log supplies the pre-fix access-denied, retained-source and absent-destination failure, while corrected Windows execution remains task 3.3.
- [x] 2.3 Add a native cancellation regression that cancels during the async recovery wait and verifies the same future releases source, stage and cache-lock authority together without a later move or partial destination. The regression is present in the independently reviewed frozen native-test source and remains pending native execution under task 3.3.

## 3. Documentation and verification

- [x] 3.1 Correct `docs/development.md` to describe the narrow bounded Windows activation recovery after access denied and checked no-move, while retaining strict failure and staging guidance for every other outcome. `mise run docs:check` passed, including the VitePress build and public link and anchor checks (`/private/tmp/kuru-activation-docs-check.log`); an initial sandbox attempt failed on tool cache and hook permissions before checks began and is not acceptance evidence.
- [x] 3.2 Run focused filesystem and provisioning regressions, memory package typecheck and lint, Rust and documentation formatting/checks, strict Cospec validation, and diff checks; record Windows-only checks as pending until native execution rather than inferring them from the host. Memory typecheck and lint passed; the host provision native-test selection passed 7/7; the exact files regression passed 1/1; Rust formatting and `git diff --check` passed. An earlier malformed exact-test invocation exited 1 before executing tests and is not acceptance evidence. Native execution remains task 3.3.
- [ ] 3.3 Run the deterministic native Windows recovery and refusal regressions plus ordinary memory, optimized executable installation and native mise acceptance in PR CI; verify the actual held-blocker recovery, persistent failure, cancellation, offline first use and reopen all pass before archive. Record the exact shipping staged Release result after merge as deferred delivery verification, not as a pre-archive gate.
