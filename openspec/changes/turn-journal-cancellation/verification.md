## 1. Journal identity and retry [critical]

- [x] 1.1 @integration (agent) complete a controlled turn in real Dolt, retry its exact ID/request, then reuse the ID with a changed prompt or target in that session and independently in another session -> `engine::publication_tests::completed_turn_retry_is_exact_and_session_scoped` passed locally on macOS: the retry was byte-equivalent JSON with the provider count and two-row transcript unchanged; changed same-session reuse failed without mutation, while a fresh session completed with the same ID. The fixture provider returned no calls, so no tool dispatch was possible. It passed again after `run_controlled` heap-pinned its admitted future at the caller boundary: `/private/tmp/kuru-pr18-stack-runtime-exact-retry.log` exits 0.
- [x] 1.2 @integration (agent) stop after durable started state before possible dispatch, reopen the store, and resume the same ID -> `engine::publication_tests::stopped_started_turn_resumes_once_but_possible_turn_never_dispatches` passed after a real store close/reopen: the started turn completed with one public user row and one four-row private deliberate/speaker history, with no pre-restart private work.
- [x] 1.3 @integration (agent) stop after the possible marker around actor mailbox admission, reopen, and retry -> the same stopped/reopened fixture passed with zero provider calls before close and no call-count increase on the uncertain retry. Because the durable marker was written through the production seam without sending actor `Work`, no downstream tool, cognitive, or A2A operation was admitted.

## 2. Atomic answer boundary [critical]

- [x] 2.1 @integration (agent) inject failure and lost acknowledgement within the start and end message/state mutations against real Dolt -> `kuru_memory::store::tests::{failed_database_batch_rolls_back_and_committed_receipt_reconciles,checkpoint_validates_one_namespace_and_atomic_state_inputs}` passed locally: the duplicate receipt failed after attempted message/state changes and rolled both back, while simulated lost acknowledgement reconciled without replay. `accepted_ended_checkpoint_wins_cancellation_and_publication_error` also passed after the real ended checkpoint and injected publication error, asserting the exact journal output, assistant row, session, topology, and single session-index entry before byte-equivalent retry.
- [x] 2.2 @runtime (agent) cancel or fail after admission at representative stages -> `provider_cancellation_is_durable_and_releases_the_actor_permit` and `ordinary_post_admission_failure_records_interruption_without_an_answer` passed locally against real Dolt. Both retained exactly the user row and `started`/`possible_dispatch`/`interrupted` lifecycle without an assistant; the cancellation case then completed a new turn. The former passed again after `run_controlled` heap-pinned its admitted future, without changing cancellation handling: `/private/tmp/kuru-pr18-stack-runtime-cancellation.log` exits 0. Native Windows remains unrun in row 5.2.
- [x] 2.3 @regression (agent) complete at the periodic-dream boundary, then cancel and fail dreaming -> `periodic_dream_cancellation_preserves_the_exact_completed_output`, `stale_periodic_dream_failure_preserves_the_exact_completed_output`, and `dreaming_uses_isolated_actor_histories_and_runs_periodically` passed locally. Cancellation after the response and a live-write-induced stale candidate failure each preserved byte-equivalent retry with no provider-count increase; returned events ended at `response`, while live activity carried later `dream` and failure events.

## 3. Explicit cancellation [critical]

- [x] 3.1 @runtime (agent) cancel before actor enqueue, during semaphore wait, and during a controlled provider future -> `provider_cancellation_is_durable_and_releases_the_actor_permit` passed locally with `max_parallel = 1` and an untargeted turn: pre-admission cancellation wrote no row and called no provider; during the turn exactly one provider entered while the other peers waited, cancellation settled them without another call, durable interruption was recorded, and a later targeted turn succeeded.
- [x] 3.2 @integration (agent) cancel built-in file/shell and stdio/HTTP MCP operations before dispatch and after a real fixture receives one request -> integrated-tree `review_tests::{cancellation_before_shared_tool_dispatch_runs_no_file_mutation,accepted_file_mutation_survives_cancellation_without_replay,cancelled_shell_turn_reaps_the_observed_owned_process_without_replay,cancelled_http_mcp_mutation_is_received_once_and_session_is_closed}` passed locally on macOS against real Dolt. The shared pre-dispatch case cancelled while the speaking provider was held before returning its file call and created no file. The accepted file remained exact once. The shell published a complete positive PID, retry issued no second call, successful ToolHost shutdown completed, and pinned `nix` signal-0 reported only `ESRCH` for that PID. HTTP received one mutation, retry did not replay it, and negotiated-session shutdown sent one DELETE. On the same integrated source, connector `mcp::tests::cancelled_stdio_call_is_never_dispatched_twice` passed with the compiled peer before state-lock admission and after one received `tools/call`; the shared runtime cancellation/journal boundary plus that owned stdio proof is the recorded composition, not a claim of one runtime-stdio fixture.
- [x] 3.3 @integration (agent) cancel peer routing, state/remember mutations, and outbound A2A before and after admission -> integrated-tree `review_tests::{accepted_cognitive_writes_reconcile_before_cancellation_stops_peer_work,cancelled_admitted_peer_consultation_is_not_replayed,cancelled_outbound_a2a_request_is_received_once_without_replay}` passed locally against real Dolt and loopback A2A. The accepted note and modeled state published exactly once, cancellation stopped the later peer call, a received consultation was not repeated, the external server received one `SendMessage`, each uncertain retry dispatched nothing, and later mutations/turns succeeded.
- [x] 3.4 @integration (agent) cancel a real-Dolt dream during actor wait, candidate writes, and promotion reconciliation -> `explicit_cancellation_keeps_live_dream_state_and_private_histories_isolated` passed locally during actor wait. Integrated-tree `dream::cancellation_tests::{cancelled_dream_keeps_an_accepted_candidate_write_isolated,accepted_dream_promotion_wins_cancellation_and_publishes_exactly}` also passed: an accepted candidate-only row and revision remained readable while live state stayed unchanged, and the promotion fixture paused only after live revision advance, then cancellation lost the race and the exact durable/in-memory topology published once. A later turn succeeded in both cases; no candidate deletion occurred.

## 4. Terminal and shutdown [critical]

- [x] 4.1 @e2e (agent) drive real PTY cancellation and compose the scheduler-sensitive answer-winning branch at the shared presentation boundary -> native macOS `real_pty_cancels_provider_work_preserves_draft_and_accepts_the_next_turn` passed locally through `run_loop_with_stream` and `cancel_operation`: the TUI rendered cancellation, retained the interrupted prompt, preserved the next draft, completed the next turn, and fenced the late response. `ui::runtime_tests::cancellation_consumes_an_answer_that_won_the_completion_race` passed through the same `cancel_operation` and `apply_completion` functions with the typed output, proving the settled answer is presented before generation fencing; runtime `accepted_ended_checkpoint_wins_cancellation_and_publication_error` proved that output was the exact durable ended checkpoint. The real PTY has no deterministic seam for the post-checkpoint scheduler instant, so this row records the three-layer production-boundary composition rather than claiming a real-PTY answer-win race. Native Windows remains in row 5.2.
- [x] 4.2 @integration (agent) stall shutdown dreaming while actor and tool fixtures are active -> integrated-tree `review_tests::shutdown_reports_dream_deadline_and_mcp_cleanup_failure_together` passed with Tokio's test clock after real dream admission: the 30-second dream deadline fired, negotiated-session DELETE was observably attempted, its injected 500 failure was retained alongside the dream timeout, and shutdown settled without conflating either result with native cleanup confirmation.
- [x] 4.3 @integration (agent) send duplicate A2A `messageId` requests and one changed request through the real loopback server -> `tests::a2a_server_enforces_auth_validates_protocol_and_returns_peer_output` passed locally against its loopback router and real Dolt: exact duplicate returned the same text with no provider-count increase, changed reuse returned `-32603`, and a 257-byte ID returned `-32602`.
- [x] 4.4 @regression (agent) integrate the archived checkpoint parent, then carry the full `Asynchronous terminal event scheduling` requirement as MODIFIED in this change's existing chat-harness delta -> the integrated delta retains all six inherited scenarios and requires normal signal, typed settlement, answer-race authority and later generation fencing. Abnormal terminal cleanup signals when it retains a token and falls back to abort-and-await only without one. The combined source retains these production branches, the typed answer-winning and real-PTY cancellation composition in row 4.1 passed, and strict cospec validation passed against the integrated living requirement.

## 5. Quality and portability

- [ ] 5.1 @integration (agent) run focused memory, connector, runtime, TUI, format, lint, typecheck, docs, and the one coordinated workspace coverage task -> final `mise run format:check`, the memory/runtime/TUI package lint and typecheck tasks, and `mise run docs:check` passed locally after the production cut. The initial sandboxed format/docs attempts stopped in tool cache/macOS configuration access before source validation; their normal-access reruns passed. Named memory/runtime/TUI tests above passed. On combined MCP/head-tail/journal source, the focused runtime tests in rows 3.2–3.4 and 4.2 plus connector `mcp::tests::cancelled_stdio_call_is_never_dispatched_twice` passed; runtime lint/typecheck and normal-access format check then passed. After the admitted-future caller-stack boundary, runtime typecheck and lint passed (`/private/tmp/kuru-pr18-stack-runtime-typecheck.log`, `/private/tmp/kuru-pr18-stack-runtime-lint.log`), along with the exact-retry and cancellation logs cited above. The first new runtime run stopped at the sandbox's denied loopback reservation before its normal-access pass; an accepted-file fixture iteration was stopped after its provider incorrectly treated retained history as a new receipt; shutdown fixture iterations first exposed the missing Tokio test clock and then an over-eager virtual cleanup deadline before their corrected passes. The coordinated coverage run remains pending.
- [ ] 5.2 @integration (agent) run native macOS/Linux and Windows cancellation fixtures for provider, shell/MCP ownership, real Dolt, and PTY shutdown -> each platform observes the same journal result while native cleanup evidence remains platform-specific

## 6. Agent-facing authority

- [ ] 6.1 @eval (agent) inspect captured provider requests, tool/A2A fixture counts, frozen `TurnOutput`, and reopened journal/transcript state across cancellation and retry -> completed retry introduces no new model/tool input, uncertain retry dispatches nothing, private actor history is not duplicated, and maintenance dream events do not alter the authoritative answer
