## 1. Rule, schema and trust parity [critical]

- [x] 1.1 @integration (agent) Parse layered TOML and validate equivalent published schema fixtures for selectors, actions, defaults, replacement arrays, bounded anchored patterns and unknown keys -> core package tests/typecheck/lint passed. Parser/schema fixtures cover documented native/MCP/A2A entries, unsupported keys/actions/patterns, Unicode scalar length bounds, DEL/C1 controls, drive prefixes and replacement arrays.
- [x] 1.2 @integration (agent) Run automatic-ancestor trust preflight with fake permission rules and changed manifests -> core provenance/manifest tests passed; app `automatic_permission_claim_is_reviewed_but_trust_does_not_grant_a_call` passed: automatic ask rules require review, approved trust still leaves an unattended write refused, and no file is created.
- [x] 1.3 @unit (agent) Permute overlapping allow/ask/deny rules and legacy boolean fallback -> core fixtures passed for deny/ask/allow precedence, explicit rules overriding fallback, false write/delete/shell asking, configured MCP/A2A default allow and unconfigured alias denial.

## 2. Effect-bound tool decisions [critical]

- [x] 2.1 @integration (agent) Exercise checked native file read/write, shell, configured stdio/HTTP MCP and runtime outbound A2A through real fixture effects under allow/ask/deny -> focused connector fixtures passed native file/shell, stdio MCP discovery without tools/call, and HTTP MCP with zero denied calls. Runtime real-Dolt fixtures passed allow/ask/deny A2A HTTP counts and typed Denied events. PTYs exercised approved and denied actual file effects.
- [x] 2.2 @integration (agent) Change target, full arguments, root identity or MCP/A2A route after a once decision; probe symlink, protected path and literal filename metacharacters -> focused physical-case and pending-approval target-replacement fixtures passed with zero changed-target effect. Exact arguments remain owned and immutable across the one-shot; frozen A2A endpoint mismatch is refused before HTTP. Literal grants, root replacement and private-store context invalidation fixtures passed. The root-replacement test now expects all tool dispatch to refuse the changed workspace context; no platform capability primitive changed.
- [x] 2.3 @runtime (agent) Invoke asked native/MCP/A2A work from direct CLI, unattended A2A and dreaming without attached foreground approval -> direct CLI and unattended runtime fixtures passed typed permission-required refusal with zero effects. The dream regression passed rejection of proposed file and A2A calls with zero effects; dreams retain their stricter existing dream_suggest-only inventory and never acquire a foreground sender.
- [x] 2.4 @eval (agent) Script a provider that proposes an asked or denied tool and then observes the typed tool-result receipt -> the focused real-Dolt provider fixture passed both ask and deny paths: it received two exact-ID ToolResult blocks with is_error=true and the respective refusal text on later speaking requests; a model-authored approval claim produced neither a file nor an HTTP request.

## 3. Foreground grants and revocation [critical]

- [x] 3.1 @e2e (agent) Drive once/session/always/deny in real PTYs with file and whole-tool displayed scopes -> macOS real PTY tests passed all four file decisions at 80x24 and 120x35, checked file effects and grant revocation, shell whole-tool cancellation/denial, and a 454-byte literal path across 48x24 to 120x35 resize. Two UI unit tests passed, including lossless-scope choices, draft preservation and inspector selection/detail scrolling.
- [x] 3.2 @integration (agent) Restart and resume sessions, mutate reviewed manifest/tool route, revoke session/always grants and inject private-store failure -> nine app store/CLI tests passed for matching reopen, context/root change, independent-handle updates, revocation and storage failures. Runtime tests passed session reset on Harness construction and changed A2A endpoint rejection with zero HTTP requests. PTYs passed revocation of both session and always scopes.
- [x] 3.3 @e2e (agent) Cancel, close UI and deliver late approval during a newer operation -> real shell PTY cancellation preserved the draft and produced no marker; late answers did not authorize a subsequent denied invocation. Runtime closed-channel and cancellation/late-reply tests passed with zero HTTP requests. Operation-scoped channels are dropped before worker cleanup on completion, cancellation and shutdown.

## 4. Repository and platform verification

- [ ] 4.1 @regression (agent) Run scoped core/connector/runtime/app tests, typecheck, lint, format, generated schema/docs checks and strict cospec validation -> final source and docs pass without weakening existing checks.
- [ ] 4.2 @regression (agent) Run the normal combined local coverage gate -> the behavioral suite passes with at least 90% workspace line coverage; record failures and corrected reruns explicitly.
- [x] 4.3 @equivalence (agent) Review final diff against trust, safe file roots, no-sandbox shell wording, current MCP preflight and unchanged unrelated protocols/modes -> independent connector and runtime reviews are clear; root app/store/UI review is clear. Checked physical targets are re-derived after approval, endpoint/context checks precede A2A dispatch, deny precedes grants, unattended operations have no approval sender, and trust/root/MCP startup boundaries remain separate.

## Local evidence in progress

Supported native Windows/macOS/Linux CI, including Windows ACL/path, tool cleanup and terminal acceptance, remains required before merge and release. Record its run/job links during PR delivery after the archived commit is pushed. This separates local archive acceptance from subsequent remote verification; no native CI pass is claimed here.

- Root app storage/CLI filters passed 9/9 with the app's configured `RUST_TEST_THREADS=2`: `/private/tmp/kuru-phase1-permission-app-focused-final-2.log`, `APP_PERMISSION_FINAL_2_EXIT=0`. This covers absent state, literal scopes, restart/revocation, independent handle updates, changed binding/root, corrupt/oversized/invalid records, held locks, capacity-preserving refusal and replaced store paths. An earlier unrestricted direct Cargo run exceeded the process file-descriptor limit; its log is retained as `...app-focused-final.log` and is not a passing check.
- Independent core/schema review resolved control-character, drive-prefix and Unicode-length parity issues. Independent service/store review resolved remembering a scope whose display was shortened/redacted; forged session/always choices now refuse when the exact scope cannot be displayed losslessly. Full integration and native CI remain pending.
- Docs build, formatting, lint, public content and link/anchor checks passed: `/private/tmp/kuru-phase1-permissions-docs-final-2.log` (exit 0). The first sandbox run failed at hook setup permissions; the next run identified schema JSON formatting, corrected with the docs app formatter before this successful rerun.
- Runtime's eight focused real-Dolt permission tests passed; connector focused checks include actual native/MCP effects and physical-case/target replacement during a pending ask. Final combined coverage and supported native CI are not yet claimed.
- Initial combined coverage failed (exit 101, 585.72s): `/private/tmp/kuru-phase1-coverage.log`. Two terminal tests and one visual test assumed fixed composer rows before the permission dock was added; they now locate the rendered separator and all three focused reruns passed. The retained-root tool-host assertion was updated to the P6 context-invalidation contract and its focused rerun passed. The two added receipt/dream acceptance tests passed. No successful coverage percentage is claimed for that failed run.
- Final docs recheck after workspace-invalidation wording passed (exit 0): `/private/tmp/kuru-phase1-permissions-docs-final-3.log`.
