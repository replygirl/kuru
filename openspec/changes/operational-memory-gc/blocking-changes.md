# Dependencies

## Blocked by

None.

## Soft-blocked by

- [ ] `turn-journal-cancellation` — supplies settled explicit dream cancellation and its accepted-write boundary for the full cancellation proof; the memory lifecycle remains independently correct without it
- [ ] `memory-export` — supplies the revision-pinned export view used to prove GC preserves active exports; receipt and candidate maintenance remain independently correct without it
