## 1. Checked removal foundation

- [x] 1.1 Add the consuming checked private-directory tree removal primitive with rejected/uncertain outcomes and native replacement/link fixtures; verify package-owned Unix checks and record native Windows separately.

## 2. Managed purge authority

- [x] 2.1 Add provider/engine-free `MemoryStore::purge` control-record, verified inventory, quiescence-before-intent, quarantine, reconciliation, and ordinary-open guard; verify real-Dolt purge/reopen, live-owner, interrupted/replaced-root, legacy, and cross-project fixtures.
- [x] 2.2 Preserve original/shared legacy snapshots, exports, caches, and stable locks while removing only recognised selected-project managed trees; verify isolated sentinel fixtures.

## 3. CLI and diagnostics cleanup

- [x] 3.1 Add the explicit `kuru memory purge --yes` command under the existing writer lease and a canonical app diagnostics-ring cleanup with truthful partial failure; verify real isolated CLI help/refusal/completion and exact cleanup scope.

## 4. Documentation and verification

- [x] 4.1 Document purge/history and retained-copy boundaries; verify owning docs checks.
- [x] 4.2 Run focused package/app checks, record evidence in the verification ledger, and leave unrun native Windows/combined coverage explicit.
