## 1. Publication survives a failed stage cleanup [critical]

- [x] 1.1 @regression (agent) force bounded cleanup exhaustion through the `#[cfg(test)]` removal-failure hook on a host cold provision -> `provision::tests::exhausted_stage_cleanup_publishes_the_engine_and_receipts_the_retained_stage` passed 3/3 on macOS: the open returns the published binary and `verify_version` runs it.
- [x] 1.2 @unit (agent) inspect the leftover after that forced exhaustion -> the same fixture asserts the `.install-*` stage is still on disk with its probe copy, and the owner-only `versions/.leftovers/<stage>.json` records relative paths, first cause with `(os error 145)`, 88 attempts, 2003 ms, the publication digest, target, engine version and `published: true`; `files::tests::a_failed_stage_cleanup_retains_the_stage_and_names_its_cause` asserts the retained identity and typed exhaustion.
- [x] 1.3 @unit (agent) exercise the rejected and uncertain publication paths unchanged -> the whole package suite passed (159 lib tests, `mise run //packages/kuru-memory:test` green), including `rejected_activation_preserves_verified_stage_and_occupied_destination` and `activation_source_open_failure_preserves_stage_before_releasing_cache_lock`; only `finish_published` changed, and the `retain` paths and the 2 s bound are untouched.
- [~] 1.4 @regression (agent) run the Windows-native variant using the existing delete-pending fixture shape from `packages/kuru-memory/src/files.rs` tests -> defer: Windows native execution is pending. Both Windows-gated activation tests now assert the retained-stage report instead of an error — `provision::native_tests::published_engine_retains_its_failed_stage_cleanup_and_releases_cache_lease` and, after review finding 5.1, `provision::native_tests::held_cold_probe_copy_does_not_block_candidate_activation` (its `unwrap_err()` on the held-probe path would have panicked on the Windows shard). `cargo check --target x86_64-pc-windows-msvc -p kuru-memory --lib --tests` cannot run on this macOS host (`libsqlite3-sys`'s vendored `sqlite3.c` fails in `cc-rs`), so the Windows-gated code is review-verified only; the same check for `kuru-platform` does pass here.

## 2. Collection

- [x] 2.1 @integration (agent) open again after a receipted leftover exists -> the sweep removes the stage and its receipt under the installation lock. `provision::tests::cold_provision_sweeps_a_receipted_stage_before_creating_a_new_one` (cold path, lock held before `PrivateTemp::new`) and the extended `exhausted_stage_cleanup_publishes_the_engine_and_receipts_the_retained_stage` (warm path, follow-up open) both passed 3/3 on macOS.
- [x] 2.2 @unit (agent) make the swept removal fail -> stage and receipt both remain. `provision::tests::sweep_leaves_a_stage_and_its_receipt_when_removal_is_rejected` forces a real `RemovalError` via a symlink descendant (the same cross-platform trick `fs.rs`'s `checked_tree_removal_rejects_a_symlink_descendant` already uses), taking the identical `Err(_) => outcome.remaining += 1` arm the sweep gives an `Uncertain` result; passed 3/3. A literal native `Uncertain` phase remains Windows-only and is review-verified (see 1.4/4.2), same as `RemovalError`'s existing Rejected/Uncertain split in `fs.rs`.
- [x] 2.3 @unit (agent) place an `.install-*` directory with no receipt beside a receipted one -> only the receipted stage is removed. `provision::tests::sweep_collects_only_receipted_stages` passed 3/3.
- [x] 2.4 @integration (agent) warm-open with no `versions/.leftovers` while another process holds the installation lock -> the open completes without acquiring or waiting on the lock. Pre-existing `provision::tests::warm_verification_does_not_wait_for_the_installation_lock` still passes unchanged; the new `provision::tests::warm_open_skips_the_sweep_when_the_installation_lock_is_busy` covers the receipts-present variant (a busy lock is skipped, not waited on, and the receipt survives for the next attempt) and passed 3/3.
- [x] 2.5 @unit (agent) exceed `LEFTOVER_STAGE_CAP` receipts -> the count is reported and nothing additional is deleted. `provision::tests::sweep_reports_the_cap_without_deleting_any_uncollectable_stage` passed 3/3: `SweepOutcome::reached_cap` is set and every stage/receipt is left in place. After review finding 5.2 the outcome is actually consumed: both call sites pass it to `report_leftover_stage_cap`, which emits one `kuru.memory` record with `collected`/`remaining` when the cap is reached, and the docs sentence was corrected to describe counting and reporting rather than a bound the code does not enforce.

## 3. Surfacing and documentation

- [~] 3.1 @e2e (agent) run a CLI command whose open retains a stage, expecting exactly one `Memory:` notice line on stderr after `Memory: ready.`, and `run --json` stdout to parse as JSON -> defer: an actual subprocess-level retained stage cannot be forced on this macOS host — the exhaustion hook that produces one (`ForcedStageCleanupFailure`) is `#[cfg(test)]`-only inside `kuru-memory` and not reachable from the compiled `kuru` binary a subprocess test spawns, and a real Windows `os error 145`/32 repro needs a Windows host (see 1.4/4.2). Verified instead at two levels: (a) unit-level, `cli::memory_progress_output_tests::notice_is_retained_after_the_terminal_ready_line_on_one_non_terminal_stream` drives the real `MemoryProgressOutput` (with an injected in-memory sink in place of stderr) through `stage()` -> `complete()` -> `notice()` in the same order `open_memory` calls them, and asserts the exact byte sequence ends with `Memory: ready.\n` then the notice line; `notice_never_writes_once_the_sink_has_failed` confirms a disabled output takes no further action. `open_memory`'s own control flow gates the notice call behind the `Ok` branch only, after `output.complete()`, and never in the `Err`/abandon branch (read, not separately unit-tested, since it is a 3-line guarded call). (b) regression, the unchanged real subprocess test `apps/kuru-tui/tests/cli.rs::cli_memory_progress_is_bounded_and_keeps_json_on_stdout` still passes 1/1, proving `run --json` stdout still parses and the stderr line sequence is unchanged on the ordinary path where this stage never fires — i.e. the new code path adds nothing on the common path. A true e2e forcing a real retained stage remains open for 4.2 / a Windows host.
- [x] 3.2 @unit (agent) inspect the diagnostics ring after a retained stage -> one structured warn record with the typed report fields; no memory write and no model-visible surface. `diagnostics::tests::layer_admits_the_retained_install_stage_target_and_its_fixed_fields` drives a real `JsonLayer`+`Ring` pair with the exact `target: "kuru.memory"` event `provision.rs`'s `emit_retained_stage_diagnostic` emits, and asserts `stage`, `digest`, `published`, `os_error`, `attempts`, `elapsed_ms` all round-trip through the JSONL ring file and that no `session`/`turn` (conversation-correlation) field is present; passed 3/3. Required extending `diagnostics.rs`'s `admitted_target` and `SafeFields::allowed`, which otherwise silently drop unlisted targets/fields (per `admitted_target`'s and `SafeFields::allowed`'s own logic and the pre-existing `layer_rejects_foreign_and_unadmitted_secret_fields_across_rotation` test, which relies on exactly that drop behavior for an unadmitted target). No memory write: `emit_retained_stage_diagnostic` only calls `tracing::warn!`, nothing in `MemoryStore`/Dolt. No model-visible surface: the event never touches a provider request or the conversation transcript; only the diagnostics ring's private JSONL files receive it.
- [x] 3.3 @unit (agent) run the progress-stage tests -> every stage including the new one reports exactly once through the widened bitmask. `provision::tests::exhausted_stage_cleanup_reports_the_retained_stage_exactly_once` (new) and `provision::tests::observed_provision_reports_actual_cold_warm_and_failure_stages` (pre-existing, unchanged) both passed 3/3; the whole `cargo test -p kuru-memory --lib` suite (165/165) and `cargo test -p kuru --lib` suite (73/73) passed once each with the widened `u16` bitmask/16-slot channel in place.
- [x] 3.4 @unit (agent) run docs build and link/content checks -> `docs/memory.md` and `apps/kuru-docs/concepts/memory.md` state leftover install stages and their collection, and checks pass. Growth sentence added to both files' existing growth paragraph; `mise run docs:check` passed (toolchain check, oxlint, oxfmt --check, vitepress build, and `kuru-delivery docs` link/anchor content check all green).

- [x] 3.5 @unit (agent) force the receipt write itself to fail -> the retention is reported as uncollectable, not as scheduled for a later cleanup. `provision::tests::a_stage_whose_receipt_cannot_be_written_is_reported_as_uncollectable` puts a plain file where `versions/.leftovers` belongs, forces the bounded exhaustion, and asserts the open still returns a usable published binary while the progress channel carries `RetainedUnreceiptedInstallStage` and never `RetainedInstallStage`; `provision::tests::a_receipt_that_cannot_be_written_is_recorded_on_the_report` asserts `StageCleanupReport::receipt_error` is set and the pre-existing file is untouched; `cli::memory_progress_output_tests::an_unreceipted_retained_stage_never_promises_a_later_cleanup` asserts that notice text does not say "for later cleanup" and does name manual removal; the extended `diagnostics::tests::layer_admits_the_retained_install_stage_target_and_its_fixed_fields` asserts `receipt_error` and the cap record's `collected`/`remaining` round-trip through the real ring. All passed 3/3.

## 4. Static and review

- [x] 4.1 @unit (agent) run scoped memory and TUI format, lint and typecheck -> all pass on the host with the verified bundle mirror. `cargo fmt --check -p kuru-memory -p kuru`, `mise run //packages/kuru-memory:lint`, `mise run //apps/kuru-tui:lint`, `mise run //packages/kuru-memory:typecheck`, `mise run //apps/kuru-tui:typecheck` all clean (each ran the prerequisite `bundle:prepare` against the exported `KURU_DOLT_BUNDLE_DIR` mirror automatically).
- [x] 4.2 @manual (agent) independently review publication exactness, the unchanged two-second bound, the no-delete-on-uncertainty rule and lock discipline -> independent review ran and returned blocking findings on the review-follow-up set (a Windows-only test still expecting the old fatal contract, an untrue documented growth bound, and a swallowed receipt-write failure). All are fixed and recorded in tasks 5.1-5.3 and verification 1.4, 2.5 and 3.5. The reviewed invariants themselves are unchanged by the follow-up: publication outcomes still come straight from `finish_published` (rejected stays rejected, uncertain-retained stays retained), `CLEANUP_RETRY_LIMIT` is untouched, the sweep still leaves both stage and receipt on any `Err(_)` removal, and every sweep still runs under a held `&CacheLock`.
