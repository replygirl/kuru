## 1. Finite built-in shell environment [critical]

- [x] 1.1 @regression (agent) Spawn an isolated test process with fake allowed, provider/auth, proxy, SSH-agent, arbitrary Kuru, askpass, loader, shell-startup and unknown values, then invoke the real Unix built-in shell through `ToolHost` -> the pre-fix native fixture failed on ambient sentinel inheritance (exit 101, `/private/tmp/kuru-shell-environment-baseline.log`). The final isolated Unix fixture passed through `mise run //packages/kuru-connectors:test -- isolated_toolhost_shell_receives_only_compatibility_environment` (exit 0, 1 test, `/private/tmp/kuru-shell-environment-unix.log`), asserting exact PATH/home/temp/locale/XDG values and excluded sentinels without printing their values; the instrumented outer child retains its profile destination while the production shell excludes it.
- [ ] 1.2 @unit (agent) Project complete fake Unix and Windows maps through the private helper, including non-Unicode values, mixed-case Windows names, duplicate case-equivalent entries, inherited and absent `PATHEXT`, and hostile OS-path replacements -> the result contains exactly the normative keys/values; duplicate inherited compatibility names reject deterministically while excluded/native-derived input duplicates are ignored; native-derived Windows values replace hostile inputs, and missing `PATHEXT` alone selects `.COM;.EXE;.BAT;.CMD`.
- [ ] 1.3 @integration (agent) Run the real built-in shell on native Windows with fake parent sentinels through absolute stock Windows PowerShell 5.1 -> stock modules and controlled bare `.exe`/`.cmd` commands run with native-derived `SystemRoot`/`WINDIR`/`ComSpec`, inherited or fallback `PATHEXT`, no case-varied `PSModulePath`, and no excluded sentinel; cross-compilation is not accepted.

## 2. Preserved command and protocol behavior [critical]

- [x] 2.1 @integration (agent) Exercise a controlled bare command, project cwd, home/temp write, real status, bounded stdout/stderr, timeout and retained-root replacement through the minimized built-in shell -> the final isolated Unix fixture resolves bare `touch` through controlled PATH and verifies actual project/home/temp writes (exit 0, 1 test, `/private/tmp/kuru-shell-environment-unix.log`). The unchanged status/output/timeout and retained-root replacement fixtures each passed (exit 0, `/private/tmp/kuru-shell-environment-existing-shell.log`). These observations preserve existing behavior and do not establish synchronous Unix process-group disappearance; native Windows has separate required rows below.
- [x] 2.2 @integration (agent) Start the real local stdio MCP fixture with one fake inherited non-allowlisted arbitrary sentinel and one configured `McpConfig.env` override -> `mise run //packages/kuru-connectors:test -- isolated_stdio_mcp_keeps_inherited_environment_and_configured_override` passed (exit 0, 1 test, `/private/tmp/kuru-shell-environment-mcp.log`). The real peer recorded only boolean observations proving arbitrary-value inheritance and configured-value precedence over a conflicting parent value; shell projection remains confined to built-in shell launches.
- [x] 2.3 @eval (agent) Inspect the constructed shell tool schema and rendered tools/protocol documentation -> Root inspected the final shell ToolSpec and documentation source; both name the limited inherited environment and explicit process authority/no filesystem sandbox. The checkpoint documentation build and public content/link checks exited 0 (`/private/tmp/kuru-phase0-checkpoint-docs.log`). The Windows description distinguishes omitting inherited PSModulePath from standard path reconstruction inside PowerShell.

## 3. Repository acceptance

- [x] 3.1 @integration (agent) Run focused connector tests through `mise run //packages/kuru-connectors:test -- <filter>` during implementation, then package lint/typecheck and the documentation check -> The final isolated Unix ToolHost and real MCP fixtures each passed 1/1 in their named logs. Connector lint/typecheck and the post-test-addition lint exited 0; checkpoint docs/format checks also exited 0. Its integrated coverage passed all 98 connector tests, including real local child fixtures. Native Windows remains separately required by 3.2.
- [ ] 3.2 @runtime (agent) Run the applicable shell environment and cleanup fixtures in the supported native Windows job before merge -> actual Windows PowerShell, environment-block and process-tree behavior passes; local deferral is recorded as unrun rather than treated as evidence.
- [x] 3.3 @regression (agent) Run the single coordinated workspace coverage task after implementation -> `mise run coverage` in `/private/tmp/kuru-phase0-checkpoint` exited 0 (session 83966), 19,989/20,960 lines = 95.37%, `/private/tmp/kuru-phase0-checkpoint-coverage.log`; the Unix shell projection and native isolated shell/MCP cases executed under instrumentation with no coverage exclusions or threshold changes.
