## 1. Truthful observed memory open [critical]

- [x] 1.1 @integration (agent) run isolated cold, warm, explicit-binary, corrupt-cache, extraction-failure, and version-mismatch opens -> each sequence reports only actual fixed stages; cold reports extraction, warm reports cache verification, failures omit ready, and every existing integrity guard remains effective. Observed `observed_provision_reports_actual_cold_warm_and_failure_stages` passed via `mise run //packages/kuru-memory:test -- observed_provision_reports_actual_cold_warm_and_failure_stages` (exit 0; `/private/tmp/kuru-startup-provision-observed-stages-test-final3.log`): it asserts exact cold/warm, corrupt-archive, version-mismatch, and explicit-binary sequences. `failed_observed_open_never_reports_ready` passed (exit 0; `/private/tmp/kuru-startup-failed-open-test.log`).
- [~] 1.2 @integration (agent) hold project and cache startup locks, drop an observer, and cancel at existing owned boundaries -> defer: this lane did not repeat the existing lifecycle lock/cancellation matrix. `dropping_open_observer_does_not_cancel_the_store` passed (exit 0; `/private/tmp/kuru-startup-drop-observer-test-retry.log`), establishing decorative receiver loss; existing lifecycle fixtures remain the ownership evidence.
- [x] 1.3 @e2e (agent) drive an isolated CLI cold and warm open through real embedded-engine provisioning -> ordered stderr stages end in ready only after usable memory, while exact JSON stdout remains parseable. Observed `cli_memory_progress_is_bounded_and_keeps_json_on_stdout` passed via `mise run //apps/kuru-tui:test -- cli_memory_progress_is_bounded_and_keeps_json_on_stdout -- --nocapture` (exit 0; `/private/tmp/kuru-startup-cli-progress-timing-final.log`), with isolated macOS cold `5.560448208s` and warm `471.479584ms` wall observations. They are measurements only, not an optimization claim.

## 2. Legacy privacy and preserved import [critical]

- [x] 2.1 @e2e (agent) open a real legacy SQLite database with committed WAL through the CLI -> current-project rows import into verified Dolt while original SQLite, WAL, and layout remain unchanged. Observed `cli_imports_a_real_legacy_wal_without_changing_its_layout` passed (exit 0; `/private/tmp/kuru-startup-cli-legacy-wal-test-final.log`): it reads back the exact committed WAL row through the CLI export JSON while preserving SQLite, WAL, and layout bytes.
- [~] 2.2 @integration (agent) use a Unix 0755 legacy data directory and a Windows owner-privacy fixture -> defer: Windows-specific owner-privacy execution requires native CI. Unix `cli_refuses_an_unsafe_legacy_directory_before_import` passed (exit 0; `/private/tmp/kuru-startup-cli-legacy-permission-test-final2.log`), including a path with spaces and shell metacharacters and unchanged source bytes.

## 3. Application presentation and platforms

- [~] 3.1 @integration (agent) run provider-free notes, early config/trust/auth, startup failure, and non-memory commands -> defer: this lane did not repeat the unrelated command matrix. Focused failure and non-memory behavior remains covered by existing routes; this lane observed JSON cold/warm output.
- [x] 3.2 @runtime (agent) exercise a real PTY startup -> progress precedes the first completed TUI frame and normal terminal restoration remains intact. Observed `real_pty_accepts_chat_navigation_commands_and_restores_terminal` passed (exit 0; `/private/tmp/kuru-startup-pty-progress-test.log`), asserting staged stderr before alternate-screen/completed TUI frame and existing restoration behavior.
- [~] 3.3 @integration (agent) run native Windows observed provisioning/import fixtures -> defer: native Windows execution requires CI after implementation.
- [~] 3.4 @integration (agent) run coordinated workspace coverage -> defer: root schedules the sole coverage writer after focused source freeze.
