## 1. Persisted ordered upgrade [critical]

- [x] 1.1 @integration (agent) Open a stopped real pinned-Dolt version-1 project containing byte-sensitive messages, state, operation history and a later main revision through the production writable path -> native macOS `mise run //packages/kuru-memory:test -- stopped_released_v1_upgrades_once_and_readonly_preserves_it` exited 0, 1/1 passed. Root reviewed the fixture and `/private/tmp/kuru-stopped-v1-upgrade.log`: full message/state/operation tuples and marker bytes survive one direct-child schema-2 upgrade and stopped reopen; the production registry validates the exact compiled receipt, and its full stored tuple and final HEAD remain identical on reopen. Read-only and Windows acceptance remain separately tracked in 1.3 and 5.2.
- [x] 1.2 @regression (agent) Present missing, changed, duplicate and out-of-order migration receipts plus supported-old and unsupported-future version rows -> native macOS `mise run //packages/kuru-memory:test -- store::migrations::tests::` exited 0, 9/9 passed in `/private/tmp/kuru-migration-registry-tests-final.log`. Root reviewed persisted missing/changed/extra receipts and future schema rejection, the private v3 registry's repeated-operation and out-of-order receipt cases, and committed future receipt authority in schema 1. Each rejection preserves the captured main HEAD, complete ref inventory and working status before another attempt; valid v1/v2/v3 progression is exercised separately in 1.1 and 2.5. Production remains schema 2; native Windows is separately required by 5.2.
- [x] 1.3 @integration (agent) Open the same old project read-only and inspect a current project -> final native macOS `mise run //packages/kuru-memory:test -- stopped_released_v1_upgrades_once_and_readonly_preserves_it` exited 0, 1/1 passed in `/private/tmp/kuru-final-stopped-v1.log`. Root reviewed the old read-only version-1/required-2 error, unchanged marker/base HEAD, clean status and sole original branch, followed by writable upgrade and successful current read-only inspection at the exact upgraded HEAD. Stopped writable reopen preserves all original message/state/operation tuples and the exact receipt. Native Windows remains separately required by 5.2.

## 2. Isolated attempt recovery and publication [critical]

- [x] 2.6 @regression (agent) Open attached/live and stopped current-schema stores and read supported historical branches with ordinary dirty data or DDL, then with dirty schema/receipt authority or malformed reserved refs including a malformed committed v1 receipt table hidden by dirty drop -> ordinary data remains readable, invalid authority fails closed at every supported version, and each outcome preserves exact heads, refs and full working status without migration or publication. Observed 2026-09-12: focused native macOS real-Dolt tests passed: `readonly_inspection_preserves_dirty_data_and_rejects_dirty_authority` (attached and stopped v2 ordinary DDL reads, retained writable-clean baseline rejection, dirty `kuru_schema`, dirty `kuru_migrations`, and malformed reserved-ref rejection) and `historical_v1_read_rejects_dirty_dropped_receipt_authority` (committed malformed v1 receipt authority followed by a dirty `DROP TABLE`; `dolt_status` exposed the receipt-table deletion and the actual `MemoryStore::history` call rejected it; the same fixture rejects dirty v2 historical receipt authority). `stopped_v1_candidate_survives_upgrade_and_stays_stale` also passed after adding ordinary dirty v1 branch DDL before actual `MemoryStore::history`. Each fixture snapshots exact head, ordered refs, and ordered full `dolt_status` before and after the read or rejection. Native Windows remains unrun locally and is tracked by 5.2.

- [x] 2.1 @integration (agent) Interrupt the production runner after branch creation and before manual commit, wait for the accepted session to disappear, then cold reopen -> native macOS `mise run //packages/kuru-memory:test -- process_loss_` exited 0, 2/2 passed in `/private/tmp/kuru-process-loss-helper-cases.log`. Root reviewed actual caller-process termination after accepted DDL, owned descendant quiescence before independent inspection, exact clean schema-1 main/base, retained failed branch HEAD/schema/ordered dirty tuple, source state and candidate HEAD/content. Cold open preserves that exact failed branch and publishes one fresh schema-2 migration; stopped second reopen adds no revision. The second case proves failed-observation cleanup retains its primary error and leaves the accepted dirty attempt preserved. Full lifecycle/platform acceptance remains separately tracked in 3.2 and 5.2.
- [x] 2.2 @integration (agent) Lose the real branch-creation reply and separately lose the real migration `DOLT_COMMIT` reply through bounded native wire faults -> native macOS named production-runner fixtures exited 0 in `/private/tmp/kuru-production-lost-branch.log` and `/private/tmp/kuru-production-upgrade-lost-commit.log`. Root reviewed actual packet interception: branch-reply loss waits for the exact generated ref at its exact base, commit-reply loss waits for durable branch advancement, and each observes its original routed session disappear. Current validation and stopped reopen preserve one migration commit without replay; the branch case also preserves seeded source state and its exact named ref. Native Windows remains separately required by 5.2.
- [x] 2.3 @integration (agent) Lose the real `DOLT_MERGE(..., '--ff-only')` reply after durable publication and exercise a base outcome -> named native macOS fixtures exited 0 in `/private/tmp/kuru-production-lost-fast-forward.log` and `/private/tmp/kuru-production-absent-fast-forward.log`. Root reviewed actual reply discard only after main equals the validated target, and a separate request discard before dispatch; both observe exact routed-session teardown. The absent case independently inspects clean schema-1 main and the exact ready branch hash, then stopped reopen reaches that same target with the retained branch unchanged, source data preserved and one migration commit/receipt. Native Windows remains separately required by 5.2.
- [x] 2.3 @regression (agent) Stage the exact version-2 receipt authority with schema DDL, then exercise committed, dirty, lost-commit-reply and lost-fast-forward-reply paths -> native macOS `mise run //packages/kuru-memory:test -- <four named filters>` exited 0, 4/4 passed in `/private/tmp/kuru-next-staged-schema-receipt-final.log`. The recovery helper now pins the registered V2 receipt ID, DDL and digest; committed views use the production current-schema validator, while dirty branches retain raw `AS OF 'HEAD'` source inspection and the public historical reader rejects pending receipt authority. The fast-forward fixture releases its proxy and original v1 main pool before acquiring a fresh current-schema view, so validation observes the published receipt rather than a closed cached pool. No production validation was changed; native Windows remains separately required by 5.2.
- [x] 2.4 @regression (agent) Present malformed/excess/unknown-step reserved refs, multiple ready targets, unexpected heads, mismatched name/receipt/ancestry, dirty changes outside the registered step and dirty non-base states -> native macOS `mise run //packages/kuru-memory:test -- migration_attempt_rejection_` exited 0, 3/3 passed in `/private/tmp/kuru-migration-rejection-tests-final.log`. Root reviewed real distinct completed targets with matching receipts, two pristine targets, mismatched name/receipt, dirty and non-base attempts, exact 64 retained-failed capacity and malformed/unknown/excess inventories, comparing main and each attempted branch's HEAD/ref/status snapshots before and after rejection. Earlier registry 9/9 evidence in `/private/tmp/kuru-migration-registry-tests-final.log` additionally covers historical receipt/parent-schema/ancestry rejection. No reset, deletion, merge or new attempt occurs; native Windows remains separately required by 5.2.
- [x] 2.5 @integration (agent) Retain clean completed and recognized dirty failed branches for an applied step, append later conversation commits, and exercise a registered next step plus stopped reopen -> native macOS `mise run //packages/kuru-memory:test -- retained_v2_attempts_and_candidate_survive_test_v3_progression` exited 0, 1/1 passed; the private test registry reused the exact pristine v3 attempt, retained v2 completed/dirty refs and the stale candidate, accepted a new candidate and later writes, then independently reopened real Dolt with identical final head, data, receipt chain and ref inventory and exactly one v3 commit. Root reviewed the fixture and `/private/tmp/kuru-migration-registry-v3-native.log`; production remains schema 2, and Windows is covered separately by 5.2.

## 3. Cancellation and lifecycle ownership [critical]

- [x] 3.1 @runtime (agent) Cancel a production `MemoryStore::open` after migration acceptance at controlled branch-create, DDL, commit and publication boundaries while keeping Tokio alive -> native macOS `mise run //packages/kuru-memory:test -- cancelled_upgrade_call_retains_writer_through_accepted_ddl_boundaries` exited 0. Root reviewed the five-boundary fixture and `/private/tmp/kuru-cancelled-upgrade-boundaries.log`: caller abort is observed, a nonblocking acquisition proves the checked startup File remains locked, a competing opener waits, and the retained worker finishes before the next writable open observes clean schema 2 with preserved source state and exactly one migration receipt/ref. Actual process loss and Windows remain separately required in 3.2 and 5.2.
- [x] 3.2 @integration (agent) Terminate a native fixture process during an accepted attempt and reopen from another process -> native macOS `mise run //packages/kuru-memory:test -- store::recovery_tests::process_loss_after_accepted_ddl_retains_attempt_until_cold_recovery -- --exact --nocapture` exited 0, 1/1 passed on 2026-09-12. Before terminating the creator, the fixture observes both its checked startup lock and exact lifecycle lease held, polls an actual production `MemoryStore::open` contender once and requires `Pending`, then terminates and reaps the creator. That same open future reaches the existing pre-branch boundary only after takeover, where an actual read-only attachment observes unchanged clean schema-1 main, the exact dirty failed attempt and the original candidate before mutation. Recovery publishes one fresh schema-2 attempt, preserves the failed attempt and candidate, and a stopped reopen adds no revision; final exact-lease acquisition proves lifecycle release. The existing concrete-guard lifecycle fixture `server::tests::owner_drop_transfers_installed_reap_guard_until_real_child_reaps` also passed 1/1, directly proving the installed startup `File` remains held until the real child exits. Native Windows remains separately required by 5.2.
- [x] 3.3 @integration (agent) Hold an inspection-owned server while attempting a writable old-schema open -> native macOS `mise run //packages/kuru-memory:test -- store::migration_lifecycle_tests::` exited 0, 2/2 passed in `/private/tmp/kuru-migration-lifecycle-tests-rerun.log`. Root reviewed the inspection fixture: valid bounded writer options reach the exact lifecycle-lock refusal, the inspector remains responsive with identical v1 HEAD/refs/status/schema, and normal upgrade succeeds only after inspection ownership closes. Native Windows remains separately required by 5.2.
- [x] 3.3 @integration (agent) Cancel `close_migration_worker` after its real guarded `Server::close_installed_guard` has entered pool shutdown -> native macOS `mise run //packages/kuru-memory:test -- interrupted_migration_close_handoff_retains_guard_until_supervisor_quiesces` exited 0, 1/1 passed in `/private/tmp/kuru-migration-close-handoff-test-immediate-final.log`. The fixture holds a checked-out pool connection solely to observe close entry, aborts the close worker, then either proves real Dolt remains responsive while the original startup guard is contended or, at the first observed successful startup-guard acquisition, requires the first poll of `Server::quiescence_at` to be ready with the lifecycle lease. Thus an early startup-guard release cannot be hidden by the later bounded cleanup wait. It drops the connection and uses the ten-second quiescence check only for fixture cleanup. No production API or cleanup behavior changed. Native Windows remains required by 5.2.

## 4. Staging and historical preservation [critical]

- [x] 4.1 @integration (agent) Initialize a fresh store and import a byte-sensitive SQLite WAL fixture through an owned live staging session -> native macOS lifecycle filter passed 2/2 in `/private/tmp/kuru-migration-lifecycle-tests-rerun.log`. Root reviewed fresh/current receipt validation and committed-WAL import of Unicode, NUL-containing messages and exact raw JSON; activation revisions and legacy receipts match, staging is absent after activation, unrelated project rows are excluded, original DB/WAL/snapshot bytes remain identical, and stopped reopen adds no migration commit. Live shutdown/move interruption is separately tracked in 4.2 and Windows in 5.2.
- [x] 4.2 @integration (agent) Interrupt staged migration before ready publication and at server shutdown/move boundaries -> final native macOS `mise run //packages/kuru-memory:test -- process_loss` passed 3/3 in `/private/tmp/kuru-process-loss-all-final.log`. Root reviewed the fresh stage's actual accepted DDL, creator termination and descendant quiescence, retained live Directory identity through stopped inspection and interrupted relocation, exact clean schema-1 main plus dirty failed-attempt snapshot, and different newly activated directory. The prior actual before/after-marker fixture passed in `/private/tmp/kuru-marker-preservation.log`, proving live quiescence refusal, observer-loss cleanup, endpoint retirement and identity-preserving ready/unready moves. These are combined boundary observations, not an injected filesystem move error; native Windows remains separately required by 5.2.
- [x] 4.3 @integration (agent) Upgrade main while a version-1 candidate contains candidate-only rows -> final native macOS `mise run //packages/kuru-memory:test -- stopped_v1_candidate_survives_upgrade_and_stays_stale` exited 0, 1/1 passed in `/private/tmp/kuru-final-v1-candidate.log`. Root reviewed the exact original candidate name/HEAD/schema-1 rows before and after stopped reopen, stale promotion rejection, successful current candidate data publication and later main conversation writes. Final HEAD and the single migration revision remain unchanged on reopen. Native Windows remains separately required by 5.2.
- [x] 4.4 @regression (agent) Recover a stopped format-1 ready stage left by the released schema-1 binary -> native macOS `mise run //packages/kuru-memory:test -- ready_released_v1_stage` exited 0, 2/2 passed. Root reviewed the fixture and `/private/tmp/kuru-ready-v1-stage.log`: original marker bytes and seeded state survive activation and stopped reopen with one direct-child schema-2 migration; dirty state, a future schema row, and canonical reserved v2/v3 attempts report their intended rejection without activation or marker rewriting. Independent stopped inspection retains the original main HEAD and rejected state or ref. Native Windows remains separately required by 5.2.

## 5. Native and repository acceptance [critical]

- [ ] 5.1 @integration (agent) Run focused named production migration, wire-fault, cancellation, lifecycle, staging and candidate cases through `mise run //packages/kuru-memory:test -- <filter>` on the current native Unix host during implementation -> each real-Dolt case passes without skip and drains every owned process/session; the complete behavioral targets run once under workspace coverage.
- [ ] 5.2 @integration (human) Run the same applicable persisted-upgrade, interruption, owner-loss and recovery fixtures in the supported native Windows job -> real Windows Dolt, process and lock behavior passes before merge; local deferral and cross-target compilation are not accepted as runtime evidence.
- [ ] 5.3 @integration (agent) Run repository format, strict package lint and typecheck plus the single coordinated workspace coverage task -> all checks pass, every production migration behavioral target executes once under coverage, production migration code remains included and workspace line coverage stays at or above 90 percent.
- [ ] 5.4 @manual (agent) Inspect final schema registry, receipt SQL, reserved-name parser, version-dispatch table and user memory documentation -> definitions are immutable and bounded, no old branch/reset/delete path exists, and schema/activation/identity/protocol versions are described separately.
