## 1. Manifest schema v2 parsing and provenance branching [critical]

- [x] 1.1 @unit (agent) both parsers accept the committed v2 manifest and reject v1, missing/unknown `provenance`, `url` on built, `build` or `notices` on upstream, unknown recipe, non-linux-x64 host, pseudo-version not ending in `upstream_commit[..12]`, malformed `h1:` sum, bad ICU digest, built `license_sha256` differing from upstream, and unknown fields -> each case yields its specific error Observed: `cargo test -p kuru-memory --test bundle_build` (`schema_two_provenance_build_and_notice_rules_fail_closed`, `malformed_or_ambiguous_manifests_cannot_produce_a_bundle` with schema 1) and `cargo test -p kuru-delivery --test bundle_prepare` (`built_entry_rules_are_enforced_by_the_independent_preparer`, `manifest_rejects_ambiguous_unbounded_and_network_unsafe_inputs`) each assert the specific error text per case; all passed on macOS arm64. The lenient published-Windows reader rejects v1 (`committed_schema_two_manifest_selects_only_the_windows_x64_engine`).
- [x] 1.2 @unit (agent) `"unpinned"`/`null` sentinels accepted only on built assets and rejected on upstream; mixed pinned/unpinned archive fields rejected -> errors observed Observed: memory cases `/assets/0/archive_sha256 = "unpinned"`, `/assets/5/compressed_bytes = 1` and `/assets/5/provenance = "upstream"` fail with "all pinned or all unpinned" / "can never be unpinned"; delivery cases `unpinned-upstream` and `mixed` fail likewise (passed locally).
- [x] 1.3 @unit (agent) bound checks: built compressed > 64 MiB, expanded > 128 MiB, and `exe + license + Σ notices != expanded` rejected; upstream zip rule unchanged -> errors observed Observed: `pinned_built_asset_is_catalogued_with_its_notices` rejects built compressed > 64 MiB and expanded > 128 MiB ("exceeds budget") and an expansion that omits the notices ("payload sizes"); the unchanged upstream zip rule still rejects `/assets/4/expanded_bytes` + 1 (passed locally).
- [x] 1.4 @integration (agent) `bundle prepare` for each upstream target against a manifest whose built entry is unpinned runs with a live HTTP fixture and never requests or reads built inputs; `bundle prepare --target aarch64-pc-windows-msvc` refuses with the build-task instruction -> fixture request log and message observed Observed: `bundle::tests::upstream_targets_fetch_only_their_own_archive_beside_an_unpinned_built_entry` parses the committed manifest, routes every preparer request through a recording proxy, and sees exactly five GETs, one per upstream target's own `dolt-*` archive, none naming ICU, llvm-mingw, Go or windows-arm64; `unpinned_built_entry_never_affects_upstream_preparation` and the CLI test `cli_requires_print_pins_for_the_committed_unpinned_entry` observe the exact build-task instruction for `aarch64-pc-windows-msvc` without creating a cache or work directory (passed locally).
- [x] 1.5 @regression (agent) the five upstream entries differ from main only by the inserted `"provenance": "upstream",` line (`git diff` on `dolt-assets.json`) and `catalog()` values for those targets are unchanged -> diff and test observed Observed: `git diff origin/main -- packages/kuru-memory/support/dolt-assets.json` removes only `"schema_version": 1,`; every other upstream change is an added `"provenance": "upstream",` line, and the sixth entry is appended. `target_selection_uses_requested_target_and_generates_coherent_versioned_payload_pins` checks each upstream catalog line keeps its pins with `notices: &[]`; the generated `ASSETS` lists only the five pinned assets.

## 2. Third-party notices required for built assets [critical]

- [x] 2.1 @unit (agent) built asset with empty or missing `notices`, duplicate or invalid notice `name`, `from` outside `{icu, llvm_mingw}`, or escaping `path` rejected -> errors observed Observed: memory and delivery tables reject empty/missing `notices`, duplicate `LICENSE-ICU`, `LICENSES`, lower-case names, `from: "go"` (unknown variant), `../`, absolute, backslash and non-`icu/` ICU paths, and half-pinned notices (passed locally).
- [x] 2.2 @integration (agent) runtime `extract_zip` on a fixture built-layout archive accepts exactly the declared notices with exact sizes, and rejects a missing, extra or resized notice; the real upstream windows-amd64 ZIP fixture still extracts unchanged -> tests observed Observed: `provision::native_tests::built_zip_extraction_accepts_exactly_the_declared_notices` extracts two declared notices privately, verifies them through `CheckedCache`, rejects a tampered cached notice by path, and rejects missing, extra, resized and undeclared notices without publishing a payload; `official_windows_archive_decodes_exact_pinned_payloads_on_every_host` still decodes the real upstream windows-amd64 ZIP with two payload files (`mise run //packages/kuru-memory:test` exit 0, 279 lib tests).
- [x] 2.3 @unit (agent) manifest toolchain pins (`go`, `llvm_mingw` url/sha256) equal `packages/kuru-memory/mise.lock` entries -> test observed Observed: `manifest_toolchain_pins_equal_the_memory_package_lockfile` (delivery `tests/bundle_build.rs`) compares the manifest `go` and `llvm_mingw` url/sha256 with `packages/kuru-memory/mise.lock` linux-x64 entries (generated by `mise lock --platform linux-x64` with mise 2026.9.4, lockfile_version 1); passed.

## 3. Source build command [critical]

- [x] 3.1 @integration (agent) `bundle build` with fake Go/clang toolchains and a fake module proxy/ICU server: refuses non-linux-x64 host and upstream assets, refuses offline mode, rejects wrong `Sum`, wrong ICU digest, wrong `go version`/clang version, non-ARM64 PE and disallowed imports, clears inherited toolchain environment, and writes the exact member layout -> subprocess fixtures observed Observed: `bundle::build::tests` (9 tests) drive the whole recipe through the injectable runner with fake Go/clang/make, a live local HTTP ICU server and generated PE images: refusals for non-linux-x64 host, offline mode, upstream target, missing `--print-pins`, reused work directory; rejects wrong Go/clang versions, wrong `Sum`, wrong version constant, mismatched LICENSES, tampered ICU bytes (no ICU build runs), non-stub data, PE machine 0x8664, `libc++.dll` import, failed steps (stderr tail reported) and a toolchain missing from PATH; asserts the exact member layout by decoding with `kuru-archive`, the go build environment/flags, pins drift with both digests, and identical bytes from fresh directories. `process_runner_clears_the_inherited_environment_of_real_children` runs a real `sh` child and proves inherited `CC`/`CFLAGS` are absent. Local smoke (macOS arm64 under `KURU_BUNDLE_BUILD_HOST_OVERRIDE=1`, non-authoritative, real Go 1.26.2 + llvm-mingw 20260922 macOS package + ICU 78.3): exit 0, PE imports advapi32, kernel32 and ten api-ms-win-crt DLLs, `dolt.exe` 118,142,976 B sha256 `0ec280ff…d3aa` (equal to the research probe), archive 40,719,327 B sha256 `3b767a14…3df5`, notices ICU 27,718 B `e55522d8…16af` (matches the pin), LLVM 15,141 B `8d85c105…afee`, mingw-w64 12,155 B.
- [ ] 3.2 @runtime (agent) GitHub Actions `ubuntu-latest` run of `mise run //packages/kuru-memory:bundle:build -- --target aarch64-pc-windows-msvc --print-pins` with the real pinned toolchains builds `dolt.exe` (PE 0xAA64) and prints pins including clang version and link inputs -> run ID and printed pins recorded Pending CI: round-1 `Bundle build` run on this PR.

## 4. Reproducible pins in CI [critical]

- [ ] 4.1 @runtime (agent) `bundle-build.yml` builds twice on `ubuntu-latest` in fresh private directories and `cmp` proves byte-identical archives; the job prints SHA-256 and size and uploads the archive -> run ID, digest and size recorded Pending CI: round-1 `Bundle build` run on this PR.
- [ ] 4.2 @runtime (agent) after the pins are committed (task P), the same workflow verifies the built archive, executable and notices against the manifest pins and passes -> run ID recorded Pending: task 4.1 (round 2).
- [x] 4.3 @integration (agent) the pin-verify step fails and reports both digests when fed an archive differing from the pin, and prints a visible unpinned notice when the manifest is unpinned -> local step invocation observed Observed: the workflow step script, extracted verbatim and run locally with GNU stat, printed `::notice::unpinned: pin verification not yet possible (observed 3b767a14…, 40719327 bytes)` (exit 0) for the committed manifest, `::error::Built archive differs from the committed pin: pinned aaaa… , observed 3b767a14…` (exit 1) for a wrong pin, and a match message (exit 0) for the equal pin.

## 5. Existing targets unaffected [critical]

- [ ] 5.1 @runtime (agent) every existing CI job for the five upstream targets (ci.yml static categories, native tests and coverage shards, installation) passes on the PR head -> run IDs recorded Pending CI: this PR head.
- [x] 5.2 @regression (agent) root `mise.lock` unchanged and `mise run cospec:managed:check`, `lint:tooling` pass locally -> commands observed Observed: `git diff origin/main --stat -- mise.lock` is empty; `mise run format:code ::: lint:rust ::: typecheck ::: lint:tooling ::: cospec:validate ::: cospec:managed:check ::: docs:check` exited 0 (actionlint included). With mise 2026.9.4, `MISE_LOCKED=1 MISE_TASK_RUN_AUTO_INSTALL=false mise run //packages/kuru-memory:bundle:prepare -- --target x86_64-pc-windows-msvc` succeeded without resolving the task-scoped Go/llvm-mingw tools.

## 6. Documentation

- [x] 6.1 @unit (agent) `mise run docs:check` passes with the updated bundled build inputs section naming linux-x64 as the only build host and the zip/flate2 re-pin rule -> command observed Observed: `mise run docs:check` passed ("Public docs artifacts, local links and anchors passed") with the new "Source-built engine inputs" section and the zip/flate2 re-pin paragraph.
