# Dependencies

## Blocked by

None.

## Soft-blocked by

None.

## Siblings

No other change is active in `openspec/changes/`. The durable receipts and
project memory service this change classifies already shipped. Planned session
claims (P30) and backup/restore (P20) consume this contract and must add their
calls to it; they are follow-ons, not prerequisites.
