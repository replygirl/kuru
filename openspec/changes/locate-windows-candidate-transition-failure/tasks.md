## 1. Capture the candidate failure

- [x] 1.1 Keep a focused real-Dolt `packages/kuru-memory/src/store/operational_gc_tests.rs` fixture that observes exact candidate-pool connection IDs, confirms their branch and server-side state after pool retirement, and reports whether checked rename succeeds without claiming an unseen race. Native result: four distinct candidate-pool IDs were verified, zero remained server-visible after close, and exact rename succeeded (1/1); this did not reproduce the hosted failure.
- [x] 1.2 Add only test-support owner diagnostics in `packages/kuru-memory/src/{service.rs,store.rs,test_support.rs}` as needed: fixed candidate stage and validated SQLSTATE/vendor class in an owned private file, with no SQL, branch, path, private row, credential, new RPC, or normal-product output. A held-branch native refusal emitted only `candidate_owner stage=branch_rename class=database sqlstate=HY000 vendor=1105` and retained its exact recovery assertions (1/1).
- [x] 1.3 Update only the failure diagnostic of `apps/kuru-tui/tests/cli.rs::candidate_commands_discover_and_abandon_one_exact_retained_ref` to prestart the actual owned service and include bounded allowlisted records; preserve every existing success, exact-ref and no-effect assertion. The actual-process CLI fixture passed 1/1; a post-sequence typed refusal proved that the captured private file belongs to the launched owner.

## 2. Verify the diagnostic

- [x] 2.1 Run the exact focused real-Dolt memory and CLI fixtures plus owning static checks, and independently review that diagnostic records reach captured test output without private content or behavior change. Native CLI, fixed formatter, four-session retirement and held-session recovery each passed 1/1; memory and TUI owning Clippy passed. Independent bounded review cleared the private 4 KiB allowlist and unchanged product RPC/CLI assertions. Hosted Windows classification remains unrun.
- [ ] 2.2 Publish #71 with normal hooks, inspect exact-head Windows application and memory-runtime jobs for the fixed stage and SQL class if the failure recurs, and use that evidence to choose a separate scoped production correction rather than a blind retry.
