## 1. Shards and collection are exact on every OS [critical]

- [x] 1.1 @unit (agent) run `mise run //packages/kuru-delivery:test` -> `SHARDS` covers exactly the eight workspace packages once; the shard package list is derived from `SHARDS`; collect rejects an artifact whose OS label differs from `artifact_os` and still selects each shard's latest valid attempt without fallback Observed 2026-09-26: macOS arm64, `mise run //packages/kuru-delivery:test` on the pre-rebase tree: `coverage::tests::shards_cover_each_workspace_package_exactly_once`, `artifact_os_labels_are_lowercase_runner_names`, `aggregate_takes_each_shards_latest_attempt_and_fails_closed` (which includes another OS's receipt being refused) and the aggregate layout tests pass.
- [ ] 1.2 @integration (agent) run the `release_workflow` tests in `mise run //packages/kuru-delivery:test` -> on every OS the shard matrix rows equal `SHARDS` (names, packages, order), five downloads land in `inputs/<shard>`, one `native-coverage-${{ inputs.os }}` key is saved only by `connectors-core-platform`, shard and collect carry identical `CARGO_PROFILE_TEST_DEBUG`, `timeout-minutes` equals `KURU_COVERAGE_JOB_MINUTES`, and no monolithic `coverage` job or `windows-coverage.ps1` reference remains Observed 2026-09-26: not yet satisfied. On the pre-rebase tree, `native_workflow_gate_rejects_incomplete_windows_results` and `native_workflow_shards_only_windows_and_keeps_the_aggregate_fail_closed` fail because they still assert the Windows-only topology. `release_workflow.rs` is rewritten against PR5's version after this branch is rebased onto `ci/native-parity-updater`.

## 2. The Rust orchestrator is fail-closed and unit-tested [critical]

- [x] 2.1 @unit (agent) run the orchestrator tests with the injected cargo/process boundary -> shard and collect sequencing, pre-existing target/diagnostics/report refusal, diagnostics written on every later failure, and the single `--fail-under-lines 90` report invocation are observed through the fake runner Observed 2026-09-26: macOS arm64: all 13 `coverage::orchestrate::tests` pass, including `shard_sequences_the_full_inventory_runner_and_receipt`, `collect_sequences_one_report_for_this_os`, `every_shard_failure_leaves_its_diagnostics` and `existing_directories_and_reports_are_refused`. A hand run of `coverage:shard` for `connectors-core-platform` against a throwaway target (a detached snapshot of this tree, `KURU_COVERAGE_OS=local`) exited 0 in 140 s. Its `attempt-1` evidence has a receipt (`target_os` macos, cargo-llvm-cov 0.9.1, 3 packages), `inventory.json`, `selection.json`, a 70-record runner ledger and 26 profiles. Only the executables of the selected packages ran; the rest were recorded as `omit`.
- [ ] 2.2 @unit (agent) run the `show-env` parser tests -> the captured Linux, macOS and Windows cargo-llvm-cov 0.9.1 fixtures parse into the expected maps; a line with an unknown escape or malformed name is rejected Observed 2026-09-26: `show_env_fixtures_parse_on_every_os` and `show_env_rejects_anything_but_exact_escapes` pass. Only the macOS fixture was captured (`macos-captured.pwsh.txt`); the Linux and Windows fixtures are authored (`*-authored.pwsh.txt`). Capture both on their runners before checking this item.
- [ ] 2.3 @integration (agent) run `mise run coverage` on macOS arm64 -> the 90% workspace line gate passes with the orchestrator and Unix supervision included Observed 2026-09-26: macOS arm64 `mise run coverage` (14:15 to 14:41) exited 101 only because of the two `release_workflow.rs` failures listed under 1.2, so the gate itself did not complete. `cargo llvm-cov report --summary-only` over the collected profiles reports TOTAL lines 93.90% (regions 89.15%, functions 91.32%). `coverage.rs` lines are at 86.53% and `coverage/orchestrate.rs` at 98.15%. Rerun after the rebase.

## 3. Unix shards have the same deadline and evidence as Windows [critical]

- [ ] 3.1 @integration (agent) run the `#[cfg(unix)]` supervision tests on macOS and Linux -> a real stalled child group with a grandchild is signalled before reap, reaped within the cleanup bound, and a `.stall.json` names unfinished tests and records `presence_after_reap`; a normally exiting child is reaped through the same path with its exit status preserved Observed 2026-09-26: macOS arm64: `unix_group_stall_is_terminated_reaped_and_reported`, `unix_group_exit_preserves_status_and_kills_group_before_reap`, `unix_group_observes_presence_only_after_reaping_its_root` and `supervision_terminates_a_stalled_tree_at_the_deadline_with_evidence` pass. Linux is pending CI.

## 4. Hosted CI runs the shard topology on every OS [critical]

- [ ] 4.1 @runtime (agent) GitHub-hosted runners for each `inputs.os` of `native-tests` (Linux x86_64, macOS arm64, Windows x86_64) on this PR's CI run -> five `Coverage shard (<os>, <shard>)` jobs succeed and upload receipts; `Coverage report (<os>)` downloads five receipts, enforces 90% lines once and uploads `ci-coverage-<os>-attempt-<n>`; the install job and `native-gate` pass Pending hosted CI: not run.
- [ ] 4.2 @runtime (agent) the same hosted runners after a failed "Re-run failed jobs" of one shard -> the collect job refuses because a shard did not succeed, and after a successful rerun accepts the later attempt Pending hosted CI: not run.
- [ ] 4.3 @runtime (agent) the same hosted runners on a throwaway draft commit with a never-ending `kuru-core` test -> on each of Linux, macOS and Windows, `connectors-core-platform` fails at `timeout-minutes` less the 10-minute reserve (65 of 75 minutes as specified) and uploads `…-coverage-<os>-connectors-core-platform-diagnostics-attempt-<n>` with a stall report, not a host cancellation Pending hosted CI: not run.
- [ ] 4.4 @runtime (agent) the first green hosted run on each OS -> per-shard wall-clock and compile/test split are recorded with run and job IDs Pending hosted CI: not run.
