## 1. Observe the existing Windows shell contract

- [x] 1.1 Add distinct bounded private stage markers to the negative control and Kuru shell flow in apps/kuru-tui/tests/windows_cli.rs, retaining all hash, environment, identity and deadline assertions. Source review and git diff --check passed; the initial native stage observations are recorded in 1.2.
- [x] 1.2 Run native Windows acceptance and record the actual stage sequence or failing stage. CI 34607501091, Windows job 103289452218, reached entered/version-checked/hash-started, then timed out after 30 seconds with the root still running. No hash completion marker appeared; command discovery and the hash body remain distinct hypotheses. Log: /tmp/kuru-windows-resume-ci-job-103289452218.log.
- [x] 1.3 Add bounded fixed-label pre/post command lookup observers and one timeout-only separate Core debugger trace, preserving the hash expression and 30-second budget; record the native result. CI 34612890798 passed the wrapped shell case at Windows log line 648, including its pre/post assertions. The timeout-only Core trace was not triggered. This establishes only wrapped-source behavior; the original unobserved request remains the required acceptance in 1.4. Log: /tmp/kuru-shell-lookup-ci-job-103307525914.log.
- [ ] 1.4 Keep the original unobserved source authoritative for both the poisoned control and Kuru request. Remove the temporary lookup observers, retain only the existing timeout-only Core trace, preserve any original failure regardless of diagnostic outcomes, and verify the original cold request in native CI. CI 34612890798 passed the observer-wrapped suite; the unchanged production source still needs this unobserved acceptance check.
- [ ] 1.5 Validate and archive the completed fixture diagnostics before the final PR commit, preserving the distinction between diagnostic evidence and a passing shell acceptance test.
