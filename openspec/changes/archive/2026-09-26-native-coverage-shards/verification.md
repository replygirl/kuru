## 1. Windows coverage runs five fail-closed shards [critical]

- [x] 1.1 @integration (agent) run `mise run //packages/kuru-delivery:test` on macOS arm64 -> `coverage` unit tests pass with five `SHARDS` (exact-shard aggregation, rerun/latest-attempt selection now keyed on `memory`), and `release_workflow` asserts the Windows matrix `include:` block equals `SHARDS` (names, packages, order), five shard downloads into `inputs/<shard>`, one `native-coverage-windows` key with `save-if` on `connectors-core-platform`, and `timeout-minutes` equal to `KURU_COVERAGE_JOB_MINUTES`. Windows-only (`#[cfg(windows)]`) tests do not compile on macOS and are named as skipped. Observed on macOS arm64 (2026-09-26): the task (`cargo test -p kuru-delivery --all-targets --all-features --locked --no-fail-fast`) exited 0; `coverage::tests::aggregate_requires_exact_shards_receipts_and_unchanged_profiles`, `native_workflow_shards_only_windows_and_keeps_the_aggregate_fail_closed` and `native_workflow_gate_rejects_incomplete_windows_results` passed. Mutation check: changing the workflow's `runtime` row to `packages: kuru-runtime,kuru-core` made the matrix assertion fail with a left/right diff; the workflow was restored. Skipped here: the `#[cfg(windows)]` tests in `powershell_diagnostics.rs` and the Windows-only test binaries.
- [x] 1.2 @integration (agent) run `mise run format:code ::: lint:rust ::: typecheck ::: lint:tooling ::: cospec:validate ::: cospec:managed:check ::: docs:check` -> exit 0, including actionlint over the edited workflows. Observed on macOS arm64: the chain exited 0; actionlint 1.7.12 `Found 0 errors in 5 files`; `Repository metadata invariants passed.`; `Public docs artifacts, local links and anchors passed (/kuru/).`; cospec validate `0 errors, 0 warnings`.
- [~] 1.3 @runtime (agent) GitHub-hosted `windows-2025` runners on this PR's CI run, expecting five `Windows coverage (<shard>)` jobs to succeed, the report job to download five receipts and enforce 90% lines, and `native-gate` to pass, with per-shard wall-clock recorded in the PR -> defer: hosted runners execute only after this archived record is pushed; the PR's own CI run is the evidence and its timings are recorded in the PR body.

## 2. native-build is cached without archiving private state

- [x] 2.1 @integration (agent) read rust-cache v2.9.2 (`6323deb1…`) `src/cleanup.ts` -> the save-time cleanup removes every non-`build`/`.fingerprint`/`deps` entry of each profile directory, so `target/<profile>/kuru-test-supervisors` is not archived; the bundle directory lives in `runner.temp`. Observed in the pinned source (raw.githubusercontent.com at `6323deb102c322ba6fcbdcafc7e3dddab59af2b6`, fetched 2026-09-26): `cleanTargetDir` identifies a profile by `build`/`.fingerprint`/`deps` and calls `cleanProfileTarget`, whose `rmExcept(profileDir, {build, .fingerprint, deps})` removes other directories through `io.rmRF`; `save.ts` runs this for every configured workspace target before saving. No pre-save cleanup step is added. `KURU_DOLT_BUNDLE_DIR` is set to `$RUNNER_TEMP/kuru-bundles` before any build (`packages/kuru-memory/build.rs` reads it).
- [~] 2.2 @runtime (agent) GitHub-hosted `ubuntu-24.04-arm` runner on this PR's CI run and a later run, expecting the rust-cache post step to save `native-build-aarch64-unknown-linux-gnu` first and restore it next, `kuru-memory:test` and `test:embedded-runtime` to pass after a restore, and job time compared with the pre-change median in the PR -> defer: only hosted CI runs of the pushed branch can show cache save/restore and timings; recorded in the PR body.
