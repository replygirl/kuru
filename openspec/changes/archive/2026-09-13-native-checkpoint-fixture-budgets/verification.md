## 1. Windows fixture truthfulness [critical]

- [x] 1.1 @regression (agent) rerun the native Windows workspace coverage command that failed at checkpoint `c2679f5` and later rejected a short/long-equivalent fixture path spelling -> exact-head run 34743192947 job 103686288053 passed the complete Windows workspace coverage command at 23,329/24,881 lines = 93.76%. All previously failing shell, refresh, picker, trust, advisory and runtime targets passed without changing production budgets or environment authority.
- [x] 1.2 @integration (agent) exercise the real stock PowerShell shell tool and model-to-tool replay -> the same Windows job passed `built_in_shell_reconstructs_stock_module_paths_without_losing_other_environment`, `isolated_toolhost_windows_shell_receives_projected_environment`, and `native_model_tool_replay_preserves_authority_and_returns_real_receipts`. Controlled real PowerShell source and side effects completed within the unchanged 30-second tool budget; typed receipts retained exact status/output and remained actor-private.
- [x] 1.3 @integration (agent) exercise real loopback connection refusal through both refresh gates -> one safe retry occurs, caller loss rolls pending state back before lease release, and an allowance held at the second gate expires without late dispatch. Observed 2026-09-12 on macOS with isolated stores and fake credentials: the three exact connector tests passed through `mise run //packages/kuru-connectors:test -- <test-name>`; caller loss completed in 0.33s, retry/publish in 0.42s, and held-gate expiration in 10.08s. Native Windows repetition remains part of 1.1.
- [x] 1.4 @e2e (agent) drive the native ConPTY picker flow, exit, and reopen the same project -> `native_conpty_chat_selectors_resize_focus_and_persistent_choices` passed in exact-head run 34743192947 job 103686288053. It selected and persisted mode, model and effort, exited, reopened the same project and observed the saved preferences while the config-only startup remained storage-free.
- [x] 1.5 @eval (agent) inspect completed model-to-tool receipts and rejected shell environment inputs -> `native_model_tool_replay_preserves_authority_and_returns_real_receipts`, the isolated Windows ToolHost shell, and the exact Windows projection inventory all passed in job 103686288053. Their existing observable assertions prove receipts stay actor-private and fake credentials, proxy, `PSModulePath` and arbitrary Kuru variables do not reach the stock PowerShell child.
- [x] 1.6 @regression (agent) launch a real Unix terminal child while the test runner owns a differently sized controlling terminal -> observed 2026-09-12 before correction: the live Kuru child controlled `/dev/ttys032` at 1x1 while fd 0/1/2 used `/dev/ttys033` at 35x120, and Crossterm remained in its UI loop without the expected frame. Observed 2026-09-13 after correction: the nested 7x19/35x120 regression reported the inner 120x35 dimensions and passed; the terminal-filtered package run under a 1x1 outer PTY passed all seven selected Unix terminal cases with one fixture entry ignored, and the separately selected provider-cancellation terminal case passed.
- [x] 1.7 @regression (agent) run the malformed, oversized and hard-linked trust-record fixture on native Windows -> `trust::tests::malformed_oversized_and_hard_linked_records_never_match` passed in exact-head run 34743192947 job 103686288053. Synthetic replacement used the checked movable operation handle under the retained pinned store anchor and every invalid record remained non-matching.
- [x] 1.8 @regression (agent) rerun the isolated ToolHost Windows shell fixture with private fixed stage markers -> `isolated_toolhost_windows_shell_receives_projected_environment` passed in exact-head run 34743192947 job 103686288053. Both inherited and fallback real PowerShell cases completed all fixed stages, exact projected values, side effects, typed receipt and owned cleanup within the unchanged production deadline.
- [x] 1.9 @regression (agent) run the incompatible-module control and Kuru shell with distinct private PowerShell cache roots -> `built_in_shell_reconstructs_stock_module_paths_without_losing_other_environment` passed in exact-head run 34743192947 job 103686288053 in 29.23 seconds. The isolated negative control reached the named real autoload failure; Kuru's independent first-use cache executed stock `Get-FileHash`, returned the independent digest and cleaned its owned process tree within the unchanged deadline. This passing native result is the acceptance evidence; it does not by itself prove the earlier shared cache was the sole cause of the historical timeout.
- [x] 1.10 @integration (agent) exercise inherited and fallback `PATHEXT` through the real stock Windows PowerShell child -> `windows_shell_environment_has_an_exact_independent_inventory` and `isolated_toolhost_windows_shell_receives_projected_environment` both passed in exact-head run 34743192947 job 103686288053. The pure launch projection retained the supplied value or conventional fallback; the real child observed PowerShell's subsequent `.CPL` addition, and controlled bare `.exe` and `.cmd` commands completed.

The hosted Windows checkpoint run `34737517678` is historical failing evidence,
not completion of these rows. It passed the earlier runtime replay, ConPTY,
refresh, environment inventory and other corrected targets, then failed the
ToolHost shell check without a labeled receipt and the trust corruption fixture
while its pinned operation handle denied removal. Follow-up run `34739401407`
proved every other Windows target green, including trust replacement and runtime
replay, but the ToolHost shell remained running with no captured output until its
production deadline. Exact-head run 34743192947 supplied the successful native
rerun; its complete job log is
`/private/tmp/kuru-checkpoint-windows-ci-91f.log`.

## 2. Local static acceptance

- [x] 2.1 @integration (agent) run formatting and granular checks for the affected packages -> Rust formatting, lint/type checks and strict Cospec validation pass. Observed 2026-09-12: `mise run format:rust`, `mise run //packages/kuru-connectors:typecheck`, `mise run //packages/kuru-connectors:lint`, `mise run //packages/kuru-runtime:typecheck`, and `mise run cospec -- validate native-checkpoint-fixture-budgets --strict` passed. Repeated 2026-09-13 after the lexical Windows shell fixture correction, the fixed per-case stage diagnostics, and the final PowerShell cache/PATHEXT fixture corrections: Rust formatting, connector and TUI typecheck/lint, and strict validation passed. A macOS-hosted Windows-target check stopped in unchanged `aws-lc-sys` because no Windows SDK headers are installed, before compiling Kuru source; native compilation and execution remain deferred below.
- [~] 2.2 @runtime (agent) run native Windows tests locally -> defer: this macOS checkout has no native Windows runtime; hosted Windows rerun is the authoritative evidence
- [x] 2.3 @integration (agent) run the focused Unix PTY tests and affected static checks -> observed 2026-09-13: `mise run format:rust`, `mise run //apps/kuru-tui:typecheck`, and `mise run //apps/kuru-tui:lint` passed. The package-owned terminal-filtered and exact cancellation tests passed under a deliberately conflicting 1x1 outer PTY, including real initial frames, input, resize, native attribute restoration, one-megabyte backpressure drain and successful-exit observation. A blocking-reader seam proved two bounded close attempts retain uncertainty while its source remains open and a final close joins the reader after release. Native Windows remains deferred in 2.2.
- [x] 2.4 @regression (agent) run the exact trust corruption fixture after moving its mutations to the checked operation handle -> observed 2026-09-13 on macOS: `mise run //apps/kuru-tui:test --lib trust::tests::malformed_oversized_and_hard_linked_records_never_match` passed 1 selected library test with 33 filtered; native Windows repetition remains pending in 1.7 (`/private/tmp/kuru-checkpoint-trust-replacement-test.log`)
