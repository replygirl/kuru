# Dependencies

## Blocked by

None.

## Soft-blocked by

None.

## Siblings

- `shell-environment-minimization` owns the production environment projection
  whose native acceptance fixture is corrected here.
- `bounded-provider-retries` owns the production refresh budget whose native
  timing fixture is corrected here.
- `native-trust-publication` owns the pinned approval store and its checked
  movable operation handle. This change reuses that boundary in the malformed
  record fixture. `native-advisory-and-path-validation` addresses separate
  failures from the same Windows checkpoint run.
