## 1. Checked connector admission

- [x] 1.1 Add a native read classification/preparation result that distinguishes eligible, serial approval/activation and ineligible calls, and verify exact names and Once boundaries in focused connector tests.
- [x] 1.2 Split checked file/list/search admission from execution, retaining bounded candidate permissions, instruction review, root and identity checks, and verify denial, changed-target and omission fixtures before an effect.
- [x] 1.3 Keep `web_fetch` per-call permission admission separate from its existing isolated connection/redirect enforcement, and verify concurrent local-server fetches retain destination and cancellation checks.

## 2. Bounded runtime waves and activity

- [x] 2.1 Replace sequential speaking-call execution with ordered admission and bounded contiguous native read waves, preserving serial barriers, budget and result slots; verify overlap and original provider-call order in controlled runtime fixtures.
- [x] 2.2 Run checked filesystem work on bounded owned blocking workers and drain all wave work on cancellation; verify actual overlap, cap one and cancelled-worker cleanup with named deadlines.
- [x] 2.3 Settle each call as it completes and track exact active call identity in the typed event/TUI projection without changing three-key wire shape; verify reverse-order same-name calls in unit and real PTY fixtures.

## 3. Integrated behavior and closeout

- [x] 3.1 Exercise the full verification ledger across native file/search/web overlap, fresh Once/denial, instruction activation, mutation/opaque barriers, cancellation, ordered receipts and activity; record the observed command and result for every row.
- [x] 3.2 Update owning tool/runtime documentation for concurrent independent calls and serial authority boundaries, then run package docs, format, lint, typecheck and targeted native checks on final source.
- [x] 3.3 Strictly validate and archive the change after evidence closes, verify the archive path, commit on the feature branch with normal hooks, and hand the exact head and remaining hosted-CI evidence to Delivery.
