## 1. Native independent calls genuinely overlap [critical]

- [x] 1.1 @integration (agent) hold one checked filesystem read at its execution barrier and prove a second admitted file/search read enters checked execution before release, with peak active calls bounded by `max_parallel` -> `authorized_native_reads_overlap_but_feed_results_back_in_provider_order` passed 1/1 on macOS. Named barriers observed two distinct call IDs in checked execution before release; exact bytes, omissions, invocation coordinates and provider-order results matched.
- [x] 1.2 @integration (agent) combine admitted `grep`/`glob` candidate planning with a bounded local-server `web_fetch` fixture and a second native read -> `authorized_web_fetch_overlaps_checked_search_and_keeps_result_order` passed 1/1 on macOS. The request-bound resolver admitted only the configured host/effective port, the search settled while the HTTP response remained held, and results retained provider order.
- [x] 1.3 @unit (agent) set `max_parallel=1` and a tool-call budget smaller than the returned batch -> `one_parallel_slot_and_tool_budget_preserve_a_serial_settlement_boundary` passed 1/1; observed starts never exceeded one and the excess call received exactly one budget receipt.

## 2. Authority and effect barriers prevent unsafe dispatch [critical]

- [x] 2.1 @integration (agent) use foreground Once, deny and nested-instruction review fixtures with two same-completion calls -> `parallel_read_admission_is_narrow_and_preserves_serial_authority_boundaries` passed 1/1. Fresh Once remained serial and non-transferable, deny produced no read, and changed instructions returned a replan refusal before exposure.
- [x] 2.2 @integration (agent) change a checked file target, candidate identity or permission grant between admission and wave execution -> the same connector fixture passed same-name file, directory and search-candidate replacement checks. Retained handles and checked parent/name identities refused each rebound target; the shared 96-handle RAII budget forced a complete ordinary serial grep for a 40-file capture, returned all 40 results, and recovered every permit after fallback and cancellation.
- [x] 2.3 @integration (agent) place `file_write`/`file_edit`/`file_delete`, cognitive, shell and MCP calls among reads -> `mutation_and_opaque_calls_split_native_read_waves` passed 1/1; observed wave settlements preceded each serial start and no mutation or opaque call overlapped a read wave.

## 3. Cancellation and continuation stay complete [critical]

- [x] 3.1 @integration (agent) cancel a controlled turn while multiple owned reads/fetches are active and hold one worker through cancellation -> `cancelled_parallel_wave_drains_every_owned_read_before_returning` passed 1/1. Every admitted call settled once, the later mutation never started, blocking joins drained, and retained-handle usage returned to zero.
- [x] 3.2 @integration (agent) complete the second call before the first under distinct provider call IDs -> both native overlap fixtures passed: settled observations followed actual reverse completion while provider continuation contained one bounded result per original call position with unchanged call IDs and full `ToolInvocationContext` coordinates.
- [x] 3.3 @integration (agent) force a checked read refusal and an ambiguous serial effect in the same completion -> `refused_parallel_read_does_not_replay_a_later_accepted_serial_effect` passed 1/1. The rebound read returned a replan receipt, the serial write held one durable Applied checkpoint, cancellation preserved exact bytes, and an exact retry was rejected without a second effect or dropped receipt.

## 4. Interactive activity reflects active work [critical]

- [x] 4.1 @e2e (agent) drive a real PTY through two overlapping same-name tool calls and release them in reverse order -> `real_pty_parallel_activity_tracks_each_same_name_call` passed 1/1 on the authored source. Completed-frame assertions observed two, one and zero active calls in reverse release order, then the final answer and terminal restoration. The fixture is deterministic and made no paid provider request.
- [x] 4.2 @unit (agent) project concurrent tool events through the stable three-key wire adapter -> the focused activity projection and `wire_adapter_stays_a_three_key_object` checks passed. Activity retained distinct call IDs while the wire object stayed three-key and excluded raw arguments and result bodies.

## 5. Repository gates

- [x] 5.0 @eval (agent) score scripted provider multi-call traces against the P06 authority/order rubric across read waves, Once approval, denial and mutating barriers -> deterministic provider fixtures passed the read-wave, cap/budget, denial/replan, serial-barrier, cancellation and mixed-refusal traces. Each asserted the exact completed call set and IDs, zero unauthorized effect and provider-order continuation; no live model was used.
- [x] 5.1 @regression (agent) run owning connector/runtime/TUI format, lint, typecheck and focused native tests -> Rust format and diff checks passed; connector/runtime/TUI all-target all-feature typecheck and Clippy `-D warnings` passed. The focused connector test and six runtime behavior filters above passed. `mise run //apps/kuru-docs:check` passed formatting, lint, VitePress build, public-content and link/anchor validation after applying the owning formatter.
- [x] 5.2 @regression (agent) run strict Cospec validation, apply gate and archive after all product rows close -> strict change validation passed with 0 errors and warnings; the JSON apply gate exited 0 with 9/9 tasks complete and no blockers; archive succeeded at `openspec/changes/archive/2026-09-22-parallel-independent-tool-execution/` and applied five capability specs. Post-archive strict validation of all 31 durable specs then passed with 0 errors and warnings.
- [~] 5.3 @regression (agent) run normal unbypassed pre-push coverage and exact-head native CI -> defer: Delivery will run these gates after the archived P06 commit is mechanically restacked onto the corrected P04 lineage. Local evidence covers macOS behavior; Windows/Linux and combined coverage are not claimed here.
