## 1. Finite streaming detector

- [x] 1.1 Extend the scanner with bounded Slack, GitLab, JWT, URL-userinfo and bare assignment recognition while preserving marker atomicity
- [x] 1.2 Add regression and false-positive controls, including every-byte streaming splits and structured keys/values, and verify the focused connector redaction suite fails before and passes after the change

## 2. Connector projection seam

- [x] 2.1 Export pure text and JSON projection operations from the connector package without changing `ToolHost::execute`'s result interface

## 3. Documentation and evidence

- [x] 3.1 Update the protocol tool-projection reference with the finite expanded inventory and limits
- [x] 3.2 Run the focused connector redaction tests, record observed verification evidence, and mark completed tasks
