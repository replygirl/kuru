## 1. Configuration authority and managed policy [critical]

- [x] 1.1 @integration (agent) invoke the CLI in a temporary Git worktree with project config, untracked local config and `-c`; inspect the effective config and trust status -> `discovered_local_config_requires_untracked_git_provenance_and_preserves_repo_claims` passed: local mode and disabled shell plus CLI budget appeared in `config`; trust status retained the repository MCP claim and omitted the disabled shell claim.
- [x] 1.2 @integration (agent) invoke the CLI with an external managed file locking write, permission and budget values; try project, local and CLI conflicts -> CLI `managed_locks_and_typed_overrides_apply_before_demo_dispatch` and core `managed_locks_reject_project_local_and_saved_conflicts` passed. CLI rejects dedicated shell and typed budget conflicts; core rejects project/local write, added MCP alias fields, altered permission arrays, and saved mode. Diagnostics identify keys without printing supplied values.
- [x] 1.3 @integration (agent) track the local file in Git and invoke `config` and `run` -> the Git child-process fixture passed: both commands reject the now-tracked local file with the explicit index diagnostic before run setup. With inherited `GIT_INDEX_FILE` or `GIT_DIR`/`GIT_WORK_TREE` pointing to a foreign repository, `config` still rejects the tracked file.
- [x] 1.4 @e2e (agent) run `kuru config` and `kuru --provider demo run` from a temporary Git worktree with the new layers -> the CLI fixture passed with untracked local mode, managed defaults/locks and typed budget override; `config` showed the resolved mode and budget, and the real demo run completed under those layers. Each command captured its own immutable snapshot.

## 2. Schema and parser parity [critical]

- [x] 2.1 @integration (agent) validate documented managed examples through the native parser and published schema, and typed examples through the native parser -> `managed_schema_and_native_parser_accept_typed_locks_and_reject_unknown_keys` and core config tests passed for valid values, unknown keys and invalid types.
- [x] 2.2 @unit (agent) exercise final-leaf provenance, atomic array/table locks and saved-choice conflicts -> core config tests passed: local and CLI final writers remove dead shell authority without blessing residual repository MCP authority; atomic permission/MCP locks and saved mode conflicts fail. A locked external-agent endpoint permits another alias but rejects a changed endpoint.

## 3. Repository checks

- [x] 3.1 @regression (agent) run relevant package tests, format/lint/typecheck, docs checks and strict cospec validation -> `MISE_NO_HOOKS=1 KURU_DOLT_BUNDLE_OFFLINE=true mise run //packages/kuru-core:test` and the full `//apps/kuru-tui:test` passed on macOS after the exact-byte capture fix; the changed CLI target passed all 21 cases again after Git-selector isolation. `//packages/kuru-core:lint`, `//apps/kuru-tui:lint`, `//apps/kuru-tui:typecheck`, `format:check`, `docs:check`, and strict Cospec validation exited 0. The first normal pre-push coverage run later exposed inherited hook `GIT_*` selectors in the test's fixture setup, failing the CLI target; all other reported coverage targets passed. A test-only fixture helper now removes those selectors only from Git setup. Both affected CLI tests passed separately under a child process explicitly given foreign `GIT_DIR`, `GIT_WORK_TREE` and `GIT_INDEX_FILE` with the package's prepared supervisor and native loopback permissions. Hosted macOS coverage for PR #54 then exposed an existing memory fixture race: `rejected_activation_preserves_verified_stage_and_occupied_destination` failed at its immediate lock reacquisition with `WouldBlock` (173 other memory lib tests passed). Its cache-lock release and immediate reacquisition now share the test-only spawn gate, as does the analogous source-open failure fixture; no production lock behavior changed. Both focused fixtures passed locally after the correction; `format:rust` and `git diff --check` passed. A later local `format:check` attempt stopped before checking files when Taplo's macOS dynamic-store access panicked in the restricted environment, so the normal pre-push hook remains the complete formatting gate. A normal pre-push coverage rerun and hosted native CI remain pending; native Windows execution remains CI-only.
- [x] 3.2 @eval (agent) compare effective configuration and authority with the selected demo route under local and managed layers -> CLI fixtures showed the untracked local mode and typed budget in inspection, real demo dispatch succeeded under managed locks, and a residual repository MCP claim still blocked an unapproved run.
