## 1. Safe Unix process-group authority [critical]

- [x] 1.1 @unit (agent) deterministic platform syscall fixture drives running, exited, bounded `EINTR`, `ECHILD`, unexpected error, group `ESRCH`, root movement, `EPERM`, reap, and repeated transition cases -> Root observed six initial `unix::tests` cases passing: interrupted observation retains authority without signalling, ownership loss disarms, and group/root signals occur once before reap. Final fixture-only count/environment correction passed its affected case (exit 0), `/private/tmp/kuru-platform-unix-process-group-unit-final.log`. Controlled signal closures prove the root attempt follows group `ESRCH`; the presence helper distinguishes `EPERM` and `ESRCH`. No native moved-root race or permission-denial occurrence is claimed.
- [x] 1.2 @integration (agent) native platform fixture spawns a real fresh group whose root exits with a known nonzero status and a same-group descendant -> Native macOS `root_exit_with_same_group_descendant_is_signalled_before_exact_reap` passed with exact exit 37, descendant-authored readiness and confirmed absence (exit 0), `/private/tmp/kuru-platform-unix-process-group-native.log`. `consumed_transition_still_reaps_and_confirms_absence` passed (exit 0), proving repeated transition refusal still permits guarded reap/absence, `/private/tmp/kuru-platform-unix-process-group-phase.log`. Linux remains separately unchecked in 1.3.
- [x] 1.3 @integration (agent) run the real platform fixtures on native macOS and Linux -> PR14 CI run 34747712455 passed both native Unix jobs, including the real process-group root/descendant, consumed-transition, exact-reap, and confirmed-absence cases; `/private/tmp/kuru-pr14-linux-ci-86f.log` and `/private/tmp/kuru-pr14-macos-ci-86f.log`.

## 2. Owned built-in shell completion and failures [critical]

- [x] 2.1 @regression (agent) inspect the pre-fix inherited-pipe source/ordering and drive the unsafe transition deterministically, then run a real post-fix Unix root and descendant that retain inherited pipes past timeout -> `/private/tmp/kuru-owned-unix-shell-baseline-ordering-evidence.md` records the old source ordering without a timing-dependent leak claim; post-fix coverage ran `unix_shell_requires_both_eof_and_root_exit_then_reaps_descendants` and the real `kuru_run_uses_the_owned_shell_and_preserves_the_responses_continuation` probe (suite exit 0), `/private/tmp/kuru-phase0-next-coverage.log`.
- [x] 2.2 @integration (agent) real roots close both pipes before delayed exit, exit normally with a silent same-group descendant, and produce exact success/failure output -> `tools::tests::unix_shell_requires_both_eof_and_root_exit_then_reaps_descendants` and `unix_shell::tests::successful_capture_never_reports_success_before_unconfirmed_cleanup` passed (12-test final Unix filter exit 0 and focused success rerun exit 0), `/private/tmp/kuru-shell-owner-final-unix-tests.log` and `/private/tmp/kuru-shell-owner-final3-success-test.log`; the probes require both EOF and root exit, then confirm descendant cleanup before success.
- [x] 2.3 @integration (agent) real timeout plus stdout overflow, stderr overflow, and injected pipe-read failure fixtures -> final 12-test Unix filter passed (exit 0), `/private/tmp/kuru-shell-owner-final-unix-tests.log`; its timeout, output, read-failure, panic, and common-cleanup probes preserve the primary result while exercising the bounded owned cleanup path.
- [x] 2.4 @runtime (agent) cancel a ready shell caller and destroy a separate parent Tokio runtime, then invoke `ToolHost::shutdown` from a live runtime -> `tools::tests::unix_shell_caller_loss_keeps_registered_owner_for_shutdown`, `tools::tests::unix_shell_outlives_a_destroyed_parent_runtime`, and `unix_shell::tests::shutdown_cancels_a_starting_owner_and_closes_registration` passed in the final 12-test Unix filter (exit 0), `/private/tmp/kuru-shell-owner-final-unix-tests.log`; they retain the registered owner through caller/runtime loss and exercise shutdown registration closure.

## 3. Startup, shutdown, and delayed confirmation [critical]

- [x] 3.1 @runtime (agent) controlled worker-start gate delays a registered invocation beyond its operation deadline plus the single five-second cleanup allowance -> `unix_shell::tests::caller_fallback_prevents_a_delayed_worker_from_launching` passed in the final 12-test Unix filter (exit 0), `/private/tmp/kuru-shell-owner-final-unix-tests.log`; it covers the delayed worker's cancellation/deadline observation, no launch, and reservation reclamation.
- [x] 3.2 @runtime (agent) controlled shutdown races one starting and one active shell owner, then attempts another launch -> `unix_shell::tests::shutdown_cancels_a_starting_owner_and_closes_registration` passed in the final 12-test Unix filter (exit 0), `/private/tmp/kuru-shell-owner-final-unix-tests.log`; it exercises shutdown cancellation and registration closure without multiplying the cleanup window.
- [x] 3.3 @unit (agent) thread-start failure, runtime-start failure, pre-spawn panic, post-spawn panic, and partial pipe-conversion fixtures -> `unix_shell::tests::{before_spawn_failures_reclaim_their_reservation_without_launching,pre_spawn_panic_reclaims_only_its_registered_reservation,post_spawn_failures_take_the_common_cleanup_path,cleanup_panic_keeps_the_timeout_primary_and_retains_the_owner}` passed in the final 12-test Unix filter (exit 0), `/private/tmp/kuru-shell-owner-final-unix-tests.log`; those probes distinguish pre-spawn reclamation from post-spawn common cleanup.
- [x] 3.4 @runtime (agent) deterministic cleanup observation first exhausts bounded `EINTR` while anchored, remains unconfirmed through the caller's five-second allowance, and later succeeds -> `unix_shell::tests::delayed_interruption_retains_the_real_worker_then_confirms_once` passed in the final 12-test Unix filter and focused delayed rerun (both exit 0), `/private/tmp/kuru-shell-owner-final-unix-tests.log` and `/private/tmp/kuru-shell-owner-final3-delayed-test.log`; it keeps the worker registered through the caller bound, performs one later transition, then confirms and self-removes.

## 4. Compatibility and quality

- [x] 4.1 @integration (agent) existing Unix shell status/output/input/timeout, finite environment, retained-root replacement, stdio MCP, Windows Job, and actor-to-tool fixtures run unchanged -> PR14 CI run 34747712455 passed complete Linux, macOS, and Windows instrumented behavioral targets. Windows passed `isolated_toolhost_windows_shell_receives_projected_environment`, all eight native Windows connector cases, the direct CLI capability/projection fixtures, and all 17 platform process cases; Unix passed the real actor-to-shell and owned group lifecycle cases.
- [x] 4.2 @integration (agent) root documentation check renders the protocol and curated tools references -> `mise run docs:check` passed (exit 0), `/private/tmp/kuru-phase0-next-docs-check.log`; rendered protocol and curated-tools wording states bounded owned-group cleanup and unconfirmed retention without sandbox, escaped-process, memory-lease, or synchronous-Drop claims.
- [x] 4.3 @integration (agent) focused platform/connectors format, lint, typecheck, and package tests plus one coordinated workspace coverage run execute through owning mise tasks -> final format, workspace typecheck, tooling lint, and workspace lint passed (all exit 0), `/private/tmp/kuru-phase0-next-{format-final,typecheck-final,lint-tooling-final,lint-final}.log`; the sole instrumented coverage run passed with 21,446/22,557 lines (95.07%), `/private/tmp/kuru-phase0-next-coverage.log` and `-coverage-report.log`. Native Linux/Windows execution remains unchecked in 1.3 and 4.1.
- [x] 4.4 @eval (agent) inspect deterministic natural, forced, caller-loss, startup-delay, and cleanup-uncertain transcripts -> root reviewed the deterministic terminal-path evidence: final Unix probes cover natural EOF/root ordering, timeout/output/read failure, caller/runtime loss, delayed start, and delayed `EINTR` confirmation; platform phase tests cover single transition, no post-reap signal, and confirmed absence, `/private/tmp/kuru-shell-owner-final-unix-tests.log`, `/private/tmp/kuru-shell-owner-final3-delayed-test.log`, and `/private/tmp/kuru-platform-unix-process-group-phase.log` (all exit 0).
- [x] 4.5 @e2e (agent) run the fake-provider conversational actor-to-tool path through real `kuru run --json` on a native Unix host and request the built-in shell, alongside the existing native terminal restoration fixture -> coverage ran `kuru_run_uses_the_owned_shell_and_preserves_the_responses_continuation`; root also observed `real_pty_accepts_chat_navigation_commands_and_restores_terminal` and `real_pty_error_unwind_restores_terminal_and_reports_the_original_error` pass under the same run (exit 0), `/private/tmp/kuru-phase0-next-coverage.log`; the actor-to-tool probe uses a fake provider and preserves the typed receipt/final JSON.
- [x] 4.6 @integration (agent) compile and run the built-in shell fixtures on native Windows after target-specific dispatch is restored -> PR14 CI run 34747712455 compiled and passed `isolated_toolhost_windows_shell_receives_projected_environment`, `windows_shell_environment_has_an_exact_independent_inventory`, `configured_stock_powershell_starts_like_direct_spawn_with_the_same_isolated_environment`, and the native runtime tool replay; `/private/tmp/kuru-pr14-windows-ci-86f.log`.
