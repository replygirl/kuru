## 1. Serialisation ordering [critical]

- [x] 1.1 @regression (agent) hold the shared spawn guard, start a thread that blocks on the exclusive lock guard, and observe through an injected channel that it has not acquired the guard while the shared holder is live, then that it acquires after release -> the ordering is proven without any wall-clock wait. Observed locally on macOS: `KURU_DOLT_BUNDLE_DIR=/private/tmp/kuru-phase1-bundles cargo test -p kuru --lib --locked spawn_gate` -> `an_in_flight_spawn_excludes_lock_acquisition_until_it_finishes ... ok`, 3/3 consecutive runs. The test is ordering-sensitive, not incidental: pointing `Gate::locking` at an unrelated lock makes it FAIL, and pointing `Gate::spawning` at an unrelated lock makes it FAIL, while the unmutated code passes.
- [x] 1.2 @unit (agent) let a guard holder panic -> the gate is released and remains usable for later acquisitions. Observed locally in the same run: `a_panicking_holder_releases_the_gate ... ok`; the gate is `tokio::sync::RwLock`, which does not poison, so a panicking holder's guard is simply dropped and the lock is released normally.

## 2. The flaky failure is gone at its cause

- [x] 2.1 @integration (agent) run the `kuru` library test binary repeatedly at default parallelism with the real bundled engine -> no `lock acquisition failed because the operation would block` failure in `apps/kuru-tui/src/permission_store.rs`, and the fail-closed assertions still hold unchanged. Observed locally on macOS (Darwin 27.0.0, aarch64): 60 consecutive `cargo test -p kuru --lib --locked` runs, 60 pass / 0 fail, zero `would block` lines. An intermediate soak with only the two originally diagnosed spawn sites gated still produced 1 `would block` failure in 30 runs, which is what identified the ungated `MemoryStore::temporary` and `cli.rs` approval sites; the complete gate is what the 60 clean runs cover. The 30-run baseline soak on the unfixed head reproduced nothing locally — the failure is rare on this host (it was first seen in CI), so the mechanism evidence is the standalone C reproduction: one thread looping `open`/`flock(LOCK_EX|LOCK_NB)`/`close` on a private file plus six `posix_spawn` threads gave 7,636 `EWOULDBLOCK` in 756,226 attempts (~1.0%), while the same loop with zero spawner threads gave 0 in 505,735.
- [x] 2.2 @unit (agent) inspect the diff -> no assertion weakened, no timeout widened, no retry or backoff added, no `#[ignore]`, and no production code path touched. Confirmed by `git diff`: every change is inside `#[cfg(test)]` code or the new test-only module; the only production-file edits are the `#[cfg(test)] mod spawn_gate;` declaration in `lib.rs` and guard acquisitions inside existing `#[cfg(test)] mod tests` blocks.

## 3. Checks

- [x] 3.1 @unit (agent) run the owning package's lint task, the workspace format check and cospec validation -> all pass on the corrected head. Observed locally: `mise run //apps/kuru-tui:lint` (clippy `-D warnings`, all targets and features) passed; `mise run format:check` passed; `mise run cospec -- validate serialize-test-lock-and-spawn --strict` passed; `mise run //apps/kuru-tui:test` passed three consecutive times (69 lib tests plus every other target, 0 failed, no `would block`).
- [~] 3.2 @runtime (agent) run the library suite natively on Windows and Ubuntu -> defer: pending hosted CI. This host is macOS. The mechanism is a Unix `flock`/`posix_spawn` property, so the gate is a no-op safeguard elsewhere; it compiles and runs on every platform because it guards only test call sites, and no Windows-specific behaviour was verified locally.
