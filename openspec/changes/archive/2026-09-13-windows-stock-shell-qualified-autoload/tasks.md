## 1. Stock PowerShell module auto-loading

- [x] 1.1 Qualify the Windows CLI fixture's real `Get-FileHash` invocation with `Microsoft.PowerShell.Utility` and verify the hostile same-name module control requires the exact qualified auto-loading failure, command, module and hash-stage evidence.
- [x] 1.2 Run granular formatting and static checks, then verify the exact integrated head completes the native Windows shell acceptance and source-install steps in hosted CI.
