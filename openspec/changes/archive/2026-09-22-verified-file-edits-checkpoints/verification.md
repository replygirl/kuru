## 1. Checked edits and mutation authority [critical]

- [x] 1.1 @integration (agent) apply valid multi-hunk UTF-8 edits, create, replace and delete through the real native ToolHost and checked project root -> owning connector task passed 235/235; exact hunk, actor edit/receipt, create/replace/delete and private checkpoint fixtures passed on macOS.
- [x] 1.2 @integration (agent) race source changes, duplicate hunk context, symlink/hardlink/protected paths and permission denial against the same host -> owning connector task passed 235/235, including stale/ambiguous/protected actor refusals and checked hardlink write refusal with both aliases unchanged.

## 2. Durable recovery and exact undo [critical]

- [x] 2.1 @integration (agent) stop or cancel admitted create/replace/delete calls around receipt and target publication, reopen the private store with prepared records and retry each logical ID -> test-only post-effect/pre-Applied interruption ran for all three effects; reopened Prepared receipts refused exact retry, including matching create bytes and absent delete, without a duplicate effect (owning connector task 235/235).
- [x] 2.2 @integration (agent) undo one settled create, replacement, edit and delete after restart, then alter the file outside Kuru and attempt another undo -> the reopened create/replacement/delete fixture and ToolHost edit/selected-undo fixture passed; an external in-place edit conflicted and retained its bytes (owning connector task 235/235).
- [x] 2.3 @integration (agent) hold a live checkpoint lease with a Prepared receipt, attempt ordinary prune, then explicitly discard the selected inactive uncertainty -> ordinary prune refused and inspection retained the receipt; substituted-stage identity refused discard; restoring the exact stage permitted explicit cleanup and a fresh write (owning connector task 235/235, final focused assertion 1/1). The fixture models the durable in-flight cutpoint; it does not claim a separate scheduler race test.

## 3. Capacity, privacy and user surface [critical]

- [x] 3.1 @integration (agent) fill individual and project checkpoint bounds under a checked private data directory, including worst-case Applied receipt growth and retained stage bytes -> owning connector task 235/235 includes 2 MiB/project-capacity, settled identity reservation and private-stage fixtures; over-capacity writes refuse before target change.
- [x] 3.2 @e2e (agent) inspect, undo and prune a selected file checkpoint through CLI and a real PTY -> separate-process CLI fixture passed 1/1; final real-PTY inspect/selected undo fixture passed 1/1 with local Dolt loopback access, rendering receipt path/status and restoring the checked file.

## 4. Package and platform gates

- [x] 4.0 @eval (agent) run a fixed offline actor tool-call corpus that proposes a valid edit, an ambiguous edit, a stale edit and a protected-path edit -> fixture passed 1/1; only the valid hunk changed its file, and each rejection retained its source bytes.
- [x] 4.1 @regression (agent) run owning connector/runtime/TUI tests, lint/typecheck, docs and strict Cospec/format checks -> connector owning task passed 235/235 with local loopback; connector/platform/Windows-cross/runtime/TUI Clippy passed; final real PTY passed; `docs:check`, `format:check`, managed Cospec drift and strict validation/apply passed. Normal pre-push coverage remains Delivery's next gate.
- [~] 4.2 @integration (agent) run exact-head native Windows, macOS and Linux file mutation/undo fixtures through delivery CI -> defer: final branch commit/push and hosted native jobs follow this archive; cross-target Windows Clippy and typecheck are compile evidence only. Require exact-head Windows protected/create/unprotected inheritance fixtures plus macOS/Linux mutation/undo results before claiming platform acceptance.

Observed local source and behavior, 2026-09-22: the initial private-stage refactor compiled after a retained-source borrow correction. The first focused `file_edits` run exposed a post-replace zero-link retained source handle at access finalization; `finalize_file_access` now accepts that checked retained state while rejecting a new hardlink alias. After the held-source access-token correction, connector all-target typecheck and Windows platform all-target cross-typecheck passed; local Unix access-token policy-change fixture passed 1/1, file-edits fixtures passed 7/7 including Prepared-delete uncertainty, and actor tool edit/exact-retry/changed-ID/selected-undo passed 1/1. The access-token fixture's first run used a false owner-read prohibition for an owner-private Unix stage; the corrected assertion checks its owner-only traversal mode and passed. Earlier separate-process CLI edit/inspect/undo/prune and real-PTY fixtures passed before the access-token delta; those do not establish final-head behavior. Native Windows access-policy/inheritance and exact-head hosted OS checks remain unrun delivery evidence. No exact-head platform behavior pass is claimed from cross-typecheck.

Final-head acceptance additions: a fixed offline actor-call corpus and a test-only interruption after each real checked create/replace/delete effect, before its Applied receipt, were added after the earlier focused run. The interruption fixture catches the one-shot test panic, reopens the private store, and requires an uncertain exact retry. Both additions were independently source-reviewed before execution.

Observed final behavior before the lint/TUI follow-up: the post-effect interruption fixture passed 1/1 across create, replace and delete; the offline actor refusal corpus passed 1/1; the separate-process CLI edit/inspect/undo/prune fixture passed 1/1. The first real-PTY invocation stopped before UI behavior because the sandbox denied the bundled Dolt supervisor's private loopback bind; the exact fixture rerun with local loopback access passed 1/1. Owning connector Clippy then found signature/type-complexity warnings, and independent source review found that TUI file checkpoint commands still crossed unrelated memory reconciliation. Both have bounded source corrections; corrected lint and PTY results remain pending, so the earlier PTY pass is not claimed for this final delta.

The post-review restart fixture now also reopens an Applied create and undoes only its selected target, then changes an Applied replacement target outside Kuru and requires its selected undo to conflict without overwriting the user bytes. Independent source review cleared this test-only extension; its final owning-task rerun passed.

Final local gates: raw Cargo's default parallelism hit the host's 256-descriptor ceiling among filesystem fixtures; the owning connector task already sets `RUST_TEST_THREADS=2`, and its affected eight fixtures passed under that policy without a source mutex. The first owning task in the sandbox could not bind loopback (`Operation not permitted`); the exact task with native loopback ran 235/235 after updating two older expectations for `file_edit` discovery and pre-effect multi-link refusal. The final Prepared-prune assertion passed 1/1. `//packages/kuru-connectors:lint`, `//packages/kuru-platform:lint`, `//packages/kuru-platform:lint:windows`, `//packages/kuru-runtime:lint`, `//apps/kuru-tui:lint`, and `format:check` passed; final real-PTY file inspect/undo passed 1/1. `docs:check` passed with pinned Node/npm tooling, including build and public link/content checks. `cospec:managed:check` reported no drift. A Windows-only fixture statement has a scoped Clippy allowance because host-side analysis otherwise applies Unix `set_readonly(false)` semantics to that Windows cleanup; no production access-policy path changed. Exact-head hosted OS behavior and coordinated pre-push coverage remain Delivery gates.
