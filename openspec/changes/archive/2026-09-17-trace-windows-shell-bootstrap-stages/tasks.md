## 1. Trace isolated Windows shell startup

- [x] 1.1 Add fixture-only pre-import and post-import markers in `packages/kuru-connectors/src/tools.rs`, preserving ordinary emitted source bytes.
- [x] 1.2 Assert ordered success stages and retain bounded partial-stage failure diagnostics in `packages/kuru-connectors/src/tools.rs`; run focused static checks and report native execution as pending.
