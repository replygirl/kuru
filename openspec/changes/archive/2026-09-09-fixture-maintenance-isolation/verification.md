## 1. Deterministic fixture lifecycle [critical]

- [x] 1.1 @regression (agent) trace actual sentinel setup and assert it launches no automatic maintenance -> old setup launched actual git maintenance run --auto --no-quiet --detach and failed the new observation (/tmp/kuru-fixture-maintenance-red.log). Setup-only controls prevent that launch; all 13 notes and 15 workflow tests pass with complete snapshots unchanged (/tmp/kuru-fixture-maintenance-green.log). This proves process launch behavior, not a reproduction of the hosted lock timing
- [x] 1.2 @integration (agent) rerun real notes/workflow suites and full mise check -> 28 scoped tests, Clippy and formatting pass; independent review found no blocker. Full gate passed in 43.46 seconds with 97.46% line coverage (8993/9227), /tmp/kuru-fixture-maintenance-check-recheck.log. The first full run saw an unchanged connector fixture close stdout before its ready event; cause remains unproven. All 33 connector tests passed in isolation before the complete successful rerun (/tmp/kuru-maintenance-connectors-recheck.log); no connector checks changed
- [~] 1.3 @runtime (agent) rerun hosted PR checks from the repaired commit -> defer: requires this branch update; both original full gates failed only on a sentinel maintenance.lock snapshot difference in CI 34430310688. Require both full gates and both native builds on the new commit before merge and update PR #9 with observed results
