## 1. Repository isolation repair

- [x] 1.1 Add a safe actual-process regression that fails before repair and passes after rooted-command sanitization, preserving intentional command overrides.
- [x] 1.2 Run scoped and full gates, verify caller metadata and record evidence; submit the completed repair to archival before its final branch commit.
- [x] 1.3 Record deferred ordinary pre-push, hosted CI and actual Release follow-through explicitly, requiring all native checks and reviewing real notes before publication and final inline Pages; update the PR as these results become observable.
