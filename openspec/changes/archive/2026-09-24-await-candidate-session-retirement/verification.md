## 1. Candidate rename waits for exact source-branch sessions [critical]

- [x] 1.1 @regression (agent) rerun exact affected macOS native cases on corrected local source -> memory `candidate_begin_recovery_keeps_clones_fenced_until_exact_ref_reattaches` passed 1/1 (5.42s) and runtime `cancelled_before_promotion_send_keeps_exact_open_candidate_for_resolution` passed 1/1 (2.13s) on corrected local source. Before the fix, exact CI `36029830458` job `107735470699` failed both, with a fixed `branch_rename/HY000/1105` owner record and Dolt's explicit active-session error in the runtime case. Corrected hosted native CI remains unrun.
- [x] 1.2 @integration (agent) run pinned real-Dolt `candidate_source_admission_waits_for_server_session_before_status_rename` -> passed 1/1 (4.44s): `DATABASE()` and processlist DB matched the exact source; a test-only signal fired after the production binary-equal query observed `active > 0`; source admission stayed fenced while main remained usable, with no Pending/ref change until exact teardown and one settled rename. The four-session exact DB/processlist retirement probe passed 1/1 (1.97s). This proves the wait path, unlike the earlier prompt-teardown-only probe.
- [x] 1.3 @integration (agent) run pinned real-Dolt `held_candidate_view_delays_explicit_abandonment_without_losing_history` -> passed 1/1 (4.26s) using a private 2-second pre-rename deadline while production retains `QUERY_TIMEOUT`: exact ID/DB stayed server-active through bounded refusal; no Pending receipt, ref or history changed; after exact teardown a fresh checked abandon succeeded. No force, kill or blind rename retry was used.

## 2. Preserve end-to-end candidate authority

- [~] 2.1 @e2e (agent) run exact final-head Windows application, memory/runtime, macOS native and required coverage/report gates -> defer: the final source-plus-archive SHA cannot exist before this archive. These are required post-push merge gates tracked in the delivery ledger, not claimed as passed. The earlier Windows `StorageFailed` remains unclassified; diagnostic head `b46ce74` passed Windows application, memory/runtime, installation and 90% report but failed macOS candidate tests.
- [x] 2.2 @equivalence (agent) independently review source admission, deadline and recovery paths -> Toolcards cleared the source-local guard, exact branch-qualified processlist query, pre-Pending bounded refusal, and unchanged post-dispatch uncertain reconciliation/ref inspection. The corrected test signal is tied to observed active server rows and its contender result is asserted. No new global lock wait, force, blind retry, uncertain replay, public RPC or schema change was found. Owning memory all-target Clippy, cargo fmt and diff-check passed on final local source.
