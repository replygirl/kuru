## 1. Provider-free durable notes inspection [critical]

- [x] 1.1 @integration (agent) Seed isolated live notes and conversation namespaces, then invoke the public runtime reader -> `notes_view_is_provider_free_and_separate_from_conversation` returned only `NOTE-MARKER` chronologically, not `CONVERSATION-MARKER`, in the direct provider-free reader fixture; `mise run //packages/kuru-runtime:test -- notes_view_` exit 0 (3/3), `/private/tmp/kuru-notes-runtime-tests.log`.
- [x] 1.2 @integration (agent) Seed an inactive topology part with retained notes and read its exact ID, then use an unknown and an archived name/role -> `notes_view_reads_exact_archived_part_and_relationship_only` accepted exact inactive part and relationship IDs and rejected archived name and unknown input; runtime filter exit 0 (3/3), `/private/tmp/kuru-notes-runtime-tests.log`.
- [x] 1.3 @integration (agent) Seed limit-plus-one and exactly-limit notes in a temporary store -> `notes_view_reports_n_plus_one_and_validates_its_live_mode` returned newest chronological `middle`, `new` with `truncated=true`, exact-limit with `false`, and accepted requested limit 1000; runtime filter exit 0 (3/3), `/private/tmp/kuru-notes-runtime-tests.log`.
- [x] 1.4 @integration (agent) Read a mode without persisted topology and pass an explicit candidate view to the public reader -> the same runtime fixture rejected both while retaining the absent selected-mode topology key and main/candidate revisions before and after; runtime filter exit 0 (3/3), `/private/tmp/kuru-notes-runtime-tests.log`.

## 2. CLI and typed TUI access [critical]

- [x] 2.1 @e2e (agent) Run the built `kuru memory notes` binary against an isolated existing store with an intentionally unavailable provider route -> coverage ran `notes_cli_reads_existing_modes_without_provider_or_legacy_import` after its Responses children removed `OPENAI_API_KEY`; it proves saved `freudian` and explicit `ifs` selections return notes without provider construction, `/private/tmp/kuru-phase0-next-coverage.log` (suite exit 0).
- [x] 2.2 @integration (agent) Invoke `memory notes` against fresh and legacy-only isolated data layouts and limits 0, 1, 1000, and 1001 -> coverage ran the final `notes_cli_reads_existing_modes_without_provider_or_legacy_import` fixture with its successful `--limit 1000` read; its existing fresh/legacy and 0/1001 observations prove no new store or legacy import, `/private/tmp/kuru-phase0-next-coverage.log` (suite exit 0; pre-adjustment focused logs retained at `/private/tmp/kuru-notes-cli-fresh-test.log` and `/private/tmp/kuru-notes-cli-test-final.log`).
- [x] 2.3 @integration (agent) Dispatch `/notes ID` through the real TUI runtime fixture -> `slash_commands_change_real_runtime_state_and_validate_errors` returned default-limit 100 metadata and demo durable note content after `/dream`, while its earlier `/memory ID` assertion still found conversation `hello`; `mise run //apps/kuru-tui:test -- slash_commands_change_real_runtime_state_and_validate_errors` exit 0 (1/1), `/private/tmp/kuru-notes-tui-dispatch-test.log`.
- [x] 2.4 @e2e (agent) Run the isolated existing-store CLI and TUI notes cases on native Windows -> PR14 CI run 34747712455 executed the complete instrumented suite on Windows Server 2025: `notes_cli_reads_existing_modes_without_provider_or_legacy_import`, `slash_commands_change_real_runtime_state_and_validate_errors`, and all three `notes_view_*` runtime cases passed; `/private/tmp/kuru-pr14-windows-ci-86f.log`.
- [x] 2.5 @integration (agent) Build and check the usage/memory and curated command/memory references -> root docs build, formatting, content, links and anchors passed after the notes command/memory updates; `mise run docs:check` exit 0, `/private/tmp/kuru-phase0-next-docs-check.log`.
