## 1. Core policy validation

- [x] 1.1 Require exactly one `ExplicitInput` in visibility-source validation and verify core policy tests reject missing or duplicate input without changing four built-in source fixtures.
- [x] 1.2 Add a reusable consolidation-plan validator for ordered unique live subsets, nonblank prompt/phase and the two-proposal ceiling, and verify alternate valid and invalid plans in focused core tests.

## 2. Visibility dispatch at runtime boundaries

- [x] 2.1 Thread typed actor phases and one validated Visibility source plan through deliberate, speaking, consultation and dream request assembly, private/public reads and P7 itemized fit accounting, and verify optional omission changes both actual provider material and fit evidence while current input and receipts remain.
- [x] 2.2 Enforce Visibility delivery before peer event or mailbox publication, and verify an alternate denial yields a truthful receipt with no recipient request or mail effect.
- [x] 2.3 Validate selected private context before reads, including relationship actors, and verify cross-private or missing-input policies dispatch neither a private read nor a provider call.

## 3. Memory and dream dispatch

- [x] 3.1 Route actor spawn, private history and notes, transcript, topology and state paths through validated Memory decisions while preserving selected-profile publication/reconcile behavior, and verify a distinct test namespace across reopen and selected note inspection.
- [x] 3.2 Route candidate operations and undo through the same selected state keys and identity namespaces, and verify real-Dolt candidate abandonment, promotion, retirement and later-history-preserving undo keep private isolation and P7 usage behavior.
- [x] 3.3 Consume and validate the actual-topology consolidation plan before every dream while retaining typed Dream phase, accounting and runtime candidate ownership, and verify restricted ordered plans and invalid no-effect refusals.

## 4. Parity, evidence and delivery

- [x] 4.1 Extend four-mode normalized runtime goldens for Visibility and Memory dispatch and verify IFS, polyvagal, Freudian and Jungian request, event, namespace, retry and durable outcomes retain the refreshed baseline.
- [x] 4.2 Run scoped core/runtime format, typecheck, lint and focused real-Dolt fixtures, record observed evidence in `verification.md`, and explicitly name unrun native Windows/Linux CI.
- [x] 4.3 Update the owning architecture and verification documentation for effective six-axis dispatch and the Phase 1 local evidence, and verify documentation formatting, links and build.
- [x] 4.4 Run the single combined instrumented workspace coverage suite at the final Phase 1 integration point, record the observed at-least-90-percent result, and verify archive readiness after the P8 hard dependency is archived.
