## 1. Effective private context and delivery [critical]

- [x] 1.1 @integration (agent) run real-Dolt alternate-visibility fixtures that omit public context or a private note -> provider material and P7 source/fit estimate omit the same optional rows while durable history remains unchanged
- [x] 1.2 @integration (agent) drive missing `ExplicitInput` and cross-private source plans -> refusal occurs before private read or provider dispatch
- [x] 1.3 @runtime (agent) drive an otherwise valid peer send denied by Visibility -> no peer event, mailbox entry or recipient request, with a truthful refusal receipt

## 2. Effective namespace and candidate lifecycle [critical]

- [x] 2.1 @integration (agent) run real-Dolt alternate-memory namespace fixtures through actor history, notes, reopen and selected note inspection -> only the selected private namespace is read or written
- [x] 2.2 @integration (agent) hold a dream candidate open with an alternate namespace, then abandon and promote separate fixtures -> candidate isolation, reconciliation, P7 usage and final live namespace behavior remain correct
- [x] 2.3 @integration (agent) retire and undo after an alternate namespace write -> later history remains preserved and no cross-private material appears

## 3. Consolidation and four-mode parity [critical]

- [x] 3.1 @integration (agent) run a restricted ordered alternate consolidation plan against real active topology -> only selected participants receive typed Dream requests with the returned prompt, phase and bounded proposal limit
- [x] 3.2 @integration (agent) exercise duplicate, inactive, blank and over-limit consolidation plans -> refusal occurs before dream provider work or candidate mutation
- [x] 3.3 @equivalence (agent) run normalized IFS, polyvagal, Freudian and Jungian policy-dispatch goldens -> request order/content, context isolation, events, topology, persistence, retries and final outputs match the refreshed baseline
- [x] 3.4 @eval (agent) evaluate all four built-in profiles against the P8/P9 behavior matrix -> no mode gains a supervisor, private context access or additional tool authority

## 4. Phase 1 delivery evidence

- [x] 4.1 @regression (agent) run scoped core/runtime format, typecheck, lint and focused real-Dolt tests plus strict Cospec validation -> commands pass with recorded logs; unrun native Windows/Linux CI is named separately
- [x] 4.2 @e2e (agent) run the single combined instrumented workspace coverage suite -> observed workspace line coverage is at least 90 percent with one retained log
- [x] 4.3 @regression (agent) review final P6-P9 local evidence and run documentation checks before archive -> local acceptance evidence and documentation pass; release/native CI evidence remains explicit

## Observed local evidence (macOS)

- Package-owned focused core policy tests passed 6/6, including exact-one `ExplicitInput`, cross-private rejection, ordered live consolidation subsets, duplicate/inactive members, blank prompt or phase and the two-proposal limit. Core lint passed. Four built-in policy fixtures retain their authored sources and namespace/plan decisions.
- `KURU_DOLT_BUNDLE_DIR=/private/tmp/kuru-phase1-bundles mise run //packages/kuru-runtime:test -- mode_visibility_tests::` passed 5/5 with real Dolt. Test-only alternate `ModeProfile` policies independently omit public rows, notes or all ambient context; recorded Demo requests and `ContextMeasured` source inventory change with those selections. Under the same bounded budget, selected optional rows cause whole-row fit retries while mandatory current input and tool receipts remain and durable rows are unchanged. Relationship requests exclude member-private context; missing input or cross-private plans fail before provider dispatch/admission; a delivery veto leaves no peer event, mailbox entry or recipient request and produces a refusal receipt.
- The package-owned `mode_memory_tests::` filter passed 4/4 with real Dolt. Test-only alternate namespaces survive selected note inspection, shutdown/reopen, candidate promotion, retirement and later-history-preserving undo. Cancellation abandons the candidate without live notes or topology effects while P7 usage records incomplete invocations. Invalid namespace and stale actual-topology plans refuse before provider or candidate effects. The package-owned `mode_consolidation_tests::` filter passed 1/1: only an ordered two-member subset receives typed Dream requests and the returned prompt/phase; the one-proposal limit is enforced with a durable refusal receipt and typed Dream accounting.
- `KURU_DOLT_BUNDLE_DIR=/private/tmp/kuru-phase1-bundles mise run //packages/kuru-runtime:test -- mode_baseline_tests::` passed 3/3 after P9 dispatch. The additional focused `mode_baseline_tests::all_four_modes_keep_builtin_dream_requests_and_own_memory_isolation` passed 1/1 with real Dolt. Across IFS, polyvagal, Freudian and Jungian, it fixes the built-in dream prompt, phase, participant set and namespace, observes one Dream admission per participant, own history/notes plus shared public context without another part's private sentinel, only `dream_suggest` tool exposure, and promoted own notes without transcript or topology drift. Earlier four-mode baselines cover ordinary request/event order, peer routing, retries and facing outcomes. These checks exercise the four built-in modes and test-only alternate policies; they do not assert a new user-facing profile selector.
- Focused P7 context-fit regressions passed 2/2 for `accounting_tests::fit_` and 1/1 for `accounting_tests::demo_effective_prompt_retries_whole_multilingual_rows_under_one_admission`. Scoped runtime typecheck, lint, Rust format and diff checks passed. Source review removed a duplicate actor-side `ExplicitInput` guard after core validation became exact; checked namespace replacement uses the actor's immutable captured namespace. Independent engine/dream review found no permission, validation-order, publication/reconcile or undo bypass.
- `mise run docs:check` passed in 19.98 s (`/private/tmp/kuru-phase1-visibility-docs.log`), including formatting, lint, build, content and link checks. The P8 dependency is archived; the P9 apply gate returned exit 0 after strict validation. Final strict validation passed with zero errors or warnings. Combined instrumented workspace coverage passed at 93.80% (44,927/47,899 lines), including all 140 runtime tests, in 652.85 s; log: `/private/tmp/kuru-phase1-visibility-coverage.log`, retained LCOV: `/private/tmp/kuru-phase1-verification/visibility-memory-coverage.lcov`. The final P6-P9 local acceptance review is complete. Native Windows/Linux CI, packaged-install checks, hosted deployment and release evidence have not been run in this local macOS pass.
