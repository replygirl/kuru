## 1. Checked source catalog and base authority

- [x] 1.1 Add a pinned YAML frontmatter parser and bounded checked user/project skill metadata discovery in core, and verify metadata-only reads, precedence, omissions and source replacement with focused core fixtures.
- [x] 1.2 Add bounded checked user/project custom-command capture and typed effective catalog, and verify built-in/project/user collision order and inert shadowed sources with focused core fixtures.
- [x] 1.3 Bind effective project metadata and command bytes to typed base manifest claims and the immutable snapshot, and verify exact-root preflight hides unapproved entries and preserves captured bytes in core/TUI integration fixtures.

## 2. On-demand skill authority

- [x] 2.1 Add checked full-body and direct-reference capture on explicit selection with exact metadata/identity matching and aggregate limits, and verify missing, changed, linked, escaping and oversized cases in core fixtures.
- [x] 2.2 Extend the existing app prompt gate and v2 approval path to publish selected source unions after once/persist/deny review and root/directory/generation revalidation, and verify revocation, denied and headless paths with integration fixtures.
- [x] 2.3 Add the fixed actor `skill_load` tool and runtime prompt-update continuation without granting tool permissions or executing scripts, and verify actual fake-provider requests contain only selected approved material.

## 3. Interactive custom commands and docs

- [x] 3.1 Combine effective custom prompt entries with P02a's typed registry for help, leading-token completion and dispatch, and verify real-PTY collision, completion, unknown-name and same-session prompt expansion behavior.
- [x] 3.2 Document skill paths, progressive disclosure, references, command format/precedence, bounds and trust/tool distinction in owning user and curated docs, and verify owning docs check.

## 4. Cross-surface verification and delivery

- [x] 4.1 Complete the predeclared verification ledger with observed core/connectors/runtime/TUI and real-PTY evidence, then run scoped mise lint/typecheck/test, Rust format, strict Cospec/apply, managed-file and diff checks.
- [x] 4.2 Obtain independent authority/source review, archive the completed Cospec change, commit with normal hooks, and hand the exact branch/head to delivery for the single normal push hook and native CI.
