## 1. CI runs on the current runner images without Intel macOS [critical]

- [x] 1.1 @integration (agent) run `mise run lint:tooling` (actionlint, shellcheck, repository invariants) in the worktree -> actionlint accepts `ubuntu-latest`, `macos-latest`, `ubuntu-24.04-arm` and `windows-2025` and every edited workflow, with the check enabled. Observed on macOS arm64: `lint:tooling` exited 0; `actionlint -verbose` reported `Found 0 errors in 5 files` with actionlint 1.7.12.
- [x] 1.2 @integration (agent) grep `.github`, `mise.toml`, `docs`, `README.md` and `apps/kuru-docs` for `macos-15-intel`, `x86_64-apple-darwin`, `macos-14`, `ubuntu-24.04` (excluding `-arm`) and Intel macOS claims -> no matches remain outside lockfiles and dated historical records. Observed: the `macos-15-intel`/`x86_64-apple-darwin` grep exits 1 with no output; the only remaining Intel text is the dated 2026-09-15 record in `docs/verification.md`; lockfile `macos-x64` platform entries are unchanged.
- [~] 1.3 @runtime (agent) GitHub-hosted CI run of the pushed PR head (`ubuntu-latest`, `macos-latest`, `windows-2025`, `ubuntu-24.04-arm` runners), expecting quality, native-tests on all three OS legs, the arm64 Linux native-build, native-platform and `ci-gate` report success on the new images, with no Intel macOS job -> defer: hosted runners execute only after this archived record is pushed; the PR's own CI run is the evidence and is reported in the PR.

## 2. Documentation and configuration match

- [x] 2.1 @integration (agent) run `mise run format:code ::: lint:tooling ::: cospec:validate ::: cospec:managed:check ::: docs:check` -> every check exits 0 with the simplified `mr_boxington` template and edited docs. Observed on macOS arm64: the chain exited 0; `Repository metadata invariants passed.` and `Public docs artifacts, local links and anchors passed (/kuru/).`
- [~] 2.2 @runtime (human) Release workflow `build` matrix on its next dispatch, expecting four native builds; `assemble-candidate` is expected to fail until the follow-on `kuru-delivery` change drops `x86_64-apple-darwin` from its release catalog -> defer: Release is a manual, authorized dispatch on main and must not run before that follow-on lands.
