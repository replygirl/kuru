## 1. CI runs on the current runner images without Intel macOS [critical]

- [x] 1.1 @integration (agent) run `mise run lint:tooling` (actionlint, shellcheck, repository invariants) in the worktree -> actionlint accepts `ubuntu-latest`, `macos-latest`, `ubuntu-24.04-arm` and `windows-2025` and every edited workflow, with the check enabled. Observed on macOS arm64: `lint:tooling` exited 0; `actionlint -verbose` reported `Found 0 errors in 5 files` with actionlint 1.7.12.
- [x] 1.2 @integration (agent) grep `.github`, `mise.toml`, `docs`, `README.md` and `apps/kuru-docs` for `macos-15-intel`, `x86_64-apple-darwin`, `macos-14`, `ubuntu-24.04` (excluding `-arm`) and Intel macOS claims -> no matches remain outside lockfiles and dated historical records. Observed: the `macos-15-intel`/`x86_64-apple-darwin` grep exits 1 with no output; the only remaining Intel text is the dated 2026-09-15 record in `docs/verification.md`; lockfile `macos-x64` platform entries are unchanged.
- [~] 1.3 @runtime (agent) GitHub-hosted CI run of the pushed PR head (`ubuntu-latest`, `macos-latest`, `windows-2025`, `ubuntu-24.04-arm` runners), expecting quality, native-tests on all three OS legs, the arm64 Linux native-build, native-platform and `ci-gate` report success on the new images, with no Intel macOS job -> defer: hosted runners execute only after this archived record is pushed; the PR's own CI run is the evidence and is reported in the PR.

## 2. Documentation and configuration match

- [x] 2.1 @integration (agent) run `mise run format:code ::: lint:tooling ::: cospec:validate ::: cospec:managed:check ::: docs:check` -> every check exits 0 with the simplified `mr_boxington` template and edited docs. Observed on macOS arm64: the chain exited 0; `Repository metadata invariants passed.` and `Public docs artifacts, local links and anchors passed (/kuru/).`
- [~] 2.2 @runtime (human) Release workflow `build` matrix on its next dispatch, expecting four native builds and an `assemble-candidate` that accepts exactly four core and four paired shell-support archives (section 3 drops `x86_64-apple-darwin` from the release catalog in this same change) -> defer: Release is a manual, authorized dispatch on main after this PR merges.

## 3. Release catalog, bundled engine and installer without Intel macOS [critical]

- [x] 3.1 @integration (agent) run `mise run //packages/kuru-delivery:test` in the worktree -> the catalog has no `x86_64-apple-darwin` (`find`/`for_platform("macos","x86_64")` fail), release assembly and checksum-count checks follow the four-target catalog, and the installer refuses both a faked `Darwin`/`x86_64` host and `--target x86_64-apple-darwin` with the v0.9.0 message, a non-zero exit, no download request and an unchanged destination. Observed: on macOS arm64 the delivery suite exited 0 with 185 passed, 0 failed, including `intel_macs_are_refused_with_the_last_supporting_release` (faked `uname` through the bootstrap harness) and the four-row `host_detection_selects_each_supported_archive_and_rejects_unknown_hosts`. The first run failed two `release_workflow` tests that pinned 11 assembled checks and uploads; they now derive `TARGETS.len() * 2 + 1`.
- [x] 3.2 @integration (agent) run `mise run //packages/kuru-memory:test` -> the four remaining pinned Dolt assets validate byte-identically, the generated catalog has four entries, and a manifest reintroducing `dolt-darwin-amd64` or selecting `x86_64-apple-darwin` is rejected. Observed: the memory suite exited 0 with 302 passed, 0 failed, including `target_selection_uses_requested_target_and_generates_coherent_versioned_payload_pins`, `malformed_or_ambiguous_manifests_cannot_produce_a_bundle` and `catalog::tests::embedded_bytes_match_the_selected_target_and_versioned_catalog`; `git diff` of `dolt-assets.json` removes only the `x86_64-apple-darwin` object.
- [x] 3.3 @integration (agent) run `mise run //apps/kuru-tui:test` -> the updater fixture's target table has no Intel entry and the suite passes; the previous-release resolver (`published_windows.rs`) keys assets by the Windows target and verifies the previous `SHA256SUMS` by digest only, so a prior release with Intel assets still resolves. Observed: the kuru-tui suite exited 0 with 245 passed, 0 failed, 4 ignored, including `updater_replaces_the_running_binary_only_after_checksum_validation`.
- [~] 3.4 @integration (agent) run the documented lock refresh with `linux-x64,linux-arm64,macos-arm64,windows-x64` for the root, `apps/kuru-docs` and `packages/kuru-delivery` -> the `macos-x64` entries are removed and nothing else changes -> defer: `mise lock` 2026.9.13 refreshed the listed platforms but does not prune unlisted ones; the root and docs locks were unchanged and the delivery lock gained an unrelated `cargo:cargo-llvm-cov` entry, which was reverted. The three `mise.lock` files are left unedited with their `macos-x64` entries rather than hand-edited; removing them needs a pruning mechanism or a maintainer-approved regeneration.

- [x] 3.5 @integration (agent) review follow-up: run `mise run //packages/kuru-delivery:test` and `lint:tooling` -> the Release `build` matrix keeps `ubuntu-24.04` for `x86_64-unknown-linux-gnu` and `ubuntu-24.04-arm` for `aarch64-unknown-linux-gnu`, a faked `Darwin`/`x86_64` host whose faked `sysctl.proc_translated` is `1` installs the `aarch64-apple-darwin` archive, and one reporting `0` (or lacking the OID) is still refused with no download. Observed on macOS arm64: the delivery suite exited 0 with 186 passed, 0 failed, including `rosetta_translated_shells_select_apple_silicon_and_intel_hosts_stay_refused` and the extended release workflow label assertions; `lint:tooling` (actionlint, shellcheck) exited 0.
