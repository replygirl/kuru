## 1. The live `codex` route completes a tool-calling turn [critical]

- [x] 1.1 @runtime (agent) with the pre-fix decoder plus the new function-call trace, run one live `codex`-route turn that forces a tool call in the isolated live environment: `kuru run "Read README.md and reply with its first heading only." --trust-workspace-once --json --debug` -> exit 1, `Error: provider errors: completed function arguments lack name` / `all peers failed to produce a contribution`, no stdout JSON; turn `5377bb7d…6115`, 7 peers, all `"status":"error"`.
- [x] 1.2 @runtime (agent) the same run's ring (`data-dir/diagnostics/f4f2ad7d…83f422/trace-{0..3}.jsonl`) -> 381 `function_call_arguments.delta` and 7 `function_call_arguments.done` records, **every one** `has_name:false` `has_call_id:false`; 4 `output_item.added` records for `function_call` items, **every one** `has_name:true` `has_call_id:true` `arguments_len:0`; 0 `output_item.done` records and 0 `stage="streamed"`/`"final"` records, because no peer got past the `.done` event.
- [x] 1.3 @runtime (agent) with the fix applied, rebuild and rerun the same command on the branch build -> exit 0; stdout JSON `"text": "# toy-notes"` (the README's real first heading), `"response_outcome": "text"`, `"input_tokens": 18700`, `"output_tokens": 768`, `"limited": false`; the event stream carries `{"kind":"tool","detail":"file_read"}` and `{"kind":"tool-observation","detail":"{\"call_id\":\"call_Ovk0xsVEopCNFWamPeIFvoKT\",\"name\":\"file_read\",\"arguments\":{\"path\":\"README.md\"},\"outcome\":\"ok\",\"argument_bytes\":20,\"result_bytes\":236,…}"}` followed by `{"kind":"response","detail":"[response completed]"}`. The call dispatched under its announced name and call ID, and the tool really ran.
- [x] 1.4 @runtime (agent) field presence across the ring after the fix -> `function_call_arguments.delta` 380x `has_name:false`/`has_call_id:false`, `function_call_arguments.done` 8x `has_name:false`/`has_call_id:false`, `output_item.added` 6x `has_name:true`/`has_call_id:true`, `output_item.done` 8x `has_name:true`/`has_call_id:true`. `output_item.done` does restate the identity; it simply arrives after the event the pre-fix decoder died on, which is why announcement must be accepted from either event.
- [~] 1.5 @runtime (agent) run the live turns in the designated live project directory itself -> defer: its Kuru writer lease is held by the TUI running in the session's cmux surface, so `kuru run` there fails with `project already has an active Kuru writer`; the turns above ran instead in a sibling directory inside the same isolated live tree, with a copy of the same `.kuru/config.toml` (`provider = "codex"`) and the same README, against the same `KURU_DATA_DIR`/`XDG_CONFIG_HOME`, and nothing outside that tree was touched.

## 2. The mirrored live shape is a regression fixture

- [x] 2.1 @regression (agent) splice `accepts_subscription_function_call_announced_only_on_output_item_added` and `resolves_function_call_identity_by_item_id_or_fails_the_turn` onto the pre-fix `sse.rs` in a detached `origin/main` worktree and run `cargo test -p kuru-connectors --lib providers::sse::tests::accepts_subscription_function_call` -> `test result: FAILED. 0 passed; 1 failed`, panicking on `called `Result::unwrap()` on an `Err` value: completed function arguments lack name` — the exact live error from 1.1.
- [x] 2.2 @unit (agent) with the fix, `cargo test -p kuru-connectors --lib providers::sse` -> `test result: ok. 14 passed; 0 failed; 0 ignored; 0 measured; 191 filtered out`, including both new tests and all ten pre-existing decoder tests unchanged.

## 3. The honesty guarantees still hold

- [x] 3.1 @unit (agent) in `resolves_function_call_identity_by_item_id_or_fails_the_turn`, a call whose name is announced nowhere — no `output_item.added`, and an `output_item.done` item carrying `call_id` and `arguments` but no `name` -> error containing `completed function call lacks name`. The turn fails rather than dispatching an unnamed call.
- [x] 3.2 @unit (agent) same test: a name announced as `file_read` on `output_item.added` and contradicted as `shell` on `output_item.done` -> error containing `disagrees`. Announcements are never merged or silently preferred.
- [x] 3.3 @unit (agent) same test: a `function_call` whose `arguments` are `{"path":` and never settle as complete JSON -> `ChatGPT completion stream contained invalid protocol data`, with no argument content in the message.
- [x] 3.4 @unit (agent) same test: arguments settling before the item's completed announcement, and a name announced only on `added` with a terminal item that omits it -> both succeed with `output[0]["name"] == "file_read"` and `output[0]["call_id"] == "call_1"`; identity is resolved by item ID in either order.
- [x] 3.5 @unit (agent) #49's guarantees are untouched -> `terminal_output_owns_order_and_presence_but_never_drops_visible_text`, `accepts_subscription_terminal_envelope_with_empty_output`, `reconciles_sparse_raw_indexes_against_full_terminal_output` (whose `function_call_arguments.done` still carries a `name`, still checked against the terminal item), `rejects_tool_fragment_for_non_function_terminal_item`, the duplicate-ID, budget and redaction tests all pass unchanged in 2.2.
- [x] 3.6 @unit (agent) `diagnostics::tests::function_call_trace_admits_shape_fields_but_never_arguments` -> the record retains `event_type`, `item_id`, `has_call_id`, `has_name`, `arguments_len` and an `arguments = "{\"path\":\"secret-sentinel\"}"` field on the same event is dropped: `assert!(!records.contains("secret-sentinel"))` and `record["arguments"].is_null()` both hold. `mise run //apps/kuru-tui:test -- diagnostics` -> `test result: ok. 9 passed; 0 failed; 0 ignored; 0 measured; 70 filtered out`.

## 4. Repo checks stay green

- [x] 4.1 @integration (agent) `mise run //packages/kuru-connectors:test` -> `test result: ok. 205 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 42.06s`.
- [x] 4.2 @integration (agent) `mise run //packages/kuru-runtime:test -- stream` -> `1 passed; 0 failed; 140 filtered out`; `mise run //apps/kuru-tui:test -- stream` -> `2 passed; 0 failed; 77 filtered out`; `mise run //apps/kuru-tui:test -- tool` -> `3 passed; 0 failed; 76 filtered out` plus `4 passed` in the embedded-runtime suite and `1 passed` in the tool-host suite, no failures anywhere.
- [x] 4.3 @unit (agent) `mise run format:check` -> `Finished in 1.29s`, no diffs; `mise run //packages/kuru-connectors:lint` -> `Finished `dev` profile` with no warnings; `mise run typecheck` -> `Finished in 88.12s`.
- [x] 4.4 @unit (agent) `mise run docs:check` -> `Public docs artifacts, local links and anchors passed (/kuru/).`
- [~] 4.5 @integration (agent) full-workspace `mise run test` / `mise run coverage` / `mise run check` -> defer: explicitly excluded by this unit's instructions, which forbid running the coverage and check aggregates; the package suites, streaming and tool filters, format, lint, typecheck and docs above were run instead.
