## 1. Equal peers and isolated persistent memory [critical]

- [x] 1.1 @integration (agent) run a deterministic multi-actor conversation with peer-directed followups and inspect captured prompts -> workspace tests passed: direct A2A peer routes, speaking-phase consultation, bounded cyclic peers, separate part/relationship notes and captured request isolation; runtime suite has 33 passing tests
- [x] 1.2 @integration (agent) reopen a SQLite store and restore session, part and relationship histories -> core persistence/concurrent initialization tests and CLI session resume passed; canonical relationship reactivation retains its history; listing sessions is read-only and competing project writers are locked out
- [x] 1.3 @integration (agent) propose relationship and dreaming topology changes, including invalid retirement and undo -> model-driven relationship selected the group speaker; valid model dream addition persisted; last-role retirement rejected; undo retained archived histories; failed persistence rolled back running state

- [x] 1.4 @eval (agent) inspect deterministic peer transcripts across all four modes for role distinction, direct peer addressing and explicit computational framing -> reviewed built-in role/instruction profiles and installed four-mode demo traces; IFS has seven members across four roles, polyvagal and Freudian three, Jungian five; captured peer envelopes identify sender/recipient and prompts explicitly frame state as modeled rather than clinical or sentient; demo output is labeled offline

## 2. Usable terminal and CLI harness [critical]

- [x] 2.1 @e2e (agent) run the built binary with demo provider and JSON output, then inspect/resume its persisted session -> real CLI integration passed in all four modes with JSON responses, unchanged resumed IDs and stored turn counts; normal OpenAI app is running in cmux workspace kuru, surface 37
- [x] 2.2 @integration (agent) exercise layered config, ancestor AGENTS.md, selectors/commands, terminal rendering and cancellation -> 13 config tests and TUI editor/picker/command tests passed; real provider future cancellation released its pool permit; Unicode editing, small terminal rendering, bounded context and writer lease regressions passed
- [x] 2.3 @runtime (agent) launch the real TUI in a PTY and send input and quit -> PTY integration typed chat, switched models/efforts/modes, invoked dreaming, resized and pasted text; persisted transcript confirmed submission and terminal attributes were restored on exit; cmux live build launched and ready screen verified

## 3. Provider authentication and protocol/tool contracts [critical]

- [x] 3.1 @integration (agent) drive mock Codex app-server and Responses peers through dynamic models and completion -> 29 connector tests passed with actual local JSON-RPC subprocesses/HTTP listeners, model pagination, new effort strings, native function outputs with encrypted reasoning, malformed responses and transport cancellation/error cases
- [x] 3.2 @runtime (agent) inspect the installed Codex app-server and exercise supported model discovery or authenticated inference when available -> connector agent observed latest Codex 0.153.4 discover 8 models with current max/ultra effort values and return OK from gpt-6-astra at low effort using isolated cwd, named read-only permission profile and structured output; no credentials printed; `cargo run -p kuru-connectors --example codex_probe -- --infer`
- [x] 3.3 @integration (agent) use temporary roots and local MCP/A2A peers for successful and malformed calls -> MCP stdio/HTTP sessions, pagination, SSE, errors and shutdown passed; real CLI A2A server authenticated requests, routed to Ego, advertised the actual ephemeral socket and shut down on SIGINT; full live gpt-6-astra Freudian turn issued file_read and returned an exact random marker without changing the file
- [x] 3.4 @integration (agent) attempt filesystem traversal, symlink escape, protected file writes, disabled shell and timeouts -> contained file CRUD and capability tests passed, including symlink/hard-link protection, write opt-in, actual shell timeout/output bounds and malformed/oversized protocol messages

## 4. Reproducible install, update and development [critical]

- [x] 4.1 @e2e (agent) install the source build into a temporary prefix and invoke the installed binary -> `KURU_INSTALL_DIR=/tmp/kuru-source-install-smoke mise run install` built release and installed kuru 0.1.0; installed binary completed all four framework demo conversations, resumed the original session and listed exactly four sessions; direct `cargo install --path apps/tui --locked --root /tmp/kuru-cargo-install-smoke` also succeeded
- [x] 4.2 @integration (agent) install/update from fixture release archives and test bad checksum, platform and path inputs -> `mise run test:install` passed 11 real filesystem/process tests on 2026-09-09, including package-to-installed-executable roundtrip, corruption, duplicate checksum/member, traversal, symlink, compressed-archive size and non-executable rejection with prior binary preserved
- [x] 4.3 @integration (agent) run mise check and coverage over the workspace -> mise run check exited 0 on 2026-09-09: 115 Rust tests and 11 installer tests passed, Clippy denied warnings, Rust/TOML/Python formatting and shell/Python/workflow lint passed, cospec strict validation and generated drift checks passed; LCOV reported 4999/5119 executable lines (97.66%), with no application modules excluded; production-only cross-check excluding trailing cfg(test) modules and test_support.rs was 3469/3567 (97.25%)
- [x] 4.4 @integration (agent) validate CI/release workflows and package an artifact with a verified checksum -> latest Actionlint, ShellCheck, Ruff and repository invariants pass through `mise run lint:tooling`; fixture release archive created by the real packager, checksum-verified by installer and executed in `test_real_packager_to_installer_roundtrip`; four-platform mise.lock regenerated; actual release binary was packaged, installed and self-updated via embedded CLI installer, then rejected a corrupted archive while preserving the installed hash
- [x] 4.5 @integration (agent) review README and architecture/config/protocol/install documentation against actual CLI help -> inspected built `kuru --help`, `update --help` and `serve --help`; README plus usage/configuration/protocol/install/development/dependency-audit docs match flags, selectors, storage, redaction and release-source requirements; source and binary self-update examples exercised without assuming a public remote
