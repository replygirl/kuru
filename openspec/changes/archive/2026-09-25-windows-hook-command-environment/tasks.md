## 1. Windows hook command compilation

- [x] 1.1 Record the exact native Windows source-install compile failure before the correction and preserve its E0308 boundary as the regression target.
- [x] 1.2 Match the private configured hook helper to the already-owned environment vector and gate the Unix-only RPC import, without changing command resolution or environment content.

## 2. Verification and delivery

- [x] 2.1 Run affected local format, lint and typecheck checks, strict Cospec validation and apply; record observed outcomes in the verification ledger.
- [x] 2.2 Preserve normal push hooks and exact-head native Windows source installation as required publication gates, with unrun results recorded as pending.
