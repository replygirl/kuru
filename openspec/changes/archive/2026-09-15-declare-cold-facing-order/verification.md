## 1. Authored cold facing [critical]

- [x] 1.1 @regression (agent) run fresh equal-activation turns in every built-in mode -> `cold_ties_follow_each_modes_authored_order` passed against actual Dolt; Self, Connection, Desire and Continuity led with `mode-authored-order`. The prior UUID minimum differs for IFS, Freudian and Jungian.
- [x] 1.2 @unit (agent) remove the first authored identities from tied eligible drafts -> `authored_tie_break_skips_missing_parts_and_falls_back_to_stable_ids` passed; the next present authored ID won without topology mutation.
- [x] 1.3 @unit (agent) tie only dream-authored parts or relationships -> the same focused control passed with ascending `dream-a` and reason `stable-id-order`.

## 2. Stronger selection rules

- [x] 2.1 @unit (agent) exercise caller target, active focus, greater activation and previous-speaker continuity -> `automatic_speaker_selection_is_stable_and_persists_after_dolt_reopen` passed with each existing reason, including continuity after real Dolt reopen.
- [x] 2.2 @unit (agent) cancel or fail before completion, then retry selection -> the 86-test runtime package task passed `failed_speaking_does_not_replace_completed_speaker` plus the cancellation/publication controls.
- [x] 2.3 @eval (agent) inspect four fresh recorded selection traces -> the all-mode control inspected each `TurnOutput.events` trace and found the approved leader and `mode-authored-order`; framework/runtime source retains equal-peer language and adds no authority.

## 3. Static checks

- [x] 3.1 @integration (agent) run core/runtime format, lint, typecheck and focused selection tests -> `//packages/kuru-core:test` passed 34 tests, core and runtime lint passed, runtime all-target typecheck passed, `cargo fmt --all -- --check` passed, and the package-owned runtime task passed 86 tests.
