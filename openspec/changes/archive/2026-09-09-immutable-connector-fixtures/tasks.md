## 1. Immutable native fixtures

- [x] 1.1 Add a regression proving shared executable identity, concurrent isolated protocol plans and final-owner cleanup; capture failure before and success after the fix.
- [x] 1.2 Replace per-fixture executable writes with owned immutable hard links and verify the existing connector suite.
- [x] 1.3 Run the full repository gate, record observed evidence, validate and archive this change before the final commit.
- [x] 1.4 Remove stale no-release-yet claims from packaged and repository installation instructions while retaining authenticated private-repository installation.
