# Dependencies

## Blocked by

None.

## Soft-blocked by

None.

## Reviewed providers

The current active-change directory contains only this change. Archived
`tool-permission-decisions`, `document-permission-engine`, and the existing
file-tool/output-bound changes provide the retained-root permission boundary,
native selector contract, and redacted bounded projection that this feature
extends. P03 does not require an unarchived sibling change.
