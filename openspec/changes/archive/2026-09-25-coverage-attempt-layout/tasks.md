## 1. Attempt layout, success precondition and evidence gaps

- [x] 1.1 Write receipts under a canonical `attempt-<n>/` directory and accept both download layouts in collection; verify with the aggregate `coverage::` tests for flat, nested, mixed and mismatched layouts
- [x] 1.2 Keep the output relay draining after log or relay errors and move the supervision tests to paused time; verify with the relay and supervision `coverage::` tests
- [x] 1.3 Refuse the report unless every shard succeeded, move the script's `try` to cover the whole shard, and correct the workflow comments and docs; verify with `release_workflow`, `powershell_diagnostics`, `lint:tooling` and `docs:check`
- [x] 1.4 Record the reversed artifact order in the archived `coverage-stall-evidence` record, run clippy and format checks, and name the unrun native Windows checks

Evidence (macOS arm64, 2026-09-25; artifacts authored, `validate --strict` passed and `apply --json` exited 0 with gate `clear` before any code change): `cargo test -p kuru-delivery --features tooling --locked --lib coverage::` 18 passed, 0 failed (new: `aggregate_accepts_the_single_artifact_download_layout`, `receipts_require_a_canonical_run_attempt`, `relay_keeps_draining_after_log_or_relay_errors`); a deliberate regression returning from `relay_output` on the log error made the relay test fail in 0.01s rather than hang. `--test powershell_diagnostics` 4 passed. `--test release_workflow` 20 passed with the pinned cocogitto 7.0.0 invoked through a PATH wrapper script and communiqué 1.3.5; with cog's install directory placed on PATH directly, the four cog tests fail with `cog failed (exit status: 1)` independently of this diff, as the previous change also observed. `cargo clippy -p kuru-delivery --features tooling --all-targets --locked -- -D warnings`, `cargo fmt --all -- --check`, `git diff --check`, `mise run lint:tooling` (actionlint, shellcheck, repository invariants) and `mise run docs:check` all exit 0; Cargo.lock unchanged. Unrun: no Windows compile (`aws-lc-sys` needs a Windows SDK) and no pwsh on this host, so the restructured `windows-coverage.ps1` and the `cfg(windows)` runner glue are first parsed and compiled by the hosted `windows-2025` shards; all hosted rows remain deferred to the lead's push.
