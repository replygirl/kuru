## 1. The report reads each shard's latest uploaded attempt in either download layout [critical]

- [x] 1.1 @unit (agent) run the aggregate `coverage::` tests with fixtures laid out as download-artifact v8.0.1 does (flat for one artifact, nested for several) -> all-flat first attempts are accepted; a partial rerun converting one shard to nested is accepted with the chosen attempts; a nested name that disagrees with its inner attempt, a mixed layout, a non-canonical inner attempt and a receipt written without a canonical attempt are refused with their specific reasons and no profiles copied. Observed: `aggregate_accepts_the_single_artifact_download_layout`, `aggregate_takes_each_shards_latest_attempt_and_fails_closed`, `aggregate_requires_exact_shards_receipts_and_unchanged_profiles` and `receipts_require_a_canonical_run_attempt` passed on macOS arm64, each refusal asserting its message and an empty target.
- [~] 1.2 @runtime (agent) GitHub-hosted `windows-2025` `Windows coverage report` on the lead's exact-head run, first with one attempt per shard and then after "Re-run failed jobs" of one shard -> defer: requires the lead's pushed exact-head CI; the flat single-artifact extraction is read from the pinned download-artifact source, not observed on a hosted run, and `needs.windows-coverage.result` after a one-leg rerun is unverified. No hosted result is claimed.

## 2. Failures keep their evidence and cannot turn the report green

- [x] 2.1 @unit (agent) run the relay and supervision `coverage::` tests -> an unwritable log leaves every byte relayed and progress observed, with the log error in the outcome; stalled supervision still terminates and reports on paused time. Observed: `relay_keeps_draining_after_log_or_relay_errors` passed (8-byte pipe, 46-byte output, log in a missing directory, and a refusing relay); a deliberate early return on the log error made it fail. Both supervision tests passed on `start_paused` time, the stalled case measuring exactly deadline plus cleanup bound.
- [x] 2.2 @integration (agent) run the `release_workflow` and `powershell_diagnostics` contract tests -> the report's first step refuses a non-successful shard result, and the script's single `try` precedes the Cargo work and records the failure. Observed: `release_workflow` 20/20 (pinned cog through a PATH wrapper) and `powershell_diagnostics` 4/4; `lint:tooling` actionlint exit 0. The script was not parsed by pwsh on this host.
- [~] 2.3 @runtime (agent) GitHub-hosted `windows-2025` shard that fails before its tests -> defer: needs a hosted shard failure; expected a diagnostics artifact holding `failure.txt`. No hosted result is claimed.
