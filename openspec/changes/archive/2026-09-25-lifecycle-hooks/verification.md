The dated results below belong to the preserved historical P14 branch, not to
this reconstruction atop archived P11 `3dc69c72`. The current source restores
the reviewed hook implementation and adapts its pre-turn projection to P11's
typed pending public turn. Current-lineage focused typecheck, behavior, and PTY
evidence is recorded below; full package suites, Linux/Windows native behavior,
and combined coverage remain unrun. The Cospec archive completed after the
focused evidence below. Historical results are retained
for provenance and do not close the unchecked rows by themselves.

Current-lineage focused macOS proof on archived P11 `3dc69c72` (2026-09-25):
`mise run //apps/kuru-tui:typecheck`, `mise run //apps/kuru-tui:lint`, and
`mise run format:rust` exited 0 on the reconstructed P14 source. The
real-memory `post_turn` runtime filter passed 2/2: a rejected public Settle
ran no post-turn hook or completed public turn, while a known rejected hook
annotation after successful Settle left the Completed turn and exact replay
intact without rerunning the hook. A separate pending-history/effective-input
provider-request filter passed 1/1, checking the original typed pending user
and prior settled records at dispatch. The connector invalid-intermediate
rewrite filter passed 1/1 for pre-turn and pre-tool chains; the later command
was never launched. The restored real-PTY hook filter passed 3/3 for
rewrite/annotation, refusal/stop followed by usable conversation, and
hook-started Kuru without recursion. These filters ran with package-owned
memory preparation and local process permissions. Strict Cospec validation
reported 0 errors and 0 warnings, and apply returned exit 0/clear. The new
CLI post-turn stderr reporting path also passed an exact native 1/1 fixture:
a separate fake-provider `kuru run --json` process returned a successful
parseable answer, emitted only a bounded failed post-hook classification on
stderr despite hostile raw hook stderr, and left a real Completed public turn
read through a checked managed attachment. Two preliminary fixture attempts
failed during an unnecessary second cold service start and then a direct-local
writer lock; the final fixture retains a canonical managed attachment instead.
Neither preliminary failure reached the reporting assertion. Native
Windows/Linux and full final-hook coverage remain open. `mise run cospec --
archive lifecycle-hooks` exited 0 and applied 12 spec deltas on 2026-09-25;
that archive does not prove the deferred native or coverage rows.

## 1. Configuration, trust, and process authority [critical]

- [~] 1.1 @integration (agent) parse documented hook chains through native TOML and the published JSON schema across user, repository, local, managed, and `-c` layers -> defer: final-head core lifecycle hook config/schema tests before merge; expected: both paths retain declaration order and bounds, arrays replace by normal precedence, managed constraints hold, and unknown events, fields, empty commands, or invalid bounds fail before activation
- [~] 1.2 @e2e (agent) run a repository hook before approval, after one-invocation approval, after persistent approval, and after changing its command, event, order, bound, or retained workspace identity -> defer: final-head TUI `lifecycle_hook_trust_preflight_binds_command_event_order_and_bounds` before merge; expected: no preflight side effect occurs, applicable approval activates only the reviewed command, and every authority change fails before hook/provider/tool startup
- [~] 1.3 @regression (agent) have an approved hook request a denied tool, an outside-root path, another provider route, or a different speaker -> defer: final-head runtime final-root, shell/MCP permission, and speaker-substitution tests before merge; expected: existing evaluator, root, route, and mode authority refuse every expansion

## 2. Pre-turn and pre-tool behavior [critical]

- [~] 2.1 @runtime (agent) run multiple pre-turn hooks that allow then rewrite, plus deny, timeout, malformed, oversized, and cancelled variants -> defer: final-head runtime `inspection_skips_hooks_while_runtime_rewrite_preserves_the_durable_input_and_replay` and connector intermediate-rewrite/protocol tests before merge; expected: declaration-order rewrites receive final input validation, failures make no provider/tool request, and the original durable user input remains unchanged. Runtime provider-request rewrite/refusal/retry tests prove final input and durable original; the shared connector HookHost tests prove oversized input refuses before command launch/count and a dropped pre-hook caller reaps its owned child. `run_admitted` awaits that same chain under the turn cancellation token and validates the returned TurnHookPayload before its possible-provider-dispatch marker. The oversized and cancelled cases were not rerun as separate runtime fixtures (2026-09-23).
- [~] 2.2 @integration (agent) rewrite tool name and arguments across read, mutation, shell, MCP, and invalid proposals -> defer: final-head runtime rewritten-file root/permission and tool-hook dispatch tests before merge; expected: Kuru discards prior decisions, reapplies schema/budget/root/permission checks to the exact final operation, executes only an allowed final value, and settles each original call ID once
- [~] 2.3 @regression (agent) change repository instruction or permission authority during ordered pre-tool admission -> defer: final-head `newly_activated_instructions_settle_stale_parallel_calls_without_effects` before merge; expected: later calls do not execute under stale authority and retain P06's truthful replan boundary
- [~] 2.4 @runtime (agent) rewrite candidate-local dream proposals to a forbidden tool class, annotate an accepted candidate receipt, then cancel another post hook after a candidate annotation -> defer: final-head runtime candidate dream rewrite/abandonment tests before merge; expected: dream-only validation refuses expansion, no user-turn or speaker event is invented, and abandonment leaves no live annotation or descendant. Observed `hook_tests::dream_tool_rewrites_stay_within_dream_validation_and_annotations_promote_from_candidate` and `hook_tests::cancelled_dream_abandons_candidate_hook_annotations_and_reaps_hook_descendants` in the native package suite, 161/161 passed on macOS (two test threads, 2026-09-23).

## 3. Settled effects and annotations [critical]

- [~] 3.1 @integration (agent) settle successful, failed, denied, mutating, and cancelled tool calls, then return valid, malformed, timed-out, denied, and oversized post-tool responses -> defer: final-head runtime tool-hook settlement, annotation-refusal, and cancellation tests before merge; expected: original effects, results, receipts, usage, and settled observations remain exact while each hook outcome is recorded separately
- [~] 3.2 @runtime (agent) settle an answer and run multiple post-turn hooks with mixed failure and annotation outcomes -> defer: final-head runtime `post_turn_failure_continues_without_changing_the_answer_or_starting_a_turn` and `post_turn_never_runs_before_settlement_and_failed_annotation_cannot_undo_it`, plus the CLI completed-JSON failure test before merge; expected: the answer and durable record never change, later post hooks still run, annotations are later-context-only, and no implicit provider request occurs
- [~] 3.3 @integration (agent) pressure actor context with visible and forbidden post-tool/post-turn annotations -> defer: final-head runtime `post_tool_annotation_stays_with_its_actor_and_can_be_omitted_by_context_fit` and later-turn post-turn context test before merge; expected: newest eligible annotations fit or omit through existing policy, forbidden content never crosses actor visibility, omission is truthful, and the unchanged tool result remains present. The native real-Dolt `post_tool_annotation_stays_with_its_actor_and_can_be_omitted_by_context_fit` test passed 1/1 on macOS (2026-09-23): a real post-tool annotation appeared in its owner's optional private context, never in another actor's actual provider request, and was omitted after typed context-fit retry while the current input and durable owner annotation remained. `post_turn_failure_continues_without_changing_the_answer_or_starting_a_turn` proves post-turn annotation enters only a later same-actor request; both events use the same actor/session annotation persistence and history path. `tool_hooks_cover_deliberation_and_speaking_calls_and_keep_results_separate` preserves the unchanged current tool result beside a post-tool annotation.

## 4. Speaker selection remains mode-owned [critical]

- [~] 4.1 @runtime (agent) run observe, stop, malformed, timeout, alternate-actor, and changed-reason speaker hooks across all built-in modes -> defer: final-head runtime speaker observe/stop/substitution tests before merge; expected: hooks see only a validated selection, stop causes no dispatch, invalid substitutions dispatch nobody, and topology/eligibility/mode policy remain unchanged
- [~] 4.2 @regression (agent) combine an explicit target with a hook that proposes another peer -> defer: final-head runtime `speaker_hook_cannot_substitute_an_explicitly_selected_peer` before merge; expected: explicit-target precedence survives and no supervisor-like substitution, fallback selection, or speaking provider request occurs. Native `hook_tests::speaker_hook_cannot_substitute_an_explicitly_selected_peer` passed 1/1 on macOS (2026-09-23): rewrite, observe-with-actor, and stop-with-actor responses all failed before speaking dispatch, preserved the exact topology, and left the durable user row intact.

## 5. Parallel ordering and exact retry [critical]

- [~] 5.1 @runtime (agent) admit multiple independent tool calls with distinct pre rewrites, reverse execution/post-hook settlement, and same-name calls -> defer: final-head runtime `parallel_post_hooks_settle_independently_but_rejoin_in_original_call_order` before merge; expected: pre chains follow original admission order, execution remains eligible for P06 concurrency, activity reflects actual settlement, and provider continuation restores original call/result/annotation order and IDs
- [~] 5.2 @integration (agent) lose the caller after an admitted tool effect and retry the completed turn by exact identity -> defer: final-head runtime managed lost-annotation-reply and exact completed-retry tests before merge; expected: owned settlement preserves the actual effect and hook outcome, retry returns stored projected bytes, and no hook, provider, or tool replays

## 6. Bounds, cancellation, cleanup, and secrecy [critical]

- [~] 6.1 @integration (agent) exercise one-shot hook children and descendants that block stdin, stdout, stderr, ignore graceful termination, exceed each byte/time/count bound, or lose their caller -> defer: final-head connector pressured-pipe/caller-loss tests and runtime cancellation tests before merge; expected: every pipe is bounded and drained, every owned tree is reaped, no later effect slips through a failed pre hook, and settled effects remain truthful under post cancellation
- [~] 6.2 @regression (agent) invoke Kuru from a hook command and return hostile UTF-8, terminal controls, paths, arguments, fake credentials, and result bodies -> defer: final-head connector hostile-output, CLI hook-origin/JSON stderr, and restored real-PTY hook tests before merge; expected: no hook recursion occurs and events, journal, CLI, TUI, errors, and traces contain only bounded projected classifications
- [~] 6.3 @integration (agent) execute the same deterministic fake hook matrix on Unix and Windows owned-process boundaries -> defer: final-head Unix connector owned-child tests and Windows `windows_owned_hooks_rewrite_annotate_and_stop_after_timeout` in the native Windows connectors job before merge; expected: command, cleanup, timeout, and cancellation semantics match without claiming an OS sandbox

## 7. User-visible contract and delivery

- [~] 7.1 @e2e (agent) drive a real PTY through allowed, rewritten, denied, stopped, annotated, failed, and cancelled turns synchronized to completed frames -> defer: final-head three restored real-PTY hook tests and shared completed-frame cancellation/next-draft test before merge; expected: help/status/activity/final output are truthful, annotations never replace answers/results, and the session remains usable. Hook-enabled completed-frame PTY tests prove rewrite/annotation, denied and malformed pre-turn with zero provider requests, stopped speaking, and an allowed later turn in the same session. The existing completed-frame PTY cancellation test proves the shared terminal cancel/draft/next-turn behavior; runtime and connector tests separately prove hook-owned caller-loss cleanup. A hook-configured PTY cancellation combination was not run (2026-09-23).
- [~] 7.2 @integration (agent) build and content-check configuration, tool, hook, trust, and protocol documentation -> defer: final-head `mise run docs:check` and core TOML/JSON schema parity tests before merge; expected: examples match parser/schema behavior and explicitly describe process authority, bounds, ordering, failures, omissions, and no plugin/sandbox claim. Observed `mise run docs:check` exit 0 (format, lint, VitePress build, content and local links) and core TOML/JSON schema parity tests on 2026-09-23.
- [~] 7.3 @runtime (agent) run affected package suites and combined workspace coverage through package-owned tasks on the final integrated tree -> defer: final-head normal hook `mise run coverage` (at least 90% lines) and native Linux/Windows package jobs before merge; expected: native platform behavior passes and workspace line coverage remains at least 90%
- [~] 7.4 @eval (agent) compare normalized actor requests and outputs for no-hook, allowed rewrite, denied rewrite, post-tool annotation, post-turn annotation, and stopped-speaker fixtures -> defer: final-head fake-provider request/output runtime pre-turn, post-tool, post-turn, and speaker tests before merge; expected: only canon-permitted input or annotation fields differ, settled results/answers remain byte-identical, and no hook output appoints a supervisor or adds an implicit turn

Observed partial evidence: `mise run //packages/kuru-runtime:test` exited 0 with 161/161 native macOS tests at the package's two-thread setting on 2026-09-23. It included exact refused-pre-turn retry, original durable input, completed replay, known rejected post-effect annotation write, candidate isolation, parallel ordering, and shared post-budget tests. The final annotation `record_id` field and TUI trust regression were added after that binary compiled, so focused final-source checks and integrated coverage remain open. `mise run format:check`, `mise run docs:check`, and `mise run cospec -- validate lifecycle-hooks --strict` exited 0; native Linux/Windows and combined coverage are not yet claimed.

P11/P14 integration evidence on 2026-09-23: P11 atomic anchor commit `23299c3` was merged into the P14 worktree atop `6655d75`; the merge source also includes the session-scoped annotation correction described in the design. The real-Dolt focused hook matrix passed 12/12 with the package-owned supervisor and two test threads in 49.41s. Its safe-retry regression covers both an interruption marker immediately after the denied original input and an older pending turn after a later answer; each passed in the same 1/1 test, with no original current input in any captured provider request and exactly one durable original user row. The matrix also covers post-tool continuation, later-turn post-turn visibility, candidate-only annotation/abandonment, refusal without duplicate input, parallel ordering, and no settled-result relabeling after a known rejected annotation write. The integrated real PTY rewrite/annotation test passed 1/1 in 9.03s, and exact-root lifecycle-hook trust preflight passed 1/1 in 15.12s. Runtime and TUI all-target typechecks and Clippy, root format check, docs check, and strict Cospec validation all passed on this integrated source. Independent bounded P11/P14 conflict and namespace/reconciliation reviews found no blocking issue. Final integrated-head combined coverage, native Linux/Windows, full final-source package suites, and archive remain open.

Focused protocol and final-authority follow-up on macOS (2026-09-23): the connector hook tests passed 6/6 in 1.31s, including strict rejection of unknown fields on otherwise empty `allow`/`observe` decisions, malformed or oversized responses, and pressured stdout/stderr that fails a pre chain before its next command. The package-owned real-Dolt runtime test `rewritten_file_read_is_checked_against_the_final_root_before_execution` passed 1/1 in 3.34s: a hook changed an allowed file read to an outside-root path, the original call ID received an error receipt, and neither file content was dispatched. Connector and runtime Clippy and root format check passed after these edits. These focused checks cover those exact cases; the remaining cases in rows 1.3, 2.2, and 6.1 are still open.

Focused final-permission and lost-reply follow-up on macOS (2026-09-23): `rewritten_file_read_cannot_borrow_its_grant_for_shell_or_mcp` passed 1/1 in 7.70s with the package-owned real-Dolt supervisor and local HTTP MCP fixture. An originally offered file read was rewritten to separately denied shell and MCP calls; each original call ID received a permission-error receipt, and neither the shell marker nor an MCP `tools/call` occurred. An interim synthetic post-append failure exposed an invalid 8,192-row session-history request against memory's 1,024-row cap; the final recovery path no longer scans history. The synthetic fixture was replaced by an actual managed-memory lost-reply test, which passed 1/1 in 3.68s: a sibling attachment observed the accepted exact session/actor annotation before only the caller's reply future was dropped; the retained unit receipt reconciled as committed, two original mutating effects and two annotations appeared once, and exact completed-turn retry reran no provider, tool, or hook. Managed annotation writes use `reconcile()`'s exact view-bound `Some(true/false)` outcome; an unresolved receipt retains the store's mutation fence. Source review found no supported accepted append that can return an uncertain `None`: local writes resolve internally or retain a pending fence, and the managed service hardcodes the expected Unit reply. `None` therefore reports a definite rejected or no-send annotation without changing settled work. Sandbox attempts failed before reaching the assertions because local loopback listeners could not bind; stated passes are native runs. Broader permission combinations, platform-native gates, and final integrated coverage remain open.

Additional focused P14 proof on macOS (2026-09-23): the existing P06 instruction-replan fixture now runs a real `pre_tool` rewrite before its first `file_write`; the native test passed 1/1, observed one hook launch, published new path-specific instructions, and left both stale proposed writes without effect. This covers the instruction-change branch of row 2.3; changed permission authority remains open. Speaker observe/stop/substitution tests passed 3/3 with the package-owned real-Dolt supervisor: an observed validated selection reached its original explicit speaker, stop dispatched no speaking request, and invalid actor-bearing decisions dispatched nobody. The existing real PTY rewrite/annotation fixture passed 1/1 after its pre-turn hook invoked `kuru tools` twice through pure inspection; no nested hook fired. A separate bounded CLI fixture passed 1/1: a hook-started `kuru run` in another project reached both ordinary demo provider lifecycles while the child-only origin marker kept the hook launch count at one. The fixture caps a missing guard at two launches. Connector hostile-output proof passed 1/1: malformed UTF-8 stdout and fake-secret/path/result-body stderr yielded only a classified failure, while an annotation redacted a fake credential and escaped terminal controls. The wider event/mode/terminal and OS matrix in rows 4.1, 6.2, 6.3, and 7.1 remains open.

Integrated-runtime fixture follow-up on macOS (2026-09-23): five focused real-Dolt public/private-context tests passed after fixtures seeded completed P11 public turns and session-owned private rows, preserving dream peer isolation, ambient visibility, whole Unicode rows, and whole-row fit behavior. Three more focused real-Dolt tests passed after legacy usage and accepted-turn fixtures used the durable session catalog and a duplicate interruption marker was correctly rejected after settlement. Runtime Clippy and native repository format check passed. These are local source checks; final integrated-head coverage and native platform gates remain open.

After integrating P11's atomic catalog/runtime mode checkpoint, four obsolete legacy-index fault fixtures passed focused real-Dolt tests with its one-shot, instance-scoped pre-send state-save refusal: preferences/mode, dream candidate save, focus/relationship/mode, and failed undo followed by successful retry. A separate real-Dolt memory test passed after the test-only refusal was carried into a newly begun candidate view: valid reads succeeded, the first candidate state write was rejected with unchanged live revision and values, and the next write remained candidate-local until abandonment. This replaces inert corruption of the removed `{scope}/sessions` index; it adds no production RPC or persistent fault setting. Final integrated-head coverage remains open.

Focused connector protocol and speaker acceptance on macOS (2026-09-23): all nine connector hook tests passed, including exact JSON stdin identity/payload, all six response variants, pre-launch oversized-request rejection without spending invocation count, and a blocked-stdin timeout whose started hook and descendant were reaped before a delayed marker could appear. The connector test-only correction was independently reviewed for false cleanup proof. Real-Dolt fake-provider speaker tests passed 3/3: observe and stop each exercised all four built-in modes; invalid actor or selection-reason fields, malformed output and timeout caused no speaking dispatch or topology change. These local tests complete scoped task 2.1 and 3.2 behavior; the wider real-PTY and native Windows/Linux matrix remains open.

Completed-frame real PTY hook refusal proof passed 1/1 in 8.60 seconds on macOS: a denied pre-turn and malformed pre-turn produced no provider request; a speaker stop reached ordinary earlier provider work but no speaking dispatch; a later allowed input in that same session reached speaking and rendered its final response. The test checks provider phases rather than assuming an internal invocation count. The first version failed only because it pinned that internal count, then the corrected test passed. The prior real PTY rewrite/annotation proof supplies original-input preservation and later-context visibility. Timeout and caller cancellation are proven in focused runtime/connector tests, but are not yet exercised through a real PTY; row 5.2 remains open for that exact terminal boundary.

Cancelled post-tool observation on macOS (2026-09-23): three focused real-Dolt tests passed for a held owned shell, a held speaking peer consultation, and an accepted deliberate `state_report` write cancelled after persistence. Each post-tool hook received the original call ID and an error receipt for the cancelled invocation, emitted one separate observed outcome, and left the tool's cancelled settlement and turn result intact. The existing tests also prove no exact-ID retry replays the shell/consultation or duplicates the accepted cognitive write, no later peer call starts after cancellation, and owned shell cleanup completes. This closes the previously missing sequential cancellation path locally; final integrated-head/native-platform gates remain open.

Final-validator mapping on the same local source (2026-09-23): `run_admitted` decodes and bounds the final pre-turn `TurnHookPayload` after the declaration-order chain; the real provider-request rewrite test proves this value reaches dispatch while the durable original remains unchanged. Its Allow then Rewrite commands append `a` then `b` to an exact `ab` marker only on the first turn, establishing declaration order and no inspection/replay launch. `run_pre_tool_hooks` decodes and bounds the final `ToolHookPayload`, then the existing `ToolHost::prepare_parallel_read` or `execute_for_actor_with_context` validates and executes that final name/argument pair under the same frozen invocation configuration. The runtime tests rewrite a read outside the root and a read into separately denied shell/MCP operations, proving the shared root and permission checks receive the rewritten pair; the P06 instruction-replan fixture proves a rewritten call still observes refreshed path instructions before a later write. The fixed invocation snapshot supplies route/mode authority; no mutable mid-invocation permission configuration or independent provider-route rewrite is admitted by these DTOs. Existing P06 validator tests cover shape, operation budget and normal read/mutation dispatch on that shared path. No separate authority-drift fixture is required for a state this protocol cannot create. The connector size-before-launch and caller-loss tests exercise the exact HookHost used by runtime pre-turn; runtime refusal tests exercise the no-provider boundary, and `run_admitted` composes them before dispatch. The real PTY generic cancellation fixture exercises terminal interruption and continuation, while hook-enabled PTY cases exercise refusal, annotation and later use. Neither a separate cancelled/oversized runtime pre-turn fixture nor a hook-configured PTY cancellation combination is claimed. Native Unix/Windows suites, integrated coverage and archive remain Delivery gates.
