# Dependencies

## Blocked by

None.

## Soft-blocked by

None.

## Existing authorization and coordination

The user authorized fixing CI and completing portable embedded dependencies.
All active proposals were reviewed: native-windows adds native consumers,
coverage-input-preparation changes task dependencies, refresh-current-dependencies
owns tool pins, and bundled-provider-client later refactors this byte preparer.
This fix requires none of their unfinished behavior. The archived embedded-runtime
and native-platform changes supply the existing downloader, staging and filesystem
contracts. No new prerequisite or permission boundary is introduced.

Serialize edits to delivery's bundle module with the blocked provider feature.
Let CI 34572200638 finish before pushing, retaining its Windows evidence. This
change does not authorize a hosted rerun, cancellation, Release or Pages dispatch.
