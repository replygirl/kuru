## 1. Useful and deliberately public documentation [critical]

- [x] 1.1 @integration (agent) Build production HTML and llms artifacts, then inspect the emitted content boundary -> `mise run docs:check` exited 0 on 2026-09-09; VitePress built 12 curated Markdown pages. After migration to app-owned npm tasks and native Rust validation, the full-check log `/tmp/kuru-native-final-check.log` records another successful production build and `kuru-delivery docs` check of artifacts, local links, anchors, and the public content boundary under /kuru/. The llms output contains only the selected guide/concepts/reference pages and landing.
- [x] 1.2 @manual (agent) Compare installation, configuration, commands, and protocols against current source and CLI help -> Read CLI definitions, framework membership, configuration/protocol docs, and source installer; `mise run run -- --help`, `update --help`, and `serve --help` exited 0 and matched the reference on 2026-09-09. OpenAI authentication links checked against the official authentication page; no account catalog or private smoke record copied.

## 2. Browser navigation and accessible presentation [critical]

- [x] 2.1 @e2e (agent) Open production preview at `/kuru/`, navigate a nested page, and search for configuration -> Headless Chrome ran `/tmp/kuru-docs-browser/verify.mjs` against the production VitePress preview on 2026-09-09 and exited 0. Local search navigated to `/kuru/reference/configuration#configuration`, a direct reload retained the page, and the mobile main menu navigated to installation. No page or resource errors were observed.
- [x] 2.2 @manual (agent) Inspect desktop and mobile screenshots in dark and light themes and exercise framework controls with keyboard and reduced motion -> The same Chrome run passed ordinary, unforced clicks for four distinct framework drawings with exactly one aria-pressed selection, Tab/Enter activation, light/dark switching, and no horizontal overflow at 1440px and 390px. Computed decorative animation changed from `kuru-breathe` to `none` under reduced motion. Visually inspected `/tmp/kuru-docs-desktop-dark.png`, `/tmp/kuru-docs-desktop-light.png`, `/tmp/kuru-docs-desktop-reference.png`, `/tmp/kuru-docs-mobile-dark.png`, `/tmp/kuru-docs-mobile-light.png`, `/tmp/kuru-docs-mobile-guide.png`, and `/tmp/kuru-docs-mobile-portrait.png`; text, navigation, artwork, and focusable controls remain readable without clipping. Removed the unused VitePress hero image background after real clicks exposed it intercepting the framework buttons.

## 3. Repository integration

- [x] 3.1 @integration (agent) Validate the cospec change and run the documentation build through mise -> Strict cospec validation/apply exited 0 before implementation; mise docs:build and docs:check exited 0. Following the browser fix, app-owned `mise run //apps/kuru-docs:format:check` passed all 18 matching files and `mise run //apps/kuru-docs:lint` passed config/theme/component checks on 2026-09-09. Final cospec revalidation is tracked in the task list while the separately diagnosed CLI JSON-output regression is repaired; this row records the observed original gate and completed build checks.
