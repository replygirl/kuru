# Dependencies

## Blocked by

None.

## Soft-blocked by

None.

## Siblings

The user-authorized `docs-delivery` work provides root workspace/toolchain and Pages deployment integration on the same branch. This site's source and build do not require that change to be archived first. Existing application behavior and the named TUI path are already archived. Parent explicitly assigned parallel implementation with these boundaries.
