## 1. CLI-owned generation

- [x] 1.1 Add pure `kuru completions` and `kuru man` output from `Cli::command()` with exact pinned generator dependencies and verify output from an empty foreign project creates no Kuru state or authority request.
- [x] 1.2 Add the app-owned mise generation task consuming an already-built selected binary, and verify bounded deterministic five-file host output plus nested-command help agreement. Delivery's five selected native release binaries and before/after hashes remain explicit release-only evidence in `verification.md`.

## 2. Exact release content

- [x] 2.1 Package the five read-only outputs as bounded tar.gz and ZIP support envelopes through delivery's existing codecs; add one strictly parsed shell-support-format marker to the checksum-covered core README, and verify local exact names, modes, sizes, hashes, marker forms and unsafe-member rejection. Actual five-target Release envelopes remain deferred in `verification.md`.
- [x] 2.2 Update release candidate assembly, `SHA256SUMS`, staged Windows and published-download inventory code for five target-paired envelopes while retaining strict three-member core archives. Local candidate inventory and decoder/refusal checks passed; actual Release assembly, staged Windows and published downloads remain deferred in `verification.md`.

## 3. Native installation and compatibility

- [x] 3.1 Extend the Unix bootstrap, stock PowerShell bootstrap and Rust updater to verify and stage the matching support envelope before existing executable replacement; after confirmed Unix executable publication, publish a checked stable man file. Local Unix direct-install/update and exact selected-root/man evidence passed; actual stock Windows installation remains an explicit hard pre-merge gate in `verification.md`.
- [x] 3.2 Preserve legacy executable-only versions and upgrade readers in the new installer and fixtures. The retained v0.4.1/v0.4.2 Unix reader and pure repair passed locally; the named retained stock PowerShell reader passed in PR88 Windows delivery/archive job 108054459359. Corrected final-head native Windows installed/repair evidence remains a hard pre-merge gate in `verification.md`; the distinct old executable updater is a post-publication Release check.
- [x] 3.3 Exercise support-stage, executable partial-publication, failure and retry outcomes with exact owned-root assertions; verify truthful diagnostics, retained previous or unresolved executable receipt, and no claim of cross-file atomicity. Existing bootstrap, archive, updater receipt and partial stable-man recovery tests provide the local contract evidence mapped in `verification.md`; native installation remains a separate pre-merge gate.

## 4. Integrated acceptance and documentation

- [x] 4.1 Document direct-install versioned files and fixed installed-executable Bash/Zsh/Fish/PowerShell/man activation without claiming Homebrew tap delivery, and verify docs build, links and examples.
- [x] 4.2 Record the completed local format/lint/typecheck/bootstrap, strict Cospec and normal 90% combined coverage checks, and assign remaining observed native and Release evidence in the verification ledger. Corrected stock Windows install/update/offline and activation fixtures still require successful exact-final-head native PR evidence before merge; their explicit pending state is neither a pass nor a waived merge gate. Exact target-binary generation, full release-candidate/staged-Windows inventory and published-download checks remain separate Release-only deferrals owned by Delivery's single Release workflow. The real v0.4.x executable-updater to new published binary to explicit pure-repair check also remains deferred to that immutable release; an old bootstrap installer is not an updater proof.
