## 1. Completion publication ordering

- [x] 1.1 Remove a confirmed or pre-spawn-terminated Unix worker's own registry entry before publishing its oneshot result.
- [x] 1.2 Add a deterministic wake-time regression with a concurrent owner, and retain existing confirmed and unconfirmed cleanup tests.

## 2. Verification

- [x] 2.1 Record actual Cospec gate, focused Unix shell suite, and scoped format/typecheck evidence.
