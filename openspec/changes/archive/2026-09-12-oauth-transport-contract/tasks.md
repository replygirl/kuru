## 1. Public contract

- [x] 1.1 Document the fixed native subscription compatibility values, API-key separation, support limit, and local-only logout; verify with the docs check.
- [x] 1.2 Document the connector literals and independent fixture locations used when updating a compatibility pin; verify no connector code is added.
