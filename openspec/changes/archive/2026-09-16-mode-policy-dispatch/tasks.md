## 1. Core relationship policy correction

- [x] 1.1 Add the bounded User-or-Peer relationship proposal origin and validate direct and participating-peer rules in `kuru-core`, and verify core unit tests cover valid and rejected origins.
- [x] 1.2 Preserve built-in profile validation and byte-exact role fixtures while adding the origin input, and verify existing four-mode profile goldens remain unchanged.

## 2. Runtime policy dispatch

- [x] 2.1 Store and publish the selected validated profile with the existing harness checkpoint/reconcile lifecycle, and verify an uncertain persistence outcome cannot expose an unpersisted profile.
- [x] 2.2 Route fresh topology and retirement/undo role checks through Roles, and verify a test profile changes seeds or rejects loss of a required role without changing dream consolidation.
- [x] 2.3 Route initial and next recipient scheduling, consultation, and shared contributions through Flow with pending/live validation, and verify policy suppression or invalid output has no enqueue, event, provider, or tool effect.
- [x] 2.4 Route direct peer and relationship admission through Peering before publication, and verify User/API parity plus peer participation, self, inactive, and nonmember refusal controls.
- [x] 2.5 Route speaker selection through Facing with target/focus/draft eligibility validation, and verify actor, reason, and provider request change only for a valid alternate policy.

## 3. Parity and integration evidence

- [x] 3.1 Build normalized four-mode golden fixtures for targeting, focus, activation, continuity, cold ties, relationships, peer delivery, consultation, limits, and durable outcomes, and verify policy-routed results match the refreshed baseline.
- [x] 3.2 Exercise test-only Roles, Peering, Flow, and Facing profiles against real runtime seams, and verify they change or refuse behavior without a shipping mode, UI control, policy executor, permission bypass, or supervisor.
- [x] 3.3 Run focused real-Dolt completion, cancellation, retry, dream role-validation, and candidate/undo controls after P7 integration, and verify P6/P7 authority, accounting, persistence, and isolation remain intact.

## 4. Scoped quality gates

- [x] 4.1 Run the affected core/runtime format, typecheck, lint, focused tests, strict Cospec validation, and independent boundary review, and record observed evidence in `verification.md` without claiming unrun native CI.
- [x] 4.2 Update the owning architecture documentation to describe the four effective dispatch axes and the remaining P9 boundary, and verify the documentation build, formatting and links.
- [x] 4.3 Run one combined instrumented workspace coverage suite with at least 90% line coverage, record its actual result, and verify archive readiness before the final branch commit and merge.
