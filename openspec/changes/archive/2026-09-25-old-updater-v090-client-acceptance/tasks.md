## 1. Parameterize the old-client acceptance helper

- [x] 1.1 Introduce an `OldClient { version, release, manifest_sha256, windows_archive_sha256 }` struct and an `OLD_CLIENTS` array in `packages/kuru-delivery/tests/support/mise_acceptance.rs`, and parameterize `old_updater_accepts_staged_release` over it; verify by `cargo check -p kuru-delivery --tests` (note: this file is only pulled into a compiled test binary via `apps/kuru-tui/tests/windows_mise.rs`'s `#![cfg(windows)]` `#[path]` include, so this macOS check does not compile the file itself — it only proves the rest of `kuru-delivery` still builds).
- [x] 1.2 Pin the real published v0.9.0 manifest and Windows archive SHA-256 digests, computed directly from the published GitHub release assets (`shasum -a 256`) and cross-checked against the release's own `SHA256SUMS` entry; verify by recomputing them independently and diffing against the constants in source.
- [x] 1.3 Run the loop in `run_staged` over both `OLD_CLIENTS` entries with the existing shared assertions (installed candidate bytes, absent managed support, upgraded-version report, sidecar regeneration match, no leaked private data); verify by `cargo fmt -p kuru-delivery` (parses the full file) and `cargo clippy -p kuru-delivery --tests -- -D warnings`.

## 2. Ground the Windows-only assertions without a Windows host

- [x] 2.1 Download and run the real published `aarch64-apple-darwin` binaries for both v0.4.2 and v0.9.0 to verify `--version` prints exactly `kuru <version>\n` for both (byte-verified via `xxd`), since version printing and CLI parsing are platform-independent; this also grounds the pre-existing v0.4.2 assertion, which had likewise never executed.
- [x] 2.2 Verify `update --help` lists `--version`/`--release-base` identically on both darwin binaries, and that a smoke `update --version 0.9.1 --release-base <empty-dir>` against the v0.9.0 binary fails with a plain "No such file or directory" (looking for `SHA256SUMS`) rather than a workspace-trust or authority error, confirming the update code path is reachable in the isolated environment the test builds.
- [x] 2.3 Confirm by diffing the `v0.4.2` and `v0.9.0` git tags that `packages/kuru-delivery/src/update.rs` is byte-identical between them (so this case proves a different build exercising the same path, not different updater logic), and that rollback/update-notice state stays rooted in the install directory (never `xdg-data`/`appdata`) in both.

## 3. Record what remains unexercised

- [x] 3.1 Record in `docs/release.md` and this change's proposal that neither pinned client proves shell-support continuity across an upgrade (both predate #88; confirmed `shell_support.rs` does not exist at all at the v0.9.0 tag, and its published README carries no `kuru-shell-support-format` marker) — tracked as the deferred verification row rather than left implicit.
- [x] 3.2 Record the unexercised risk surface this addition doubles: two independent ~50 MB downloads, each under `archive::read_asset`'s 60-second HTTP timeout, and two real `kuru update` subprocess runs, each cleaned up under `kuru_delivery::command`'s shared 5-second process-tree-quiescence bound (`command.rs::CLEANUP_TIMEOUT`) — a generic test-harness constant reused unmodified from the rest of the delivery test suite, not something specific to either old binary's own behavior, and not adjusted here.
- [x] 3.3 Record that native execution of `native_mise_installs_and_activates_exact_staged_windows_archive` against both old clients is a hard prepromotion gate this change cannot run itself; the first live `verify-staged-windows` Release job run is the actual verification, not any local check here.

## Observed checks and release obligation

- `cargo check -p kuru-delivery --tests`, `cargo clippy -p kuru-delivery --tests -- -D warnings`, `cargo fmt --all -- --check`: all clean. `mise run cospec -- validate --all --strict`: 0 errors, 0 warnings. hk's pre-commit hooks (cospec, tooling/repo-metadata, conventional-commit) passed on the implementing commit.
- Neither the v0.4.2 nor the v0.9.0 old-client subcase inside `old_updater_accepts_staged_release` has ever executed natively: `apps/kuru-tui/tests/windows_mise.rs` is `#![cfg(windows)]` and this host cannot cross-compile-check it (`cargo check --target x86_64-pc-windows-msvc` fails building `aws-lc-sys`, a `reqwest` transitive dependency, for lack of the `windows.h` SDK header — a pre-existing environment limitation). The existing `verify-staged-windows` release job runs both on the exact staged candidate; `publish` already depends on that job. Its Windows pass is a hard requirement before promotion.
- Real published-asset digest computation and real darwin-binary execution (tasks 1.2, 2.1, 2.2 above) are the actual evidence standing in for that unrun native check; they ground every assertion that is platform-independent, but not the Windows-specific process/IPC mechanics themselves.
