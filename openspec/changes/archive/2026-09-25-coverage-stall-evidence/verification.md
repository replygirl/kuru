## 1. A stalled Windows shard fails inside its job with evidence [critical]

- [x] 1.1 @unit (agent) run the `coverage::` deadline, runner-configuration, libtest-progress and fake-process supervision tests -> the deadline is job start plus limit less the 10-minute reserve, expired or future starts are refused, a tree past its deadline is sampled, terminated and awaited with bounded cleanup and bounded output drain, and the report names the unfinished tests and latest results. Observed: 15/15 `coverage::` tests passed on macOS arm64, including `shard_deadline_keeps_the_evidence_reserve_inside_the_job_limit`, `cargo_runner_config_and_ledger_bind_the_full_test_inventory`, `libtest_progress_names_unfinished_tests_and_recent_results` and `supervision_terminates_a_stalled_tree_at_the_deadline_with_evidence`.
- [~] 1.2 @runtime (agent) GitHub-hosted `windows-2025` `Windows coverage (<shard>)` jobs of the lead's exact-head CI run, expecting each shard to finish or fail inside its 90-minute limit, a failed shard to upload `…-<shard>-diagnostics-attempt-<n>`, and receipts to upload only on success -> defer: requires the lead's pushed exact-head CI on windows-2025; the Windows-only runner glue could not be compiled on macOS (`aws-lc-sys` C build needs a Windows SDK). No hosted result is claimed.

## 2. The report takes each shard's latest valid attempt [critical]

- [x] 2.1 @unit (agent) run the aggregate `coverage::` tests -> a partial rerun combining attempts 2 and 3 is accepted with the chosen attempts reported; a stale lower attempt is ignored; an invalid latest attempt (profile, source, tree, shard or attempt mismatch) is refused without fallback; later-than-current, missing, unknown and non-canonical artifacts are refused. Observed: `aggregate_takes_each_shards_latest_attempt_and_fails_closed` and the adapted `aggregate_requires_exact_shards_receipts_and_unchanged_profiles` passed, asserting each refusal reason.
- [x] 2.2 @integration (agent) run the `release_workflow` and `powershell_diagnostics` contract tests with the package-owned tools -> the workflow records the job start first, ties `KURU_COVERAGE_JOB_MINUTES` to `timeout-minutes`, downloads by per-shard attempt pattern, and the script passes the deadline and `--max-attempt` inputs. Observed: `release_workflow` 20/20 with package-pinned cocogitto 7.0.0 and communiqué 1.3.5 on PATH; `powershell_diagnostics` 4/4; `mise run lint:tooling` exit 0.
- [~] 2.3 @runtime (agent) GitHub-hosted `windows-2025` `Windows coverage report` job after a "Re-run failed jobs" of one shard, expecting pattern downloads to include the earlier attempt's artifacts and collection to print the accepted attempt per shard -> defer: needs a hosted run with a partial rerun; that `download-artifact` pattern downloads in a later attempt include earlier attempts' artifacts, and that `needs.windows-coverage.result` reports `success` when only one matrix leg was rerun, are both unverified here. No hosted result is claimed.
