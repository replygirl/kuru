## 1. Rejected removal of a delete-pending stage child [critical]

- [~] 1.1 @regression (agent) run `files::tests::private_temp_retries_a_denied_child_delete_after_the_holder_releases` on Windows native: it must fail before this change with `left: Some(Uncertain)`, `right: Some(Rejected)`, because the delete-on-close holder shared `FILE_SHARE_DELETE` and the child delete completed, and pass after, because a genuinely delete-pending name refuses the checked open and the removal is rejected before anything is removed -> defer: Windows native execution is pending; this fixture is `#[cfg(windows)]` and cannot run on the macOS host.
- [~] 1.2 @regression (agent) confirm on Windows native that the fixture's own premise assertion holds — an ordinary `File::open` of the blocked name reports native error 5 while the retained handle is held -> defer: Windows native execution is pending; the assertion is inside the `#[cfg(windows)]` fixture.
- [x] 1.3 @unit (agent) type-check the changed Windows-only fixture and exhaustion reporting against `x86_64-pc-windows-msvc` -> an isolated `rustc --target x86_64-pc-windows-msvc --edition 2024 --emit=metadata` snippet reproducing `delete_pending_private_file` and `wait_for_cleanup_retry` compiled clean; a full `cargo check --target x86_64-pc-windows-msvc -p kuru-memory` is impossible on this macOS host because `libsqlite3-sys` builds `sqlite3.c` with the MSVC target and no MSVC C toolchain is present.

## 2. Preserved cleanup contract and diagnostics

- [x] 2.1 @integration (agent) run the host-runnable staged activation and cleanup fixtures -> `cargo test -p kuru-memory --lib -- files::tests provision::native_tests` passed 12/12 on macOS with the shared verified bundle mirror, including `actual_warm_cache_verifies_concurrently_while_installation_lock_is_held` and `successful_activation_removes_its_disposable_stage`.
- [x] 2.2 @unit (agent) run memory all-target/all-feature typecheck, lint and repository formatting -> `mise run //packages/kuru-memory:typecheck`, `mise run //packages/kuru-memory:lint` and `mise run format:check` all reported clean.
- [x] 2.3 @manual (agent) confirm from the failing native evidence that the recovery predicate itself is correct and only the fixture was wrong -> runs 35235509039, 35235507600, 35235510132, 35235507684 and 35235507906 each report exactly one failure, this fixture, with the identical `Some(Uncertain)` vs `Some(Rejected)` values, while `real_embedded_windows_engine_installs_offline_and_corrupt_cache_fails_before_execution`, `actual_warm_cache_verifies_concurrently_while_installation_lock_is_held` and `fresh_staging_process_loss_after_ddl_is_preserved_and_never_reused` all pass; the predicate widened for rejected native error 5 stays.
- [~] 2.4 @regression (agent) run `provision::native_tests::published_engine_reports_failed_stage_cleanup_and_releases_cache_lease` on Windows native and confirm a persistent holder still exhausts the same bound, now reporting the reconcile attempt count and elapsed window alongside the retained first cause, and still preserving the stage and cache lease -> defer: Windows native execution is pending; the fixture and the bound are unchanged.
