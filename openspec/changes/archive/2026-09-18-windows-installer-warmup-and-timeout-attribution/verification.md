## 1. The flake mitigation and attribution are real changes, verifiable on this host only up to compilation [critical]

- [~] 1.1 @regression (agent) reproduce the original stall locally, then show the fixed harness no longer exhibits it -> defer: the underlying failure is a ~8.6% (3/35) intermittent PowerShell 5.1 engine cold-start stall observed only on Windows CI runners (`plan-flake-windows-installer.md`), explicitly marked "Diagnosable: false" there because no Defender/CPU/IO sampling was captured and the standalone investigation found no reliable local reproduction. This host is macOS: `powershell.exe` does not exist here, and cross-compiling to `x86_64-pc-windows-msvc` type-checks but cannot execute (see 1.2/1.3). There is no deterministic failing-before/passing-after unit test to write for a Defender/contention-dependent CI-runner cold start without fabricating one, which the operating instructions for this fix forbid. The mitigation is a design-level change (evidence and rationale in `design.md`) rather than a defect with a reliable local repro; genuine acceptance evidence is the next several Windows `application-shard` CI runs' flake rate and, on any future stall, the presence of `samples=[...]` in its error text (row 1.4).
- [x] 1.2 @unit (agent) `cargo check --target x86_64-pc-windows-msvc -p kuru-platform` -> clean: `Finished` with no errors or warnings, confirming `NativeChild::duplicate_diagnostic_handle`, `ProcessSample`, `sample_process`, and the `duplicate_process`/`duplicate_process_with_access` refactor all type-check for the real target, including every unsafe FFI call site.
- [~] 1.3 @unit (agent) `cargo check --target x86_64-pc-windows-msvc -p kuru-delivery` (and transitively `apps/kuru-tui`'s `embedded_runtime` test target) -> defer: this host cannot complete it. `reqwest` (a required, non-optional `kuru-delivery` dependency) pulls in `aws-lc-sys`, whose Windows build script requires `<windows.h>` from the Windows SDK; that header is unavailable when cross-compiling from macOS. Reproduced identically with and without `--no-default-features`, confirming this is a pre-existing host limitation, not something this change introduced (see 1.2, which isolates and confirms the actually-changed crate). Unverified on this host; must be confirmed by native Windows CI or a Windows SDK-equipped cross toolchain.
- [x] 1.4 @unit (agent) code review of the diff -> `output_with_limit_and_timeout`'s success path (`Ok(Output { .. })`) and every pre-existing field in its timeout error format string (`phase`, `elapsed`, `stdout_eof`, `stderr_eof`, `cleanup`, `stdout_close`, `stderr_close`, `stdout`/`stderr` prefixes) are unchanged; only a new `samples=[...]` segment is added and the sampler is `abort()`-ed unconditionally immediately after the timed future settles, on both the success and failure branches, before the success branch returns. `COMMAND_TIMEOUT` in `embedded_runtime.rs` and the install command's own construction (env, args, `-Verbose`) are unchanged; `warm_up_powershell_engine` is a new, separate call inserted strictly before them with its own `ENGINE_WARM_UP_TIMEOUT` constant.
- [x] 1.5 @unit (agent) confirm no existing access right changed -> `duplicate_process_handle`, `duplicate_inherited_process_handle`, and `current_process_handle` still resolve to `duplicate_process`, which still requests exactly `PROCESS_SYNCHRONIZE | PROCESS_QUERY_LIMITED_INFORMATION` via the new shared `duplicate_process_with_access` helper; only the new `duplicate_diagnostic_handle` requests the additional `PROCESS_VM_READ` right, and only for the diagnostic duplicate the sampler task owns.
- [x] 1.6 @regression (agent) the one part of this change reachable on this host — `apps/kuru-tui/tests/embedded_runtime.rs`'s `#[cfg(unix)]` `install_packaged`/`execute` path, which `warm_up_powershell_engine` is deliberately Windows-gated to leave untouched — still passes end to end -> `cargo test -p kuru --all-targets --all-features --locked -- packaged_install_and_update_preserve_complete_offline_memory --exact`: `test packaged_install_and_update_preserve_complete_offline_memory ... ok` (39.84s), packaging and installing the real `kuru` executable, running it, and round-tripping offline memory through a direct install and a self-update. This is not evidence for the Windows fix itself (1.1); it is evidence the Windows-gated additions did not disturb the shared/non-Windows harness code around them.

## 2. No weakened assertion, no widened timeout, no unproven retry

- [x] 2.1 @unit (agent) code review of the diff -> no existing assertion in `embedded_runtime.rs` or `command.rs` was removed or relaxed, no `#[ignore]` was added, no retry/backoff loop was introduced, and the install step's `COMMAND_TIMEOUT` (100 s) is numerically and semantically unchanged from before this change.
- [x] 2.2 @unit (agent) confirm the warm-up bound is genuinely separate -> `ENGINE_WARM_UP_TIMEOUT` is its own named constant with its own distinct `.context(...)` failure message ("warm up the stock PowerShell 5.1 engine ahead of the timed install step..."), applied to a different (trivial inline `-Command`) invocation than the install step; a warm-up timeout cannot be confused with, and does not extend, the install step's own bound or its error text.
- [x] 2.3 @unit (agent) confirm sampling is genuinely inert on the success path -> `sample_process`/the sampler task never influence `captured`'s `Ok`/`Err` outcome, a sample error is formatted into the trace (`sample_error=...`) rather than returned, and the trace (`Arc<Mutex<Vec<String>>>`) is only ever read after the timeout branch of `captured` is already known to be an error.

## 3. Checks

- [x] 3.1 @unit (agent) `mise run format:check` -> clean, after running `cargo fmt --all` once to apply this change's own formatting.
- [x] 3.2 @unit (agent) owning packages' lint tasks (`//apps/kuru-tui:lint`, `//packages/kuru-delivery:lint`, `//packages/kuru-platform:lint`) -> all clean: `cargo clippy --all-targets --all-features --locked -D warnings` passed for all three packages, no new warnings.
- [x] 3.3 @unit (agent) `mise run typecheck` -> clean across every workspace member (`kuru-core`, `kuru-delivery`, `kuru-memory`, `kuru-runtime`, `kuru` (apps/kuru-tui)).
- [x] 3.4 @unit (agent) `mise run //packages/kuru-delivery:test -- command` (targeted) -> `test result: ok` in every selected binary; 0 tests matched/ran (the changed Windows module and `tests/windows_command_capture.rs` are both `#[cfg(windows)]`-gated and compile to nothing on macOS) — expected on this host, not a pass on the new sampling code itself (see 1.3).
- [x] 3.5 @unit (agent) `mise run //apps/kuru-tui:test -- embedded_runtime` -> `test result: ok` in every selected binary; the literal name filter `embedded_runtime` matches 0 test *function* names (the acceptance test is named `packaged_install_and_update_preserve_complete_offline_memory`, in the `embedded_runtime.rs` *file*), so this specific invocation is a vacuous pass. The actual non-Windows path was verified directly instead — see 1.6.
- [x] 3.6 @unit (agent) `mise run cospec -- validate windows-installer-warmup-and-timeout-attribution --strict` -> `0 errors, 0 warnings — validation passed`. `mise run cospec:validate` (workspace-wide, all 22 specs) also passed.
- [~] 3.7 @runtime (agent) `mise run coverage` / `mise run check` -> defer: deliberately not run per this task's operating instructions.
