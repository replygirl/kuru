## 1. Workflow text pins the new topology [critical]

- [x] 1.1 @integration (agent) run `mise run //packages/kuru-delivery:test` on macOS arm64 -> `release_workflow` asserts: one `install` job without an OS job condition, carrying `cargo fetch --locked`, the offline install smoke, the installed offline runtime and the updater step (`GITHUB_TOKEN: ${{ github.token }}`, `KURU_UPDATE_CANDIDATE_BINARY` with the Unix `target/release/kuru` and Windows `target\x86_64-pc-windows-msvc\release\kuru.exe` paths, no `CARGO_NET_OFFLINE` or `KURU_DOLT_BUNDLE_OFFLINE`) after the runtime step; Windows-only steps guarded by `runner.os`; no `windows-install` job and no install steps in `coverage`; `native-gate` needs `install`; no `windows-2025` label; release.yml has no `native-tests.yml` call, `plan` needs `[source-quality, tests]`, `build` needs `[plan, bump, verify]`, `verify-staged` has exactly the three OS/target rows, and `deploy-docs`/`publish` need `verify-staged`. Observed on macOS arm64 (2026-09-26): the task exited 0 with `release_workflow` 21/21 passing, including `required_release_checks_precede_the_only_publication_job`, `native_workflow_shards_only_windows_and_keeps_the_aggregate_fail_closed` and the new `native_workflow_installs_and_accepts_the_previous_release_update_on_every_os` (which also pins the three-row `verify-staged` matrix, the Unix checksum-before-extract order, the Windows staged step as the job's last, token-free step, and identical Secret Service session text in `tests` and the Unix coverage job). Mutation check: adding `CARGO_NET_OFFLINE: "true"` to the install job's updater step made the new test fail; the workflow was restored. One earlier full run hit a 30 s timeout in the unrelated `bootstrap_install` checksum test under load average ~13; an immediate rerun of the whole task passed. Skipped here: `#[cfg(windows)]` tests and Windows-only test binaries.
- [x] 1.2 @integration (agent) run the `native-gate` script from the workflow in a real `bash` child (existing gate test, `#[cfg(unix)]`) -> accepts the Windows and Unix success topologies with install required or skipped, and rejects a missing, failed or cancelled install leg on both OS branches as well as the existing shard/report failures. Observed: `native_workflow_gate_rejects_incomplete_results` passed in the same run (five accepted topologies; nine Windows and eleven Linux/macOS rejected combinations, including an unrequested install).
- [x] 1.3 @integration (agent) run `mise run format:code ::: lint:rust ::: typecheck ::: lint:tooling ::: cospec:validate ::: cospec:managed:check ::: docs:check` -> exit 0, including actionlint over every workflow. Observed on macOS arm64: the chain exited 0 in 71 s; `lint:workflows` ran actionlint with no findings; `Repository metadata invariants passed.`; cospec validate `0 errors, 0 warnings`; `cospec update --check: no drift`; `Public docs artifacts, local links and anchors passed (/kuru/).` A concurrent mise postinstall `hk install` printed a `.git/config` lock warning without failing any task.

## 2. Hosted runners execute the new jobs

- [~] 2.1 @runtime (agent) GitHub-hosted runners for the CI matrix labels (`ubuntu-24.04` and `macos-14` on this base, `ubuntu-latest` and `macos-latest` after #106, and `windows-latest`) on this PR's CI run, expecting three `native-tests (<os>) / Installation, offline runtime and update` jobs to succeed, each updater step log to name the previous published release selected for the host target and the candidate installed, and `native-gate` and `ci-gate` to pass -> defer: this stage does not push; hosted jobs run only on the PR after #108 lands, and their timings are recorded in the PR body
- [~] 2.2 @runtime (human) an authorized Release dispatch on `main` after merge, expecting `tests` to pass before `plan`, all three `verify-staged` legs to pass before `deploy-docs` and `publish`, and no native coverage workflow to run inside the release -> defer: release dispatch is an external action requiring authorization; locally only the workflow text and actionlint are verified

Rebase onto main after #107 and #108 (2026-09-26): #107's pre-squash commit was dropped. #108's `verify:staged-windows` now re-runs previous-release update acceptance and reads an optional `GITHUB_TOKEN`, so the Windows staged step passes `GITHUB_TOKEN: ${{ github.token }}` and `release_workflow` now pins exactly that one token (no `GH_TOKEN`) instead of a token-free step; the fixture, mise and Kuru children still clear it. The install job's updater env names and per-OS binary paths were checked against `test:previous-release-update` in `packages/kuru-delivery/mise.toml` and needed no change.
