## 1. Per-OS install/update, release trim and staged parity

- [x] 1.1 Replace `windows-install` and the Unix `coverage` job's install steps with one per-OS `install` job (offline install smoke, installed offline runtime, previous-release updater acceptance, Windows-only offline-input and DLL-import steps behind `runner.os`), require it in `native-gate`, move step OS conditionals to `runner.os`, and verify with actionlint via `mise run lint:tooling`
- [x] 1.2 Rename the `windows-2025` runner label to `windows-latest` in `ci.yml`, `native-tests.yml` and `release.yml`, and verify no `windows-2025` remains under `.github` or in `release_workflow.rs`
- [x] 1.3 In `release.yml` remove `source-tests` and `verify-tests`, add the pre-bump Ubuntu `tests` job (ordinary `mise run test`, native Secret Service session, `CARGO_PROFILE_TEST_DEBUG=0`), replace `verify-staged-windows` with the three-OS `verify-staged` matrix and point `deploy-docs`/`publish` at it, and verify with actionlint
- [x] 1.4 Update `release_workflow.rs` for the new jobs, needs, labels, matrix rows, updater step and gate topology, and verify with `mise run //packages/kuru-delivery:test`
- [x] 1.5 Update `AGENTS.md`, `docs/release.md`, `docs/development.md` and `docs/verification.md` (staged acceptance on every supported platform, the new CI and release graph, the Unix staged leg's limitation), and verify with `format:code` and `docs:check`
- [x] 1.6 Validate the change record and verify with `cospec:validate` and `cospec:managed:check`

Evidence (macOS arm64, 2026-09-26; artifacts authored, `validate --strict` passed and `apply --json` exited 0 with gate `clear` before any edit): `mise run //packages/kuru-delivery:test` exited 0 and `mise run format:code ::: lint:rust ::: typecheck ::: lint:tooling ::: cospec:validate ::: cospec:managed:check ::: docs:check` exited 0 (details in `verification.md`). `grep windows-2025` finds no match under `.github` or in `release_workflow.rs`. Unrun: `mise run coverage` (CI-only by instruction), the `test:previous-release-update` task itself (defined by #108, not in this tree), every hosted job (install on three OSes, the release `tests` job and `verify-staged` legs), and any Release dispatch. `docs/verification.md` dated records are unchanged.

Follow-up before the first push: `bundle:test-fixtures` (the upstream Windows ZIP for decoder tests, which `bundle:verify-native-build` uses) stays Windows-only in the install job, as in the former `windows-install`. The Linux install job clears the same unused SDKs as the coverage job. Release history shows the staged Windows job took 6–7 min, well inside `verify-staged`'s 45-minute limit. After these edits, `lint:workflows` and `mise run //packages/kuru-delivery:test` passed again (`release_workflow` 21/21).
