## 1. Windows owner lifetime modes [critical]

- [~] 1.1 @regression (agent) launch an independent fixture under a permitting kill-on-close Job, exit the starter and close that Job, requiring the child to remain usable and its retained handle to prove exact cleanup -> defer: the existing native fixture is retained and source-reviewed, but this macOS host cannot execute Windows Job behavior; exact corrected-head hosted Windows execution is required
- [~] 1.2 @regression (agent) launch a project memory starter under a nonpermitting outer Job, attach a second client, exit the starter while retaining the outer Job, and require the same owner generation and Dolt store to remain usable -> defer: the new source-reviewed native fixture requires hosted Windows execution
- [~] 1.3 @regression (agent) close the nonpermitting outer Job after a committed mutation, requiring owner and Dolt termination, endpoint/lifecycle recovery without stale takeover, and the committed row after a later ordinary open -> defer: the new source-reviewed native fixture requires hosted Windows execution

## 2. Narrow create fallback

- [~] 2.1 @integration (agent) exercise exact access-denied plus confirmed Job membership, requiring one retry with recreated mutable state, no breakaway and the same explicit inherited-handle list -> defer: Windows cross-compilation and independent source review verified the bounded branch and retained launch state; native access-denied execution awaits hosted Windows
- [~] 2.2 @integration (agent) exercise nonmember access denied, membership-query failure, and another create error, requiring no retry and the original or query error to remain observable -> defer: source review verified these errors remain fatal, but native negative-branch execution awaits hosted Windows
- [x] 2.3 @equivalence (agent) run platform and memory all-target checks plus formatting and strict Cospec validation -> existing owned Job, trusted supervisor, private IPC, and non-Windows call sites remain unchanged (observed: `//packages/kuru-platform:typecheck` passed in 5.43 seconds and `//packages/kuru-memory:typecheck` passed in 43.58 seconds with the verified bundle mirror and isolated target; `kuru-platform` all-target/all-feature Windows cross-check passed; `cargo fmt --check`, `git diff --check`, strict Cospec validation and the clear apply gate passed)

Independent bounded source review found no blocker. It confirmed that only `IndependentService` retries exact `ERROR_ACCESS_DENIED` after successful current-Job membership detection, with a fresh mutable command line and zeroed process result while retaining the same executable, environment, working directory, startup attribute list and explicit inherited handles. The permitting path still performs true breakaway; the fallback creates no Kuru Job and remains subject to the inherited outer Job. The platform fixture observes the starter root by its retained handle, and the memory fixture requires the same generation after starter exit, commits through that attachment, then requires a new generation and both committed rows after outer-Job closure before authenticated cleanup. Hosted Windows execution remains the explicit deferred evidence above; no local result is presented as native Job behavior.
