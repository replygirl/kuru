## 1. Durable usage survives dream branches and uncertain writes [critical]

- [x] 1.1 @runtime (agent) Real Dolt candidate open during admitted dream invocation and usage writes, then promotion and separate abandonment/rejection -> main revision unchanged by usage; promotion remains eligible; each invocation is present once after reopen regardless of candidate outcome.
- [x] 1.2 @integration (agent) Lose a ledger write reply after possible commit and reconcile exact receipt before another mutation -> one durable observation with no replay or doubled total; conflict under the same identity is rejected.
- [x] 1.3 @integration (agent) Writable open, close, reopen, schema mismatch, purge and memory export -> operational branch is validated before inference; invalid ledger fails new dispatch closed; live-only export explicitly excludes usage; purge leaves no detached ledger.

## 2. All provider invocations have honest partial accounting [critical]

- [x] 2.1 @runtime (agent) Scripted and real-Dolt deliberation, speaking, consultation, continuation and explicit/periodic/shutdown dream calls, including failure and cancellation -> one admitted record per actual invocation, ordered observed usage retained, no fabricated zero on absent terminal and no usage lost with rejected dreams.
- [x] 2.2 @integration (agent) Connector emits partial usage, terminal missing some components, explicit zero, terminal override and stream/sink failure -> local ordered observations reach the ledger before acknowledgement; terminal components win; missing remains unknown/incomplete; failure never authorizes partial calls.
- [x] 2.3 @runtime (agent) Completed exact retry, interrupted possible-dispatch turn and process restart -> retry adds zero records, unsafe replay stays refused, admitted-but-unobserved work is marked incomplete.

## 3. Effective native request fits or refuses before HTTP [critical]

- [x] 3.1 @integration (agent) Two-call native continuation with encrypted pending data and small window -> preflight measures selected pending input/output, matching receipts, instructions and schemas; mandatory overflow yields typed refusal with zero HTTP requests and no private bytes in diagnostics.
- [x] 3.2 @runtime (agent) Multilingual and tool-heavy history with optional older rows and a reserved output budget -> complete older rows alone are omitted, persisted rows/current receipts stay intact, and actual per-source omission counts match the request sent.
- [x] 3.3 @integration (agent) Several tool-loop rounds with a body growing beyond the window after an initial fit -> final pre-send check refuses that later round; byte transport cap remains independently enforced.
- [x] 3.4 @eval (agent) Scripted multilingual and tool-heavy conversations compare submitted request inventories with the estimator's source labels -> each sent request fits its selected bound, mandatory material remains coherent, and omission labels match actual removed rows rather than a fixed truncation guess.

## 4. Totals, price basis and provenance are truthful [critical]

- [x] 4.1 @runtime (agent) Reopen a pre-ledger session and a P7-marked session with partial/zero/cached/reasoning usage -> known input/output sums exclude subset double counts; old history stays permanently incomplete and absent values are not rendered as zero.
- [x] 4.2 @integration (agent) Freeze priced and unpriced invocation records, change catalog metadata, and inspect both API-key and subscription routes -> historical schedule/basis/source remain unchanged; missing price yields unknown/incomplete estimate; subscription amount is labelled API-equivalent rather than invoice/quota.
- [x] 4.3 @unit (agent) Resolve route-advertised, pinned and unknown-window models with configured/built-in assumptions -> provenance and bounds are retained; an unfamiliar model remains usable unless its effective mandatory request exceeds the assumed limit.

## 5. User-facing cost and context fit [critical]

- [x] 5.1 @e2e (agent) Real PTY `/cost` and request status at 120, 80 and 40 columns with P6 permission state present -> relevant active/facing request shows estimated context and provenance; token/cost gaps, pre-ledger history and actual omissions are legible without losing composer/cancellation.
- [x] 5.2 @integration (agent) Headless JSON and TUI conversation with optional history omitted -> machine output remains valid and private native continuation never appears in transcript, operational diagnostics or cost display; persisted history is unchanged.
- [x] 5.3 @integration (agent) New output-reserve setting and existing `assumed_context_window_tokens` through TOML and published JSON Schema -> valid documented examples match, unknown keys and invalid bounds fail in both, and oversized reserve refuses before dispatch.

## 6. Repository checks and review

- [x] 6.1 @regression (agent) Package-owned memory, connector, core, runtime and TUI format/typecheck/lint/doc tasks plus strict Cospec validation -> all relevant commands exit zero with recorded logs; native fixture evidence is identified separately.
- [x] 6.2 @regression (agent) One combined instrumented workspace coverage run -> behavioral suite passes and workspace line coverage remains at least 90% without exclusions or duplicate full-suite runs.
- [x] 6.3 @equivalence (agent) Independent scoped review against approved P7 contract and P6/P5 boundaries -> no unresolved ledger, continuation-privacy, branch-lifecycle or user-presentation blocker remains.

Native Windows/macOS/Linux CI, installation and packaging are post-archive delivery verification for the eventual PR and release. Their run/job links must be recorded in delivery review when available; this ledger does not claim an unrun native pass or make remote CI a fabricated local archive prerequisite.

Combined coverage: `mise run coverage` passed (exit 0, 629.44s) with 44,474 of 47,443 workspace lines covered, or 93.74%. All behavioral targets passed, including 121 runtime tests. Log: `/private/tmp/kuru-phase1-accounting-coverage.log`; preserved LCOV: `/private/tmp/kuru-phase1-verification/accounting-coverage.lcov`. No threshold, exclusion or fixture dependency was relaxed.

Independent review: the runtime owner reviewed core/native connector preflight and usage observation; the core/connector/TUI owner separately reviewed runtime admission, cancellation, dream/retry identities, mandatory receipts and presentation; root reviewed memory, lifecycle and reducer behavior plus the application adapter. Resolved findings included absent versus zero, original-terminal completeness, frozen-price partial estimates, removing lifetime record caps, atomic count/suffix reads, exact interruption-marker omission counts, and a bounded two-slot context projection that retains the facing measurement across a following dream. All reported source blockers are resolved. The final regression additions exercise these behaviors; no production scope was deferred by review.

Final static checks: workspace `mise run typecheck` passed (34.33s); all workspace Rust lint targets passed after the connector test-only cleanup described below. `mise run format:code` and strict Cospec validation passed. Logs: `/private/tmp/kuru-phase1-accounting-typecheck.log`, `/private/tmp/kuru-phase1-accounting-lint.log`, `/private/tmp/kuru-phase1-accounting-lint-final.log`, `/private/tmp/kuru-phase1-accounting-format-final.log`, and `/private/tmp/kuru-phase1-accounting-cospec.log`. The last aggregate lint attempt caught only the two redundant test macros; their final scoped lint rerun passed, with no suppression.

Observed local memory evidence: `mise run //packages/kuru-memory:test -- usage_ledger::tests` passed 7/7 using the package-owned verified real-Dolt fixture. It establishes permanent-branch isolation while a candidate is open then promoted, main-head stability, reopen durability, pre-ledger markers, duplicate/conflicting receipts, committed-but-unacknowledged receipt reconciliation, branch-pinned history-window totals, terminal-gap incompleteness, explicit-zero versus absent values, and frozen normal/long-context price arithmetic with unpriced cache-write uncertainty. The focused frozen-price reducer additionally proves fully reported normal-tier API-standard and API-equivalent sums, pre-ledger uncertainty, and a missing cached rate preserving only the known normal-input/output subtotal. Focused real-Dolt purge and active-export fixtures passed after admitting a ledger invocation: whole-project purge reopens with no record, and main-only export has no ledger key or invocation identity. A focused reserved-branch fixture drops `operations.label`, commits the malformed branch, and proves writable reopen rejects it. `mise exec -- cargo check -p kuru-memory --all-targets --all-features --locked` and `mise run //packages/kuru-memory:lint` passed. Native Windows/Linux CI remain unrun here.

Documentation evidence: `mise run docs:check` passed (exit 0, 5.38s), including formatting, lint, VitePress build, generated public artifacts, links and anchors. Log: `/private/tmp/kuru-phase1-accounting-docs.log`. The initial restricted attempt could not initialize mise's installed-tool cache and repository hooks; the same authorized local check passed with host access.

Observed local core/connector evidence: `mise run //packages/kuru-core:typecheck`, full `//packages/kuru-core:test`, and `//packages/kuru-core:lint` passed; the focused new accounting serde/sequence test passed after its final edit. `mise run //packages/kuru-connectors:typecheck`, full `//packages/kuru-connectors:test` (198 passed), and `//packages/kuru-connectors:lint` passed. The native Responses fixture makes one accepted two-call request and one accepted continuation, then enlarges an older input before the third round: typed `ContextTooLarge` reports a mandatory native continuation, exactly two HTTP requests remain recorded, and neither its diagnostic nor `ContextMeasured` contains the private sentinel. A separate first-request overfit records zero HTTP; DemoProvider's local prompt also refuses oversize. The two held-terminal API/subscription SSE tests passed after their expected `ContextMeasured` prefix was made explicit. The first connector suite run had exactly those two stale event-order assertions fail; the corrected rerun passed. The first focused native test attempt was blocked by sandbox loopback bind (`Operation not permitted`); its identical host-access rerun passed. No native OS CI result is claimed here.

Observed local TUI evidence: `mise run //apps/kuru-tui:typecheck` and `//apps/kuru-tui:lint` passed, and root's final workspace typecheck/lint passed after the two-slot adapter. Focused package-owned tests passed for `/cost` after a real Demo turn, unchanged session usage across completed `/retry`, exact `503-500=3` stored-history display omission, active-operation context fencing, and a coalesced same-turn Speak then Dream snapshot that retains the facing fit. The real PTY `/cost` fixture passed at 120x35, 80x24 and 40x18, showing assumed-window provenance and keeping the composer usable. A separate real PTY resumed a stored large prior turn under a smaller assumed window, completed a short follow-up, and displayed the actual optional-row omission notice. A headless JSON fixture likewise completed the short follow-up under that smaller window, parsed its unchanged machine output, found no prior optional-row sentinel in output/diagnostics, and confirmed the prior row still existed in the live memory export. Its first attempt failed only because the new fixture put root config keys under `[memory]`; the corrected root-key placement passed. Native connector preflight separately confirms private pending bytes stay inside the connector and its `ContextMeasured`/error contain no sentinel; source review confirms UI `/cost` and transcript consume only non-content accounting projections. Native OS CI remains unrun.

Additional connector/core evidence: `mise run //packages/kuru-connectors:test -- native_sse_preserves_partial_usage_and_stops_on_observer_write_failure` passed. A real loopback SSE response emitted `Usage(input=7, output=2)` before `Failed`, with no Completed and exactly one HTTP request; a second real loopback terminal made a fallible observer reject its Usage, yielding no Completed and exactly one HTTP request. The existing collector test passed in the full 198-test connector suite and proves the raw terminal report reaches the observer before absent components are filled into the returned Completion; memory's real-Dolt reducer tests prove terminal override, explicit zero and partial absence. `mise run //packages/kuru-core:test -- unknown_models_keep_absent_prices_and_resolve_labelled_fallbacks` passed after adding an explicit `Pinned` provenance assertion; the earlier full core suite covered advertised precedence and unknown-model configured/built-in fallback. The direct Demo preflight fixture permits an unfamiliar model under the assumed bound and refuses over-fit before inference.

Observed local runtime evidence: `mise run //packages/kuru-runtime:typecheck` and `mise run //packages/kuru-runtime:lint` passed after the final runtime changes. With the package-owned real-Dolt supervisor prepared, `RUST_TEST_THREADS=2 KURU_TEST_SUPERVISOR_PREPARED=1 cargo test -p kuru-runtime --lib --locked accounting_tests:: -- --nocapture` passed 12/12. Those fixtures exercise separate typed phase admissions for deliberation, speaking, consultation and explicit/periodic/shutdown dream calls; success, failure before or after a usage observation, cancellation, terminal gaps and exact retry; permanent pre-ledger incompleteness; whole-row private/public/note fit retries under one admission with intact current tool receipts and matching omission counts; and an abandoned dream whose observed usage is identical after close/reopen while the main revision is unchanged. The failure-before-usage fixture verifies one admitted incomplete invocation with unknown input/output, without manufacturing zero. The real DemoProvider fixture records each effective serialized prompt and actual `ContextMeasured`: oversized multilingual private history is removed only as complete rows until the final request fits its 3,500-token window with 200 tokens reserved for output. Every rejected measurement exceeds that same bound, while the final accepted request retains its exact current user/tool receipt and schema. Runtime source units and omission counts match the submitted final request; all source history rows remain stored and the whole sequence uses one admitted invocation. A prior restricted fixture launch was blocked by sandbox loopback binding; the same tests passed with authorized host access. The focused `engine::publication_tests::stopped_started_turn_resumes_once_but_possible_turn_never_dispatches` restart/replay test passed 1/1. The `mode_baseline_tests::` filter passed 3/3 after restoring prompt order, preserving all four built-in modes. Focused `review_tests::large_private_context_keeps_every_current_tool_receipt_whole`, `review_tests::shared_transcript_keeps_whole_unicode_rows_without_byte_slicing`, and `review_tests::malformed_current_tool_receipts_fail_before_provider_invocation` passed 1/1 each. `progress_tests::` passed 4/4 before the final bounded context-watch shape change; the subsequent accounting fixture checks that a same-turn automatic dream retains the latest facing Speak context while a shutdown dream clears it. The final accounting file was formatted with Rustfmt and `git diff --check -- packages/kuru-runtime openspec/changes/context-usage-accounting` passed. Native OS CI remains unrun.

Post-coverage syntax cleanup: root's single combined instrumented suite passed (exit 0), 44,474/47,443 lines or 93.74%; retained LCOV is `/private/tmp/kuru-phase1-verification/accounting-coverage.lcov`. After that measurement, two single-literal `concat!` wrappers in the new native SSE test were removed without changing either escaped SSE string literal or its runtime bytes. `mise run //:format:rust:fix`, `mise run //:format:rust`, `mise run //packages/kuru-connectors:lint`, and the focused `native_sse_preserves_partial_usage_and_stops_on_observer_write_failure` test all passed; `git diff --check` was clean. The syntax-only cleanup did not require another full coverage run. Native OS CI remains delivery verification for the eventual PR.
