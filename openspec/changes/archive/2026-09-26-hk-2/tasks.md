## 1. Upgrade hk to 2.2.0

- [x] 1.1 Pin `aqua:jdx/hk` to `2.2.0` in `mise.toml`, refresh only its `mise.lock` entries through mise, remove `HK_PKL_BACKEND`, and verify `hk --version` reports 2.2.0 and `git diff mise.lock` touches only the hk block on every previously locked platform
- [x] 1.2 Amend the hk 2.2.0 `Config.pkl` in `hk.pkl` and verify with `hk validate`, `hk run pre-commit` and `hk run pre-push` that the same steps run and pass, and that the six repository-scoped `hook.hk-*` Git config entries still route through `mise x -- hk run`
- [x] 1.3 Update `docs/dependencies.md` and `docs/release.md`, and verify with `format:code`, `lint:tooling`, `cospec:validate`, `cospec:managed:check` and `docs:check`

Evidence (macOS arm64, 2026-09-26; artifacts authored, `validate --strict` passed and `apply --json` exited 0 with gate `clear` before any edit): `mise install` with `lockfile = true` rewrote only the hk block of `mise.lock` (29 lines each way: version, specifiers, and URL/checksum for the same nine platforms), and `mise x -- hk --version` printed `hk 2.2.0`. `hk validate` exit 0; `hk run pre-commit --all` passed format, docs, tooling, cospec and cospec-managed (run type `check`, nothing staged, 40 s); `hk run pre-push` passed format, lint, typecheck, tooling, cospec, cospec-managed and docs (118 s). After rebasing onto the amended `pre-push-static` head, the static chain exited 0 and `git diff --check` was clean. Unrun: `mise run coverage` and `mise run test` (unaffected by a hook-runner pin; CI runs them). Because the worktree sits beneath the root checkout, mise also ran the root checkout's postinstall with hk 2.2.0 against the root's still-1.58.1 `hk.pkl`, which failed to evaluate and was reported as a warning; the worktree's own `hk install --mise` succeeded. This disappears once the root checkout is on the upgraded config.
