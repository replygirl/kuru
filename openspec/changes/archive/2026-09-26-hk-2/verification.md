## 1. hk 2.2.0 accepts the unchanged hook configuration [critical]

- [x] 1.1 @integration (agent) run `mise install`, `hk --version` and `hk validate` in the worktree -> hk reports 2.2.0, `hk.pkl` amending the 2.2.0 `Config.pkl` validates, and `git diff mise.lock` changes only the hk version, URLs and checksums for the platforms previously locked. Observed: `mise x -- hk --version` printed `hk 2.2.0`; `hk validate` printed `hk.pkl is valid` (exit 0); the lock diff is confined to the `aqua:jdx/hk` block with nine `v2.2.0` URLs replacing the nine `v1.58.1` platform entries (linux arm64/x64 gnu, musl and baseline variants, macos-arm64, windows-x64 and baseline).
- [x] 1.2 @e2e (agent) run `hk run pre-commit` and `hk run pre-push` in the worktree -> pre-commit runs only its glob-filtered check steps without staging anything, and pre-push runs exactly `format`, `lint`, `typecheck`, `tooling`, `cospec`, `cospec-managed` and `docs`, all passing. Observed on macOS arm64: `hk run pre-commit --all --plan` reported run type `check` with format, docs, tooling, cospec and cospec-managed; the real run passed all five in 40 s with `git status` showing no staged paths. `hk run pre-push` reported `✔` for exactly the seven static steps in 118 s, exit 0. The actual `git push` hook run is additionally observed at push time and reported in the PR.
- [x] 1.3 @integration (agent) inspect `git config --get-regexp 'hook\.'` and `git config --global --get-regexp 'hook\.|core.hookspath'` after `hk install --mise` -> the six repository hook entries run `mise x -- hk run <hook> --from-hook`, and no global hook or `core.hooksPath` is set. Observed: `hook.hk-{pre-commit,pre-push,commit-msg}.{command,event}` are present with commands `test "${HK:-1}" = "0" || mise x -- hk run <hook> --from-hook`; the global query returned nothing (exit 1).

## 2. Guidance matches the pin

- [x] 2.1 @integration (agent) run `mise run format:code ::: lint:tooling ::: cospec:validate ::: cospec:managed:check ::: docs:check` and grep docs for `1.58`, `pklr` and `HK_PKL_BACKEND` -> checks exit 0 and no current guidance names the old hk version or backend variable. Observed: the chain exited 0 in 10 s; the grep matches only dated archived change records, which describe past runs.
