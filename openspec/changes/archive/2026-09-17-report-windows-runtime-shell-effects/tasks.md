## 1. Report observed runtime shell effects

- [x] 1.1 Add two bounded receipt-equality facts to the existing `windows_tool_tests.rs` shell parse-error context, preserving all assertions and operation limits.
- [x] 1.2 Run focused static validation; record that the native timeout path requires a future Windows recurrence and do not rerun merely to obtain a green result.
