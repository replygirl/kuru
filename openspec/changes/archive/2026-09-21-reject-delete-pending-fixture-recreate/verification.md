## 1. Recreate at a just-removed fixture name no longer races a delete-pending teardown [critical]

- [~] 1.1 @regression (agent) run `provision::native_tests::real_embedded_windows_engine_installs_offline_and_corrupt_cache_fails_before_execution` on Windows native: it must fail before this change (`new_private_file(&binary).unwrap()` panics with "Access is denied (os error 5)", as in CI run 35584395348, because `remove_fixture_binary` returned as soon as the delete call succeeded while the just-executed image's name was still delete-pending) and pass after (the fixture now waits for genuine absence before returning, so the immediate `new_private_file` create-new no longer races the asynchronous teardown) -> defer: Windows native execution is pending; this fixture is `#[cfg(windows)]` and cannot run on the macOS host.
- [~] 1.2 @regression (agent) run `provision::native_tests::concurrent_cold_windows_provision_publishes_one_verified_native_identity`'s sibling corrupt-cache fixture on Windows native and confirm it no longer races the same teardown now that its `remove_fixture_binary` call routes through the same wait -> defer: Windows native execution is pending; the fixture is `#[cfg(windows)]`.
- [x] 1.3 @unit (agent) add unix-runnable unit tests directly exercising `wait_for_fixture_binary_absence` since the production fixture itself cannot run pre-merge on this host -> `wait_for_fixture_binary_absence_returns_immediately_for_an_absent_path`, `wait_for_fixture_binary_absence_succeeds_once_a_few_polls_observe_absence` and `wait_for_fixture_binary_absence_exhausts_with_the_established_message` all pass on macOS (`cargo test -p kuru-memory --lib -- provision::`, 46/46 including these three).
- [x] 1.4 @unit (agent) type-check the changed Windows-only call site against `x86_64-pc-windows-msvc` -> an isolated `rustc --target x86_64-pc-windows-msvc --edition 2021 --emit=metadata` snippet reproducing `wait_for_fixture_binary_absence` and the `remove_fixture_binary` call-site shape compiled clean; a full `cargo check --target x86_64-pc-windows-msvc -p kuru-memory` is impossible on this macOS host because `libsqlite3-sys` builds `sqlite3.c` for the MSVC target and no MSVC-compatible `stdlib.h` is present in the Xcode Clang toolchain.

## 2. Preserved removal-retry contract and diagnostics

- [x] 2.1 @integration (agent) run the host-runnable provision and stage-cleanup fixtures -> `cargo test -p kuru-memory --lib -- provision::` passed 46/46 on macOS across three consecutive runs (no flake); `cargo test -p kuru-memory --lib -- files::` passed 6/6.
- [x] 2.2 @unit (agent) run memory all-target/all-feature typecheck, lint and repository Rust formatting -> `mise run //packages/kuru-memory:typecheck`, `mise run //packages/kuru-memory:lint` (`-D warnings`) and `cargo fmt --all -- --check` all reported clean.
- [x] 2.3 @manual (agent) confirm no other kuru-memory test fixture shares this just-executed-image recreate-after-remove shape -> grepped `native_tests.rs`, `files.rs` tests, `store/*tests.rs` and `test_support.rs` for `remove_*` followed by a create/reopen at the same path; the two `remove_fixture_binary` call sites are the only sites that remove a just-executed binary and immediately recreate at its name, so no other site was changed.
