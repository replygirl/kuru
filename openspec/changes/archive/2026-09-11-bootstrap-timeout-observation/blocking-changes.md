# Dependencies

## Blocked by

None.

## Soft-blocked by

None.

## Existing scope and authorization

The user has authorized completing native integration and fixing failing CI.
Every active proposal was read: native-windows supplies Windows integration;
bundled-provider-client is still blocked by it; coverage-input-preparation,
bundle-download-recovery and refresh-current-dependencies maintain other checks.
None supplies a missing prerequisite for this Unix fixture correction.
The archived native-delivery, direct-release-install and portable-bootstrap-tools
changes establish the existing shell installer and real system-tool fixture.
The safe process dependency is already pinned in the workspace. This change
neither bypasses the provider gate nor changes the installer or CI topology.
