## 1. Settled admission records

- [x] 1.1 Emit one redacted `kuru.tool` admission record at the speaking and deliberation settled-admission sites, sharing one status function with external and cognitive records, and verify that `debug_cli_rotates_the_fixed_private_diagnostic_ring` fails before the fix and passes after
- [x] 1.2 Assert the `admission` record and the absence of the proposed tool name in the rotation test, and verify with the full `unix_shell_turn` file and at least 10 isolated rotation runs

## 2. Documentation and evidence

- [x] 2.1 Describe the admission records in `docs/usage.md` and `apps/kuru-docs/reference/commands.md`, and verify with `docs:check`
- [x] 2.2 Run focused checks and record evidence and unrun checks in verification.md
