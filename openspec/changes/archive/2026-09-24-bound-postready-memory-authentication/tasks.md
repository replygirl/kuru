## 1. Correct the owned startup deadline

- [x] 1.1 Carry one existing startup allowance through readiness, initial authenticated SQLx acquisition and identity verification without changing ordinary branch-pool timeouts.
- [x] 1.2 Preserve bounded probe close, owner reap and lifecycle-lease release on every failed or expired initial verification.

## 2. Prove the corrected boundary

- [x] 2.1 Add a real-Dolt regression with an instance-scoped delay inside the initial acquisition beyond the old two-second limit that succeeds within the remaining startup budget.
- [x] 2.2 Prove past-deadline refusal, owned cleanup and fresh checked reopen; run the focused P29 checkpoint and owning static checks.
- [x] 2.3 Record local evidence and the explicit exact-head hosted native/coverage merge gate, then archive this fix before merging P29.
