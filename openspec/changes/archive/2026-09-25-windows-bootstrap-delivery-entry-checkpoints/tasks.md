## 1. Localize first Windows delivery fixture launches

- [x] 1.1 Add existing verbose installer checkpoint mode to the first direct `-File` launch in `packages/kuru-delivery/tests/bootstrap_windows.rs`; verify the alias/refusal contract and bound remain unchanged.
- [x] 1.2 Add flushed fixed-text markers to the first encoded PowerShell wrapper in `packages/kuru-delivery/tests/bootstrap_windows.rs` around entry, dot-sourced recovery, and native fault dispatch; verify the same executable path and assertions remain.
- [x] 1.3 Run strict Cospec validation, the apply gate, source diff check, and independent bounded review before archive.
