## 1. Native migration observation

- [x] 1.1 Add the private additive migration observation allowance and apply it only to open-to-AfterDdl, resume-to-completed-reopen, and parent child-readiness boundaries in `recovery_tests.rs`
- [x] 1.2 Prefer a ready opening result over a closed pause semaphore, add boundary context to cancelled-loop errors, and preserve the 10-second invalid-options regression plus every existing recovery, ownership, and cleanup assertion
- [x] 1.3 Pass the two affected real migration recovery fixtures, the deterministic early-open-error regression, memory formatting, all-target/all-feature typecheck, strict Cospec validation, and diff checks. *(Observed: the deterministic early-open-error observer passed; the exact cancelled-upgrade fixture passed 1/1 in 9.61 seconds; the exact fresh-staging process-loss fixture passed 1/1 in 3.03 seconds and preserved the interruption; package prefetch and verified-supervisor preparation, all-target/all-feature memory typecheck, formatting, and diff checks passed. Initial setup attempts stopped before behavior because the prepared snapshot receipt was missing and sandboxed loopback was denied; neither is acceptance evidence.)*
- [x] 1.4 Record the pre-fix native timeouts without claiming a production defect and preserve post-push required GitHub checks as the merge gate. *(Observed: the pre-fix Windows logs reported the fixture's generic 10-second observation deadline and later channel closure without establishing why open had not reached the boundary. Required GitHub checks remain pending after archive and push.)*
