## 1. Pure four-shell and manual generation [critical]

- [x] 1.1 @integration (agent) generate all five bounded outputs from a foreign directory with no configured project -> `shell_support_generation_bypasses_invalid_workspace_authority_without_state` passed 1/1. It generated Bash, Zsh, Fish, PowerShell and the manual with invalid project configuration and no private state or provider output.
- [x] 1.2 @unit (agent) compare generated scripts and manual across repeat runs -> the focused foreign-root CLI test passed 1/1 after its bounded repeat assertion was added. Each of the four generated scripts and the manual had byte-equal stdout and stderr on two actual invocations with the same invalid foreign workspace; the manual exposed the current `memory export` command without the hidden callback.

## 2. Completion answers and activation [critical]

- [~] 2.1 @integration (agent) invoke the hidden answer path for Bash, Zsh, Fish and PowerShell with spaces, apostrophes, Unicode and described candidates -> defer: `usage_completion_answer_and_external_option_stay_pure` passed 1/1 and returned `memory` through the real hidden executable route for all four shells without state; `usage_completion_protocol_preserves_unicode_spaces_and_quotes` passed in the owning 2/2 library filter using Usage's public renderer with synthetic described candidates. The full combination of those candidate forms through each real shell callback remains for final-head/native checks.
- [~] 2.2 @e2e (agent) activate default generated scripts in supported native shells, including stock Bash without bash-completion -> defer: `packaged_install_and_update_preserve_complete_offline_memory` passed 1/1 with real offline install and update. Its isolated stock Bash callback returned `memory` with the selected installed executable first on PATH and a shadow `kuru` later; it did not preload bash-completion. Zsh registration and all four executable answer protocols passed, but actual Zsh/Fish/PowerShell callback activation on their supported native hosts remains pending. The unchanged 128 MiB shipping archive cap held; the private stripped fixture copy was 115,786,600 bytes.
- [x] 2.3 @integration (agent) generate explicit external-Usage scripts -> `usage_completion_answer_and_external_option_stay_pure` passed 1/1 and checked that each optional four-shell script selects Usage's external completion protocol. Default scripts select the Kuru-hosted callback. This does not claim an external Usage installation was exercised.

## 3. Release support compatibility [critical]

- [~] 3.1 @integration (agent) package and install the exact five generated support files -> defer: the focused embedded-runtime test passed real target-paired packaging, direct install, self-update, checked stock Bash callback and cold offline conversations on this macOS host. The existing five-member sidecar codec and delivery paths are unchanged; final-head native packaging/install checks on all targets remain required before merge.
- [~] 3.2 @runtime (agent) run final-head native CI and release verification -> defer: no Usage branch commit, final-head CI or release exists yet. Require normal hooks including workspace line coverage >=90%, native Windows/macOS/Linux install and shell support checks, exact-main CI after merge, and the later immutable Release candidate/public verifier before claiming release acceptance.
