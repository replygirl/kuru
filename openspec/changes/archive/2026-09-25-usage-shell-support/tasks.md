## 1. Usage-backed CLI

- [x] 1.1 Pin compatible published Usage library, CLI and native-script crates, remove unused generators, and verify Cargo.lock resolves one Usage version.
- [x] 1.2 Convert the current Clap tree into a Usage spec, generate four scripts and a manual, and verify deterministic bounded output.
- [x] 1.3 Add a hidden pure Kuru completion-answer route using Usage's native request/render protocol with optional external Usage script mode, and verify four-shell callbacks without bash-completion in default Bash.

## 2. Delivery and evidence

- [x] 2.1 Update owned CLI and installation documentation, and verify default self-hosted and optional external activation instructions.
- [x] 2.2 Verify the existing five-file support sidecar install/update/repair contract against Usage-derived bytes in the focused host integration, and identify the exact native checks still required before merge.
- [x] 2.3 Record observed local results and explicit final-head native/coverage/release obligations in verification.md; validate strictly and prepare the reviewed archive before the final source commit.
