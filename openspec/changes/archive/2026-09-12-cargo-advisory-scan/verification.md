## 1. Isolated advisory data and locked scan [critical]

- [x] 1.1 @integration (agent) run delivery fixtures against exact-origin, wrong-origin, dirty, stale, and changing RustSec database checkouts -> reviewed native macOS CLI rejection and changed-head/scanner-failure fixtures passed in `/private/tmp/kuru-advisory-cli-rejections-final.log` and `/private/tmp/kuru-advisory-cli-changed-head-rooted.log`; rejected missing/dirty/attached/stale/wrong-origin inputs never invoke the scanner fixture. The exact-origin allowlist and local stale-refresh path passed in `/private/tmp/kuru-advisory-local-refresh.log`. Actual scans report matching full pre/post SHA and timestamp.
- [x] 1.2 @integration (agent) inspect direct scanner invocation with a fixture lockfile and advisory configuration -> the direct-arguments fixture verifies the root lockfile, database, `--no-fetch` and `--no-yanked`. The pinned scanner's source selects delivery's project config before Cargo-home fallback; the wrapper additionally supplies an owned empty Cargo home. The hostile-home CLI fixture passed 1/1 in `/private/tmp/kuru-advisory-hostile-config-test-final.log`; root reviewed its isolated invalid home configuration, child cwd and noninherited home assertions. Project advisory ignores remain empty.
- [x] 1.3 @e2e (agent) deliberately refresh an isolated checkout from `https://github.com/RustSec/advisory-db.git` and run the ordinary local task -> real native macOS source install, refresh and offline scans exited 0. `/private/tmp/kuru-advisory-scan-hostile-cargo-home-final.log` and `/private/tmp/kuru-advisory-task-scan-config-final.log` record clean detached SHA `b50980aad8b8f14f77e25a97b32dd94bf008b0af`, commit timestamp `1788950992`, 1,243 advisories and 389 locked crates, with no findings and identical pre/post revision. Explicit scanner setup and the ordinary task also pass with Quality's auto-install disabled and mise postinstall hooks disabled; the exact task-only source-tool options are locked.

## 2. Ubuntu quality execution [critical]

- [~] 2.1 @runtime (human) observe an authorized reusable Quality workflow run on GitHub-hosted Ubuntu -> defer: no branch push or hosted workflow dispatch was authorized or performed. The isolated job, explicit tool installation and quality-gate dependency are reviewed; equivalent local setup/scan passed with the workflow's auto-install-disabled environment. Record actual hosted Ubuntu execution before merge; local macOS evidence does not establish it.

## 3. Static workflow and documented remediation

- [x] 3.1 @integration (agent) run the delivery package's focused checks and repository actionlint -> final owned rustfmt/Taplo, delivery lint/typecheck, docs:check, actionlint and strict cospec validation all exited 0 in `/private/tmp/kuru-advisory-*-config-final.log`. Rejection fixtures exercise absent/stale remediation; five native bounded-output cases cover output overflow, delayed root exit and descendant cleanup. One earlier mis-scoped package test invocation exited 101 on unrelated sandbox-blocked fixtures and is not a package-suite pass; complete workspace behavior/coverage and native Windows remain separate branch acceptance work.
