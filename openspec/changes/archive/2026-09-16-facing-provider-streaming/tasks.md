## 1. Provider stream contract

- [x] 1.1 Implement the required async provider stream and shared completion collector with bounded normalized events; verify terminal cardinality, sink errors, ordered typed results and absent/zero usage with focused collector tests.
- [x] 1.2 Surface native SSE deltas on both existing routes, reconcile raw items before final mapping and preserve private Pending; verify delayed/chunked HTTP fixtures, native continuation, malformed/bounded/cancelled streams and terminal-only success.
- [x] 1.3 Adapt demo and provider fixtures to the canonical stream path; verify existing connector behavior and explicit cached/reasoning usage through granular connector checks.

## 2. Runtime facing progress

- [x] 2.1 Thread owned optional progress through actor Work and observe selected speak-and-act only; verify real Dolt part/relationship speaker isolation from deliberation, consultations and dreams.
- [x] 2.2 Publish bounded replaceable turn/round/sequence snapshots and retain final-only durable append/tool authority; verify multi-round replacement, cancellation, retry/resume and transient summary handling with runtime fixtures.

## 3. TUI rendering and scheduling

- [x] 3.1 Add transient preview state and progress wakes with operation-generation fencing, partial-line tail and summary/activity region; verify bounded bursts, draft/composer preservation, cancellation and final settlement through focused view/scheduler checks.
- [x] 3.2 Exercise delayed native streaming in real PTYs at narrow and wide sizes with resizing, private sentinels, cancel and final-only CLI behavior; record completed-frame evidence of preview before completion and exactly one settled answer.

## 4. Integration and delivery

- [x] 4.1 Document live preview, privacy, cancellation and unchanged final CLI behavior; run docs checks and relevant static/format checks.
- [x] 4.2 Complete one combined coverage run and independent contract review; record observed pass/failure evidence, resolve concrete findings and validate the complete change strictly before archive.
