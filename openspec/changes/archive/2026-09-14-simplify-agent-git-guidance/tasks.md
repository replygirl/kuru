## 1. Contributor instruction cleanup

- [x] 1.1 Remove the blanket Git repository-selector clearing requirement from `AGENTS.md`, preserve the foreign-repository child-process test guidance, and verify the focused diff, strict Cospec validation, and whitespace checks. *(Observed: the focused diff deletes only the three-line blanket requirement; the foreign-repository child-process guidance remains; strict Cospec validation and `git diff --check` pass.)*
