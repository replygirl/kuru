# Dependencies

## Blocked by

None.

## Soft-blocked by

None.

## Context

No active change provides anything this one consumes. Its prerequisites are all
archived: `2026-09-17-report-dolt-stage-cleanup` (the fatal post-publication
cleanup report this change relaxes),
`2026-09-17-reconcile-windows-stage-delete-completion` (the bounded two-second
reconcile window this change keeps unchanged) and
`2026-09-18-complete-absent-checked-removal` (checked removal of an already
absent target, which the sweep relies on).
