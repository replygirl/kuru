## 1. Proven no-move activation recovery [critical]

- [x] 1.1 @regression (agent) use the actual Windows checked move with a held candidate descendant, observe the first access-denied no-move result, release only that blocker, and continue through the production activation path -> Windows memory/runtime job `104266775971` at exact head `13303e5075270bbd2cfe323774d59fcef0dbdf0e` passed `held_descendant_releases_after_checked_no_move_and_activation_recovers`; the released pre-fix staged acceptance log records the initial access-denied no-move failure
- [~] 1.2 @e2e (agent) run staged Windows release acceptance with the exact candidate ZIP and real mise installation path -> defer: the exact shipping release runs only after merge; record its result as delivery verification without making it a pre-archive gate

## 2. Strict failure and ownership boundaries

- [x] 2.1 @integration (agent) retain the real descendant blocker for the full recovery period -> Windows memory/runtime job `104266775971` passed `persistent_held_descendant_exhausts_checked_recovery_and_preserves_stage`, including the bounded allowance, original access-denied result and retained ownership assertions
- [x] 2.2 @regression (agent) exercise moved-success, occupied or rebound destination, changed or unobservable source, observation failure, and non-access-denied errors -> the host controls passed 7/7 and Windows memory/runtime job `104266775971` passed all 101 library tests, including the native unsafe-state controls; only moved-success reconciles as success and ineligible outcomes retain their errors
- [x] 2.3 @integration (agent) cancel the async provisioning caller while Windows activation recovery is waiting -> Windows memory/runtime job `104266775971` passed `cancelling_checked_activation_recovery_drops_stage_before_cache_lock`, including its no-later-move, absent-destination and ownership-release assertions

## 3. Repository verification

- [x] 3.1 @unit (agent) run focused memory filesystem and provisioning regressions, package typecheck, lint, formatting and strict Cospec validation -> host provision tests passed 7/7, the exact files regression passed 1/1, memory typecheck and lint passed, Rust formatting and diff checks passed, docs checks passed, and strict Cospec validation passed; Windows-only execution remains pending separately
- [x] 3.2 @runtime (agent) inspect the first PR native run at exact head `d8242fe95358db6428a613846f215f62a583124c` -> all four Windows coverage shards failed during compilation before runtime behavior, and the helper reported only its runner-local Cargo JSON path without rendering the compiler diagnostic
- [x] 3.3 @regression (agent) exercise `json-render-diagnostics` with a small isolated portable Cargo fixture -> Rust E0308 rendered on standard error while standard output retained parseable `build-finished` success false JSON with exit 101; after correcting the fixture, standard output contained the test executable `compiler-artifact` and `build-finished` success true with exit 0, and repository tooling lint passed (`/private/tmp/kuru-cargo-diagnostics.VNH77A`, `/private/tmp/kuru-activation-diagnostics-tooling.log`)
- [x] 3.4 @integration (agent) observe the coverage helper's successful native CI inventory -> all four head `13303e5075270bbd2cfe323774d59fcef0dbdf0e` Windows coverage shards compiled and entered checked execution through the diagnostic-bearing helper, and head `3551a236bd5ce617b867a2c7a8cdb301a17bc4ad` rendered Rust E0382 on standard error while preserving its Cargo JSON stream (`/private/tmp/kuru-pr26-3551-windows-memory-runtime-104284333646.log`)
- [x] 3.5 @runtime (agent) run the ordinary native memory and installation shards, including the genuine optimized executable and ordinary native mise acceptance -> exact-head run `34940694839` passed: Windows memory/runtime job `104288586845` passed 102/102 library and 12/12 lifecycle tests including all activation controls, application job `104288586942` passed the real native-mise offline test, installation job `104288587053` passed, report job `104294078572` collected all four receipts and enforced at least 90% coverage, and native aggregate job `104296240808` passed
