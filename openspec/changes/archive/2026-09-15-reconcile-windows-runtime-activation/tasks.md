## 1. Checked move outcome

- [x] 1.1 In `packages/kuru-memory/src/files.rs`, add a private typed proof for an initially uncertain move whose held source identity and name remain present while the destination is absent; preserve the original `PublicationError` and OS code, reclassify it as rejected, and keep existing `move_directory` callers and moved-success reconciliation behavior unchanged. The frozen `files.rs` is `98d1f3d2e170d2d07e73cdb2e22aa48b4dacb67098bbd0b0dd73b70b5d153872`; independent review confirmed the typed proof and unchanged erased caller contract.
- [x] 1.2 Add focused filesystem regressions for moved success, proven no-move, occupied or rebound destination, changed or unobservable source, observation failure, and non-access-denied errors; verify only the exact proven no-move result exposes the private retry marker and every original typed error remains available. The host provision native-test selection passed 7/7, the exact untyped uncertain no-move error test passed 1/1, and final native Windows memory/runtime job `104288586845` passed the complete refusal and recovery suite.

## 2. Windows runtime activation recovery

- [x] 2.1 In `packages/kuru-memory/src/provision.rs`, retry only Windows bundled-runtime activation marked proven no-move with raw OS error 5, at 20-millisecond spacing for at most two seconds, using one retained source `Directory`, private stage and cache lock in the same async future; use async waits, preserve explicit stage-before-lock release, and leave all other activation and non-Windows behavior unchanged. The frozen `provision.rs` is `5c87c5cc158b68b0d8c5685ca952987521ff829eb6b9982a1b8c825fa8475304`; independent review confirmed the narrow predicate, async timing, ownership and release ordering.
- [x] 2.2 Replace the existing manual held-descendant recovery with an actual native regression that fails before the fix, releases the real blocker only after observing the first denied no-move attempt, and passes through production activation after the fix; add persistent-blocker, preflight-permission-denied, unsafe-state and original-error controls that prove bounded failure, stage and lock retention, and no ineligible retry. The final native tests are `07818d2a8354a62b6043c9f1a5e4aac9f1d8c94903a5581d76be42538c871eed`; the release log supplies the pre-fix access-denied, retained-source and absent-destination failure, and final Windows memory/runtime job `104288586845` passed the released-descendant, persistent-descendant and source-open controls.
- [x] 2.3 Add a native cancellation regression that cancels during the async recovery wait and verifies the same future releases source, stage and cache-lock authority together without a later move or partial destination. Final Windows memory/runtime job `104288586845` passed `cancelling_checked_activation_recovery_drops_stage_before_cache_lock` with the independently reviewed source.

## 3. Documentation and verification

- [x] 3.1 Correct `docs/development.md` to describe the narrow bounded Windows activation recovery after access denied and checked no-move, while retaining strict failure and staging guidance for every other outcome. `mise run docs:check` passed, including the VitePress build and public link and anchor checks (`/private/tmp/kuru-activation-docs-check.log`); an initial sandbox attempt failed on tool cache and hook permissions before checks began and is not acceptance evidence.
- [x] 3.2 Change only the full Cargo arguments in `packages/kuru-delivery/support/windows-coverage.ps1` from `--message-format=json` to `--message-format=json-render-diagnostics`; use a small isolated portable Cargo fixture to verify a compile error renders on standard error and a successful build retains parseable standard-output JSON. The isolated fixture produced Rust E0308 on standard error and `build-finished` success false on standard output with exit 101, then produced a test executable `compiler-artifact` and `build-finished` success true on standard output with exit 0 after correction (`/private/tmp/kuru-cargo-diagnostics.VNH77A`). `mise run lint:tooling` also passed (`/private/tmp/kuru-activation-diagnostics-tooling.log`).
- [x] 3.3 Verify native CI still produces the successful executable inventory with the diagnostic-bearing Cargo message format, without deliberately breaking a PR build. At exact head `13303e5075270bbd2cfe323774d59fcef0dbdf0e`, all four Windows coverage shards compiled and entered checked execution through this helper; at exact head `3551a236bd5ce617b867a2c7a8cdb301a17bc4ad`, the same native helper rendered Rust E0382 on standard error while retaining its Cargo JSON stream, exposing the shared compile failure without a deliberately broken CI fixture (`/private/tmp/kuru-pr26-3551-windows-memory-runtime-104284333646.log`).
- [x] 3.4 Run focused filesystem and provisioning regressions, memory package typecheck and lint, Rust and documentation formatting/checks, strict Cospec validation, and diff checks; record Windows-only checks as pending until native execution rather than inferring them from the host. Memory typecheck and lint passed; the host provision native-test selection passed 7/7; the exact files regression passed 1/1; Rust formatting and `git diff --check` passed. An earlier malformed exact-test invocation exited 1 before executing tests and is not acceptance evidence. Native execution remains task 3.5.
- [x] 3.5 Run the deterministic native Windows recovery and refusal regressions plus ordinary memory, optimized executable installation and native mise acceptance in PR CI; verify the actual held-blocker recovery, persistent failure, cancellation, offline first use and reopen all pass before archive. Exact head `6ad9ea51798fc862411ca24105e35c15d63794d3` passed complete CI run `34940694839`: memory/runtime job `104288586845` passed 102/102 library tests and 12/12 Windows lifecycle tests including every activation control (`/private/tmp/kuru-pr26-6ad9-windows-memory-runtime-104288586845.log`); application job `104288586942` passed the real native-mise offline activation test (`/private/tmp/kuru-pr26-6ad9-windows-application-104288586942.log`); installation job `104288587053` passed native source install, offline-input refusal and installed offline runtime; report job `104294078572` collected receipt artifacts delivery `10386121057`, application `10385646307`, memory `10385254352` and connectors `10385780602`, enforced at least 90% coverage, and uploaded aggregate artifact `10386396301` with SHA-256 `6bd702033bdd73d2d7a3b401bd9441f4db1f9ee4ac6e5e649a8b9fda373ca081`; native aggregate job `104296240808` passed. The exact shipping staged Release result remains deferred until after merge.
