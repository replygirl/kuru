## 1. Sequenced active notes

- [x] 1.1 Add memory-owned sequenced note reads and an exact notes-row mutation using the existing transaction and uncertain-receipt path, and verify one selected sequence disappears without changing other rows or the prior revision.
- [x] 1.2 Project note sequences and roles through selected-mode runtime identity resolution, and verify archived exact IDs plus remembered and dream-authored rows remain readable.

## 2. Human controls and disclosure

- [x] 2.1 Add provider-free `kuru memory forget ID --note SEQUENCE` dispatch with absent-store refusal before legacy import, and verify its result and follow-up notes read disclose the one active deletion and retained history.
- [x] 2.2 Render note sequence IDs through existing TUI `/notes` output and update curated memory/command documentation with the active-view and retained-history limits.

## 3. Focused verification

- [x] 3.1 Run the owning focused memory/runtime/TUI checks when the shared Cargo target is idle, and record native Windows execution as required or deferred rather than inferred from another platform.
