## 1. Actor and session isolation [critical]

- [x] 1.1 @integration (agent) seed one real-Dolt actor with distinct rows in two sessions plus null legacy rows, compact both sessions, then inspect provider inputs and exported records -> each summary/range contains only its session, legacy and sibling sentinels stay absent, and every source row remains stored
- [x] 1.2 @runtime (agent) compact one part and one relationship whose private histories contain distinct sentinels -> each provider request and settled summary contains only its policy-admitted namespace
- [x] 1.3 @integration (agent) admit one cross-session summary through a test policy while denying raw cross-session history and P08 reasoning records -> only the latest cursor-selected admitted summary appears in later context

## 2. Atomic publication and recovery [critical]

- [x] 2.1 @integration (agent) compact an unchanged bounded snapshot through managed memory with a settled reasoning sidecar -> one operation-attributed context record, cursor and private sidecar settle in one revision with exact actor/session/view/revision/range/operation/invocation provenance and no fabricated turn
- [x] 2.2 @integration (agent) race a sibling append and a competing cursor advance before checkpoint -> typed stale refusal publishes neither summary nor cursor and the prior summary remains usable
- [x] 2.3 @integration (agent) interrupt after accepted checkpoint send but before reply, retire the owner and reconcile on a successor -> exactly one summary/cursor settles with no repeated provider invocation
- [x] 2.4 @integration (agent) compact on a candidate while main changes, then exercise promote conflict and abandon -> unpromoted summary never enters live context and no source conversation is lost
- [x] 2.5 @integration (agent) hold a Compact provider after dispatch while a different actor writes, then release it through managed real Dolt -> the original actor's exact source publishes once despite an advanced project HEAD, private rows stay separate, and usage/receipt settle once
- [x] 2.6 @integration (agent) change a consumed row's typed content or selected prior summary without changing its sequence/count, and separately advance a same-actor source/cursor during held provider work -> historical/current proof returns typed stale with no summary/cursor/sidecar effect; the paid post-dispatch invocation retains usage once and is not replayed
- [x] 2.7 @integration (agent) prove captured source revision ancestry and exact historical rows on live and candidate views through the checkpoint transaction -> a candidate-only or malformed historical revision cannot authenticate main, while a valid candidate ancestor with unchanged rows succeeds

## 3. Threshold, fitting and non-recursion [critical]

- [x] 3.1 @runtime (agent) exercise below-threshold, exact-threshold and hard-overflow full candidate requests with a small effective model window -> P7 omission does not hide the threshold, only threshold/overflow cases attempt one compaction, and the rebuilt ordinary request fits or refuses before dispatch
- [x] 3.2 @unit (agent) vary whole source-row sizes against the exact compaction estimator -> it selects the largest oldest prefix, never splits a row, and refuses when instruction plus prior summary plus one row plus reserve cannot fit
- [x] 3.3 @runtime (agent) make one compaction result still leave mandatory current input, tool receipts or opaque continuation over window -> no recursive compaction or ordinary provider dispatch occurs and mandatory bytes remain intact
- [x] 3.4 @regression (agent) validate threshold/reserve parser and published schema defaults, bounds and reserve-greater-than-window behavior -> parser and schema agree and the runtime refuses before inference

## 4. Accounting and cancellation [critical]

- [x] 4.1 @integration (agent) run successful, failed and cancelled compaction providers with usage observations -> each has one durable Compact-phase invocation, correct reserve/frozen price, honest incomplete components and no ordinary-turn misclassification
- [x] 4.2 @runtime (agent) cancel automatic and manual compaction while provider work and checkpoint settlement are separately held -> owned work drains, no later manual identity starts, and no post-cancel summary/cursor continuation occurs
- [x] 4.3 @regression (agent) retry a completed turn whose automatic compaction already settled -> stored turn output returns with no topology read, provider request, summary or usage duplicate

## 5. Manual command and truthful notice [critical]

- [x] 5.1 @e2e (agent) drive a real PTY with `/help`, `/comp` completion, `/compact ID`, `/compact` and cancellation using deterministic providers -> the registered usage agrees across surfaces, notices are stable-ordered and name exact ranges plus retained originals, and slash text never becomes a provider prompt
- [x] 5.2 @runtime (agent) submit inactive/malformed identity and no-eligible-source cases -> actionable refusal/no-op occurs before provider dispatch or cursor mutation
- [x] 5.3 @e2e (agent) resume after automatic compaction and inspect `/status`, context use and cost -> the summary notice remains truthful, raw history remains available, and compaction usage is included once

## 6. Final compatibility and quality

- [x] 6.1 @regression (agent) run core, connectors, memory, runtime and TUI all-target typecheck/lint plus docs checks -> final source and published configuration/usage documentation pass owning checks
- [x] 6.2 @regression (agent) run combined coverage and native Linux/macOS/Windows memory and PTY groups -> final tree retains the workspace coverage gate and platform lifecycle behavior
- [x] 6.3 @eval (agent) give a deterministic compaction provider policy-admitted rows plus adversarial sibling-session, legacy, reasoning and instruction sentinels -> the settled rolling summary preserves admitted continuity, contains none of the forbidden sentinels or instruction authority, and later context uses it only through policy selection
- [x] 6.4 @e2e (agent) repeat the native CLI and PTY multi-peer fixtures that exposed peer errors -> all intended peer calls finish, the stored shell receipt and final answer remain correct, and usage includes every settled invocation under both debug and ordinary presentation

Focused P09 runtime evidence, 2026-09-23 (local macOS, deterministic providers and verified bundled-Dolt mirror): `manual_compact_visits_active_parts_then_relationships_in_topology_order` passed 1/1 in 3.93s, proving stable part-then-relationship order and exact private namespace sentinels for row 1.2. `own_history_admits_only_typed_same_actor_cross_session_summaries` passed 1/1 in 9.28s. It seeds typed current-cursor summaries, raw rows and operation-private P08 sidecars in current, older foreign, newest foreign and other-actor scopes; the current summary appears once, only the newest fitting same-actor foreign summary appears, the oversized older foreign summary is omitted as one whole optional record and reported as `omitted_summary_rows = 1`, OwnHistory denial removes all shared continuity, and the current-session cursor remains exact. No foreign/current pre-cursor raw sentinel, P08 sidecar or other-actor summary reaches the provider. This closes row 1.3 and supplies the P09 half of P29's policy-selected continuity contract. A first run was rejected before startup because the sandbox could not bind Dolt's private loopback port; the authorized native run produced the result above.

Cursor-coherence regression, 2026-09-23: `ordinary_context_refuses_a_cursor_advanced_after_its_prior_summary_read` passed 1/1 in 3.92s. A managed reply barrier retains cursor/summary A while a sibling appends and checkpoints B; ordinary assembly rejects the mixed revision/cursor before provider dispatch. The shared projection and current-session raw suffix are then revalidated through the existing exact cursor/snapshot proof. This is stale-read evidence for ordinary context assembly, not row 2.2's still-unrun stale checkpoint mutation race.

Sizing and overflow evidence, 2026-09-23: the connector's exhaustive whole-prefix regression passed 1/1 and compares logarithmic selection against every brute-force prefix, including base overflow and one-row overflow, closing row 3.2. `mandatory_overflow_after_one_compaction_refuses_without_ordinary_dispatch` passed 1/1 in 2.88s: one compaction settles, mandatory current material still cannot fit, no ordinary dispatch or recursive compaction occurs, and the summary cursor remains durable, closing row 3.3. The cross-session fixture above separately proves foreign summaries use the same whole-record fit loop, retaining the newest fitting suffix and reporting older omission.

Cancellation and command evidence, 2026-09-23: `cancellation_after_compact_settlement_drains_the_atomic_checkpoint` passed 1/1 in 2.95s with a managed accepted checkpoint reply held; the caller stays pending, the exact operation sidecar and cursor commit, the reply drains, and ordinary dispatch remains zero before TurnCancelled. `real_pty_cancels_manual_compaction_before_later_identities` passed 1/1 in 6.51s with completed-frame synchronization: provider-held `/compact` cancellation starts no later identity, then a named retry reports its exact retained range and original-record disclosure without replay. The main deterministic PTY compaction flow passed 1/1 in 5.97s and covered `/help`, completion, empty/no-op, named success, exact range, retained-original wording, `/status`, and absence of slash text from provider input. The shared handler regression passed 1/1 in 4.12s for invalid target, named success and no-op without an extra provider call. Together these close rows 4.2, 5.1 and 5.2.

Other observed focused evidence on this tree: automatic compaction retains its rolling summary and only the newest post-cursor raw suffix (1/1, 3.50s); immutable manual-result reporting survives a concurrent cursor advance (1/1, 3.00s); the current runtime all-features test target compiled after the continuity correction (10.10s); the TUI visual target compiled (15.32s); and the exact omission projection unit passed 1/1. `cargo fmt --all -- --check`, `git diff --check` and strict active-change validation pass at this checkpoint.

Atomic publication evidence, 2026-09-23: `cancellation_after_compact_settlement_drains_the_atomic_checkpoint` additionally binds the committed context record and private reasoning sidecar to the exact actor, session, view, revision, range, operation and invocation with no turn identifier, closing row 2.1. `stale_compact_checkpoint_keeps_the_competing_cursor_and_writes_no_sidecar` passed 1/1 in 3.02s after a sibling advanced both source and cursor; it returned typed `ContextSummaryStale`, retained the competing current summary/cursor, wrote no operation sidecar and made no ordinary dispatch, closing row 2.2. The accepted-lost-reply successor case passed 1/1 in 3.93s: generic receipt reconciliation settled the exact ordinary checkpoint once after owner retirement, with no provider replay or cursor/summary duplicate, closing row 2.3. The candidate isolation/conflict/abandon case passed 1/1 in 3.13s: compaction and forced overflow remained candidate-only, a sibling live append caused typed promotion conflict with both live rows intact, and explicit abandon removed the candidate, closing row 2.4.

Threshold and accounting evidence, 2026-09-23: `automatic_compaction_observes_below_exact_and_hard_overflow_boundaries_once` passed 1/1 in 7.93s and exercised 3,007/3,008-token sides of the exact 75% threshold plus hard overflow, with at most one compaction attempt. Native configuration and published-schema tests accept the canonical 50 and 95 percent endpoints and reject 49 and 96; the policy unit checks the same limits and reserve/window refusal, closing row 3.4. `compact_failure_cancellation_and_later_operation_keep_exact_usage_and_retryability` passed 1/1 in 3.39s with success/failure/cancellation terminal attempts, partial usage and frozen-price accounting. `completed_turn_retry_returns_stored_output_without_replaying_its_compaction` passed 1/1 in 2.59s and returned the stored bytes before a deliberately malformed later topology could be read, with no provider, usage or cursor replay. These close rows 3.1, 4.1 and 4.3.

Isolation and deterministic-evaluator evidence, 2026-09-23: `compacted_sessions_and_export_keep_sibling_and_legacy_rows_isolated` passed 1/1 in 2.60s. It compacts two exact sessions while retaining a null-attributed legacy row in export. Session-one raw contains an admitted continuity fact and a hostile instruction sentinel: the provider sees both only as message data, the immutable Compact instructions contain neither injected authority nor derived text, the fixed durable summary retains the admitted fact but not the sentinel, and a later ordinary request contains only that summary while excluding the pre-cursor sentinel, sibling-session raw and legacy raw. Combined with row 1.3's explicit P08-sidecar and other-actor exclusion, this closes rows 1.1 and 6.3 without treating the model output as an evaluator.

Invalid-result, authority and resumed-PTY evidence, 2026-09-23: `empty_refusal_and_tool_compaction_outputs_keep_the_prior_summary_usable` passed 1/1 in 7.35s; each invalid output publishes no cursor/summary and leaves the prior current record usable. `manual_compaction_refuses_a_target_deactivated_in_persisted_topology` passed 1/1 in 4.52s and failed before source/provider work after reading the current persisted topology. `resumed_pty_reports_automatic_compaction_cost_once_and_reuses_its_summary` passed 1/1 in 6.38s with the valid 50% minimum and an 8,192-token fixture window: real automatic compaction ran, exact priced usage covered the captured provider calls, resumed `/status` and `/cost` made no call or duplicate charge, and a later ordinary request contained the persisted summary. An earlier 1% fixture and a 4,096-token envelope overflow were setup failures and are not acceptance evidence.

Owning checks, 2026-09-23: the root all-target/all-feature typecheck graph passed in 11.01s using the verified local bundle mirror; strict Rust lint passed in 8.92s; and docs format, build, content, local-link and anchor checks passed in 2.69s. The first typecheck attempt tried a restricted-network bundle download and the first docs attempt found the subsequently formatted configuration table; neither setup failure is product evidence. Strict active-change validation and the apply gate pass with 0 errors/warnings and a clear gate. This closes row 6.1 and documentation task 5.3.

Integrated peer-concurrency correction, 2026-09-23: the first combined P09 run exposed real global-Dolt-HEAD false stale errors in parallel peers. Focused native CLI output showed debug had one `shared context summary window coordinates changed` peer error and usage 64/40, while ordinary had three `context source changed before compaction dispatch` peer errors and usage 48/30. After the source-specific correction, `kuru_run_uses_the_owned_shell_and_preserves_the_responses_continuation` and `real_pty_cancels_provider_work_preserves_draft_and_accepts_the_next_turn` each passed 1/1 with exact shell receipt/final text and the PTY's expected 32/20 usage. The CLI retains full JSON comparison after only session identity and measured tool duration normalization. The first attempt to run the new managed peer fixture used an outdated unprepared supervisor and returned stale; it is invalid setup evidence. After `//packages/kuru-memory:prefetch` rebuilt the package-owned supervisor and `KURU_TEST_SUPERVISOR_PREPARED=1` selected it, `unrelated_peer_write_during_compact_inference_preserves_source_and_usage` passed 1/1 in 3.24s: an unrelated actor wrote while provider inference was held, the original captured historical revision published its own summary/cursor through managed RPC, no foreign source entered the request, and usage settled once at 7/3. This closes row 2.5 and runtime task 4.4.

Relevant-change and provenance evidence, 2026-09-23: `context_checkpoint_rejects_changed_content_and_prior_summary_at_old_revision` passed 1/1 in 3.65s on real Dolt: an in-place typed row change kept the same sequence/count, and a separate prior-summary content change kept the cursor identity, yet each historical/current proof returned typed stale without publishing the new checkpoint. `stale_compact_checkpoint_keeps_the_competing_cursor_and_writes_no_sidecar` passed again 1/1 with the package-prepared supervisor and new assertions that the held paid invocation retained one 7/3 usage record and one provider call after a sibling advanced the source/cursor. `candidate_context_checkpoint_proves_ancestor_without_accepting_sibling_hash` passed 1/1 in 2.85s: a valid candidate ancestor succeeded after an unrelated candidate write; a candidate-only or malformed hash did not authenticate main. The real-Dolt probe confirmed `AS OF ?` is rejected by Dolt, while the strictly validated hash literal works with bound source filters. `ordinary_context_refuses_a_shared_summary_changed_after_its_window_read` passed 1/1 in 2.68s through managed RPC: a newer same-actor cross-session summary arriving after the first shared window is rejected before ordinary provider dispatch. Sessions independently reviewed the final source proof, ancestry, shared-window recheck, receipt and usage boundaries clear. These close rows 2.6, 2.7 and 6.4. Combined coverage and hosted native gates remain separate.

Combined-coverage attempt on exact commit `9b2140565aa8f8174fea3263a4b647e93a769f1c`, session `48839`, terminated exit 101: native memory lib passed 241 and failed 1; runtime lib passed 181 and failed 1; the 90% line gate did not run. The previously failing CLI shell continuation passed 5/5 and the PTY target passed 27 with one intentionally ignored fixture. Memory's failed atomic-sidecar test had advanced only an unrelated project key and expected stale; that expectation conflicted with the reviewed relevant-source contract. Its fixture now commits a changed typed source row in the checkpoint's consumed range while retaining the stale no-partial-sidecar and later conflict assertions. Runtime's cursor-race test failed only because its diagnostic assertion omitted the correct pre-dispatch `context summary changed before compaction dispatch` route; an exact prepared-supervisor reproduction captured that text. The assertion now admits that route and still requires zero provider requests. After `//packages/kuru-memory:prefetch` refreshed the ordinary supervisor, the exact memory and runtime regressions each passed 1/1. Independent review and a coherent combined rerun remain pending; the failed coverage attempt is not a gate pass.

Still unverified and deliberately left open: combined coverage and native Linux/macOS/Windows memory and PTY groups (6.2) on the current prerequisite-integrated delivery lineage. No hosted-native or final combined-coverage claim is made here.

Reviewed-stack reconstruction, 2026-09-24: the P09 cursor, atomic checkpoint, actor-compaction, independent-peer and stale-source changes were applied above the separately published P29 head. The one v1 migration-fixture conflict now follows all five ordered v1→v6 commits while retaining exact data and read-only no-mutation assertions; the runtime dispatch union retains P08's admitted turn ID and gives manual Compact its own operation identity. On this reconstructed source, the package-prepared real-Dolt `stopped_released_v1_upgrades_once_and_readonly_preserves_it`, `compact_checkpoint_sidecars_are_atomic_on_stale_and_late_conflict`, `unrelated_peer_write_during_compact_inference_preserves_source_and_usage`, and `compacted_sessions_and_export_keep_sibling_and_legacy_rows_isolated` fixtures each passed 1/1. TUI all-target/all-feature typecheck, root all-target lint, docs format/build/content/link check, Rust format, strict Cospec validation (0 errors/warnings) and apply gate (clear) passed. The first root-lint attempt stopped during pinned Node setup because sandboxed `hk install` could not write Git config; the same unchanged lint task passed with repository access. This focused reconstruction proof does not replace P09's still-open combined coverage and hosted native row 6.2 or its archive gate.

Current-main local preparation, base `9ac13ab25d3dac54f7ff84c872258605b1b34918`: P09 was staged as the reviewed 59-file delta with one TUI dispatch union retaining both main's `/tools` handler and P09's `/compact` handler; independent conflict review and staged diff check were clear. The package-owned native real-Dolt migration, atomic checkpoint, unrelated-peer compaction and selected-session/export isolation filters each passed 1/1. The TUI all-target typecheck and root Rust all-target lint passed. Owning docs format/build/content/local-link/anchor checks and source Rust/TOML/fixture format checks passed. Initial sandbox-only attempts could not reserve Dolt loopback, write mise/hk setup state, or initialize taplo's macOS system configuration; the same focused commands passed natively and those setup failures are excluded from behavior evidence. Combined coverage, hosted native Linux/macOS/Windows gates, archive, and publication remain open until the final dependency-integrated source is checked.

Failed combined-coverage attempt on `ae2cd8ea`, 2026-09-25: normal push hooks passed static checks, then combined behavioral coverage failed in eight memory migration fixtures, three runtime context fixtures and one existing cancellation PTY; the public ref did not advance and the 90% gate did not pass. P09 registered schema v6 while those migration fixtures still used v6 as synthetic future/corrupt authority or expected a v5 live/candidate view. The runtime's two Demo retry fixtures expected provider-side late rejection, but the new accurate preflight compacted or omitted optional rows before streaming. A third fixture's 64 required large receipts measured 1,156,229 input tokens against the default 128,000-token window, so pre-dispatch refusal was correct. The shared PTY fake provider marked `started` only for compaction, leaving the ordinary cancellation fixture waiting after its response had already completed. These are distinct observed failures, not a successful coverage result.

Focused correction evidence on the same local source, 2026-09-25: all eight exact formerly failing real-Dolt memory filters passed natively after moving synthetic future attempts to v7, unknown target to v8, forged extra receipts beyond real v6, and current/candidate expectations to v6; the retained-v2 fixture also kept its exact old-candidate content through the synthetic v7 progression. The multilingual and selected-source Demo late-retry filters each passed 1/1 with test-only optimistic preflight and unchanged authoritative in-stream measurement, preserving whole-row retries and one ordinary admission. The 64-receipt filter passed 1/1 with every original large payload exact and all 16 optional notes whole and durable under an explicit valid 1,300,000-token window; `mandatory_overflow_after_one_compaction_refuses_without_ordinary_dispatch` remains the separate observed required-input refusal proof. The ordinary cancellation/draft/next-turn and manual-compaction cancellation real PTYs each passed 1/1 with the shared fake provider's start signal restored. Memory, runtime and TUI owning all-target Clippy, Rust format, strict Cospec validation and apply gate passed; independent source review was clear. The first focused Dolt attempt failed only because the sandbox denied private loopback and was rerun natively. A root format aggregate also hit taplo's macOS system-configuration panic in the sandbox; targeted Rust format passed. Combined coverage, hosted native platforms, archive and publication remain open on the final release-integrated head.

Exact release-base normal push, 2026-09-25: public `delivery/p09-current-main`
advanced to `be261442` after the ordinary format, lint, typecheck, docs,
tooling and Cospec hooks passed. Its combined instrumented suite passed the
90% gate with 74,607/79,480 covered lines (93.87%), including 247 memory,
188 runtime and 33 terminal integration tests. This supersedes the earlier
failed local combined attempts for the final source. Hosted exact-head Linux,
macOS and Windows evidence is recorded below; this paragraph records the
local hook result only.

Exact-head native acceptance, 2026-09-25: GitHub Actions run `36095993300`
on `be26144230ac405997b23ea78e5f493b84d61b74` completed successfully.
Ubuntu native coverage job `107948274562` passed 247 memory and 188 runtime
tests, including the named manual compaction, stale/candidate and summary/raw
context cases; its packaged cold offline install/update fixture passed 3/3.
macOS native coverage job `107948274697` passed the same 247 memory and 188
runtime targets and packaged offline fixture 3/3. Windows application job
`107948274568` passed 102 application tests, including local `/compact`
dispatch and the six native ConPTY tests; Windows memory/runtime job
`107948274689` passed 187 runtime tests, including the manual compaction,
accepted-lost-reply, candidate-isolation and summary-retention cases. Windows
platform, connector, delivery/archive and install/offline jobs passed. The
Windows coverage report `107956315159` and aggregate native gate
`107958271319` completed successfully with the required 90% coverage check;
Ubuntu, macOS and quality aggregate gates passed. These are hosted results on
the public P09 head, separate from the 93.87% local push result. No paid model
or live credential request was used. This closes critical row 6.2; archive
execution remains a separate final gate.
