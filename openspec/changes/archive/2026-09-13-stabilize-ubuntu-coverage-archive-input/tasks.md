## 1. Ubuntu packaged-runtime coverage fixture

- [x] 1.1 Scope `CARGO_PROFILE_TEST_DEBUG=0` to Ubuntu's existing coverage step and verify the other native jobs, full workspace graph, test command, instrumentation, and 90% gate remain unchanged. *(Observed: an isolated exact cargo-llvm-cov 0.9.1 fixture retained identical source line counts and execution records with `debug=0` and `line-tables-only`; uncovered `DA` records remained zero and a 100% threshold still exited 1. Its 9.01% executable reduction is directional evidence only, not a Kuru margin measurement.)*
- [x] 1.2 Add early selected-executable byte-size and unchanged expanded-limit diagnostics to `apps/kuru-tui/tests/embedded_runtime.rs` without changing archive calculations or policy.
- [x] 1.3 Document the Ubuntu coverage backtrace tradeoff and run focused static checks; retain actual hosted Ubuntu acceptance as pending until the next exact-head CI run. *(Observed locally: actionlint, Rust formatting, and `git diff --check` passed. The package docs-format task could not install its already-pinned Node/npm tools because the sandbox denied mise cache and hook writes; normal guarded hooks remain required. Actual Kuru executable size, archive acceptance, full native suite, and 90% report are pending hosted CI.)*
