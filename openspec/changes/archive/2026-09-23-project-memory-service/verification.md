## 1. One service and owned Dolt across processes [critical]

- [x] 1.1 @integration (agent) barrier-start two native processes on one cold project with real bundled Dolt -> one service generation, one owned engine, one initialized revision chain and both storage clients attach (observed: archived owner-foundation real-Dolt barrier fixture)
- [x] 1.2 @e2e (agent) run sequential normal `kuru run` processes on one project -> later process attaches to the same warm service/engine during its idle interval and retains the prior conversation state (observed: local sequential normal CLI probe with retained endpoint record and resumed session)
- [x] 1.3 @integration (agent) terminate the starter while another client remains -> surviving client completes a write/read and owned service remains alive (observed: archived independent Windows owner fixture)

## 2. Identity, compatibility and private IPC [critical]

- [x] 2.1 @integration (agent) exercise wrong project, store instance, generation, secret, protocol and schema handshakes against a live owner -> each fails before storage access, with an actionable diagnostic and no owner replacement (observed: archived owner-foundation and durable-receipt handshake fixtures)
- [~] 2.2 @integration (agent) test private endpoint permissions, wrong-user access and same-user connection without the current secret on Unix and Windows native runners -> defer: final exact-P27-head hosted Windows permission proof remains required
- [x] 2.3 @integration (agent) publish stale endpoint/PID fixtures and hold an unrelated process/port -> recovery uses verified lease authority and never kills or unlocks the unrelated owner (observed: archived owner-foundation stale-owner fixture)

## 3. Disconnect, idle race and uncertain recovery [critical]

- [x] 3.1 @integration (agent) leave two clients attached, kill one, then race reconnect against idle timeout -> one owner remains; the survivor and reconnected client can operate without a second engine (observed: archived independent-owner and durable-receipt real-process fixtures)
- [x] 3.2 @integration (agent) crash service and Dolt separately around an accepted operation -> successor waits for reap, reconciles receipt and either returns committed result or a definite noncommit without duplicate effect (observed: archived durable-receipt crash reconciliation plus managed P27 successor fixtures)
- [x] 3.3 @integration (agent) drop the last client with accepted work in flight -> focused drain/close fixtures and final exact-head combined coverage passed on `f62bcc02`.
- [x] 3.4 @integration (agent) disconnect a client after an accepted candidate write, then retire/restart the owner -> the original candidate ref, committed head and private rows remain unchanged until an explicit checked resolution (observed: P27 same/successor candidate rebind fixture)
- [x] 3.5 @integration (agent) lose an accepted dream candidate-unit or promotion reply, including a successor owner and later sibling live write -> typed recovery reattaches the exact candidate, retains the report, publishes topology only for its promoted revision, and preserves an open conflict without automatic abandonment
- [x] 3.6 @integration (agent) inspect retained exact candidate refs after the initiating client and owner restart, then explicitly abandon one selected ref through CLI and registered TUI commands -> inventory/status is bounded and read-only, changed/active/historical-stale/uncertain refs refuse, selected open ref is durably resolved with no sibling or live-history loss, and no hidden promotion or replay occurs; a cancelled pre-dispatch abandonment restores explicit selection only from a checked still-open ref; an accepted abandonment with lost reply is queried by its own ID and exact ref/base/head across owner restart without retrying the transition; empty/no-op target==base loss after ref reclamation stays uncertain and reports absent ref with unproved operation outcome (observed: real CLI/TUI routes, cancelled close, retiring handshake and staged/empty lost-reply fixture)

## 4. Existing memory behavior and native packaging [critical]

- [x] 4.1 @integration (agent) exercise real-memory migration, candidate, usage-ledger, read-only inspection, forget and purge fixtures through the service -> existing revisions and receipts remain coherent with no direct-writer bypass (observed: archived receipt/migration fixtures and P27 maintenance, candidate and managed-facade checks)
- [~] 4.2 @e2e (agent) install and update the packaged binary on Windows, macOS and Linux native jobs, then run a cold offline conversation and reopen -> defer: exact-P27-head packaged native install/update CI remains required
- [~] 4.3 @regression (agent) run package-scoped format, lint, typecheck, real-memory tests, docs checks and required combined coverage -> defer: local final exact-head combined coverage passed on `f62bcc02` in 810.24s; hosted native coverage remains required.
- [x] 4.4 @integration (agent) start maintenance during a warm idle owner and while another client is active -> idle owner retires and reaps before the 30-second grace; busy owner refuses without disturbing the client or publishing purge intent; retained start gate excludes a replacement during purge (observed: real-Dolt maintenance idle-owner fixture)

Focused P27 dream recovery evidence, 2026-09-22 (local macOS, dependent worktree based on P28 `58d6998`): `//packages/kuru-runtime:typecheck` passed for all targets/features in 12.07s. With the package-owned prepared real-Dolt supervisor, `accepted_candidate_promotion_publishes_after_a_later_sibling_revision` passed 1/1 in 3.10s and `stale_dream_candidate_keeps_its_report_and_open_ref_until_explicit_abandon` passed 1/1 in 2.17s. The first fixture retains the exact confirmed promotion revision when an independent later main write advances live history. The second pauses a real dream before promotion, advances live main, proves the typed stale-base conflict retains its report and checked candidate ref without topology publication or automatic abandonment, then explicitly abandons it and continues. This covers only the local-store proof and conflict branches of row 3.5. Managed accepted-reply loss, successor-owner recovery, public exact-ref inspection/resolution and native Windows/Linux execution remain unobserved; row 3.5 stays open. A first sandbox test attempt could not bind Dolt's loopback port; the authorized native rerun produced the results above. A reused dev-profile `kuru-platform` artifact initially lacked current short-socket APIs despite a passing check-profile typecheck; scoped package-artifact invalidation and current-source prefetch resolved that build-cache mismatch without product edits.

Focused P27 fixture and candidate-resolution correction evidence, 2026-09-22 (local macOS, exact `f89ccee` integration): package-owned prefetch used `/private/tmp/kuru-phase1-bundles`. `dolt_tests::stale_periodic_dream_failure_preserves_the_exact_completed_output` and `dolt_tests::stale_promotion_keeps_later_live_data_and_discards_all_candidate_effects` each passed 1/1, proving typed exact-ref resolution preserves the candidate publication proof, report, topology and no-replay boundary. `cli_supports_all_modes_model_discovery_persistent_sessions_and_dreaming` and practical PTY `real_pty_commands_complete_and_clear_only_the_visible_conversation` each passed 1/1, proving the reordered-tolerant startup assertions and registered candidate completion against the cleanup-integrated fixtures. An initial unqualified runtime filter selected zero tests and is not evidence; the module-qualified reruns above are the claimed results. These focused results do not replace the deferred normal coverage/hosted gates.

P27 successor-recovery source review, 2026-09-22: ConfigSol independently cleared the six retired-generation typed paths after a cancellation fix. Each terminal proof now marks its old session closed and retains a checked rebind capability under the mutation guard, then returns without another await; old attachment draining snapshots its weak registry so cancellation can retry. Checked main rebind drains first and requires the same stable store instance, while original clones stay closed. Runtime records a recovered transition before any rebind wait. New test-only transport and idle-owner seams extend the managed accepted-lost-promotion fixture through exact old owner/Dolt reap, successor typed proof, one topology/dream-log publication and a fresh main write. Direct cancelled-close and checked clone-rebind assertions are also source-reviewed. At this source-review checkpoint, no post-edit native execution was yet claimed; the focused evidence below supersedes that pending state.

Focused P27 managed-recovery execution, 2026-09-22 (local macOS, dependent worktree based on P28 `58d6998`, verified read-only bundled-engine mirror and package-owned prepared supervisor): `managed_facade_preserves_views_ledger_export_and_independent_clients` passed 1/1 in 4.64s, including cancellation-safe attachment draining; `lost_candidate_unit_reply_reattaches_before_read_write_and_promotion` passed 1/1 in 4.64s across same-owner and successor-owner modes; and `managed_lost_promotion_reply_keeps_report_and_publishes_only_typed_revision` passed 1/1 in 4.54s across same-owner and successor-owner modes. The last fixture requires a completed `PromoteCandidate` frame, retains the exact report/candidate across dropped reply wait, permits a later sibling live write, obtains typed transition proof before one topology/dream-log publication, rebinds after exact owner retirement and continues on fresh main. It also proves the owner rejects a captured candidate head after a later candidate write without changing main. That assertion exposed and corrected an RPC boundary that had converted the definite `CandidateConflict` fault to an untyped string; uncertain storage faults remain fenced. An initial promotion fixture run paused the preliminary revision read and therefore timed out before sending promotion; the corrected runtime sends its already-captured publication target through exact owner recheck, while ordinary generic promotion retains read-current behavior. The first native attempt was separately rejected by the sandbox before startup because loopback bind was not permitted; authorized reruns produced the results above. This closes the local integration scenarios in row 3.5. Native hosted Windows/Linux acceptance remains pending under rows 4.2/4.3.

Observed local macOS sequential CLI evidence, 2026-09-22: the current dependent schema-4 `kuru` executable built through `//apps/kuru-tui:build` (14.41s; binary SHA-256 `09392b7e171fd5ed4250a00df63fe44d5ebd8fafdde5ab38214f10587bd9de25`). In a second private isolated project, two distinct normal `kuru --provider demo --no-dream run ... --json` processes ran sequentially, the second with `--resume` set to the first result's session ID. Both completed within named 90-second child deadlines, the second retained the exact session and produced its second sentinel, and the private service endpoint record remained byte-identical across processes. The record was hashed only; its secret was never printed. After both clients exited, the endpoint retired within the checked idle deadline. This is local functional evidence for warm same-generation attachment and state reuse, paired with the package's passing real-process one-owner/one-engine fixture. It is not a simultaneous-session admission claim or hosted Windows/Linux packaged acceptance. The first manual wrapper attempt stopped before its second process because a mise-managed JSON-parser shim attempted an out-of-sandbox cache update; a rerun using the system JSON parser passed without production changes. Private fixture outputs: `/private/tmp/kuru-p28-compat.rBaKJD/sequential-first2.json`, `sequential-second2.json`, and endpoint SHA files. Full integrated TUI test task and hosted native P27 acceptance remain pending, so the checklist is not marked complete from this manual probe alone.

After that local CLI probe, source comparison against final foundation head `cffe57f` found its Windows endpoint-retirement `NameRetention::Movable` correction missing from the then-`d653415` dependent base. The dependent branch has now fast-forwarded to exact foundation head `cffe57f29428bde2fe2a675837304cd2f2aa96fc` and restored its P27/P28 source and Cospec files; the checked retirement correction and Windows fixture return type are present. Restored tracked content matches the pre-rebase stash, all 18 untracked content files match the saved backup byte-for-byte, `git diff --check` passes, and both active changes strictly validate with zero errors/warnings. On reconciled source, package-owned current-supervisor `prefetch` passed (14.55s), `service::tests::maintenance_retires_only_an_idle_owner_and_holds_election` passed 1/1 with real Dolt (3.41s fixture), and package-owned all-target/all-feature `typecheck` passed (4.19s). The preceding CLI probe still predates this source correction; it supports the design but is not a final-head packaged/hosted native run.

The package-owned `//apps/kuru-docs:check` passed on the current service/receipt documentation in 41.90s, including the public artifact build, local links and anchors. Its `setup:tools` step emitted sandbox-only postinstall warnings when hk could not change `.git/config`, but the task completed exit 0 and the content checker reported success. This does not replace final exact-head documentation checks in the normal push hook.

Focused P27 managed command evidence, 2026-09-22 (local macOS, temporary dependent base `b715eaf` combining corrected P28 `a5ac109` with the merged P02 command registry): package-owned bundled-engine prefetch passed in 25.03s and the TUI all-target/all-feature typecheck passed in 3.02s. `candidate_commands_discover_and_abandon_one_exact_retained_ref` passed 1/1 in 3.97s: a real service-created canonical candidate remained discoverable through a separate built CLI process, an intentionally wrong head was refused by exact read-only preinspection without outcome polling or a retained mutation fence, and the exact branch/base/head then abandoned successfully before status reported `Missing` with operation outcome `unproved`. `writer_commands_retain_the_project_conversation_driver_lease` passed 1/1 in 3.86s, preserving the transitional single-conversation admission gate. The shared-registry TUI route first exposed a real application mismatch between its page size and the service's 1..=16 bound; one shared app constant now fixes CLI validation/default and the TUI page at 16. After separately correcting two fixture-only noncanonical branch spellings, the route passed 1/1 in 2.46s. A strengthened rerun using a real candidate created from the same store passed 1/1 in 2.55s and exercised registered inventory, status, exact abandon and post-abandon Missing/unproved projection. The first CLI attempt was rejected before product startup by sandbox loopback restrictions; the authorized native run produced the result above. These checks establish the public local command path and no-public-promote boundary, but do not by themselves close row 3.6's owner-restart, accepted-lost-reply, empty/no-op or hosted-platform cases; row 3.6 remains open.

Prerequisite evidence retained by this dependent change: archived `project-memory-owner-foundation` and `independent-windows-memory-owner` contain the authenticated handshake/election, stale-owner, starter-exit/surviving-client and owned-reap proofs used by tasks 2.2, 3.1 and 3.2. Archived `durable-service-operation-receipts` contains the full 215/215 local memory suite plus exact accepted-write, candidate/ledger recovery, real-process crash and old-schema refusal evidence used by tasks 2.1, 3.1 and 3.2. P27's focused facade/rebind fixtures above exercise those typed seams through the managed client. This records inherited local evidence without converting P28's still-pending corrected-head hosted Windows/Linux checks into a P27 pass.

Final dependency restack checkpoint, 2026-09-22: Delivery range-diffed the corrected P28 commits patch-identically onto merged foundation/main, producing `966943dbd758f369400149f9a212c88f082741d6`; P27 now applies directly to that head. The preserved tracked source is byte-identical to the saved pre-restack worktree tree, and every untracked P27 artifact matches its backup. The temporary-base deletion of a later-main instruction-modal test was removed, restoring the complete upstream instruction/permission modal priority fixture. `git diff --check`, Rust format, strict Cospec validation and apply gate passed at the source checkpoint; the focused exact-lineage results follow below.

Final-lineage selected-abandon recovery evidence, 2026-09-22 (local macOS, P27 applied byte-for-byte to corrected P28 head `966943dbd758f369400149f9a212c88f082741d6` on merged foundation/main): the package-owned real-Dolt task used the verified read-only bundled-engine mirror and `RUST_TEST_THREADS=2`. `service::tests::retiring_endpoint_close_during_handshake_is_transient` passed 1/1 in 2.67 seconds, proving only BrokenPipe/ConnectionReset/UnexpectedEof during an undecoded retiring handshake is retried through the existing election path; decoded compatibility and project rejection assertions remain fatal. `facade::tests::selected_abandon_lost_reply_proves_staged_but_not_empty_ref` passed 1/1 in 6.93 seconds across isolated staged and empty modes. It flushes the real selected-abandon frame, waits for the exclusive resolution reservation to release, retains an authenticated old-generation sibling long enough to prove Missing plus random same-generation uncertainty, then explicitly retires/reaps that owner and starts a successor. The staged request resolves only from its retained exact ID/branch/base/head as typed Abandoned, grants checked main rebind, leaves the old main closed and permits a fresh write. The empty target==base request remains `SelectedAbandonUncertain`, fenced, and Missing with operation outcome unproved. A preceding run failed with `early eof` because the fixture tried read-only status through the retired exact-generation attachment before recovering the retained request; that failure exposed the bounded retiring-handshake race corrected and exercised here. This closes the local accepted-lost selected-abandon and empty/no-op portions of row 3.6; combined coverage and hosted native evidence remain under rows 4.2/4.3.
