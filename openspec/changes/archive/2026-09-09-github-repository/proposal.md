## Why

Kuru is ready for its first hosted repository. The user authorized creating
`replygirl/kuru` privately, with eventual public availability and cospec as the
repository baseline.

## What Changes

- Add the repository URL, source-clone instructions, contributor and security guidance.
- Add a stable aggregate CI gate and conventional PR-title check for branch rules.
- Configure private GitHub hosting, main, squash merges and supported branch safeguards.
- Publish the existing reviewed history without release tags; observe hosted CI.

## Impact

Repository metadata, documentation, CI and GitHub settings. Application behavior,
credentials, releases and visibility changes beyond private creation are outside scope.
