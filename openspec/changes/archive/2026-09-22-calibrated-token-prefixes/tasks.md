## 1. Sourced local sizing

- [x] 1.1 Record the upstream GPT-5 family encoding source/version and exact supported route/model eligibility in core metadata; verify catalog validation rejects unsupported encodings while live unknown models remain selectable.
- [x] 1.2 Pin one embedded `o200k_base` Rust tokenizer dependency and its license in the owning manifests; verify offline initialization and upstream text vectors on the supported toolchain.
- [x] 1.3 Replace the supported native route's byte-only preflight with a labelled final-body tokenizer estimate plus structural allowance, retaining a conservative labelled fallback for unknown, custom and opaque material; verify local HTTP fixtures cover pre-dispatch fit and exact sent-body shape without remote counting.

## 2. Reusable prompt structure

- [x] 2.1 Reorder runtime instruction assembly so stable common mode/project instructions precede actor identity and mutable context while keeping private material scoped; verify multi-actor captured request projections and behavior tests.
- [x] 2.2 Stabilize common tool-definition ordering and serialization where needed; verify repeated requests produce matching tool projections even as actor-specific input changes.

## 3. Evidence and delivery

- [x] 3.1 Add real-reported-usage or authorized offline count calibration evidence for multilingual, code and tool-heavy supported requests, and record numeric errors and unavailable classes honestly in the verification ledger; verify evidence provenance is real rather than synthetic.
- [x] 3.2 Exercise absent/zero/positive cached usage through the integration path and document route-specific cache claims, estimator limits and fallback labels; verify docs checks and observed ledger behavior.
- [x] 3.3 Run relevant core, connector, runtime, TUI, docs, format, lint and Cospec checks; record exact observed results and pending native/live evidence in verification before archiving.
