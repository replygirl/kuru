## 1. Retained indexed receipts and exact retry [critical]

- [x] 1.1 @integration (agent) commit one append, lose its reply, then commit a sibling append on the same real Dolt branch and query the first UUID -> observed in `service::tests::lost_reply_receipt_survives_sibling_write_and_owner_restart`; both rows and the first indexed receipt survived. Passed in the exact isolated memory suite.
- [x] 1.2 @integration (agent) retry one store/view/UUID key with changed method or argument digest, then query the same UUID on a different view -> observed exact retry without duplication, changed-work conflict and distinct-view non-alias in `store::tests::indexed_logical_receipt_survives_sibling_write_and_scopes_exact_retry_to_view`; passed in the exact isolated memory suite.
- [x] 1.3 @integration (agent) terminate the owner after commit before reply, start a successor and query by the original UUID -> observed in the real-process `service::tests::crashed_owner_retains_accepted_receipt_after_sibling_write`, including successor exact retry without duplicate rows; passed in the exact isolated memory suite.

## 2. Native branch migration and historical preservation [critical]

- [x] 2.1 @integration (agent) upgrade main with a permanent usage branch and pre-upgrade candidate under real Dolt -> `store::migrations::tests::current_upgrade_migrates_usage_without_rewriting_historical_candidate` passed in the exact isolated suite; it checked usage v4 writes, preserved old candidate head/rows/schema, and new candidate v4 inheritance.
- [x] 2.2 @integration (agent) interrupt usage branch migration before and after checked publication -> `store::recovery_tests::{usage_upgrade_preserves_ready_attempt_when_publication_never_dispatches,usage_upgrade_reconciles_lost_publication_reply_without_replacing_legacy_receipt}` passed in the exact isolated suite; both retained the exact attempt/legacy row and reopened safely.
- [x] 2.3 @regression (agent) open a newer receipt store with an older-schema binary fixture and inspect historical v1–v3 branches -> the actual clean pre-v4 local macOS binary refused writable schema-4 open with unchanged before/after Dolt refs/schema/counts, as recorded below; exact isolated-suite historical validators preserved pre-upgrade candidate heads and rows. Hosted cross-OS old-binary evidence is not claimed.

## 3. Four-state outcome and domain-specific results [critical]

- [x] 3.1 @integration (agent) race an outcome query with in-flight SQL then with verified worker/reap completion -> the final-tree real-Dolt `service::tests::registered_in_flight_write_cannot_be_reported_absent` passed 1/1: after exact handler registration and before SQL dispatch, a sibling authenticated query returned `InFlight`, then after release `Committed`; the exact isolated-suite lost-reply/restart fixture kept a same-generation unknown ID `StillUncertain` and returned `Absent` only after verified owner/Dolt reap.
- [x] 3.2 @integration (agent) cancel a mutating RPC and attempt writes through sibling store, candidate and ledger handles -> exact isolated-suite facade cancellation/unit, candidate-unit and usage-ledger fixtures passed; the logical client's clones remained fenced until typed proof while a separately authenticated client wrote successfully.
- [x] 3.3 @integration (agent) lose candidate promotion and usage settlement replies in native fixtures -> `service::tests::{lost_candidate_transition_replies_survive_sibling_write_and_owner_restart,lost_usage_reply_uses_natural_key_after_sibling_write_and_restart}` passed in the exact isolated suite; candidate exact refs/base-target and usage natural keys resolved without replay or duplicate charge.
- [x] 3.4 @integration (agent) lose an accepted candidate-creation reply, disconnect and restart the owner -> `service::tests::lost_candidate_begin_reply_uses_only_read_only_exact_ref_outcomes` and `facade::tests::candidate_begin_recovery_keeps_clones_fenced_until_exact_ref_reattaches` passed in the exact isolated suite; original UUID/ref/head/private rows survived successor reattachment without Begin replay.
- [x] 3.5 @integration (agent) cancel an accepted candidate unit-write reply and query its exact indexed receipt and original still-open ref -> `facade::tests::lost_candidate_unit_reply_reattaches_before_read_write_and_promotion` passed in both same- and successor-generation modes in the exact isolated suite; generic reconciliation kept the fence until the fresh checked handle read, wrote and promoted without replay.

## 4. Repository and native support gates

- [x] 4.1 @regression (agent) run owning package checks, strict Cospec, docs and normal combined coverage hook -> exact isolated package native suite and docs/format passed; after the final test-only InFlight addition, its focused native fixture, all-target typecheck/lint, strict Cospec and diff-check passed. Delivery's retained normal hook on committed `58d6998` exited 0; full workspace coverage passed the 90% gate (coverage task elapsed 930.21s). No raw percentage was retained, so none is claimed.
- [~] 4.2 @integration (agent) run exact-head hosted Windows, macOS and Linux service/memory jobs -> PR #68 reached native CI. Its first Windows job stopped at a test-support compile mismatch and its Linux ARM memory job passed 215/216 library tests before a spawned-owner fixture lock-release race failed. Corrected-head native results remain pending; no cross-OS pass is claimed.

Observed focused evidence, 2026-09-22: `//packages/kuru-memory:typecheck` passed all targets/features after the current unit-receipt/facade source. With the prepared offline full-Dolt supervisor, `service::tests::lost_reply_receipt_survives_sibling_write_and_owner_restart` passed 1/1 (2.79s): an append frame was sent without consuming its reply, a sibling append committed, the original receipt remained indexed, a same-generation unknown ID stayed `StillUncertain`, and that exact unknown ID became `Absent` only after owner/Dolt close and successor open. `store::tests::indexed_logical_receipt_survives_sibling_write_and_scopes_exact_retry_to_view` passed 1/1 (1.86s): exact retry did not duplicate effect; changed work conflicted; a candidate using the same logical UUID had a different physical receipt, promotion preserved both receipts, and an abandoned/reclaimed candidate view did not falsely prove noncommit. The first typecheck invocation lacked the already prepared bundle override and failed at offline DNS before compilation; rerunning with the verified local bundle mirror passed. These fixtures do not yet exercise a crashed owner before reply, actual facade cancellation after an accepted write, candidate begin/promotion, usage settlement, the full owning suite, or hosted native checks; their checklist entries remain open.

Follow-up focused evidence, 2026-09-22: a source review found that the candidate unit-write fingerprint included an attachment-local handle. Client and owner now hash one stable domain-tagged encoding of pinned branch, method and operation arguments. ConfigSol independently cleared this correction. `service::rpc::tests::candidate_receipt_fingerprint_uses_pinned_view_not_attachment_handle` passed 1/1, `//packages/kuru-memory:typecheck` passed all targets/features, and the real-Dolt lost-reply/sibling/owner-restart fixture passed again 1/1 (2.58s). These checks do not close the typed candidate-transition or usage-outcome acceptance items above.

Further focused evidence, 2026-09-22: `store::usage_ledger::tests::natural_key_proofs_survive_sibling_writes_and_reopen` passed 1/1 (2.27s), checking each compact session/admit/observe/settle proof and changed-value conflict on real Dolt. `service::tests::lost_usage_reply_uses_natural_key_after_sibling_write_and_restart` passed 1/1 (2.53s): an accepted Admit reply was dropped, a sibling Admit committed, the original natural key proved committed, a different missing natural key remained uncertain on the same generation and became absent after owner/Dolt close and successor open, and exact natural-key retry left two total invocations. `facade::tests::usage_reply_recovery_fences_clones_until_natural_key_is_proven` passed 1/1 (2.17s), modeling an accepted lost reply and proving clone-wide mutation fencing/release through the public facade. All-target memory typecheck passed after pending-ledger source integration; `git diff --check` passed. The service fixture tests a dropped reply after acceptance; the facade fixture models that boundary rather than timing cancellation itself. Candidate promotion/abandon result proof, actual owner crash before reply, full suite/coverage, and hosted native P28 acceptance remain open; checklist items stay incomplete.

Candidate transition focused evidence, 2026-09-22: `store::tests::candidate_transition_observation_uses_exact_refs_and_revisions` passed 1/1 (3.02s), `service::tests::candidate_transition_query_preserves_open_conflict_and_promoted_revision` passed 1/1 (2.44s), and `facade::tests::candidate_promotion_recovery_uses_exact_target_and_releases_clone_fence` passed 1/1 (2.05s). A later real-Dolt `service::tests::lost_candidate_transition_replies_are_proven_after_sibling_write` passed 1/1 (4.51s): accepted promotion and abandonment frames were sent through authenticated IPC, their replies were dropped, and exact branch/base/target queries resolved after a sibling main write. `facade::tests::preserved_candidate_conflict_reattaches_before_releasing_mutation_fence` passed 1/1 (2.12s): a settled promotion conflict with a modeled lost reply held clone writes fenced until a read-only exact-creation-ID query returned a new authenticated candidate handle; retained private data was readable, explicit abandonment succeeded, and main writes resumed. ConfigSol independently cleared the proof mapping and reattachment source after an old-generation lock-order correction: retire/mark the old session closed under the mutation guard, release the guard, then await attachment drain. An unchanged candidate with all exact refs reclaimed remains `StillUncertain`, because branch history cannot distinguish the no-op transition from another resolution. The accepted-reply transport fixture and modeled facade fixture are distinct evidence. Latest edits have a focused compiled native pass, but all-target typecheck, full package suite/coverage and hosted native P28 acceptance have not run on this dependent head; checklist items remain open.

Post-expansion focused evidence, 2026-09-22: the earlier accepted-transition fixture was renamed `service::tests::lost_candidate_transition_replies_survive_sibling_write_and_owner_restart` and its successor-owner assertions passed 1/1 (2.70s). Both accepted lost replies still resolve from exact refs after a sibling main write and a verified old owner/Dolt close followed by a successor owner. `facade::tests::candidate_promotion_recovery_uses_exact_target_and_releases_clone_fence` passed 1/1 (2.10s) with an added inverse resolved-conflict assertion: it returned no fabricated candidate handle and clone writes resumed after typed proof. `service::tests::candidate_transition_query_preserves_open_conflict_and_promoted_revision` passed again 1/1 (2.42s) with the distinct `OpenConflict` result, and `facade::tests::preserved_candidate_conflict_reattaches_before_releasing_mutation_fence` passed again 1/1 (1.98s) with its fresh-handle continuation assertion. `//packages/kuru-memory:typecheck` passed all targets/features (5.49s), and Rust formatting passed. Full memory suite/coverage and hosted P28 checks remain unrun.

The `cfg(test)` per-attachment post-flush reply-pause fixture `facade::tests::cancelling_after_accepted_unit_frame_fences_clones_until_indexed_proof` is source-reviewed clear by ConfigSol, including its bounded waits and abort-on-drop cleanup. It passed 1/1 (2.66s) with the prepared offline full-Dolt supervisor: a sibling observed the accepted durable write while the actual writer awaited its reply; the writer future was aborted and joined; its clone stayed fenced through a later sibling write; the original indexed receipt resolved without replay. The post-edit `//packages/kuru-memory:typecheck` passed all targets/features (4.42s). This focused check does not prove crashed-owner behavior, the full suite/coverage, or hosted native P28 acceptance.

The independently source-reviewed extension of `service::tests::lost_candidate_begin_reply_uses_only_read_only_exact_ref_outcomes` passed 1/1 (5.12s) with real Dolt. After the accepted Begin reply was dropped, repeat read-only queries retained the exact branch/base. A private candidate value and its head survived a later sibling main write, owner close/restart and successor query unchanged; explicit abandonment then removed the ref. This proves graceful-generation recovery and no query-driven ref recreation, not a crashed-owner or full facade old-generation acceptance result.

A further test-only extension of `facade::tests::candidate_begin_recovery_keeps_clones_fenced_until_exact_ref_reattaches` was independently source-reviewed clear and passed 1/1 (4.55s) with real Dolt. It runs both same-generation and successor-generation cases. The latter closes only the old client transport, waits for owner/Dolt reap, starts a successor, resolves the original pending UUID through read-only outcome, uses the fresh candidate handle, rejects writes through the old clone, and explicitly abandons before final owner retirement. This remains a graceful owner-retirement fixture, not process-crash proof.

The test-only `service::tests::crashed_owner_retains_accepted_receipt_after_sibling_write` was independently source-reviewed clear. It retains the exact spawned service child, discards the accepted write reply, waits for another client to observe the durable row and commit a second row, terminates/reaps only that retained child, and requires a successor to prove the original receipt committed, an unknown old-generation ID absent, and exactly two rows. Its early-failure guard terminates the same child handle and installs the native reaper; no stale PID is used. Its first direct libtest attempt compiled but failed with a receipt conflict because the package's prepared service executable snapshot in the shared target predated the P28 receipt protocol; direct `cargo test` rebuilt the libtest only. That was invalid current-source crash evidence. The package-owned `//packages/kuru-memory:prefetch` then rebuilt/snapshotted the current executable (12.10s), and the exact focused real-Dolt fixture passed 1/1 (2.78s) without a production source change. It proves accepted write and later sibling effects survive an abrupt service-process termination once, plus exact committed/absent outcomes after successor startup. Native Windows/Linux hosted execution, full suite and coverage remain outstanding.

After these fixtures, `//packages/kuru-memory:lint` initially found seven new Clippy source-shape findings. The correction boxes only large typed enum payloads, groups the outcome-query arguments and renames a constructor returning a view; JSON fields and outcome behavior are unchanged. The owning lint task then passed with all targets/features (8.28s), and `//packages/kuru-memory:typecheck` passed with all targets/features (4.07s). `format:rust:fix` and `git diff --check` passed after the correction. Full suite/coverage and native P28 CI remain pending.

The v4 migration review found historical fixture expectations for two registry receipts and a direct v2-parent relationship at the final head. They now require one exact receipt per registered migration and prove that a reconciled v2 target is an ancestor of the final v4 head. A new test runs the released v3 registry validator against a v4 store and checks exact head/ref/working-set preservation on refusal; it is an in-process old-registry simulation, not an old executable. The first sandbox-only attempt could not bind Dolt's private loopback listener, before reaching the assertion. After package-owned current-source `//packages/kuru-memory:prefetch` passed (11.99s), six sequential real-Dolt fixtures passed 1/1 with native loopback permission: old-registry refusal (1.71s), fresh/WAL import receipt chain (4.49s), accepted-DDL process-loss recovery (3.66s), lost branch reply (2.97s), lost publication reply (2.86s), and absent publication retaining its exact ready attempt (5.88s). These focused checks do not replace the full owning suite, combined coverage or hosted native P28 checks.

The permanent usage-branch upgrade now has test-only accepted-reply-drop and pre-dispatch-drop fixtures, independently source-reviewed clear after adding an abort-on-drop guard and bounded reopen. Both derive the pre-upgrade usage branch from an exact retained v3 head and commit a legacy operation row. The accepted case pauses before v4 publication, drops only the accepted `DOLT_MERGE` reply after observing the usage branch at the exact target, then proves the retained attempt, legacy receipt format, reopened owner and one new ledger write. Its first native run failed before publication because the fixture looked for a nonexistent `kuru_migration_3_` prefix; `attempt_name_in` actually uses `kuru_migration_v0000000003_`. ConfigSol source-cleared the test-only prefix correction, and the exact real-Dolt fixture passed 1/1 (2.08s). The shared fixture then added a pre-dispatch case: it drops the merge frame before forwarding, verifies the usage branch remains at its v3 head while the exact ready v4 attempt survives, and requires cold reopen to publish that target once, retain the original legacy row and allow one new ledger write. After this refactor, both real-Dolt cases passed sequentially 1/1 (accepted-reply loss 2.07s; pre-dispatch loss 2.20s). After the earlier source, `//packages/kuru-memory:typecheck` passed all targets/features (7.85s), and the actual service-process crash/typed-IPC fixture passed again 1/1 (3.17s) after the enum-boxing change. A later test-only extension of that crash fixture now requests explicit same-ID exact retry after successor startup, requires no duplicate row, then requires changed same-ID arguments to conflict; ConfigSol independently source-cleared it; the current-source real-process fixture then passed 1/1 (2.40s), preserving exactly two rows after same-ID success and changed-ID conflict. The full owning suite/coverage, an actual older executable, and hosted native P28 checks remain outstanding.

The first package-owned `//packages/kuru-memory:test` full run used the current prepared supervisor and established two-thread policy. It exited 101 after 286.59s: the lib target passed 212/214 tests and every integration target passed, while two synthetic migration fixtures failed. One tried to fast-forward an advanced historical v3 attempt onto already-v4 main; the other used current-v4 `MemoryStore` methods after a test-registry-only v5 upgrade and received the correct unsupported-writable-schema error. No production behavior or validator was changed. The first fixture now advances the current v4 historical attempt and checks its expected v4/3 parent-version rejection. The second uses the test registry and direct SQL for the future-v5 write/branch exercise, retaining exact old candidate and failed-v2 attempt snapshots across reopen; it does not claim a retained v4 handle cannot read. ConfigSol independently source-cleared both corrections. Their focused real-Dolt reruns passed 1/1 (8.30s and 3.48s, respectively). The full-suite rerun is recorded below.

The post-correction package-owned `//packages/kuru-memory:test` run used the freshly prepared current-source supervisor and `RUST_TEST_THREADS=2`; it exited 0 in 271.74s. The lib target passed 214/214 tests, and every integration target passed (6/6, 5/5, 12/12, 1/1; zero-test targets also completed). Exact log: `/private/tmp/p28-full-memory-test-2.log`. This is local macOS owning-suite evidence; actual older-executable evidence follows below. Combined coverage, dependent runtime/CLI integration and hosted native P28 checks remain pending.

Actual older-executable local macOS evidence: clean foundation commit `cffe57f29428bde2fe2a675837304cd2f2aa96fc` has production schema version 3; its package-owned `//apps/kuru-tui:build` passed in 24.58s. The selected binary was copied without modifying its Cargo artifact, and source/copy SHA-256 matched `3addfa7661bdb9dfefa9faa8b61f1f130c54c3edc8765a62ee9bba61d1b9b194`. The current dependent schema-4 CLI also built through its owning task in 14.41s (binary SHA-256 `09392b7e171fd5ed4250a00df63fe44d5ebd8fafdde5ab38214f10587bd9de25`; source base `d653415` plus uncommitted dependent diff) and created an isolated demo store. After its warm service endpoint retired, the old binary's provider-free writable `memory forget nonexistent --note 1` exited 1 with the exact `unsupported Dolt memory schema version 4` error and empty stdout. Offline Dolt JSON snapshots before and after a second old-binary attempt were byte-identical for branch refs, working-set status, schema version 4, 18 messages and 21 retained receipts; current `memory status` JSON was also identical. Fixture and outputs are under private `/private/tmp/kuru-p28-compat.rBaKJD`. This proves old writable-open refusal and no observed mutation on one v4 store; pre-upgrade historical candidate preservation is separately covered by the real-Dolt migration fixture. It does not establish hosted Windows/Linux old-binary behavior or finish the P27 CLI/runtime integration gates.

Final facade source review found that generic unit receipt reconciliation could clear the shared mutation fence for a lost candidate write while leaving its attachment-local candidate handle unusable. The correction retains the candidate's original creation UUID with the pending unit receipt; generic Boolean reconciliation refuses that case. Typed `recover_candidate_unit()` proves the indexed unit outcome, queries the exact still-open ref on the same fresh authenticated attachment, checks its pinned branch, and returns a new candidate handle before clearing the fence. A missing/resolved ref or uncertain indexed result leaves the fence intact; no write or Begin is replayed. ConfigSol independently source-reviewed the proof and lock order clear. On the reconciled `cffe57f` base, `//packages/kuru-memory:typecheck` passed all targets/features (5.12s). The first real-Dolt `facade::tests::lost_candidate_unit_reply_reattaches_before_read_write_and_promotion` passed 1/1 (2.07s): another attachment witnessed the accepted private write, the actual reply wait was aborted, generic reconciliation kept the fence, and the fresh handle read, wrote and promoted. A successor-owner extension initially failed at maintenance because the fixture did not wait for just-closed server handlers to drain; a bounded exact-busy wait was source-reviewed clear. Its next run reached final cleanup but failed because the fixture had not closed the recovered candidate's distinct new-generation session; ConfigSol source-cleared the explicit close and final-gate diagnostic. The exact extended fixture then passed 1/1 (4.50s), covering both same-generation and verified owner-reap/successor recovery with original handles closed and a fresh main observer seeing both promoted values. These were test-lifecycle corrections, not production changes. The package-owned full current-source `//packages/kuru-memory:test` then passed exit 0 in 285.97s using the prepared supervisor and `RUST_TEST_THREADS=2`: 215/215 library tests and all integration targets (6/6, 5/5, 12/12, 1/1; zero-test targets completed). Exact log: `/private/tmp/p28-current-memory-test.log`. The latest package-owned all-target/all-feature lint passed in 8.49s. Combined coverage and hosted native P28 execution remain pending.

The P28 prerequisite is now isolated in `/Users/rg/repos/rg/kuru/tmp/worktrees/phase2-durable-receipts`, branch `feat/durable-service-operation-receipts`, on reviewed rebased foundation head `23a27826939acef43b555ba05fd6982c9a233726`. At isolation its twelve memory source files were byte-identical to the local passing broad checkout; its user docs contain only receipt/schema changes. It does not include the normal CLI managed-open switch, P27 service rollout/lifetime docs or P27 active Cospec, all preserved in the dependent worktree. The preceding macOS native suite/lint evidence exercised those same memory source bytes but predates this newer main base and isolated docs scope, so the exact-tree checks are recorded separately below. Combined coverage and hosted native P28 checks remain unrun.

Exact isolated-tree checks on the reviewed `23a2782` foundation base passed before the final test-only InFlight fixture: package-owned `//packages/kuru-memory:typecheck` (9.19s), `//packages/kuru-memory:lint` (8.53s), and the prepared-supervisor `//packages/kuru-memory:test` (299.03s, 215/215 library tests plus all integration targets). `//apps/kuru-docs:check` passed (41.15s) with public artifact, local-link and anchor validation. `format:rust`, strict Cospec validation, apply gate 0, managed drift and `git diff --check` passed. The first sandboxed `format:check` could not start Taplo because macOS system-configuration returned a NULL object; the native-permission rerun passed the complete format task (5.23s). ConfigSol independently cleared the isolated P28 source/artifact boundary, including unchanged ordinary CLI local open and no concurrent-session claim. Critical row 3.1 lacked an observed `InFlight` result despite its committed/unknown/retired-absence evidence, so a one-shot `cfg(test)` owner-local pause after receipt registration and a real-Dolt sibling Outcome fixture were added. ConfigSol reviewed that test-only seam clear. The first final-tree prefetch attempted to fetch a native archive because the ordinary target's verified bundle mirror had been reclaimed, and failed on offline DNS before testing; rerunning package-owned prefetch against the existing pinned P02a mirror passed (44.97s). The focused real-Dolt InFlight fixture then passed 1/1 (5.94s), and final-tree all-target/all-feature typecheck (51.11s) and lint (22.02s) passed. The earlier full suite predates this test-only addition and is not claimed as a final-tree run; normal combined coverage and hosted native checks remain post-commit delivery gates.

First hosted PR #68 evidence, 2026-09-22: Windows application job `106836490554` failed compilation at `test_support/windows.rs:77,81`: `marker_fixture::prepare` opens private `store::MemoryStore` but `report_marker_startup` still accepted facade `crate::MemoryStore` after the facade split. Linux ARM memory job `106836490166` ran 216 library tests with 215 passing; `independent_clients_elect_one_real_process_and_keep_it_warm` failed after observing endpoint retirement and immediately requiring owner-lock release. `ServiceOwner::close` finishes Dolt cleanup, retires the endpoint, verifies the lock, then drops it on return; these are distinct observable steps. The correction changes only the Windows test helper parameter to the private store type and makes two asynchronous spawned-owner fixtures poll the exact owner lock under their existing bounded idle deadline after endpoint absence. Production service ordering is unchanged. Local focused and corrected-head hosted results are pending.

Focused correction evidence, 2026-09-22: package-owned `//packages/kuru-memory:typecheck` passed all targets/features on current source using the verified read-only bundle mirror; package-owned `//packages/kuru-memory:prefetch` prepared the current supervisor. The first focused test invocation in the sandbox failed before its assertion because the child service exited before readiness; rerunning with native loopback/process permission passed `service::tests::independent_clients_elect_one_real_process_and_keep_it_warm` 1/1 (32.07s). The analogous `service::tests::separate_cold_starters_share_one_owner_and_preserve_both_writes` also passed 1/1 (31.87s). These macOS runs prove both bounded cleanup waits on real Dolt; they do not prove the Windows-only helper compiles or replace the corrected-head ARM rerun. Those remain hosted CI gates.

Second hosted PR #68 evidence, 2026-09-22: exact head `059c48a` passed its normal combined-coverage hook locally, then Windows application job `106955714937` failed two child-process cases with exit `0xc00000fd` and the native diagnostic `thread 'main' ... has overflowed its stack`: `cli_file_crud_and_shell_require_real_capabilities` and `built_in_shell_reconstructs_stock_module_paths_without_losing_other_environment`. The packaged-install case reached `Memory: ready.` before the same child main-stack overflow, so its later ownership wait was companion fallout rather than a separate asserted memory defect. Windows memory job `106955714095` separately passed 177/178 library tests and every runtime test; `service::tests::crashed_owner_retains_accepted_receipt_after_sibling_write` failed with the unqualified `private pipe connect timed out`, which did not identify initial readiness versus sibling reattachment. The bounded source correction runs Windows application dispatch on a named 8 MiB stack thread and changes only that crash fixture to classify the exact platform-owned pipe-connect timeout as readiness-pending under its existing named 20-second/10-second outer deadlines, with distinct stage and child-exit diagnostics. Product attachment timeout/retry/lifecycle behavior is unchanged. ConfigSol independently source-cleared both boundaries. The corrected-source native macOS crash-receipt regression passed 1/1 (2.76s) after package-owned prefetch from the verified bundle mirror. A combined Windows-MSVC all-target check downloaded its locked Windows-only crates and entered dependency compilation, but this macOS host lacks a Windows C sysroot; bundled SQLite compilation stopped at missing `stdlib.h` before either changed Rust file was checked. Windows compilation and runtime execution therefore remain hosted CI gates and are not claimed.

Third hosted PR #68 evidence, 2026-09-22: exact corrected head `1240d897` again passed 177/178 Windows memory library tests and failed only `service::tests::crashed_owner_retains_accepted_receipt_after_sibling_write` with the still-unqualified `private pipe connect timed out` (job `106988782527`, run `35800269856`). Because the corrected initial-owner and sibling readiness helpers would attach their own stage text, while the later maintenance path already has context, the remaining unlabelled path is the fixture's one post-crash `attach_or_start` call; the log does not yet prove which internal election/start stage timed out. The follow-up therefore adds diagnostics only: every existing `try_attach` in `attach_or_start` names its pre-election, lock-wait, post-lock, active-owner or post-spawn stage, and a post-spawn error reports whether the exact retained child exited, remained running or could not be observed before normal owned cleanup. It adds no retry, timeout or election behavior. Later successor receipt, exact retry, history and maintenance calls also have distinct fixture context. ConfigSol independently source-cleared that boundary. After package-owned prefetch, the exact native real-Dolt crash-receipt fixture passed 1/1 in 2.34s, preserving the accepted receipt, sibling write, successor generation, exact retry/conflict and final cleanup. This macOS pass does not classify the Windows pipe failure; the next exact hosted Windows run is the required diagnostic and behavioral gate.
