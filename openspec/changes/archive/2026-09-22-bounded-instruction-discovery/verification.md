## 1. Imported project authority is reviewed before dispatch [critical]

- [x] 1.1 @e2e (agent) run the real CLI in a temporary project with `AGENTS.md`, a sibling `CLAUDE.md` importing it, and another imported Markdown source; inspect `trust status`, then attempt a normal command before and after a one-invocation review -> one copy of each active source in the manifest/prompt, no provider or tool side effect before trust, and an exact-root changed manifest when imported bytes change
- [x] 1.2 @unit (agent) capture a snapshot, replace an imported source, and compare old prompt bytes with a newly parsed snapshot -> current invocation keeps captured reviewed bytes while the new snapshot has a different claim digest

## 2. Import graph and path boundaries [critical]

- [x] 2.1 @integration (agent) build temporary source trees with a nested relative import, duplicate alias, cycle, symlinked source and `..` escape -> deterministic once-only order, cycle notice, and redacted rejection without reading linked or escaped bytes
- [x] 2.2 @unit (agent) exercise standalone directive parsing against prose, fenced examples and ordinary `@` text -> only documented import lines activate; ordinary text remains literal
- [x] 2.3 @eval (agent) capture fake-provider instructions for a two-directory project before and after an imported source is added -> the common prompt has each source exactly once in documented precedence, and an omitted or unapproved import contributes no behavioral instruction text

## 3. Capacity omissions remain truthful and usable [critical]

- [x] 3.1 @integration (agent) launch a temporary project with one over-256-KiB source plus a valid fitting source, then inspect diagnostics and the captured prompt under a one-time grant -> the oversized body is absent, the valid body remains, omission is visible before inference, and project startup is not rejected solely by the cap
- [x] 3.2 @unit (agent) reach the 1-MiB aggregate, 128-file and eight-edge bounds with synthetic files and duplicate imports -> no partial source injection, bounded omission report, no second charge for duplicates, and no recursion after the cap

## 4. Repository checks and published behavior

- [x] 4.1 @regression (agent) run core and CLI package tests, affected lint/typecheck, format, docs checks, strict Cospec validate/apply and diff check -> observed results and any native/CI limitations are recorded before archive
- [x] 4.2 @integration (agent) validate user docs against the shipping CLI behavior and confirm nested subtree instructions are not advertised by this slice -> docs accurately describe import syntax, order, caps, trust and the separate nested follow-on

Observed 2026-09-22: `mise run //packages/kuru-core:test` passed all targets (32 config tests) after the final Markdown fence correction and its syntax-only lint adjustment, including a four-backtick fence containing a shorter triple and a non-ASCII-space tail that must not close the fence. `mise exec -- cargo test -p kuru-runtime --lib captured_instruction_graph_reaches_provider_in_reviewed_order --locked -- --nocapture` passed 1/1 after the second-snapshot addition and test-only lint adjustments: the first captured provider request keeps original imported bytes, while a new capture after file changes reaches a second provider request with changed and newly imported bytes. `mise exec -- cargo test -p kuru --test cli --locked -- --nocapture` passed 23/23 before the fence correction; both affected `imported_project_instructions_require_exact_workspace_review_before_demo_dispatch` and `oversized_instruction_is_reported_before_usable_project_dispatch` passed individually 1/1 afterward. Native tests used `KURU_DOLT_BUNDLE_DIR=/private/tmp/kuru-phase1-bundles`, `KURU_DOLT_BUNDLE_OFFLINE=true` and the owning prefetch task's prepared supervisor. `mise run //packages/kuru-core:lint`, `//packages/kuru-runtime:lint`, `//apps/kuru-tui:lint`, and `//apps/kuru-tui:typecheck` passed after the final source/test adjustments. `mise run //apps/kuru-docs:check` passed after the documentation edit and no subsequent docs edit occurred. `format:rust` and `git diff --check` passed after the parser/import changes; the strict Cospec validate and apply gate passed before implementation and strict validate is repeated immediately before archive. Native loopback tests required the authorized unsandboxed execution profile; no native Windows run was performed locally. Independent source review cleared the parser/import/trust diff and the final syntax/test-only delta. The separate nested-activation follow-on remains required and is not claimed as implemented here.

Post-archive, the all-spec commit hook reported only the archive-generated `project-instructions` Purpose placeholder. The living spec now has a concrete Purpose; `mise run cospec:validate` passed with 0 errors and 0 warnings before commit retry.

Post-push Windows native coverage on PR #60 exposed one test-only portability assertion in `instructions_preserve_scope_order_content_and_local_precedence`: the assertion compared an unescaped `Path::display()` string with the prompt's bounded, escaped source display. On Windows the latter escapes backslashes, so the assertion failed although the scoped source content and order checks passed. The corrected assertion counts the two `AGENTS.md` source headings, preserving the two-scope behavior check without depending on the path display format. `mise run //packages/kuru-core:test` passed all targets on macOS after the correction, and `cargo fmt --all --check` plus `git diff --check` passed. The corrected exact-head Windows result remains pending; this entry does not claim a Windows pass.
