## 1. Checked source graph

- [x] 1.1 Add bounded checked regular-file and containing-directory capture for ancestor `AGENTS.md` and `CLAUDE.md`, and verify links, path escape and changed identity fixtures.
- [x] 1.2 Add deterministic standalone `@path` expansion, physical-source dedup and cycle detection, and verify sibling `CLAUDE.md` → `@AGENTS.md` plus nested relative import ordering.
- [x] 1.3 Add whole-source and branch omissions for byte, file-count and depth caps with bounded user/prompt notices, and verify per-file, combined and graph-cap fixtures.

## 2. Review and user contract

- [x] 2.1 Bind rendered active source bytes and directory/file identities into the existing instruction claim, and verify changed import bytes invalidate approval while a captured snapshot remains immutable.
- [x] 2.2 Expose the omission report through the existing CLI review/launch path and document import syntax, precedence, bounds and the not-yet-shipped nested scope; verify a temporary-project CLI fixture and docs check.

## 3. Cross-layer acceptance

- [x] 3.1 Run the full scoped acceptance probes in verification.md, granular repository checks and strict Cospec gate; record observed evidence and archive before the feature-branch commit.
