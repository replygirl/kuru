## 1. Absent target completes a checked removal [critical]

- [x] 1.1 @regression (agent) run `fs::tests::checked_tree_removal_completes_when_an_enumerated_child_vanishes_first` on this host: it must fail before the change and pass after -> with the `NOENT` arm removed from `unix::remove_children` the test failed (rejected removal); with the change it passed 3/3 consecutive runs of `cargo test -p kuru-platform --lib`.
- [~] 1.2 @regression (agent) run `fs::tests::checked_tree_removal_completes_when_a_pending_delete_finishes_after_enumeration` on Windows native: a delete-pending entry whose last handle closes between enumeration and the removal's open must complete, not report `Rejected ... (os error 2)` -> defer: the fixture is `#[cfg(windows)]` and cannot execute on this macOS host; it type-checks under `cargo check --target x86_64-pc-windows-msvc -p kuru-platform --all-targets`.
- [~] 1.3 @regression (agent) run `apps/kuru-tui/tests/embedded_runtime.rs::packaged_install_and_update_preserve_complete_offline_memory` on Windows native and observe the first cold offline conversation from an empty engine cache complete without "publication succeeded, but private stage cleanup failed" -> defer: Windows native execution is pending; run 35382536799 attempt 3 job 105746719916 recorded the `Rejected removal ... The system cannot find the file specified. (os error 2)` this change addresses.
- [x] 1.4 @integration (agent) run the host-runnable staged activation and cleanup fixtures that close a real private stage after publication -> `mise run //packages/kuru-memory:test -- provision::native_tests` passed 9/9 on macOS, including `successful_activation_removes_its_disposable_stage` and `actual_warm_cache_verifies_concurrently_while_installation_lock_is_held`; `-- files` passed its 4 host-runnable fixtures.

## 2. Preserved removal contract

- [x] 2.1 @regression (agent) confirm a genuine rejection is still rejected and touches nothing -> `checked_tree_removal_rejects_a_replaced_root_without_touching_it`, `checked_tree_removal_rejects_a_pinned_root_before_mutation` and `checked_tree_removal_rejects_a_symlink_descendant` passed unchanged in `mise run //packages/kuru-platform:test` (15 lib tests), together with the 20 integration fixtures covering publication, privacy, identity and lock contention.
- [~] 2.2 @regression (agent) run the Windows-only removal fixtures on native CI: junction refusal, rebound names, and the held-root final unlink that must stay `Uncertain` -> defer: `checked_tree_removal_rejects_a_junction_without_touching_its_target`, `checked_removal_rejects_rebound_names_without_touching_either_identity` and `checked_tree_removal_reports_uncertain_after_a_held_root_blocks_final_unlink` are `#[cfg(windows)]`; they are unchanged by this fix and pending native CI.
- [x] 2.3 @unit (agent) run repository formatting, platform and memory lint, and a Windows-target type check -> `mise run format:check` clean; `mise run //packages/kuru-platform:lint` and `mise run //packages/kuru-memory:lint` passed; `cargo check --target x86_64-pc-windows-msvc -p kuru-platform --all-targets` clean apart from a pre-existing unused-import warning in `tests/filesystem.rs`.
- [x] 2.4 @manual (agent) confirm no retry, window or memory-layer policy changed -> `packages/kuru-memory/src/files.rs` is untouched: `recoverable_child_removal` still accepts only `Uncertain` plus `Rejected` with raw 5 or 32, `CLEANUP_RETRY_LIMIT` is still two seconds, and `wait_for_cleanup_retry` still returns the first typed cause on exhaustion; the absence rule sets no `removed` flag, so a later failure in the same attempt keeps its original phase.
