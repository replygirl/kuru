## 1. Denied private-stage delete completion [critical]

- [~] 1.1 @regression (agent) run `files::tests::private_temp_retries_a_denied_child_delete_after_the_holder_releases` on Windows native: it must fail before this change, because a rejected native error 5 returned immediately, and pass after, because the holder releases inside the existing two-second window -> defer: Windows native execution is pending; this fixture is `#[cfg(windows)]` and cannot run on the macOS host.
- [~] 1.2 @regression (agent) run `provision::native_tests::real_embedded_windows_engine_installs_offline_and_corrupt_cache_fails_before_execution` on Windows native and observe a cold offline provision that publishes and closes its stage without "publication succeeded, but private stage cleanup failed" -> defer: Windows native execution is pending; run 35219037077 recorded the failing `Rejected removal ... Access is denied. (os error 5)` this change addresses.
- [~] 1.3 @regression (agent) run `provision::native_tests::actual_warm_cache_verifies_concurrently_while_installation_lock_is_held` on Windows native and observe the cache-invalidation fixture reconcile a denied removal of the binary its warm probes just executed, in either reported phase -> defer: Windows native execution is pending; run 35219035614 recorded the failing `Uncertain removal ... dolt.exe: Access is denied. (os error 5)` and the rejected shape is reachable at the same call site.
- [x] 1.4 @integration (agent) run the host-runnable staged activation and cleanup fixtures -> `cargo test -p kuru-memory --lib -- provision::native_tests files::tests` passed 12/12 on macOS, including `successful_activation_removes_its_disposable_stage` and `actual_warm_cache_verifies_concurrently_while_installation_lock_is_held`.

## 2. Preserved cleanup contract

- [~] 2.1 @regression (agent) run `provision::native_tests::published_engine_reports_failed_stage_cleanup_and_releases_cache_lease` on Windows native and confirm a persistent holder still exhausts the same bound, still reports the post-publication cleanup error with its native cause, and still preserves the stage and cache lease -> defer: Windows native execution is pending; the fixture is unchanged and passed in runs 35219035614 and 35219037077 before this change.
- [x] 2.2 @unit (agent) run memory all-target/all-feature typecheck, lint and repository formatting -> `mise run //packages/kuru-memory:typecheck`, `:lint` and `mise run format:check` all passed on the host with the shared verified bundle mirror.
- [x] 2.3 @manual (agent) inspect handle ordering before cleanup and confirm the fix belongs in the retry policy rather than in a platform primitive -> `StagedActivation::finish_published` drops the candidate source and the checked cold probe before `staging.close()`, and `Directory::remove_tree` documents that "the caller owns inventory and retry policy"; the bound, identity checks and first-cause exhaustion are unchanged.
