## 1. Failed native coverage report retention [critical]

- [~] 1.1 @runtime (agent) run the Windows native-platform coverage job through the hosted runner with a failing line threshold -> defer: a subsequent hosted Windows coverage-gate failure must observe upload of that report while the 90% gate remains failed. Observed before this correction: PR18 run `34761185068`, job `103734189399`, passed every native platform test and wrote `target/kuru-platform-coverage.lcov`, but the following upload step was skipped after the required 90% gate exited 1; the run retained no platform artifact. `mise run lint:tooling` passed locally after adding the non-cancelled exact-file guard while preserving successful-run upload and its missing-file error (`/private/tmp/kuru-pr18-platform-artifact-tooling.log`).
