## 1. Failed native coverage report retention [critical]

- [x] 1.1 @runtime (agent) run the Windows native-platform coverage job through the hosted runner with a failing line threshold -> PR18 run `34762133025`, job `103736755532`, preserved the required 90% coverage failure (`3184/3568 = 89.2377%`) after every native platform test completed, and uploaded `coverage-native-platform-windows` artifact `10319536424` containing `kuru-platform-coverage.lcov` (SHA-256 `1f7f582a0a3fcc7c677341ea8bb0b21183f84a04d15de0a0505478aa27997cb8`). This confirms failed-run retention only; it does not make the platform coverage gate pass. Before the correction, PR18 run `34761185068`, job `103734189399`, wrote the same report but skipped upload after the gate failed. `mise run lint:tooling` passed locally while retaining the successful-run missing-file error policy (`/private/tmp/kuru-pr18-platform-artifact-tooling-final.log`).
