## 1. Retained Windows replacement [critical]

Native CI run 35886468805 at be2c831 failed four replacement cases with Win32 error 87; new-only publication passed. The published rename record used `offset_of!(FILE_RENAME_INFO, FileName) + FileNameLength`, four bytes below the documented `sizeof(FILE_RENAME_INFO) + FileNameLength` minimum on x64. Exact-head run 35891284306 at 48f4f9a still failed all four replacements with error 87 after that correction, so the short record was real but not the full cause. Its restrictive-DACL fixture also failed before publication because the private stage parent's `DELETE_CHILD` authority made a late file `DELETE` reopen possible. Exact-head run 35895689290 at 2d8b736 then failed three native platform cases before rename: the added destination-directory `ReOpenFile` returned access denied on ordinary parents, and the fixture's separate directory `ReOpenFile` also returned access denied. The reviewed `CreateFileW` correction at 05e4de6 fixes that pre-call phase, but exact-head native Windows job 107311381545 in run 35899247462 still failed all four replacements at `SetFileInformationByHandle(FileRenameInfoEx)` with error 87; no parent-open failure remained. Exact-head platform job 107561171334 in CI 35977448001 at 96362167 recorded the isolated differential: Win32 legacy same-directory/NULL-root returned 17, Win32 extended same-directory/retained-root returned 87, Win32 extended same-directory/NULL-root returned 17, Win32 extended cross-directory/retained-root returned 87, and `NtSetInformationFile(FileRenameInformationEx)` cross-directory/retained-root succeeded. Every probe preserved the asserted source, destination and held-old bytes/identities according to its outcome. The four required product replacements still failed through the Win32 extended call. The narrow production correction switches only that final call to the native extended class with its already-checked record and parent.

Exact-head CI 35980343388 at af7b994 shows that native replacement now returns success in the three platform cases, but their subsequent old-handle inspection fails because POSIX replacement marks the retired object delete-pending. The connector file-edit failures report the same post-publication final-access error. A scoped retained-handle metadata query now permits only zero-link delete-pending objects for that post-publication check; ordinary source/destination validation remains strict. This correction has local format, Clippy and independent source review, while native replacement, ACL restoration, archive and merge remain pending.

Exact-head CI 35984320636 at 6fb5888 passed every Windows platform behavioral test, including the required native replacement, but the package's unchanged 90% line gate failed at 87.00%: an answered five-call diagnostic matrix contributed 134 uncovered instrumented lines. That temporary matrix is removed without changing the required replacement assertion; a focused platform regression now exercises the retired zero-link handle, copied access token, new destination bytes and wrong-parent pre-call rejection. The same CI's connector shard found a second post-publication strict check of the retired source, so the reviewed correction uses the zero-link-only retained inspection for that check while retaining exact access-policy bytes and strict pre-publication checks. Four separate MCP fixtures rejected their lexical `.` working directory at the Windows verbatim-path pin with OS code 123; the scoped correction removes only lexical current-directory components before the existing checked pin. The temporary MCP phase logger is removed now its cause is known. These corrections and the 90% native platform gate still require exact-head Windows CI; no release claim is made from the local cross-check alone.

Exact-head CI 36004230622 at 89fc470 now passed native Windows platform job 107648153169, including the retained-old replacement fixture, with its platform coverage artifact uploaded. Its bounded diagnostic observed all three ordered ACE flags change from `0x10` to `0x00` immediately after NT publication and remain that way after finalization: three inherited-provenance bits cleared, zero added and zero other flag changes. Owner, DACL protection, revision, ACE count, order/type/size/mask/payload stayed equal. The narrowly guarded verifier permits this metadata difference only for the same captured, held source identity after it is delete-pending with zero links; strict admission and every other access-policy byte remain unchanged. The separate Windows connectors/core/platform coverage shard 107648153563 ran its connector library 206 passed/0 failed/1 ignored and platform library 38 passed/0 failed, with the retained-old fixture passing again. The ignored `windows_shell_eof_launcher_retains_only_stdout` is the subprocess entrypoint for the separately passing `windows_shell_timeout_keeps_eof_complete_redacted_stderr`, not an unrun product assertion. The shard's hosted post-step and aggregate CI are still in progress. Final archive and merge require the complete exact-head native and 90% checks.

The 89fc470 run ultimately failed only its separate Windows memory-runtime warm-cache test (184 passed, one fixture cleanup error) and dependent report/gate; the platform and connector replacement proofs above remained green. Reviewed test-only b200240 added a bounded retry for native checked-open codes 5/32 before the existing exact-identity removal proof. Exact-head CI 36010519061 Windows memory-runtime job 107669711038 passed the formerly failing `actual_warm_cache_verifies_concurrently_while_installation_lock_is_held` and its memory library 185/185. Windows platform, connectors, application, delivery and install jobs passed, as did macOS Intel build job 107669709943 and Ubuntu/macOS native jobs. Windows aggregate report job 107682744513 passed the unchanged 90% gate; its uploaded LCOV has 63,977 covered of 69,192 lines (92.46%). The complete exact-head CI run succeeded. Final archive and merge follow this evidence checkpoint.

- [x] 1.1 @regression (agent) replace a checked regular destination while its validated data handle remains open on native Windows -> the call succeeds, the retained handle keeps the old identity and bytes, and a fresh destination open has the staged identity and bytes
- [x] 1.2 @integration (agent) copy an unprotected ordinary file's access policy through retained-handle replacement and mutate the parent DACL afterward on native Windows -> the published file inherits identically to an ordinary sibling while the replaced handle remains bound to the old object
- [x] 1.3 @regression (agent) replace a target whose copied DACL denies file DELETE while the stage parent does not grant DELETE_CHILD and the destination parent authorizes child replacement -> a late DELETE reopen is denied, the retained private-stage authority publishes once, and the published file keeps the copied DACL

## 2. Publication boundaries

- [x] 2.1 @regression (agent) exercise occupied new-only publication and invalid source/destination identity cases -> new-only still refuses without changing old bytes and mismatches reject before publication
- [x] 2.2 @integration (agent) compile all `kuru-platform` targets and run its native Windows filesystem/security suite -> the handle-relative record, flags and error mapping compile and all platform contracts pass
