## 1. Windows shell fixture observation

- [x] 1.1 In `packages/kuru-connectors/src/tools.rs`, give only the isolated Windows environment-projection ToolHost call a 60-second timeout and derive its parent wait and outer watchdog with the existing 10- and 15-second margins, preserving every assertion and production bound
- [x] 1.2 Pass Rust formatting, connector all-target/all-feature typecheck, strict Cospec validation, and diff checks; record the pre-authored-payload timeout without claiming its exact startup cause and retain native Windows as required acceptance. *(Observed: the failed native fixture reached its prior 30-second default with a running owned PowerShell process, open non-EOF pipes, no first authored stage file, and confirmed cleanup; this does not distinguish engine initialization from bootstrap imports. Connector all-target/all-feature typecheck, the complete repository `format:code` task, and diff checks passed. Native Windows verification remains pending.)*
