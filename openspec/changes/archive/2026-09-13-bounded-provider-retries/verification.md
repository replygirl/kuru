## 1. Bounded provider retries [critical]

- [x] 1.1 @regression (agent) local Responses completion fixture changes an explicit rejected 503 then success from the current single-send behavior -> The isolated pre-loop HEAD016f021 fixture exited 101 on the first 503 with its queued success unused (`/private/tmp/kuru-provider-baseline-503.log`). The corrected identical-request regression passed in the 41/41 provider filter (`/private/tmp/kuru-provider-final-provider-tests.log`) and integrated checkpoint coverage.
- [x] 1.2 @integration (agent) local API-key JSON completion, subscription pre-acceptance SSE completion, and both model catalogs exercise 429, 500, and 503 then success -> Both API-key and subscription status-matrix fixtures passed for completion and catalog 429/500/503 schedules, with exact request counts and stable body/route/header assertions (41/41, exit 0, `/private/tmp/kuru-provider-final-provider-tests.log`); the checkpoint coverage reran them successfully.
- [x] 1.3 @integration (agent) local quota/billing, access/auth/model, and second-401 fixtures -> The API terminal-status/quota cases and subscription repeated-401/partial-stream cases passed in the same 41/41 filter. Source review confirms billing-code interpretation is restricted to the Responses 429 route, with no provider transport or accepted-stream replay.
- [x] 1.4 @integration (agent) combined 503, 401, refresh, and success schedules -> `rejected_503_then_401_shares_one_finite_rotation_budget` passed in the same provider filter: three identical completion bodies plus one OAuth POST use exactly four sends, and a later independent operation uses the durable new credential. The pending-tool continuation fixture preserves its body and tool ordering across a canceled retry and later successful call.
- [x] 1.5 @integration (agent) fixed-seed/clock Retry-After fixtures for delta seconds, leading zeroes, canonical IMF-fixdate, obsolete date, duplicate/invalid/oversized values, numeric overflow, 500-millisecond/one-second equal jitter, aggregate cap, and operation deadline -> The four retry-policy fixtures passed (exit 0, `/private/tmp/kuru-provider-final-retry-tests.log`), covering bounded parsing, canonical-date fractional rounding, numeric overflow, duplicate/oversized/invalid hints, fallback ranges and deadline rejection. The additional seeded boundary fixture returned exactly 30s twice (sum 60s) then Exhausted, exit 0, 1/1 (`/private/tmp/kuru-provider-aggregate-delay-test.log`). All five ran in checkpoint coverage.
- [x] 1.6 @runtime (agent) cancellation during retry sleep and later independent call -> `retry_exhaustion_and_backoff_cancellation_never_send_late` passed in the provider filter and checkpoint coverage: cancellation prevents a later retry, and a subsequent independent completion sends the same pending-tool continuation body before normal validated state update.

## 2. No replay after uncertainty [critical]

- [x] 2.1 @regression (agent) raw local peer reads the complete provider request then closes without a response -> The isolated HEAD016f021 raw-peer baseline received a full request then closed and observed exactly one connection with no second during its bounded observation (exit 0, 1/1, `/private/tmp/kuru-provider-baseline-disconnect.log`). The current raw-peer regression also passed in the 41/41 provider filter and checkpoint coverage. This is a before/after no-replay observation, not a baseline defect claim.
- [x] 2.2 @integration (agent) existing partial, malformed, failed, idle, and canceled SSE fixtures run with retry controller -> The existing malformed, truncated, failed, idle, canceled and partial subscription cases passed within the 41/41 provider filter and checkpoint coverage; their request-count and no-partial-completion assertions remain intact.
- [x] 2.3 @integration (agent) local completion JSON body and transport failures after response acceptance -> Accepted-body, protocol and transport diagnostics passed their existing full-chain fake-secret checks in the provider filter and checkpoint coverage. Root reviewed the fixed local diagnostic boundary and the absence of any replay after successful HTTP acceptance.

## 3. Owned rotating refresh [critical]

- [x] 3.1 @regression (agent) force local refresh request preparation/clone failure, then exercise direct and proxy native HTTP/1 fake endpoints for the audited auth-local `reqwest::Error::is_connect` connection-acquisition path -> The native preparation/refusal fixture passed in the 22/22 auth filter (`/private/tmp/kuru-provider-final-auth-tests.log`): request preparation failure made zero calls and preserved exact store bytes; audited connection refusal allowed only the bounded safe repeat and restored the previous session. Source comparison to HEAD016f021 establishes that the old refresh path lacked this typed retry. The separate owner-gate success fixture proves the second execution receives the only OAuth POST.
- [x] 3.2 @integration (agent) local proxy-tunnel and direct-refusal fixtures count CONNECT separately from token POST -> The native proxy fixture passed in the same auth filter and checkpoint coverage, recording two CONNECT attempts and checking complete captured transcripts contain no OAuth POST. Direct refusal and source-premise evidence establish the narrow acquisition class; CONNECT observation alone is not used to classify a generic result.
- [x] 3.3 @integration (agent) HTTP response, response loss, timeout outside the exact audited auth-local `is_connect` class, malformed-token, proxy/CONNECT observation without that class, and final canceled/send refresh outcomes -> The auth filter passed rejection/account-change, possible-response-loss, owner-lifetime and publication-reconciliation fixtures. Exact pending/new-generation publication outcomes and an injected read failure are distinguished; unknown state is reported rather than inventing rollback or success. Root reviewed explicit reconciliation on post-pending exits and the removal of the whole-owner cancellation timeout.
- [x] 3.4 @runtime (agent) owner-runtime gate after a proved-unsent refresh result then explicit release -> `owned_refresh_gate_retries_once_after_safe_refusal_and_publishes` passed in the auth filter and checkpoint coverage: a real refused first execution reaches the owner-local gate, explicit release permits the sole received OAuth POST, and the store publishes generation one. It uses the owner's real runtime rather than a paused caller clock.
- [x] 3.5 @runtime (agent) caller loss or an expired grant-time deadline before worker start, first POST, or second POST -> Owner-gate caller-loss/expiry cases and the already-expired allowance/second-gate fixture passed (22/22 auth filter plus `/private/tmp/kuru-provider-expired-allowance.log`). Before-first cancellation/expiry makes no POST and restores exact prior session with a new rollback revision; expired-before-start leaves original bytes unchanged, and closure/expiry before a second dispatch prevents it. Production closure/deadline checks run after test gates.
- [x] 3.6 @integration (agent) execute the native HTTP/1 zero-token-POST and proxy CONNECT refresh fixtures on Windows native CI before merge -> exact-head run 34743192947 job 103686288053 passed `refresh_preparation_failure_and_connect_refusal_preserve_credentials`, `native_proxy_connect_is_counted_separately_from_token_posts`, `owned_refresh_gate_retries_once_after_safe_refusal_and_publishes`, and the caller-loss/expired-second-gate cases in the 90/90 Windows connector suite. The real loopback fixtures retained exact zero-token-POST and separate CONNECT counts; complete Windows coverage was 23,329/24,881 lines = 93.76%.

## 4. Focused quality and documentation

- [x] 4.1 @integration (agent) focused provider, SSE, and auth fixture filters plus an exact locked offline Cargo feature-tree/source-premise check through owning mise tasks -> Provider 41/41, auth 22/22 and retry 4/4 filters exited 0 in their final named logs; the added aggregate-delay fixture subsequently passed 1/1. Locked offline reqwest/hyper/hyper-util feature-tree checks exited 0 in `/private/tmp/kuru-provider-retry-feature-tree.log`, `/private/tmp/kuru-provider-retry-hyper-feature-tree.log` and `/private/tmp/kuru-provider-retry-hyper-util-feature-tree.log`; source audit in the private roadmap records their HTTP/1 acquisition premise. Checkpoint coverage then passed all 98 connector tests. No live credentials or provider calls were used.
- [x] 4.2 @integration (agent) root documentation check -> `mise run docs:check` in the checkpoint exited 0, including VitePress build and public artifact/link/anchor checks (`/private/tmp/kuru-phase0-checkpoint-docs.log`). Root reviewed exact jitter ranges, unfit-hint refusal, audited connect/timeout distinction, route limits and absence of an idempotency promise.
- [x] 4.3 @integration (agent) scoped format, lint, and typecheck through mise -> Primary connector lint/typecheck exited 0 (`/private/tmp/kuru-connectors-final-lint.log`, `/private/tmp/kuru-connectors-final-typecheck.log`). After the final test-only addition, connector lint again exited 0 (`/private/tmp/kuru-connectors-post-aggregate-lint.log`); the checkpoint root format check exited 0 (`/private/tmp/kuru-phase0-checkpoint-format-final.log`).
- [x] 4.4 @eval (agent) evaluate deterministic multi-attempt transcripts for rejected retry, terminal quota, accepted partial stream, and possibly dispatched refresh -> Root evaluated the observed fake-peer schedules for rejected retry, terminal API quota, accepted partial SSE, combined 503/401 rotation and possibly dispatched refresh. Exact request/body/generation assertions establish one actor/session route, bounded recovery and completion only after validation; no model-behavior evaluation or remote idempotency is claimed.
