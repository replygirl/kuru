## 1. Failure-only import diagnostic

- [x] 1.1 Keep authoritative fixture sources unchanged when no diagnostic import is selected, and add a fixed named-module import diagnostic through the Core-qualified cmdlet to each failure control.
- [x] 1.2 Replace the encoded/console matrices with one encoded inherited-console control per helper while retaining bounded drain, termination, reap, pipe close, and original failure authority.

## 2. Verification

- [x] 2.1 Run applicable Cospec gates and focused format/type checks; record that native Windows diagnostic execution remains pending.
