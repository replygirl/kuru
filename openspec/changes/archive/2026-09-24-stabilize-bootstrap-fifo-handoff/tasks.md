## 1. Stabilize the FIFO handoff

- [x] 1.1 Exercise the missing-source regression and FIFO stress probe to demonstrate that the writer-first reader can strand and the corrected launch order passes.
- [x] 1.2 Start the retained bounded consumer before the producer while preserving exact status, size, signal, and cleanup behavior.

## 2. Verify the installer

- [x] 2.1 Verify normal, missing, corrupt, malformed, oversized, and held-open bootstrap inputs with the real shell installer.
- [x] 2.2 Run formatting, shell lint, strict cospec validation, and the scoped delivery checks.
