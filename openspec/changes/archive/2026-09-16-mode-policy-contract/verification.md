## 1. Built-in identity and policy parity [critical]

- [x] 1.1 @unit (agent) compare all four ordered seed IDs, roles, names, complete instruction bytes and required coverage against fixed pre-extraction goldens -> exact equality and unchanged Mode serialization. `packages/kuru-core/tests/policy.rs` checks fixed full-byte seed digests, ordered identities, roles, names, authored order and Mode serialization; the combined coverage run passed.
- [x] 1.2 @integration (agent) run all four built-in modes through scripted runtime turns before extraction -> same initial recipients, phase/tool requests, contribution envelope, speaker/reason, transcript and limits. All three `mode_baseline_tests` passed in `mise run coverage` (exit 0): four-mode request/facing, direct peer routing/round limit, and one-hop relationship consultation. These exercise the unchanged engine loops with real Dolt.
- [x] 1.3 @unit (agent) exercise pure target/focus/max activation/continuity/cold authored/nonseed stable facing, relationship participation and flow order -> existing decisions and reason strings. Core policy tests passed under combined coverage; runtime baseline additionally asserts `caller-target`, `active-focus`, `maximum-activation`, `previous-completed-speaker`, `mode-authored-order`, and `stable-id-order` outcomes.
- [x] 1.4 @eval (agent) compare deterministic scripted provider requests and speaking responses across all four built-ins against pre-extraction goldens -> model-facing phase, tools, context and final answer semantics match. The four-mode scripted baseline asserts deliberation and speaking phases, tool inventories, recipient actor IDs, current-input context, relationship context, final response and selection reasons against the pre-extraction contract; all passed under combined coverage.

## 2. Safety and independently callable seams [critical]

- [x] 2.1 @unit (agent) substitute each test-only component and validate malformed seeds, source grants, namespace outputs and inactive recipients -> decisions are independent and invalid outputs refuse. Core policy tests cover all six alternate components, invalid/duplicate seeds, unsupported context sources, namespace keys, peer and relationship recipients, facing results and contribution isolation; passed in combined coverage.
- [x] 2.2 @integration (agent) run peer/relationship, public/private context and candidate dream/undo baseline scenarios on the existing runtime -> no cross-private prompt content, supervisor privilege, uncommitted dream write or lost later history. The combined coverage run passed the new three four-mode baselines and existing runtime isolation, relationship-history, dreaming and undo tests (`review_tests::a_different_speaker_sees_public_answers_but_not_private_memories`, `tests::relationships_preserve_their_own_history_without_access_to_part_notes`, `tests::dreaming_uses_isolated_actor_histories_and_runs_periodically`, `review_tests::undo_archives_new_members_and_preserves_their_memories`). No P3 policy executes model/provider/storage authority.

## 3. Contract delivery

- [x] 3.1 @unit (agent) run package-owned core and focused runtime checks plus scoped format/lint/typecheck -> all pass without changing durable or CLI formats. `mise run coverage` exited 0 in 553.55 s with the workspace's `--fail-under-lines 90` gate; all three P3 real-Dolt baselines and the existing runtime suite passed (93/93 runtime unit tests). `mise run //:format:rust:fix`, `//packages/kuru-core:typecheck`, `:lint`, `//packages/kuru-runtime:typecheck`, `:lint`, and `git diff --check` exited 0. Earlier package-owned core tests and focused runtime baselines passed; coverage repeated them after final changes.
- [x] 3.2 @manual (agent) compare architecture/framework documentation with implemented six-axis operations and P8/P9 boundary -> descriptions match shipped P3 behavior without claiming extraction complete. Compared `docs/architecture.md` and `apps/kuru-docs/concepts/frameworks.md` with the six policy traits and unchanged engine loop. `mise run //apps/kuru-docs:check` exited 0: format, lint, VitePress build, public artifact/local link/anchor checks passed. Documentation says P8/P9 consume the contract later and preserves equal peer authority.
