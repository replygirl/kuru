## 1. Core policy contract

- [x] 1.1 Define six independently callable pure policy components and typed decision inputs/results in `kuru-core`; verify a test-only alternative can change each operation without another component or executor handle.
- [x] 1.2 Convert four seed tables into validated reference profiles while preserving `Framework::builtin` and serialized mode; verify fixed full-byte seed ID/name/role/instruction and authored-order goldens.
- [x] 1.3 Add profile and result validation for role coverage, relationship/peer eligibility, visibility isolation and exact namespace shapes; verify negative unit cases reject before use.

## 2. Pre-extraction evidence

- [x] 2.1 Add four-mode scripted runtime turn baselines before modifying engine dispatch; verify requests, recipient/contribution order, speaker/reason, target/focus, activation/continuity/cold ties and final outcomes.
- [x] 2.2 Add scoped peer/relationship, budget, isolation and dream/candidate/undo baselines; verify actual runtime state and persisted outcomes through the existing loops.
- [x] 2.3 Document the six-axis reference contract and P8/P9 ownership boundary in architecture/framework docs; verify text against the implemented operations and existing equal-peer semantics.

## 3. Integrated validation

- [x] 3.1 Run package-owned core and focused runtime tests plus scoped format, lint and typecheck; record exact observed results in verification.md.
- [x] 3.2 Compare the complete built-in baseline with the old semantic behavior and confirm no new user mode, focus control, persisted format or execution authority appeared; record evidence in verification.md.
