## 1. Exact selected-port collision [critical]

- [x] 1.1 @regression (agent) force an owned, unrelated listener to take the selected loopback port before actual Dolt spawn -> the original Dolt attempt reports its exact bind collision, is fully reaped, and a new selected port reaches Ready under the same deadline while the holder remains live; before the fix, the same collision failed at startup (observed P9 Ubuntu CI run 35219035824, job 105194846228). Observed locally on macOS with the real bundled Dolt: `KURU_DOLT_BUNDLE_DIR=/private/tmp/kuru-phase1-bundles mise run //packages/kuru-memory:test -- port_takeover` -> `selected_port_takeover_retries_actual_dolt_without_touching_holder ... ok`; the fixture asserts two distinct selected ports, Ready on the second, and that the unrelated holder is still bound afterwards.
- [x] 1.2 @integration (agent) keep every selected port occupied through the finite attempts -> no Ready or endpoint is published, the original deadline bounds work, and the exact final collision remains diagnostic. Observed locally in the same run: `persistent_selected_port_takeovers_exhaust_three_owned_attempts ... ok` — exactly three unrelated listeners retained and still bound, no Ready frame written, no `endpoint.json`, final error still `Dolt exited before readiness`.

## 2. Fail closed on other startup results

- [x] 2.1 @integration (agent) make an owned child exit early without the exact selected-port diagnostic -> one attempt fails with original error, no endpoint, and no unrelated process action. Observed locally: `mise run //packages/kuru-memory:test -- unrelated_premature_exit` -> `unrelated_premature_exit_does_not_retry_or_publish ... ok` (1 passed), asserting exactly one port selection.
- [x] 2.2 @integration (agent) close the parent during failed startup -> no further Dolt attempt is launched and the owned child is reaped. Observed locally: `parent_close_during_selected_port_takeover_never_retries ... ok` — one port selection, error contains `parent closed`, no endpoint published.

## 3. Checks

- [x] 3.1 @unit (agent) run focused real-Dolt filters and package static checks on local macOS -> `mise run //packages/kuru-memory:test -- server::` passed 18/18 (the whole supervisor module, including the four new fixtures); `mise run //packages/kuru-runtime:test -- cancellation_settles_admitted_usage_without_a_terminal_report` passed 1/1 (the exact test that failed in CI run 35219035824); `mise run //packages/kuru-memory:lint` (clippy `-D warnings`, all targets and features) and `mise run format:check` passed; `git diff --check` clean.
- [~] 3.2 @runtime (agent) run Ubuntu and Windows native memory coverage on the corrected exact head -> defer: pending native execution — hosted CI runs only after this branch is restacked and pushed. This host is macOS, so no Windows evidence exists locally; `server_tests.rs` is `#[cfg(all(test, unix))]`, so the four new fixtures are Ubuntu/macOS-only by construction, while the production retry path is platform-independent and is exercised on Windows through the existing real-Dolt lifecycle suites.
