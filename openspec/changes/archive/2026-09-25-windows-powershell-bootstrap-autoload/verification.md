## 1. Installer never discovers its own stock commands [critical]

- [x] 1.1 @regression (agent) run `powershell_diagnostics::stock_installer_imports_pshome_modules_before_any_discovered_command` against the unfixed and fixed `install.ps1` -> observed on macOS host 2026-09-25: with the fix stashed it FAILED ("install.ps1 must import exact PSHOME Management"); with an injected `Write-Verbose` before the imports it FAILED ("reaches Write-Verbose before its exact PSHOME module imports"); with an injected `Get-Acl` it FAILED ("uses Get-Acl; import its exact stock PSHOME module"); the fixed script PASSED (`powershell_diagnostics` 5 passed, 0 failed).
- [~] 1.2 @regression (agent) run native `bootstrap_windows::bootstrap_supplies_its_stock_commands_without_module_auto_discovery` on stock Windows PowerShell 5.1 with a fresh isolated `LOCALAPPDATA` and autoloading disabled -> defer: no Windows host is available to this change, and CI runs only for pull requests, which this delivery was not authorized to open. Expected: the fixed installer publishes the fixture release with exactly one PSHOME Management and Utility module loaded; the unfixed script fails at `Write-Verbose` with command-not-found. Not observed.
- [~] 1.3 @e2e (agent) run the Windows installation job's `packaged_install_and_update_preserve_complete_offline_memory` -> defer: same native-CI constraint. Expected: ordered direct checkpoints include `management module ready` and `utility module ready` before `native bridge starting`, and every documented `-Verbose` phase appears. Not observed.

## 2. Stall no longer recurs [critical]

- [~] 2.1 @runtime (agent) observe repeated native Windows CI runs of the delivery-archive `bootstrap_windows` suite, the application shard and the installation job at the fixed head -> defer: requires PR CI. The pre-fix baseline is observed: stalls at #89 5416a0f3 (job 108226893444), #91 d2aa74e6 (job 108225753971, two tests), and #89 c325a3ba (jobs 108178594748, 108178594914); passes on main 67448442 (jobs 108215727185, 108215727216, 108215727226) with byte-identical script lines. One green run will not prove a historically intermittent (~3/35) stall fixed.

## 3. Repository gates

- [x] 3.1 @integration (agent) run owning delivery format, lint/typecheck, docs checks, strict Cospec validation/apply and normal hooks -> observed on macOS host: `cargo fmt --all --check` exit 0; `mise run //packages/kuru-delivery:lint` (clippy all targets/features, `-D warnings`) finished clean; `mise run //packages/kuru-delivery:lint:shell` passed; `mise run //:docs:check` reported "Public docs artifacts, local links and anchors passed"; Cospec strict validation 0 errors/0 warnings and apply exit 0 (gate clear). `cargo check --target x86_64-pc-windows-msvc -p kuru-delivery --features tooling --test bootstrap_windows` could not run: the host lacks Windows SDK headers for `aws-lc-sys`, so it is not Windows compile evidence. Normal hk pre-commit hooks run on the archiving commit; their result is that commit's, not recorded here. No product or fixture timeout changed.
