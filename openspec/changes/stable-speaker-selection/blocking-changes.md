# Dependencies

## Blocked by

None.

## Soft-blocked by

None.

## Shared surfaces

Existing archived peer-harness and Dolt-memory changes provide selection and
session persistence. Active dream-part-instruction-preamble touches a separate
peer-cognition requirement. Human-notes-read and tool-result-redaction share the
runtime engine file but do not provide selection semantics; owned shell lifecycle,
shell environment, provider retries, process observation and async terminal events
are likewise independent. Root coordinates these edits and their final checks.
Phase 1's mode contract is not a prerequisite for this bounded correction.
