# generated by cospec — do not edit here.
# Customize via openspec/config.yaml (context, rules); full ownership: `cospec schema fork ci <name>`.
name: ci
version: 2
description: 'ci — CI configuration and automation pipelines. Light workflow: proposal → blocking-changes → tasks'
artifacts:
  - id: proposal
    generates: proposal.md
    description: One-paragraph why + what for a CI change
    template: proposal.md
    instruction: |
      This is a CI-only change (workflows, pipelines, automation scripts). Keep
      it short: ## Why — one or two sentences. ## What Changes — bulleted list
      of workflow/script paths. ## Impact — jobs, secrets, required checks
      affected. A ci change must not modify application source or specs — if it
      does, retype it: `cospec new <correct-type> <slug>`.
    requires: []
  - id: blocking-changes
    generates: blocking-changes.md
    description: Cross-change dependency ledger
    template: blocking-changes.md
    instruction: |
      Most ci changes are independent. Quickly scan the NAMES of active changes
      in openspec/changes/ for obvious collisions with the workflows/scripts
      this change touches — do NOT read every proposal. If nothing collides,
      write `None.` under both sections and move on.
      FORMAT (machine-parsed — deviations fail `cospec validate`):
      - entry:    - [ ] `change-slug` — what it provides
      - shipped:  - [x] `change-slug` — what it provides *(archived YYYY-MM-DD)*
      - empty section: the literal line `None.`
    requires: [proposal]
  - id: verification
    generates: verification.md
    description: Acceptance-evidence ledger — each observable behavior, the layer it is exercised at, who runs the check, and the recorded evidence
    template: verification.md
    instruction: |
      Optional and light — a trivial pipeline tweak needs none. Author it only when
      the proposal checks the `deploy` surface (CI-execution topology): then include
      at least one `@runtime` row proving the workflow actually ran in its real
      runner (correct runtime, secrets, required checks). Name that row's execution
      environment. Rows: `- [ ] N.M @<layer> (<owner>) <probe> -> <result>`. A `[x]`
      row needs observed evidence after ` -> `; defer as `[~] … -> defer: <reason>`.
    requires: [proposal]
  - id: tasks
    generates: tasks.md
    description: Implementation checklist tracked during apply
    template: tasks.md
    instruction: |
      Usually one group: the edits plus one verification task (e.g.
      "- [ ] 1.2 actionlint passes / workflow dry-run"). Use `## N. Group` and
      `- [ ] N.M task` exactly — the checkbox format is parsed.
    requires: [proposal]
apply:
  requires: [proposal, blocking-changes, tasks]
  tracks: tasks.md
  instruction: |
    BEFORE any implementation work, run:
        cospec apply "<change>" --json
    - exit 0 → gate clear. Read the returned contextFiles, work through pending
      tasks in order, marking each `- [x]` as you complete it.
    - exit 2 → BLOCKED (missing artifacts or unchecked hard blockers). STOP.
      Report each item the command lists and what it provides; suggest
      implementing and archiving those first. Do NOT proceed or work around it.
    - exit 3 → soft-blocked. List each soft blocker and what degrades without
      it, ask the user to confirm, then re-run with --allow-soft. Never skip
      silently.
    The command auto-checks entries whose blocker is already archived — trust
    its output over a manual read of blocking-changes.md. Pause on ambiguity,
    errors, or scope creep.
